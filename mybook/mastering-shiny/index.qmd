---
title: "Mastering Shiny"
subtitle: "R로 구동되는 대화형 앱, 보고서, 대시보드 만들기"
author: "해들리 위컴(Hadley Wickham)"
---

# 서문

## Shiny란 무엇인가?

Shiny를 한 번도 사용해 본 적이 없다면, 환영합니다! Shiny는 풍부한 대화형 웹 앱(Interactive Web Apps)을 쉽게 만들 수 있게 해주는 R 패키지입니다. Shiny를 사용하면 R에서 작업한 결과를 웹 브라우저를 통해 노출할 수 있어 누구나 사용할 수 있습니다. Shiny는 최소한의 노력으로 세련된 웹 앱을 쉽게 만들어 주기 때문에 여러분의 모습을 빛나게 해 줍니다. 과거에는 대부분의 R 사용자에게 웹 앱을 만드는 일이 어려웠습니다. 그 이유는 다음과 같습니다:

웹 기술에 대한 깊은 지식, 즉 HTML, CSS, JavaScript가 필요했습니다.
복잡한 대화형 앱을 만들려면 인터랙션 흐름을 면밀히 분석해야 했는데, 그래야 입력이 변경될 때 관련된 출력만 업데이트될 수 있었기 때문입니다.

Shiny는 다음 방식으로 R 프로그래머가 웹 앱을 만드는 것을 크게 수월하게 해줍니다:

일반적인 작업에 필요한 HTML, CSS, JavaScript를 자동으로 생성하는 꼼꼼하게 선별된 사용자 인터페이스(User Interface, 줄여서 UI) 함수를 제공합니다. Shiny가 제공하는 기본 기능을 넘어서고 싶을 때까지 HTML/CSS/JavaScript 세부사항을 알 필요가 없습니다.
반응형 프로그래밍(Reactive Programming)이라는 새로운 프로그래밍 스타일을 도입합니다. 이 방식은 코드 조각들의 의존성을 자동으로 추적합니다. 입력이 변경될 때마다 Shiny는 관련 출력을 모두 업데이트하기 위해 필요한 최소한의 작업을 자동으로 파악합니다.

사람들은 Shiny를 다음과 같은 용도로 사용합니다:

중요한 고위 성과 지표를 추적하면서도 더 자세한 조사가 필요한 세부 지표로 드릴다운(Drill-down)할 수 있는 대시보드(Dashboard)를 만듭니다.
사용자가 관심 있는 결과의 정확한 부분으로 바로 이동할 수 있게 해주는 대화형 앱으로 수백 페이지에 달하는 PDF를 대체합니다.
정보 시각화와 대화형 민감도 분석(Sensitivity Analysis)을 통해 비기술 대중에게 복잡한 모델을 전달합니다.
이메일 요청 대신 사용자가 데이터를 업로드하고 표준 분석을 수행할 수 있는 Shiny 앱으로, 일반 워크플로우에 대한 셀프서비스 데이터 분석을 제공합니다. 프로그래밍 기술이 없는 사용자도 정교한 R 분석을 활용할 수 있습니다.
통계와 데이터 과학 개념을 가르치기 위한 대화형 데모를 만들어, 학습자가 입력을 조정하고 분석에서 그 변화가 미치는 영향을 관찰할 수 있게 합니다.

요컨대, Shiny는 웹을 사용할 수 있는 누구에게든 여러분의 R 슈퍼파워를 전수할 수 있는 늰력을 제공합니다.

이 책은 누구를 위한 것인가? 이 책은 두 가지 주요 독자를 대상으로 합니다:

Shiny를 배워 자신의 분석을 대화형 웹 앱으로 만들고 싶은 R 사용자. 이 책에서 최대한의 효과를 거두려면 R로 데이터 분석을 수행하는 데 익숙하고 적어도 몇 개의 함수를 작성해 본 경험이 있어야 합니다.
더 높은 품질의 앱을 더 빠르고 쉽게 작성하기 위해 Shiny의 기반 이론에 대한 지식을 향상시키고 싶은 기존 Shiny 사용자.

이 책이 특히 유용할 것입니다:

앱이 점점 커지기 시작하고 복잡성을 관리하는 데 문제가 생기기 시작하는 경우.

무엇을 배울 수 있는가? 이 책은 네 부분으로 나뉩니다:

1부에서는 Shiny의 기초를 배워 가능한 한 빨리 시작할 수 있습니다. 앱 구조의 기본, 유용한 UI 컴포넌트, 반응형 프로그래밍의 기초를 배웁니다.
2부는 기초 위에 세워져 사용자에게 피드백 제공, 데이터 업로드 및 다운로드, 코드로 UI 생성, 코드 중복 감소, tidyverse 프로그래밍에 Shiny 사용 등 일반적인 문제를 해결하는 방법을 다룹니다.
3부에서는 Shiny의 기반이 되는 반응형 프로그래밍의 이론과 실천을 깊이 파고듭니다. 기존 Shiny 사용자라면 이 장에서 가장 큰 가치를 얻을 수 있습니다. 자신의 문제에 맞게 새로운 도구를 만들 수 있는 탄탄한 이론적 기초를 제공하기 때문입니다.
4부에서는 프로덕션 환경에서 Shiny 앱을 원활하게 작동시키기 위한 유용한 기법을 살펴보며 마무리합니다. 복잡한 앱을 함수와 모듈로 분해하는 방법, 패키지로 코드를 정리하는 방법, 코드가 올바르게 작동하는지 테스트하는 방법, 성능을 측정하고 개선하는 방법을 배웁니다.

무엇을 배우지 못하는가? 이 책의 초점은 효과적인 Shiny 앱을 만들고 반응성(Reactivity)의 기반 이론을 이해하는 데 있습니다. 데이터 과학, R 프로그래밍, 소프트웨어 엔지니어링의 모범 사례를 최대한 소개하겠지만, 이러한 중요한 기술을 마스터하려면 다른 참고 자료가 필요할 것입니다.

이 책에서 제 글이 마음에 든다면, 이 주제에 대한 다른 책들도 즐기실 수 있을 것입니다: *R for Data Science*, *Advanced R*, 그리고 *R Packages*.

Shiny에 특화된 중요한 주제 중 이 책에서 다루지 않는 것도 몇 가지 있습니다:

이 책은 내장 사용자 인터페이스 툴킷만 다룹니다. 가장 매력적인 디자인은 아니지만 배우기 쉽고 상당히 유용합니다. 추가적인 요구 사항이 있거나(기본값에 싫증이 난다면) 다른 프론트엔드를 제공하는 패키지가 여러 개 있습니다. 자세한 내용은 "Bootstrap"을 참조하세요.
Shiny 앱의 배포. Shiny를 "프로덕션에 투입"하는 것은 이 책의 범위 밖인데, 회사마다 크게 다르고 대부분 R과 관련이 없는 경우가 많기 때문입니다(대부분의 도전은 기술적이기보다 문화적 또는 조직적인 경향이 있습니다). 프로덕션에서의 Shiny가 처음이라면 Joe Cheng의 2019 rstudio::conf 기조연설을 시작으로 하는 것을 추천합니다. Shiny를 프로덕션에 투입하는 것이 무엇을 의미하는지, 직면할 가능성이 높은 과제를 어떻게 극복할 수 있는지 넓은 관점을 얻을 수 있을 것입니다. 그 후 RStudio Connect 웹사이트에서 사내 앱 배포를 위한 RStudio 제품에 대해, 그리고 Shiny 웹사이트에서 기타 일반적인 배포 시나리오에 대해 알아보세요.

사전 요구 사항 계속하기 전에 이 책에 필요한 모든 소프트웨어가 준비되어 있는지 확인하세요:

R. 아직 R이 설치되어 있지 않다면 잘못된 책을 읽고 계실 수 있습니다. 이 책 전반에 걸쳐 R에 대한 기본적인 이해를 전제로 합니다. R 사용법을 배우고 싶다면, 최소한의 번거로움으로 R을 시작할 수 있게 설계된 제 *R for Data Science*를 추천합니다.
RStudio. RStudio는 R용 무료 오픈소스 통합 개발 환경(IDE)입니다. 모든 R 환경(R GUI와 ESS 포함)에서 Shiny 앱을 작성하고 사용할 수 있지만, RStudio에는 Shiny 앱 작성, 디버깅, 배포에 특화된 기능이 있습니다. RStudio Desktop을 다운로드해 보시길 추천하지만, Shiny나 이 책을 성공적으로 활용하는 데 반드시 필요한 것은 아닙니다.
R 패키지. 이 책은 여러 R 패키지를 사용합니다. 다음 명령으로 한 번에 모두 설치할 수 있습니다:

install.packages(c( "gapminder", "ggforce", "gh", "globals", "openintro", "profvis", "RSQLite", "shiny", "shinycssloaders", "shinyFeedback", "shinythemes", "testthat", "thematic", "tidyverse", "vroom", "waiter", "xml2", "zeallot" ))

이전에 Shiny를 설치한 적이 있다면 최소한 1.6.0 버전 이상인지 확인하세요.

이 책에서 사용된 기호 이 책에서는 다음과 같은 타이포그래피 규칙을 사용합니다:

이탤릭체
새로운 용어, URL, 이메일 주소, 파일명, 파일 확장자를 나타냅니다.
고정폭 글꼴
프로그램 목록과 단락 내에서 변수나 함수 이름, 데이터베이스, 데이터 타입, 환경 변수, 명령문, 키워드와 같은 프로그래밍 요소를 나타낼 때 사용합니다.
고정폭 굵은 글꼴
사용자가 그대로 타이핑해야 하는 명령어나 기타 텍스트를 나타냅니다.
고속폭 이탤릭체
사용자 제공 값이나 문맥에 의해 결정된 값으로 대체해야 하는 텍스트를 나타냅니다.

提示
팁이나 제안을 나타냅니다.

참고
일반적인 참고 사항을 나타냅니다.

코드 예제 사용 부가 자료(코드 예제, 연습 문제 등)는 https://mastering-shiny.org 에서 다운로드할 수 있습니다. 이 책의 코드 샘플은 MIT 라이선스 하에 라이선스가 부여되어 있습니다. 기술적인 질문이나 코드 예제 사용에 문제가 있는 경우 bookquestions@oreilly.com 으로 이메일을 보내주세요.

이 책은 여러분의 작업을 돕기 위해 마련되었습니다. 일반적으로 이 책에 포함된 예제 코드는 프로그램과 문서에서 사용할 수 있습니다. 코드의 상당 부분을 재현하는 경우를 제외하고는 허락을 받기 위해 연락할 필요가 없습니다. 예를 들어 이 책의 코드 여러 부분을 사용하는 프로그램을 작성하는 것은 허가가 필요하지 않습니다. O'Reilly 서적의 예제를 판매하거나 배포하는 것은 허가가 필요합니다. 이 책을 인용하고 예제 코드를 인용하여 질문에 답하는 것은 허가가 필요하지 않습니다. 이 책의 예제 코드를 상당량 제품 문서에 포함하는 것은 허가가 필요합니다. 인용에 대해서는 감사하지만, 일반적으로 요구하지는 않습니다. 인용에는 제목, 저자, 출판사, ISBN이 포함됩니다. 예를 들어: "Mastering Shiny by Hadley Wickham (O'Reilly). ." 코드 예제의 사용이 공정 사용이나 위에서 부여한 허가의 범위 밖이라고 느끼면 permissions@oreilly.com 으로 연락해 주세요.

O'Reilly 온라인 학습

참고
40년 이상 O'Reilly Media는 기술과 비즈니스 교육, 지식, 통찰력을 제공하여 기업이 성공하도록 도왔습니다. 독자적인 전문가와 혁신가 네트워크는 도서, 기사, 온라인 학습 플랫폼을 통해 지식과 전문 지식을 공유합니다. O'Reilly의 온라인 학습 플랫폼은 라이브 교육 과정, 심화 학습 경로, 대화형 코딩 환경, O'Reilly와 200개 이상의 출판사에서 제공하는 방대한 텍스트 및 비디오 컬렉션에 대한 온디맨드 액세스를 제공합니다. 자세한 내용은 http://oreilly.com 에서 확인하세요.

문의처本书에 대한 의견과 질문은 다음 주소로 보내주세요:

O'Reilly Media, Inc.
1005 Gravenstein Highway North
Sebastopol, CA 95472
800-998-9938 (미국 또는 캐나다 내)
707-829-0515 (국제 또는 지역)
707-829-0104 (팩스)

이 책의 웹 페이지가 있으며, 정오표, 예제, 추가 정보를 게시하고 있습니다. 이 페이지는 https://oreil.ly/mastering-shiny 에서 확인할 수 있습니다. 의견이나 기술 질문은 bookquestions@oreilly.com 으로 이메일을 보내주세요. 도서 및 과정에 대한 뉴스와 정보는 http://oreilly.com 에서 확인하세요. Facebook에서 만나요: http://facebook.com/oreilly Twitter에서 만나요: http://twitter.com/oreillymedia YouTube에서 만나요: http://youtube.com/oreillymedia

감사의 말 이 책은 공개적으로 작성되었으며, 각 장이 완성되면 Twitter에서 홍보되었습니다. 정말 공동체의 노력입니다: 많은 사람들이 초안을 읽고, 오타를 수정하고, 개선안을 제안하고, 콘텐츠를 기여했습니다. 이러한 기여자들이 없었다면 이 책은 지금만큼 좋지 않았을 것이며, 그들의 도움에 깊이 감사드립니다.

83명의 기여자 여러분께 특별히 감사드립니다(사용자 이름 알파벳 순): Adam Pearce (@1wheel), Adi Sarid (@adisarid), Alexandros Melemenidis (@alex-mffm), Anton Klåvus (@antonvsdata), Betsy Rosalen (@betsyrosalen), Michael Beigelmacher (@brooklynbagel), Bryan Smith (@BSCowboy), c1au6io_hh (@c1au6i0), @canovasjm, Chris Beeley (@ChrisBeeley), @chsafouane, Chuliang Xiao (@ChuliangXiao), Conor Neilson (@condwanaland), @d-edison, Dean Attali (@daattali), DanielDavid521 (@Danieldavid521), David Granjon (@DivadNojnarg), Eduardo Vásquez (@edovtp), Emil Hvitfeldt (@EmilHvitfeldt), Emilio (@emilopezcano), Emily Riederer (@emilyriederer), Eric Simms (@esimms999), Federico Marini (@federicomarini), Frederik Kok Hansen (@fkoh111), Frans van Dunné (@FvD), Giorgio Comai (@giocomai), Hedley (@heds1), Henning (@henningsway), Hlynur (@hlynurhallgrims), @hsm207, @jacobxk, James Pooley (@jamespooley), Joe Cheng (@jcheng5), Julien Colomb (@jcolomb), Juan C. Rodriguez (@jcrodriguez1989), Jennifer (Jenny) Bryan (@jennybc), Jim Hester (@jimhester), Joachim Gassen (@joachimgassen), Jon Calder (@jonmcalder), Jonathan Carroll (@jonocarroll), Julian Stanley (@julianstanley), @jyuu, @kaanpekel, Karandeep Singh (@kdpsingh), Robert Kirk DeLisle (@KirkDCO), Elaine (@loomalaine), Malcolm Barrett (@malcolmbarrett), Marly Gotti (@marlycormar), Matthew Wilson (@MattW-Geospatial), Matthew T. Warkentin (@mattwarkentin), Mauro Lepore (@maurolepore), Maximilian Rohde (@maxdrohde), Matthew Berginski (@mbergins), Michael Dewar (@michael-dewar), Mine Cetinkaya-Rundel (@mine-cetinkaya-rundel), Maria Paula Caldas (@mpaulacaldas), nthobservation (@nthobservation), Pietro Monticone (@pitmonticone), psychometrician (@psychometrician), Ram Thapa (@raamthapa), Janko Thyson (@rappster), Rebecca Janis (@rbjanis), Tom Palmer (@remlapmot), Russ Hyde (@russHyde), Talita Amorim (@talitaamor), and Tracey Warren (@traceywarren), Yohan (@the-yv), Thomas Lin Pedersen (@thomasp85), Tim Atkinson (@TimAtkinsonDS), Tim Flynt (@timflynt), Toni (@tonielrovetto), Tünde Varga (@tundevl), Valentin Valter (@ValentinValter), Diego Valverde (@valverde95), Dharmesh Kakadia (@Sumidu), Tanner Stauss (@tmstauss), Tony Fujs (@tonyfujs), Stefan Moog (@trekonom), Jeff Allen (@trestletech), Trey Gilliland (@treygilliland), Albrecht (@Tungurahua), Valeri Voev (@ValeriVoev), Vickus (@Vickusr), William Doane (@WilDoane), 黄湘云 (@XiangyunHuang), 그리고 gXcloud (@xwydq).

이 책은 어떻게 만들어졌는가 이 책은 RStudio에서 bookdown을 사용하여 작성되었습니다. 책 웹사이트는 netlify에서 호스팅되며 GitHub Actions를 통해 모든 커밋 후 자동으로 업데이트됩니다. 전체 소스는 GitHub에서 확인할 수 있습니다. 이 책 버전은 R 버전 4.0.3 (2020-10-10)과 다음 패키지로 빌드되었습니다:

| 패키지 | 버전 | 소스 |
| --- | --- | --- |
| gapminder | 0.3.0 | standard (@0.3.0) |
| ggforce | 0.3.2 | standard (@0.3.2) |
| gh | 1.2.0 | standard (@1.2.0) |
| globals | 0.14.0 | standard (@0.14.0) |
| openintro | 2.0.0 | standard (@2.0.0) |
| profvis | 0.3.7.9000 | GitHub (rstudio/profvis@ca1b272) |
| RSQLite | 2.2.3 | standard (@2.2.3) |
| shiny | 1.6.0 | standard (@1.6.0) |
| shinycssloaders | 1.0.0 | standard (@1.0.0) |
| shinyFeedback | 0.3.0 | standard (@0.3.0) |
| shinythemes | 1.2.0 | standard (@1.2.0) |
| testthat | 3.0.2.9000 | GitHub (r-lib/testthat@4793514) |
| thematic | 0.1.1 | GitHub (rstudio/thematic@d78d24a) |
| tidyverse | 1.3.0 | standard (@1.3.0) |
| vroom | 1.3.2 | standard (@1.3.2) |
| waiter | 0.2.0 | standard (@0.2.0) |
| xml2 | 1.3.2 | standard (@1.3.2) |
| zeallot | 0.1.0 | standard (@0.1.0) |

1부. 시작하기 다음 네 장의 목표는 가능한 한 빨리 Shiny 앱을 작성하도록 안내하는 것입니다. 1장에서는 작지만 완성된 앱의 모든 주요 구성 요소와 그 연결 방식을 작고 완결된 형태로 보여드릴 것입니다. 2장과 3장에서는 Shiny 앱의 두 가지 주요 부분인 프런트엔드(Frontend, 사용자가 브라우저에서 보는 것)와 백엔드(Backend, 모든 것을 작동시키는 코드)의 세부 사항을 살펴봅니다. 4장에서는 지금까지 배운 개념을 확고히 하기 위한 사례 연구로 마무리합니다.

1장. 첫 번째 Shiny 앱 소개 이 장에서는 간단한 Shiny 앱을 만들어 볼 것입니다. Shiny 앱에 필요한 최소한의 보일러플레이트를 먼저 보여준 다음, 앱을 시작하고 중지하는 방법을 배웁니다. 다음으로 모든 Shiny 앱의 두 가지 핵심 구성 요소를 배웁니다: 앱의 외형을 정의하는 UI(User Interface)와 앱의 동작을 정의하는 서버 함수(Server Function)입니다. Shiny는 반응형 프로그래밍을 사용하여 입력이 변경될 때 출력을 자동으로 업데이트하므로, 장의 마지막 부분에서 Shiny 앱의 세 번째 중요한 구성 요소인 반응형 표현식(Reactive Expression)을 배웁니다.

Shiny가 아직 설치되어 있지 않다면 지금 설치하세요:

install.packages("shiny")

Shiny가 이미 설치되어 있다면 packageVersion("shiny")로 1.5.0 이상인지 확인하세요. 그런 다음 현재 R 세션에 로드합니다:

library(shiny)

앱 디렉토리와 파일 만들기 Shiny 앱을 만드는 방법은 여러 가지가 있습니다. 가장 간단한 방법은 앱을 위한 새 디렉토리를 만들고 app.R이라는 파일 하나를 넣는 것입니다. 이 app.R 파일은 Shiny에게 앱이 어떻게 보여야 하고 어떻게 동작해야 하는지 알려줍니다. 새 디렉토리를 만들고 다음 내용의 app.R 파일을 추가해 보세요:

library(shiny)
ui <- fluidPage(
  "Hello, world!"
)
server <- function(input, output, session) {
}
shinyApp(ui, server)

이것은 완전하지만 사소한 Shiny 앱입니다! 앞의 코드를 자세히 살펴보면, app.R은 네 가지 일을 합니다:

1. library(shiny)를 호출하여 shiny 패키지를 로드합니다.
2. 사용자가 상호작용하는 HTML 웹페이지인 사용자 인터페이스를 정의합니다. 이 경우에는 "Hello, world!"라는 단어를 포함하는 페이지입니다.
3. 서버 함수를 정의하여 앱의 동작을 지정합니다. 현재는 비어 있어서 앱이 아무것도 하지 않지만, 곧 다시 돌아와 살펴볼 것입니다.
4. shinyApp(ui, server)를 실행하여 UI와 서버로부터 Shiny 애플리케이션을 구성하고 시작합니다.

팁 RStudio에서 새 앱을 만드는 두 가지 편리한 방법이 있습니다:

File → New Project를 클릭한 다음 New Directory와 Shiny Web Application을 선택하여 기본 앱이 포함된 새 디렉토리와 app.R 파일을 한 번에 만듭니다.
이미 app.R 파일을 만들었다면, shinyapp을 타이핑하고 Shift+Tab을 눌러 앱 보일러플레이트를 빠르게 추가할 수 있습니다.

시작과 중지 이 앱을 실행하는 방법은 여러 가지가 있습니다:

도구 모음의 Run App(그림 1-1) 버튼을 클릭합니다.
키보드 단축키를 사용합니다: Cmd/Ctrl+Shift+Enter.
RStudio를 사용하지 않는 경우 전체 문서를 source()1 하거나 app.R이 포함된 디렉토리의 경로와 함께 shiny::runApp()을 호출할 수 있습니다.

그림 1-1. Run App 버튼은 소스 창의 오른쪽 상단에 있습니다.

이 중 하나를 선택하고 그림 1-2와 같은 앱이 나타나는지 확인하세요. 축하합니다! 첫 번째 Shiny 앱을 만들었습니다.


그림 1-2. 앞의 코드를 실행했을 때 보이는 매우 기본적인 Shiny 앱입니다. 앱을 닫기 전에 RStudio로 돌아가 R 콘솔을 살펴보세요. 다음과 같은 내용이 표시되는 것을 확인할 수 있습니다:

#> Listening on http://127.0.0.1:3827

여기서는 앱의 URL을 알려줍니다. 127.0.0.1은 "이 컴퓨터"를 의미하는 표준 주소이고, 3827은 무작위로 할당된 포트 번호입니다. 해당 URL을 호환 가능한 웹 브라우저에 입력하면 앱의 다른 복사본을 열 수 있습니다.

R이 바쁘게 동작하고 있다는 점도 눈여겨보세요. R 프롬프트가 보이지 않고 콘솔 툴바에 정지 아이콘(Stop-Sign Icon)이 표시됩니다. Shiny 앱이 실행되는 동안 R 콘솔을 "차단(Blocking)"합니다. Shiny 앱이 중지될 때까지 R 콘솔에서 새로운 명령을 실행할 수 없다는 뜻입니다. 앱을 중지하고 콘솔에 다시 접근하려면 다음 옵션 중 하나를 사용하면 됩니다:

1. R 콘솔 툴바의 정지 아이콘을 클릭합니다.
2. 콘솔을 클릭한 다음 Esc 키를 누릅니다(RStudio를 사용하지 않을 경우 Ctrl+C를 누릅니다).
3. Shiny 앱 창을 닫습니다.

Shiny 앱 개발의 기본 워크플로우는 코드를 작성하고, 앱을 실행하고, 앱을 사용해 보고, 다시 코드를 작성하는 과정을 반복하는 것입니다. RStudio를 사용할 경우 앱을 중지하고 다시 시작하지 않아도 변경 사항을 확인할 수 있습니다. 툴바의 "Reload App" 버튼을 누르거나 Cmd/Ctrl+Shift+Enter 키보드 단축키를 사용하면 됩니다. 다른 워크플로우 패턴은 5장에서 다루겠습니다.

UI 컨트롤 추가

다음으로 UI에 일부 입력과 출력을 추가하여 앱이 너무 미니멀하지 않게 만들 것입니다. datasets 패키지에 포함된 모든 내장 데이터프레임을 보여주는 매우 간단한 앱을 만들 것입니다. ui를 다음 코드로 교체하세요:

ui <- fluidPage(
  selectInput("dataset", label = "Dataset",
              choices = ls("package:datasets")),
  verbatimTextOutput("summary"),
  tableOutput("table")
)

이 예시는 네 가지 새로운 함수를 사용합니다:

fluidPage()
페이지의 기본적인 시각적 구조를 설정하는 레이아웃 함수입니다. "단일 페이지 레이아웃"에서 자세히 알아보겠습니다.

selectInput()
사용자가 값을 제공하여 앱과 상호작용할 수 있게 하는 입력 컨트롤(Input Control)입니다. 이 경우 "Dataset"이라는 레이블이 있는 셀렉트 박스로, R에 포함된 내장 데이터셋 중 하나를 선택할 수 있습니다. "입력"에서 자세히 알아보겠습니다.

verbatimTextOutput()과 tableOutput()
렌더링된 출력을 어디에 배치할지 Shiny에게 알려주는 출력 컨트롤(Output Control)입니다(방식은 곧 살펴볼 것입니다). verbatimTextOutput()은 코드를 표시하고, tableOutput()은 테이블을 표시합니다. "출력"에서 자세히 알아보겠습니다.

레이아웃 함수, 입력, 출력은 서로 다른 용도를 가지지만, 본질적으로는 같은 방식으로 동작합니다. 모두 HTML을 생성하는 화려한 방법일 뿐이고, Shiny 앱 외부에서 이 중 하나를 호출하면 콘솔에 HTML이 출력되는 것을 볼 수 있습니다. 여러 레이아웃과 컨트롤이 내부에서 어떻게 작동하는지 직접 살펴보는 것을 두려워하지 마세요.

앱을 다시 실행해 보세요. 이제 그림 1-3과 같이 셀렉트 박스가 포함된 페이지가 나타납니다. Shiny에게 입력과 출력이 어떻게 연결되는지 아직 알려주지 않았기 때문에, 입력만 보이고 두 출력은 보이지 않습니다.

그림 1-3. UI가 추가된 datasets 앱.

동작 추가

다음으로 서버 함수에서 출력을 정의하여 생명을 불어넣을 것입니다.

Shiny는 반응형 프로그래밍(Reactive Programming)을 사용하여 앱을 대화형으로 만듭니다. 반응형 프로그래밍에 대해서는 3장에서 자세히 알아보겠지만, 지금은 Shiny에게 계산을 수행하는 방법을 알려주는 것이지, 실제로 계산을 명령하는 것이 아니라는 점만 알아두세요. 누군가에게 레시피를 제공하는 것과 샌드위치를 만들어 달라고 요구하는 것의 차이와 같습니다. 샘플 앱에서 요약과 테이블 출력을 채우는 방법을 Shiny에게 알려줄 것입니다.

비어 있는 서버 함수를 다음으로 교체하세요:

server <- function(input, output, session) {
  output$summary <- renderPrint({
    dataset <- get(input$dataset, "package:datasets")
    summary(dataset)
  })

  output$table <- renderTable({
    dataset <- get(input$dataset, "package:datasets")
    dataset
  })
}

대입 연산자(<-)의 왼쪽에 있는 output$ID는 해당 ID를 가진 Shiny 출력의 "레시피"를 제공하고 있음을 나타냅니다. 대입의 오른쪽은 사용자가 제공하는 코드를 감싸는 특정 렌더 함수(Render Function)를 사용합니다. 각 render{Type} 함수는 특정 유형의 출력(텍스트, 테이블, 플롯 등)을 생성하도록 설계되었으며, 일반적으로 {type}Output 함수와 쌍을 이룹니다. 예를 들어, 이 앱에서 renderPrint()는 verbatimTextOutput()과 쌍을 이루어 고정폭(verbatim) 텍스트로 통계 요약을 표시하고, renderTable()은 tableOutput()과 쌍을 이루어 입력 데이터를 테이블로 표시합니다.

앱을 다시 실행하고 놀아보세요. 입력을 변경할 때 출력이 어떻게 변하는지 지켜보세요. 그림 1-4는 앱을 열었을 때 나타나야 하는 모습입니다.

그림 1-4. 출력과 입력을 연결하는 서버 함수를 제공함으로써 완전히 기능하는 앱을 갖게 되었습니다. 입력 데이터셋을 변경할 때마다 요약과 테이블이 업데이트되는 것을 확인할 수 있습니다. 이 의존성은 출력 함수 내부에서 input$dataset을 참조함으로써 암묵적으로 생성됩니다. input$dataset은 ID가 dataset인 UI 컴포넌트의 현재 값으로 채워지며, 해당 값이 변경될 때마다 출력이 자동으로 업데이트됩니다. 이것이 반응성(Reactivity)의 본질입니다. 출력은 입력이 변경되면 자동으로 반응(재계산)합니다.

반응형 표현식으로 중복 감소

이 간단한 예시에서도 중복된 코드가 있습니다. 다음 줄은 두 출력 모두에 나타납니다:

dataset <- get(input$dataset, "package:datasets")

어떤 종류의 프로그래밍이든, 중복된 코드가 있는 것은 바람직하지 않습니다. 계산 자원을 낭비할 수 있고, 더 중요한 것은 코드를 유지하거나 디버깅하기 어렵게 만들기 때문입니다. 여기서는 중요하지 않지만, 매우 간단한 맥락에서 기본적인 아이디어를 설명하고 싶었습니다. 전통적인 R 스크립팅에서는 두 가지 기술을 사용하여 중복된 코드를 처리합니다. 변수를 사용하여 값을 캡처하거나 함수를 사용하여 계산을 캡처합니다. 불행히도, 이 두 가지 접근 방식 모두 여기서는 작동하지 않습니다. "왜 반응형 프로그래밍이 필요한가?"에서 그 이유를 알게 될 것이며, 새로운 메커니즘이 필요합니다: 반응형 표현식(Reactive Expression)입니다.

반응형 표현식은 코드 블록을 reactive({...})로 감싸고 변수에 대입하여 만들고, 함수처럼 호출하여 사용합니다. 함수처럼 보이지만, 반응형 표현식은 중요한 차이점이 있습니다: 처음 호출될 때만 실행되고, 결과를 캐시(Cache)한 다음 업데이트가 필요할 때까지 유지됩니다. 다음 코드에 보이는 대로 server()를 반응형 표현식을 사용하도록 업데이트할 수 있습니다. 동일하게 작동하지만, 데이터셋을 한 번만 가져오면 되므로 두 번 가져오는 것보다 약간 더 효율적입니다:

server <- function(input, output, session) {
  # 반응형 표현식 생성
  dataset <- reactive({get(input$dataset, "package:datasets")})
  output$summary <- renderPrint({
    summary(dataset())
  })
  output$table <- renderTable({
    dataset()
  })
}

주의: 함수를 호출할 때 소괄호()를 사용하는 것과 마찬가지로 dataset()으로 dataset을 호출해야 합니다. 반응형 표현식은 일반 함수와 마찬가지로 항상 소괄호를 사용하여 호출합니다. 데이터셋을 가져오고 요약과 테이블을 만드는 코드를 dataset이라는 변수에 캡처함으로써 코드 중복을 제거했습니다. 데이터셋을 변경할 때 Shiny는 이 두 출력이 모두 dataset이라는 하나의 반응형 표현식에 의존한다는 것을 파악하고, 변경이 필요할 때마다 dataset을 업데이트한 다음 요약과 테이블을 업데이트합니다. 도전적인 과제를 풀기 전에, 대화형으로 반응성의 동작을 탐색하는 것을 추천합니다:

1. 앱을 시작합니다.
2. 데이터셋을 선택합니다.
3. 요약과 테이블을 서서히 변경합니다.
4. 또는 적어도 방법을 알 때까지 변경하지 마세요.

이 탐색을 통해 Shiny의 핵심 메커니즘과 관련 메시지 상자를 이해할 수 있을 것입니다. 다음 장에서 살펴볼 모든 UI 컨트롤과 레이아웃이 작동하는 방식이기 때문입니다.

코드 실행의 타이밍 Shiny가 렌더링 함수를 호출할 때, 사용자 코드는 결과를 바로 반환하지 않습니다. 함수를 thunk라고 부르는 인자로 감쌉니다. 이 thunk는 Shiny가 수행할 작업 목록에 추가되지만, 나중에 수행됩니다. Shiny는 향후 작업을 예약하는 "반응적인" 프로그래밍 모델을 사용합니다. Shiny에게 무엇을 하라고 말하고 나면 Shiny는 나중에 실제 작업을 수행할 때까지 기다립니다. 모든 계산은 Shiny에게 가능할 때(개입할 사용자가 없을 때) 수행할 작업을 알려주는 종류의 함수로 시작된다는 것을 명심하세요. 프로그래머의 관점에서 프로그램의 전체 흐름을 완전히 제어할 수는 없습니다. 이 모델에 익숙해지면 Shiny로 코드를 작성하는 것이 약간 어색하게 느껴질 수 있지만, 반응성을 제대로 이해하면 프로그래머의 관점에서 어떻게 보이든 Shiny에게 적합한 방식으로 동작하는 앱을 만들 수 있습니다.


# 반응형 표현식을 함수처럼 호출하여 사용
summary(dataset())
})

output$table <- renderTable({
  dataset()
})
```

반응형 프로그래밍은 여러 차례 다시 다루겠지만, 입력, 출력, 반응형 표현식에 대한 개략적인 지식만으로도 꽤 유용한 Shiny 앱을 만들 수 있습니다!

## 요약

이 장에서는 간단한 앱을 만들었습니다. 흥미롭거나 유용하지는 않지만, 기존 R 지식을 사용하여 웹 앱을 얼마나 쉽게 만들 수 있는지 확인할 수 있었습니다. 다음 두 장에서는 Shiny의 두 가지 기본 구성 요소인 사용자 인터페이스와 반응형 프로그래밍에 대해 자세히 알아볼 것입니다.

지금이 바로 Shiny 치트 시트(Shiny Cheat Sheet)를 챙기기에 좋은 시점입니다. Shiny 앱의 주요 구성 요소를 떠올리는 데 도움이 되는 훌륭한 자료입니다.

그림 1-5. Shiny 치트 시트.

## 연습 문제

1. 사용자의 이름으로 인사하는 앱을 만드세요. 아직 모든 필요한 함수를 알지 못할 수 있으므로, 몇 줄의 코드를 여기에 포함시켰습니다. 어떤 줄을 사용할지 생각한 다음 Shiny 앱의 적절한 위치에 복사해서 붙여넣으세요:

```r
tableOutput("mortgage")
output$greeting <- renderText({
  paste0("Hello ", input$name)
})
numericInput("age", "How old are you?", value = NA)
textInput("name", "What's your name?")
textOutput("greeting")
output$histogram <- renderPlot({
  hist(rnorm(1000))
}, res = 96)
```

2. 친구가 1부터 50 사이의 숫자(x)를 설정하고, 이 숫자에 5를 곱한 결과를 표시하는 앱을 설계하고 싶어 한다고 가정해 봅시다. 이것이 그들의 첫 번째 시도입니다:

```r
library(shiny)
ui <- fluidPage(
  sliderInput("x", label = "If x is",
              min = 1, max = 50, value = 30),
  "then x times 5 is",
  textOutput("product")
)
server <- function(input, output, session) {
  output$product <- renderText({
    x * 5
  })
}
shinyApp(ui, server)
```

안타깝게도 오류가 있습니다. 오류를 찾아 수정하는 데 도움을 줄 수 있나요?

3. 이전 연습의 앱을 확장하여 사용자가 곱셈 인수 y의 값을 설정할 수 있게 하세요.这样 앱은 x * y의 값을 반환합니다. 최종 결과는 다음과 같아야 합니다:.

4. 다음 앱을 살펴보세요. 앞의 연습에서 설명한 마지막 앱에 몇 가지 기능을 추가한 것입니다. 새로운 것이 무엇인가요? 반응형 표현식을 사용하여 앱의 중복 코드를 어떻게 줄일 수 있을까요?

```r
library(shiny)
ui <- fluidPage(
  sliderInput("x", "If x is", min = 1, max = 50, value = 30),
  sliderInput("y", "and y is", min = 1, max = 50, value = 5),
  "then, (x * y) is",
  textOutput("product"),
  "and, (x * y) + 5 is",
  textOutput("product_plus5"),
  "and (x * y) + 10 is",
  textOutput("product_plus10")
)
server <- function(input, output, session) {
  output$product <- renderText({
    product <- input$x * input$y
    product
  })
  output$product_plus5 <- renderText({
    product <- input$x * input$y
    product + 5
  })
  output$product_plus10 <- renderText({
    product <- input$x * input$y
    product + 10
  })
}
shinyApp(ui, server)
```

5. 다음 앱은 이 장에서 이전에 본 앱과 매우 유사합니다. 패키지에서 데이터셋을 선택하면(이번에는 ggplot2 패키지를 사용합니다) 앱이 데이터의 요약과 플롯을 출력합니다. 좋은 관행을 따르고 코드 중복을 피하기 위해 반응형 표현식도 사용합니다. 하지만 다음 코드에는 세 가지 버그가 있습니다. 버그를 찾아 수정할 수 있나요?

```r
library(shiny)
library(ggplot2)
datasets <- c("economics", "faithfuld", "seals")
ui <- fluidPage(
  selectInput("dataset", "Dataset", choices = datasets),
  verbatimTextOutput("summary"),
  tableOutput("plot")
)
server <- function(input, output, session) {
  dataset <- reactive({
    get(input$dataset, "package:ggplot2")
  })
  output$summary <- renderPrint({
    summary(dataset())
  })
  output$plot <- renderPlot({
    plot(dataset)
  }, res = 96)
}
shinyApp(ui, server)
```

1 바깥쪽의 추가 ()는 중요합니다. shinyApp()은 인쇄될 때만 앱을 생성하며, ()는 source되는 파일에서 마지막 결과를 강제로 인쇄합니다. 그렇지 않으면 눈에 보이지 않게 반환됩니다.

2 Shiny는 모든 최신 브라우저를 지원하기 위해 노력하고 있습니다. R 세션에서 Shiny를 직접 실행할 때 Internet Explorer 11 이전 버전은 호환되지 않습니다. 하지만 Shiny Server나 ShinyApps.io에 배포된 Shiny 앱은 IE10과 작동할 수 있습니다(IE 이전 버전은 더 이상 지원되지 않음).

# 2장. 기본 UI

이제 기본적인 앱을 익혔으므로, Shiny의 작동 방식을 설명하는 세부 사항을 탐색할 수 있습니다. 이전 장에서 보았듯이, Shiny는 사용자 인터페이스를 생성하는 코드(프런트엔드)와 앱의 동작을 구동하는 코드(백엔드)의 분리를 장려합니다. 이 장에서는 프런트엔드에 초점을 맞추고 Shiny가 제공하는 HTML 입력과 출력을 빠르게 둘러볼 것입니다. 이를 통해 다양한 유형의 데이터를 캡처하고 다양한 유형의 R 출력을 표시할 수 있습니다. 입력과 출력을 연결하는 방법은 아직 많지 않지만, 6장에서 다시 살펴볼 것입니다. 여기서는 주로 Shiny 자체에 내장된 입력과 출력을 다루겠습니다.

하지만 shinyWidgets, colourpicker, sortable과 같은 확장 패키지의 풍부하고 활발한 커뮤니티가 있습니다. Nan Xiao는 다른 Shiny 패키지의 활발하고 포괄적인 목록을 유지하고 있습니다. 예상대로 Shiny 패키지를 로드하는 것부터 시작하겠습니다:

```r
library(shiny)
```

## 입력

이전 장에서 보았듯이, sliderInput(), selectInput(), textInput(), numericInput() 등의 함수를 사용하여 UI 사양에 입력 컨트롤을 삽입합니다. 이제 모든 입력 함수에 공통으로 적용되는 구조를 논의하고 Shiny에 내장된 입력을 빠르게 살펴볼 것입니다.

### 공통 구모

모든 입력 함수는 동일한 첫 번째 인수를 가집니다: `inputId`. 이것이 프런트엔드와 백엔드를 연결하는 데 사용되는 식별자입니다. UI에 ID가 "name"인 입력이 있다면, 서버 함수는 input$name으로 접근합니다.

inputId에는 두 가지 제약이 있습니다:

1. 문자, 숫자, 밎표시만 포함하는 단일 문자열이어야 합니다(공백, 하이픈, 마침표 또는 기타 특수 문자는 허용되지 않음). R에서 변수를 지명하듯이 지명하세요.
2. 고유해야 합니다. 고유하지 않으면 서버 함수에서 이 컨트롤에 접근할 방법이 없습니다!

대부분의 입력 함수는 `label`이라는 두 번째 매개변수를 가집니다. 이는 컨트롤에 대한 사람이 읽을 수 있는 레이블을 만드는 데 사용됩니다. Shiny는 이 문자열에 아무 제한을 두지 않지만, 앱이 사람이 사용할 수 있도록 하려면 신중하게 고려해야 합니다!

세 번째 매개변수는 일반적으로 `value`로, 가능한 경우 기본값을 설정할 수 있습니다. 나머지 매개변수는 컨트롤에 고유합니다. 입력을 만들 때는 `inputId`와 `label` 인수는 위치로, 나머지 모든 인수는 이름으로 제공하는 것을 추천합니다:

```r
sliderInput("min", "Limit (minimum)", value = 50, min = 0, max = 100)
```

다음 섹션에서는 Shiny에 내장된 입력을 설명하며, 생성하는 컨트롤 유형에 따라 느슨하게 분류합니다. 목표는 모든 인수를 상세히 설명하는 것이 아니라 선택 사항을 빠르게 개괄하는 것입니다. 여기서는 각 컨트롤의 가장 중요한 매개변수를 보여주지만, 전체 세부 사항은 문서를 읽어야 합니다.

### 자유 텍스트

textInput()으로 소량의 텍스트를 수집하고, passwordInput()으로 비밀번호를 수집하며,1 textAreaInput()으로 단락 텍스트를 수집합니다:

```r
ui <- fluidPage(
  textInput("name", "What's your name?"),
  passwordInput("password", "What's your password?"),
  textAreaInput("story", "Tell me about yourself", rows = 3)
)
```

텍스트에 특정 속성이 있는지 확인하려면 validate()를 사용할 수 있으며, 이에 대해서는 8장에서 다시 살펴볼 것입니다.

### 숫자 입력

numericInput()으로 제한된 텍스트 상자를 만들거나 sliderInput()으로 슬라이더를 만들어 숫자 값을 수집합니다. sliderInput()의 기본값에 길이가 2인 숫자 벡터를 제공하면 양쪽 끝이 있는 "범위" 슬라이더가 생성됩니다:

```r
ui <- fluidPage(
  numericInput("num", "Number one",
               value = 0, min = 0, max = 100),
  sliderInput("num2", "Number two",
              value = 50, min = 0, max = 100),
  sliderInput("rng", "Range",
              value = c(10, 20), min = 0, max = 100)
)
```

일반적으로 슬라이더는 작은 범위나 정확한 값이 중요하지 않은 경우에만 사용하는 것이 좋습니다. 작은 슬라이더에서 숫자를 정확하게 선택하려고 하면 좌절감을 느낄 수 있습니다! 슬라이더는 매우 커스터마이징이 가능하며, 모양을 조정하는 방법이 많이 있습니다. 자세한 내용은 `?sliderInput`과 "슬라이더 사용"을 참조하세요.

### 날짜

dateInput()으로 단일 날짜를 수집하거나 dateRangeInput()으로 이틀의 범위를 수집합니다. 이 함수들은 편리한 캘린더 피커를 제공하며, datesdisabled 및 daysofweekdisabled와 같은 추가 인수로 유효한 입력의 집합을 제한할 수 있습니다:

```r
ui <- fluidPage(
  dateInput("dob", "When were you born?"),
  dateRangeInput("holiday", "When do you want to go on vacation next?")
)
```

날짜 형식, 언어, 주 시작일은 미국 표준을 따릅니다. 국제 청중을 대상으로 앱을 만드는 경우, format, language, weekstart를 설정하여 날짜가 사용자에게 자연스럽도록 하세요.

### 제한된 선택지

미리 지정된 옵션 집합에서 선택할 수 있는 두 가지 접근 방식이 있습니다: selectInput()과 radioButtons():

```r
animals <- c("dog", "cat", "mouse", "bird", "other", "I hate animals")
ui <- fluidPage(
  selectInput("state", "What's your favourite state?",
              state.name),
  radioButtons("animal", "What's your favourite animal?",
               animals)
)
```

라디오 버튼에는 두 가지 좋은 특성이 있습니다: 모든 가능한 옵션을 표시하여 짧은 목록에 적합하고, choiceNames/choiceValues 인수를 통해 평문 텍스트가 아닌 옵션을 표시할 수 있습니다. choiceNames는 사용자에게 표시되는 것을 결정하고, choiceValues는 서버 함수에서 반환되는 것을 결정합니다:

```r
ui <- fluidPage(
  radioButtons("rb", "Choose one:",
    choiceNames = list(
      icon("angry"),
      icon("smile"),
      icon("sad-tear")
    ),
    choiceValues = list("angry", "happy", "sad")
  )
)
```

selectInput()으로 만든 드롭다운은 옵션 수에 관계없이 동일한 양의 공간을 차지하므로 더 긴 옵션에 더 적합합니다. multiple = TRUE를 설정하여 사용자가 여러 요소를 선택할 수도 있습니다:

```r
ui <- fluidPage(
  selectInput(
    "state", "What's your favourite state?",
    state.name,
    multiple = TRUE
  )
)
```

가능한 옵션이 매우 많으면 "서버 측" selectInput()을 사용하는 것이 좋습니다. 이렇게 하면 가능한 옵션의 전체 집합이 UI에 포함되지 않아(로딩이 느려질 수 있음) 대신 서버에서 필요할 때 전송됩니다. 이 고급 주제에 대한 자세한 내용은 Shiny 문서에서 알아볼 수 있습니다.

```r
ui <- fluidPage(
  selectInput(
    "state", "What's your favourite state?",
    state.name,
    selected = "Alabama"
  )
)
```

radioButtons로는 여러 값을 선택할 수 없지만, 개념적으로 유사한 대안이 있습니다: checkboxGroupInput():

```r
ui <- fluidPage(
  checkboxGroupInput("animal", "What animals do you like?",
                     animals)
)
```

단일 예/아니오 질문에 대한 단일 체크박스가 필요한 경우 checkboxInput()을 사용합니다:

```r
ui <- fluidPage(
  checkboxInput("cleanup", "Clean up?", value = TRUE),
  checkboxInput("shutdown", "Shutdown?")
)
```

### 파일 업로드

fileInput()으로 사용자가 파일을 업로드할 수 있게 합니다:

```r
ui <- fluidPage(
  fileInput("upload", NULL)
)
```

fileInput()은 서버 측에서 특별한 처리가 필요하며, 9장에서 자세히 다룹니다.

### 액션 버튼

actionButton() 또는 actionLink()로 사용자가 작업을 수행하게 합니다:

```r
ui <- fluidPage(
  actionButton("click", "Click me!"),
  actionButton("drink", "Drink me!",
               icon = icon("cocktail"))
)
```

액션 링크와 버튼은 서버 함수에서 observeEvent() 또는 eventReactive()와 자연스럽게 쌍을 이룹니다. 이 중요한 함수들은 아직 배우지 않았지만, "평가 타이밍 제어"에서 다시 살펴볼 것입니다.

appearance를 커스터마이징하려면 class 인수에 "btn-primary", "btn-success", "btn-info", "btn-warning", "btn-danger" 중 하나를 사용합니다. "btn-lg", "btn-sm", "btn-xs"로 크기를 변경할 수도 있습니다. 마지막으로 "btn-block"을 사용하여 버튼이 포함된 요소의 전체 너비를 차지하게 할 수 있습니다:

```r
ui <- fluidPage(
  fluidRow(
    actionButton("click", "Click me!",
                 class = "btn-danger"),
    actionButton("drink", "Drink me!",
                 class = "btn-lg btn-success")
  ),
  fluidRow(
    actionButton("eat", "Eat me!",
                 class = "btn-block")
  )
)
```

class 인수는 기본 HTML의 class 속성을 설정하여 요소의 스타일 방식에 영향을 줍니다. 다른 옵션을 보려면 Shiny가 사용하는 CSS 디자인 시스템인 Bootstrap의 문서를 읽을 수 있습니다.

## 연습 문제

1. 공간이 제한될 때, 텍스트 입력 영역 안에 나타나는 플레이스홀더를 사용하여 텍스트 상자에 레이블을 붙이는 것이 유용합니다. 다음 UI를 생성하려면 textInput()을 어떻게 호출해야 하나요?

2. sliderInput()의 문서를 주의 깊게 읽어서 다음에 표시된 것처럼 날짜 슬라이더를 만드는 방법을 알아내세요:

3. 슬라이더에서 선택 가능한 값 사이의 간격이 5인 0에서 100 사이의 값을 선택하는 슬라이더 입력을 만드세요. 그런 다음, 사용자가 재생 버튼을 누를 때 입력 위젯이 범위를 자동으로 스크롤하도록 애니메이션을 추가하세요.

4. selectInput()에 중간 정도로 긴 목록이 있는 경우, 목록을 여러 부분으로 나누는 하위 제목을 만드는 것이 유용합니다. 문서를 읽고 방법을 알아내세요(힌트: 기본 HTML은 `<optgroup>`이라고 합니다).

## 출력

UI의 출력은 나중에 서버 함수로 채워지는 자리 표시자를 만듭니다. 입력과 마찬가지로 출력은 고유 ID를 첫 번째 인수로 사용합니다.2 UI 사양에서 ID가 "plot"인 출력을 만들면 서버 함수에서 output$plot으로 접근합니다.

프런트엔드의 각 출력 함수는 백엔드의 렌더 함수와 쌍을 이룹니다. 세 가지 주요 유형의 출력이 있으며, 이는 보고서에 일반적으로 포함되는 세 가지 항목에 해당합니다: 텍스트, 테이블, 플롯.

다음 섹션에서는 프런트엔드의 출력 함수 기본 사항과 백엔드의 해당 렌더 함수를 보여줍니다.

### 텍스트

textOutput()으로 일반 텍스트를, verbatimTextOutput()으로 고정폭 텍스트(예: 콘솔 출력)를 표시합니다:

```r
ui <- fluidPage(
  textOutput("text"),
  verbatimTextOutput("code")
)
server <- function(input, output, session) {
  output$text <- renderText({
    "Hello friend!"
  })
  output$code <- renderPrint({
    summary(1:10)
  })
}
```

{}은 여러 줄의 코드를 실행해야 할 때만 렌더 함수에서 필요하다는 점에 유의하세요. 곧 알게 되겠지만, 렌더 함수에서는 가능한 한 적은 계산을 수행하는 것이 좋으며, 이는 {}를 생략할 수 있다는 뜻입니다. 앞의 서버 함수를 더 간결하게 작성하면 다음과 같습니다:

```r
server <- function(input, output, session) {
  output$text <- renderText("Hello friend!")
  output$code <- renderPrint(summary(1:10))
}
```

두 렌더 함수는 약간 다르게 동작한다는 점에 유의하세요:

- `renderText()`: 결과를 단일 문자열로 결합하며, 일반적으로 textOutput()과 쌍을 이룹니다.
- `renderPrint()`: R 콘솔에서처럼 결과를 인쇄하며, 일반적으로 verbatimTextOutput()과 쌍을 이룹니다.

간단한 앱으로 차이점을 볼 수 있습니다:

```r
ui <- fluidPage(
  textOutput("text"),
  verbatimTextOutput("print")
)
server <- function(input, output, session) {
  output$text <- renderText("hello!")
  output$print <- renderPrint("hello!")
}
```

이는 기본 R에서 cat()과 print()의 차이와 같습니다.

### 테이블

데이터프레임을 테이블로 표시하는 두 가지 옵션이 있습니다:

- `tableOutput()`과 `renderTable()`: 정적 테이블을 렌더링하여 모든 데이터를 한 번에 표시합니다.
- `dataTableOutput()`과 `renderDataTable()`: 동적 테이블을 렌더링하여 고정된 행 수와 함께 어떤 행이 표시될지 제어하는 컨트롤을 표시합니다.

tableOutput()은 작고 고정된 요약(예: 모델 계수)에 가장 유용하고, dataTableOutput()은 완전한 데이터프레임을 사용자에게 노출하려는 경우에 가장 적합합니다. 다음 코드는 tableOutput()과 dataTableOutput()의 매우 간단한 예시를 보여줍니다:

```r
ui <- fluidPage(
  tableOutput("static"),
  dataTableOutput("dynamic")
)
server <- function(input, output, session) {
  output$static <- renderTable(head(mtcars))
  output$dynamic <- renderDataTable(mtcars,
    options = list(pageLength = 5))
}
```

dataTableOutput()의 출력을 더 세밀하게 제어하려면 Greg Lin의 reactable 패키지를 강력히 추천합니다.

### 플롯

plotOutput()과 renderPlot()을 사용하여 모든 유형의 R 그래픽(예: 기본 또는 ggplot2)을 표시할 수 있습니다:

```r
ui <- fluidPage(
  plotOutput("plot", width = "400px")
)
server <- function(input, output, session) {
  output$plot <- renderPlot(plot(1:5), res = 96)
}
```

기본적으로 plotOutput()은 컨테이너의 전체 너비를 차지하며(곧 자세히 다룸) 높이가 400픽셀입니다. height와 width 인수로 이 기본값을 재정의할 수 있습니다. res = 96을 항상 설정하는 것을 권장합니다. 이렇게 하면 Shiny 플롯이 RStudio에서 보이는 것과 최대한 일치하게 됩니다.

플롯은 출력이면서 동시에 입력으로도 작동할 수 있기 때문에 특별합니다. plotOutput()에는 click, dblclick, hover와 같은 여러 인수가 있습니다. 이 인수에 click = "plot_click"과 같은 문자열을 전달하면, 플롯에서의 사용자 상호작용(예: 플롯 클릭)을 처리하는 데 사용할 수 있는 반응형 입력(input$plot_click)이 생성됩니다. 대화형 플롯에 대해서는 7장에서 다시 살펴볼 것입니다.

### 다운로드

downloadButton() 또는 downloadLink()로 사용자가 파일을 다운로드할 수 있게 합니다. 서버 함수에서 새로운 기술이 필요하므로, 9장에서 다시 살펴볼 것입니다.

## 연습 문제

1. 다음 렌더 함수 각각은 textOutput()과 verbatimTextOutput() 중 어떤 것과 쌍을 이뤄야 하나요?

a. `renderPrint(summary(mtcars))`
b. `renderText("Good morning!")`
c. `renderPrint(t.test(1:5, 2:6))`
d. `renderText(str(lm(mpg ~ wt, data = mtcars)))`

2. "플롯"의 Shiny 앱을 다시 만들되, 이번에는 height를 300px로, width를 700px로 설정하세요. 시각 장애인 사용자가 이것이 무작위 숫자 다섯 개의 산점도임을 알 수 있도록 플롯의 "alt" 텍스트를 설정하세요.

3. renderDataTable() 호출의 옵션을 업데이트하여 데이터는 표시되지만 다른 모든 컨트롤은 억제되도록 하세요(즉, 검색, 정렬, 필터링 명령을 제거). `?renderDataTable`을 읽고 기본 JavaScript 라이브러리의 옵션을 검토해야 합니다:

```r
ui <- fluidPage(
  dataTableOutput("table")
)
server <- function(input, output, session) {
  output$table <- renderDataTable(mtcars,
    options = list(pageLength = 5))
}
```

4. 또는 reactable에 대해 읽고 앞의 앱을 이를 사용하도록 변환하세요.


## 요약

이 장에서는 Shiny 앱의 프런트엔드를 구성하는 주요 입력 및 출력 함수를 소개했습니다. 방대한 정보를 다루었으므로, 한 번 읽고 모두 기억할 것을 기대하지 마세요. 대신 특정 컴포넌트를 찾아볼 때 이 장으로 돌아오세요. 그림을 빠르게 훑고 필요한 코드를 찾을 수 있습니다.

다음 장에서는 Shiny 앱의 백엔드, 즉 사용자 인터페이스에 생명을 불어넣는 R 코드로 이동합니다.

1 `passwordInput()`이 하는 일은 사용자가 입력하는 내용을 숨기는 것뿐으로, 어깨 너머로 들여다보는 사람이 읽지 못하게 합니다. 비밀번호가 우연히 노출되지 않도록 하는 것은 사용자의 몫이므로, 보안 프로그래밍에 대한 훈련을 받지 않는 한 비밀번호 사용을 권장하지 않습니다.

2 입력(inputId)과 출력(outputId)의 인수 이름이 다르다는 점에 유의하세요. 첫 번째 인수의 이름은 너무 중요하기 때문에 사용하지 않으며, 추가 힌트 없이도 그 기능을 기억할 수 있기를 기대합니다.

# 3장. 기본 반응성

## 소개

Shiny에서는 서버 로직을 반응형 프로그래밍(Reactive Programming)으로 표현합니다. 반응형 프로그래밍은 우아하고 강력한 프로그래밍 패러다임이지만, 스크립트를 작성하는 것과 매우 다른 패러다임이기 때문에 처음에는 혼란스러울 수 있습니다. 반응형 프로그래밍의 핵심 아이디어는 의존성 그래프를 지정하여, 입력이 변경될 때 관련된 모든 출력이 자동으로 업데이트되도록 하는 것입니다. 이렇게 하면 앱의 흐름이 상당히 단순해지지만, 모든 것이 어떻게 맞아떨어지는지 이해하는 데는 시간이 걸립니다. 이 장에서는 반응형 프로그래밍에 대한 부드러운 소개를 제공하여, Shiny 앱에서 사용할 가장 일반적인 반응형 구성 요소의 기초를 가르쳐 드립니다.

서버 함수를 개괄하면서 input과 output 인수가 어떻게 작동하는지 더 자세히 논의할 것입니다. 그런 다음 가장 단순한 형태의 반응성(입력이 출력에 직접 연결됨)을 검토한 다음, 반응형 표현식이 중복 작업을 제거하는 방법을 논의합니다. 마지막으로 새로운 Shiny 사용자가 자주 직면하는 일반적인 문제를 검토하며 마무리합니다.

## 서버 함수

앞에서 보았듯이, 모든 Shiny 앱의 핵심은 다음과 같습니다:

```r
library(shiny)

ui <- fluidPage(
  # 프런트엔드 인터페이스
)

server <- function(input, output, session) {
  # 백엔드 로직
}

shinyApp(ui, server)
```

이전 장에서는 프런트엔드의 기본 사항을 다루었습니다. 앱의 모든 사용자에게 표시되는 HTML을 포함하는 ui 객체입니다. 모든 사용자가 동일한 HTML을 받으므로 ui는 단순합니다. 서버는 더 복잡합니다. 각 사용자가 독립적인 앱 버전을 받아야 하기 때문입니다. 사용자가 슬라이더를 움직이면 다른 사용자의 출력이 변경되어서는 안 됩니다.

이 독립성을 달성하기 위해, Shiny는 새 세션이 시작될 때마다 server() 함수를 호출합니다.1 다른 모든 R 함수와 마찬가지로, 서버 함수가 호출되면 함수의 다른 모든 호출과 독립적인 새로운 로컬 환경이 생성됩니다. 이를 통해 각 세션이 고유한 상태를 가지고 함수 내부에서 생성된 변수를 격리할 수 있습니다. 이것이 Shiny에서 수행하는 거의 모든 반응형 프로그래밍이 서버 함수 내부에서 이루어지는 이유입니다.2

서버 함수는 세 가지 매개변수를 받습니다: input, output, session. 서버 함수를 직접 호출하지 않으므로这些 객체를 직접 만들지 않습니다. 대신 세션이 시작될 때 Shiny에 의해 만들어지며, 특정 세션에 연결됩니다. 지금은 input과 output 인수에 초점을 맞추고 session은 이후 장에서 다루겠습니다.

### Input

input 인수는 브라우저에서 보낸 모든 입력 데이터를 포함하는 리스트 형태의 객체이며, input ID에 따라 명명됩니다. 예를 들어, UI에 다음과 같이 input ID가 count인 숫자 입력 컨트롤이 있다면:

```r
ui <- fluidPage(
  numericInput("count", label = "Number of values",
               value = 100)
)
```

input$count로 해당 입력의 값에 접근할 수 있습니다. 처음에는 값 100을 포함하며, 사용자가 브라우저에서 값을 변경하면 자동으로 업데이트됩니다.

일반적인 리스트와 달리, input 객체는 읽기 전용입니다. 서버 함수 내부에서 입력을 수정하려고 하면 오류가 발생합니다:

```r
server <- function(input, output, session) {
  input$count <- 10
}
shinyApp(ui, server)
#> Error: Can't modify read-only reactive value 'count'
```

이 오류는 input이 브라우저에서 일어나고 있는 일을 반영하기 때문에 발생하며, 브라우저는 Shiny의 "단일 진실의 소스(Single Source of Truth)"입니다. R에서 값을 수정할 수 있다면 불일치가 발생할 수 있습니다. 브라우저의 슬라이더는 한 가지를 말하고 input$count는 R에서 다른 것을 말하는 식이 될 수 있습니다. 이렇게 되면 프로그래밍이 어려워질 것입니다!

나중에 8장에서 updateNumericInput()과 같은 함수를 사용하여 브라우저의 값을 수정하는 방법을 배우게 되면, input$count가 그에 따라 업데이트됩니다.

input에 대해 또 한 가지 중요한 점은, 읽을 수 있는 대상을 선택한다는 것입니다. 입력에서 읽으려면 renderText()나 reactive()와 같은 함수가 만든 반응형 컨텍스트(Reactive Context) 안에 있어야 합니다. 이 아이디어는 곧 다시 살펴볼 것이지만, 입력이 변경될 때 출력이 자동으로 업데이트되도록 하는 중요한 제약입니다.

다음 코드는 이 실수를 했을 때 보게 될 오류를 설명합니다:

```r
server <- function(input, output, session) {
  message("The value of input$count is ", input$count)
}
shinyApp(ui, server)
#> Error: Can't access reactive value 'count' outside of
#>   reactive consumer.
#> ℹ Do you need to wrap inside reactive() or observer()?
```

### Output

output은 input과 매우 유사합니다: output ID에 따라 명명된 리스트 형태의 객체입니다. 주요 차이점은 입력을 수신하는 대신 출력을 보내는 데 사용한다는 것입니다. 항상 render 함수와 함께 output 객체를 사용합니다:

```r
ui <- fluidPage(
  textOutput("greeting")
)

server <- function(input, output, session) {
  output$greeting <- renderText("Hello human!")
}
```

(ID는 UI에서는 따옴표로 감싸지만 서버에서는 그렇지 않습니다.)

render 함수는 두 가지 일을 합니다:

1. 출력이 사용하는 입력을 자동으로 추적하는 특수 반응형 컨텍스트를 설정합니다.
2. R 코드의 출력을 웹 페이지에서 표시할 수 있는 HTML로 변환합니다.

input과 마찬가지로 output은 사용 방식에 대해 까다롭습니다:

render 함수를 잊어버린 경우 오류가 발생합니다:

```r
server <- function(input, output, session) {
  output$greeting <- "Hello human"
}
shinyApp(ui, server)
#> Error: Unexpected character object for output$greeting
#> ℹ Did you forget to use a render function?
```

출력에서 읽으려고 하면 오류가 발생합니다:

```r
server <- function(input, output, session) {
  message("The greeting is ", output$greeting)
}
shinyApp(ui, server)
#> Error: Reading from shinyoutput object is not allowed.
```

## 반응형 프로그래밍

앱에 입력만 있거나 출력만 있으면 상당히 지루할 것입니다. Shiny의 진정한 마법은 입력과 출력이 모두 있을 때 일어납니다. 간단한 예시를 살펴봅시다:

```r
ui <- fluidPage(
  textInput("name", "What's your name?"),
  textOutput("greeting")
)

server <- function(input, output, session) {
  output$greeting <- renderText({
    paste0("Hello ", input$name, "!")
  })
}
```

책에서 이것이 어떻게 작동하는지 보여주기는 어렵지만, 그림 3-1에서 최선을 다해 설명합니다. 앱을 실행하고 이름 상자에 입력하면, 입력하면서 인사말이 자동으로 업데이트되는 것을 볼 수 있습니다.3

그림 3-1. 반응성은 입력이 변경될 때 출력이 자동으로 업데이트된다는 것을 의미합니다. 이 앱에서는 "J", "o", "e"를 입력합니다. https://hadley.shinyapps.io/ms-connection 에서 실시간으로 확인하세요.

이것이 Shiny의 핵심 아이디어입니다: 출력이 언제 업데이트되어야 하는지 알려줄 필요가 없습니다. Shiny가 자동으로 파악하기 때문입니다. 어떻게 작동할까요? 함수 본문에서 정확히 무슨 일이 일어나고 있을까요?

서버 함수 내부의 코드를 더 정확하게 생각해 봅시다:

```r
output$greeting <- renderText({
  paste0("Hello ", input$name, "!")
})
```

이 코드를 "'hello'와 사용자의 이름을 합친 다음 렌더링된 텍스트를 output$greeting으로 보낸다"고 읽을 수 있습니다. 하지만 이 정신 모델은 미묘하지만 중요한 방식에서 잘못되어 있습니다. 생각해 보세요: 이 모델에서는 명령을 한 번만 내립니다. 하지만 Shiny는 input$name을 업데이트할 때마다 동작을 수행하므로, 더 많은 일이 일어나고 있어야 합니다.

코드가 Shiny에게 문자열을 만들어 브라우저로 보내라고 말하는 것이 아니라, 필요할 때 문자열을 만드는 방법을 알려주기 때문에 앱이 작동합니다. 코드가 언제(그리고 심지어 whether!) 실행될지는 Shiny에 달려 있습니다. 앱이 시작되자마자 실행될 수도 있고, 상당히 나중에 실행될 수도 있습니다. 여러 번 실행될 수도 있고, 결코 실행되지 않을 수도 있습니다! 이는 Shiny가 변덕스럽다는 것을 암시하는 것이 아니라, 코드 실행 시점을 결정하는 것이 Shiny의 책임이지 사용자의 책임이 아니라는 뜻입니다. 앱을 명령이 아닌 레시피를 Shiny에게 제공하는 것으로 생각하세요.

### 명령형 대 선언형 프로그래밍

명령과 레시피의 차이는 두 가지 중요한 프로그래밍 스타일의 핵심 차이점 중 하나입니다:

- **명령형 프로그래밍(Imperative Programming)**: 특정 명령을 내리면 즉시 실행됩니다. 분석 스크립트에서 익숙한 프로그래밍 스타일입니다: R에게 데이터를 로드하고, 변환하고, 시각화하고, 결과를 디스크에 저장하라고 명령합니다.

- **선언형 프로그래밍(Declarative Programming)**: 더 높은 수준의 목표를 표현하거나 중요한 제약을 설명하고, 누군가가 이를 어떻게 그리고/또는 언제 실행으로 전환할지 결정하도록 의존합니다. 이것이 Shiny에서 사용하는 프로그래밍 스타일입니다.

명령형 코드에서는 "샌드위치를 만들어 줘"라고 말합니다.4 선언형 코드에서는 "냉장고를 열어볼 때마다 샌드위치가 있도록 해줘"라고 말합니다. 명령형 코드는 단호하고, 선언형 코드는 수동 공격적입니다.

대부분의 경우, 선언형 프로그래밍은 엄청나게 자유롭습니다. 전체 목표를 설명하면 소프트웨어가 추가 개입 없이 달성 방법을 파악합니다. 단점은 정확히 원하는 것을 알고 있지만 선언형 시스템이 이해할 수 있는 방식으로 프레이밍하는 방법을 알지 못하는 경우가 가끔 있다는 것입니다.5 이 책의 목표는 이러한 일이 최대한 적게 일어나도록 기초 이론에 대한 이해를 높이는 데 도움을 주는 것입니다.

### 게으름

Shiny의 선언형 프로그래밍의 강점 중 하나는 앱이 극도로 게으를 수 있다는 것입니다. Shiny 앱은 현재 표시되는 출력 컨트롤을 업데이트하는 데 필요한 최소한의 작업만 수행합니다.6 하지만 이 게으름에는 알아야 할 중요한 단점이 있습니다.

다음 서버 함수에 어떤 문제가 있는지 눈치챌 수 있나요?

```r
server <- function(input, output, session) {
  output$greting <- renderText({
    paste0("Hello ", input$name, "!")
  })
}
```

자세히 보면 greting이라고 적었而不是 greeting인 것을 알아챌 수 있을 것입니다. Shiny에서 이 오류는 발생하지 않지만, 원하는 대로 작동하지 않습니다. greting 출력은 존재하지 않으므로 renderText() 내부의 코드는 결코 실행되지 않습니다.

Shiny 앱을 작업하면서 코드가 결코 실행되지 않는 이유를 파악할 수 없는 경우, UI와 서버 함수가 동일한 식별자를 사용하고 있는지 다시 확인하세요.

### 반응형 그래프

Shiny의 게으름에는 또 다른 중요한 속성이 있습니다. 대부분의 R 코드에서는 코드를 위에서 아래로 읽어 실행 순서를 이해할 수 있습니다. Shiny에서는 코드가 필요할 때만 실행되므로这种方法은 작동하지 않습니다. 대신 반응형 그래프(Reactive Graph)를 살펴봐야 하며, 이 그래프는 입력과 출력이 어떻게 연결되는지를 설명합니다.

앞의 앱에 대한 반응형 그래프는 매우 단순하며 그림 3-2에 나와 있습니다.

그림 3-2. 반응형 그래프는 입력과 출력이 어떻게 연결되는지를 보여줍니다.

반응형 그래프는 모든 입력과 출력에 대한 기호를 포함하며, 출력이 입력에 접근할 때마다 입력을 출력에 연결합니다. 이 그래프는 name이 변경될 때마다 greeting이 다시 계산되어야 함을 알려줍니다. 이 관계를 설명할 때, 우리는 흔히 greeting이 name에 반응형 의존성(Reactive Dependency)을 가진다고 말합니다.

입력과 출력에 사용한 그래픽 관례에 유의하세요: name 입력은 greeting 출력에 자연스럽게 맞아떨어집니다. 그림 3-3처럼它们を密に描いて fit하는 방식을 강조할 수도 있지만, 가장 단순한 앱에서만 작동하므로 일반적으로 하지 않습니다.

그림 3-2. 반응형 그래프의 컴포넌트가 사용하는 모양은 연결 방식을 연상시킵니다.

반응형 그래프는 앱이 어떻게 작동하는지를 이해하는 강력한 도구입니다. 앱이 더 복잡해지면, 모든 조각이 어떻게 맞아떨어지는지를 상기시키기 위해 반응형 그래프의 빠른 개요를 그리는 것이 유용합니다. 이 책 전반에 걸쳐 예제가 어떻게 작동하는지를 이해하는 데 도움이 되도록 반응형 그래프를 보여줄 것이며, 나중에 14장에서 그래프를 그려주는 reactlog를 사용하는 방법을 배울 것입니다.

### 반응형 표현식

반응형 그래프에서 보게 될 또 다른 중요한 구성 요소가 있습니다: 반응형 표현식(Reactive Expression)입니다. 반응형 표현식에 대해서는 곧 자세히 다시 살펴볼 것이지만, 지금은 반응형 그래프에 추가 노드를 도입하여 반응형 코드의 중복을 줄이는 도구로 생각하세요. 매우 단순한 앱에서는 반응형 표현식이 필요하지 않지만, 반응형 그래프에 미치는 영향을 볼 수 있도록 하나 추가하겠습니다:

```r
server <- function(input, output, session) {
  string <- reactive(paste0("Hello ", input$name, "!"))
  output$greeting <- renderText(string())
}
```

그림 3-4. 반응형 표현식은 양쪽에 각진 모양으로 그려지며, 입력을 출력에 연결합니다.

반응형 표현식은 입력을 받아 출력을 생성하므로, 입력과 출력의 특징을 모두 결합한 모양을 가집니다. 이 모양이 컴포넌트가 어떻게 맞아떨어지는지를 기억하는 데 도움이 되었으면 합니다.

### 실행 순서

코드의 실행 순서가 오직 반응형 그래프에 의해서만 결정된다는 것을 이해하는 것이 중요합니다. 이는 실행 순서가 줄의 순서에 의해 결정되는 대부분의 R 코드와 다릅니다.

예를 들어, 간단한 서버 함수의 두 줄 순서를 뒤집을 수 있습니다:

```r
server <- function(input, output, session) {
  output$greeting <- renderText(string())
  string <- reactive(paste0("Hello ", input$name, "!"))
}
```

output$greeting이 아직 만들어지지 않은 반응형 표현식 string을 참조하므로 오류가 발생할 것이라고 생각할 수 있습니다. 하지만 Shiny는 게으르기 때문에, 이 코드는 세션이 시작될 때 string이 만들어진 후에만 실행됩니다. 이 코드는 이전과 동일한 반응형 그래프를 생성하므로, 코드가 실행되는 순서는 정확히 동일합니다.

하지만 이렇게 코드를 구성하면 사람이 이해하기 어렵고, 피하는 것이 좋습니다. 대신 반응형 표현식과 출력이 아래쪽이 아닌 위쪽에 정의된 것만 참조하도록 하세요.7 이렇게 하면 코드를 더 쉽게 이해할 수 있습니다.

이 개념은 매우 중요하고 대부분의 다른 R 코드와 다르므로 다시 한번 말씀드리겠습니다: 반응형 코드가 실행되는 순서는 서버 함수 내부의 순서가 아닌 오직 반응형 그래프에 의해서만 결정됩니다.

## 연습 문제

1. 다음 UI가 주어졌을 때:

```r
ui <- fluidPage(
  textInput("name", "What's your name?"),
  textOutput("greeting")
)
```

다음 세 서버 함수 각각에서 발견되는 단순한 오류를 수정하세요. 먼저 코드를 읽어서 문제를 찾아보고, 그런 다음 코드를 실행하여 수정했는지 확인하세요:

```r
server1 <- function(input, output, server) {
  input$greeting <- renderText(paste0("Hello ", name))
}

server2 <- function(input, output, server) {
  greeting <- paste0("Hello ", input$name)
  output$greeting <- renderText(greeting)
}

server3 <- function(input, output, server) {
  output$greting <- paste0("Hello", input$name)
}
```

2. 다음 서버 함수에 대한 반응형 그래프를 그려보세요:

```r
server1 <- function(input, output, session) {
  c <- reactive(input$a + input$b)
  e <- reactive(c() + input$d)
  output$f <- renderText(e())
}

server2 <- function(input, output, session) {
  x <- reactive(input$x1 + input$x2 + input$x3)
  y <- reactive(input$y1 + input$y2)
  output$z <- renderText(x() / y())
}

server3 <- function(input, output, session) {
  d <- reactive(c() ^ input$d)
  a <- reactive(input$a * 10)
  c <- reactive(b() / input$c)
  b <- reactive(a() + input$b)
}
```

3. 왜 이 코드는 실패할까요?

```r
var <- reactive(df[[input$var]])
range <- reactive(range(var(), na.rm = TRUE))
```

왜 range()와 var()는 반응형 표현식에 나쁜 이름일까요?

## 반응형 표현식

우리는 반응형 표현식을 몇 번 빠르게 살펴보았으므로, 그것이 무엇을 하는지 감을 잡았을 것입니다. 이제 더 많은 세부 사항으로 들어가 실제 앱을 구축할 때 왜 그렇게 중요한지 보여주겠습니다.

반응형 표현식이 중요한 이유는 Shiny에게 더 많은 정보를 제공하여 입력이 변경될 때 덜 재계산하도록 하고, 앱을 더 효율적으로 만들며, 반응형 그래프를 단순화하여 사람이 앱을 더 쉽게 이해할 수 있게 하기 때문입니다.

반응형 표현식은 입력과 출력의 특징을 모두 가지고 있습니다:

- 입력처럼, 출력에서 반응형 표현식의 결과를 사용할 수 있습니다.
- 출력처럼, 반응형 표현식은 입력에 의존하며 업데이트가 필요한 시점을 자동으로 알 수 있습니다.

이 이중성은 새로운 용어가 필요함을 의미합니다: 반응형 입력과 표현식을 "생산자(Producers)"라고 부르고, 반응형 표현식과 출력을 "소비자(Consumers)"라고 부릅니다. 그림 3-5가 벤 다이어그램으로 이 관계를 보여줍니다.

그림 3-5. 입력과 표현식은 반응형 생산자이고, 표현식과 출력은 반응형 소비자입니다.

반응형 표현식의 이점을 보려면 더 복잡한 앱이 필요할 것입니다. 먼저 앱을 구동하는 데 사용할 일반 R 함수를 정의하여 무대를 마련하겠습니다.

### 동기

시각화와 가설 검정으로 두 개의 시뮬레이션된 데이터셋을 비교하고 싶다고 상상해 보세요. 약간의 실험을 통해 다음 함수들을 만들었습니다:

- `freqpoly()`: 두 분포를 8개의 빈도 다각형으로 시각화합니다.
- `t_test()`: t-검정을 사용하여 평균을 비교하고 문자열로 결과를 요약합니다:

```r
library(ggplot2)
freqpoly <- function(x1, x2, binwidth = 0.1,
                     xlim = c(-3, 3)) {
  df <- data.frame(
    x = c(x1, x2),
    g = c(rep("x1", length(x1)),
          rep("x2", length(x2)))
  )
  ggplot(df, aes(x, colour = g)) +
    geom_freqpoly(binwidth = binwidth, size = 1) +
    coord_cartesian(xlim = xlim)
}

t_test <- function(x1, x2) {
  test <- t.test(x1, x2)
  # 테스트 결과를 사용자 친화적인 문자열로 만듭니다
  sprintf("p value: %0.3f [%s]\nCI: [%s]",
          test$p.value,
          ifelse(test$p.value < 0.01, "very strong",
                 ifelse(test$p.value < 0.05, "strong",
                        ifelse(test$p.value < 0.1, "moderate",
                               "weak"))),
          paste(round(test$conf.int, 2), collapse = ", "))
}
```

(이 함수의 정확한 세부 사항은 걱정하지 마세요. 단지 예시일 뿐입니다.)

이 함수를 사용하여 간단한 앱을 만들고, 두 개의 표준 정규 분포에서 추출된 샘플 크기와 표준편차를 조정할 수 있게 해 보겠습니다. 앱의 일부인 코드를 시뮬레이션하겠습니다. 가장 먼저 해야 할 일은 UI를 만드는 것입니다:

```r
ui <- fluidPage(
  fluidRow(
    column(4,
      "Distribution 1",
      numericInput("n1", label = "n", value = 1000,
                   min = 1),
      sliderInput("m1", label = "Mean",
                  value = 0, min = -2, max = 2, step = 0.1),
      sliderInput("sd1", label = "SD",
                  value = 1, min = 0.1, max = 2, step = 0.1)
    ),
    column(4,
      "Distribution 2",
      numericInput("n2", label = "n", value = 1000,
                   min = 1),
      sliderInput("m2", label = "Mean",
                  value = 0, min = -2, max = 2, step = 0.1),
      sliderInput("sd2", label = "SD",
                  value = 1, min = 0.1, max = 2, step = 0.1)
    ),
    column(4,
      "Frequency polygon",
      numericInput("n3", label = "Number of samples",
                   value = 300, min = 1, step = 1)
    )
  )
)
```

이 코드는 next为止 relevantを行います:
- 3개의 column()을 사용하여 사용자 인터페이스를 3개의 열로 나눕니다.
- 첫 번째 열에서는 첫 번째 분포의 표본 크기, 평균, 표준편차를 선택할 수 있습니다.
- 두 번째 열에서는 두 번째 분포의 표본 크기, 평균, 표준편차를 선택할 수 있습니다.
- 세 번째 열에서는 시뮬레이션에 사용할 표본의 수를 선택할 수 있습니다.

서버 함수는 어떻게 보일까요? 가장 단순한 버전은 다음과 같습니다:

```r
server <- function(input, output, session) {
  x1 <- reactive(rnorm(input$n1, input$m1, input$sd1))
  x2 <- reactive(rnorm(input$n2, input$m2, input$sd2))
  output$hist <- renderPlot({
    freqpoly(x1(), x2(), binwidth = 0.05, xlim = c(-4, 4))
  }, res = 96)
}
```

하지만 이것만으로는 충분하지 않습니다. 다음 장에서 살펴볼 두 가지 결점이 있습니다:

1. 두 분포의 분포만 시각화할 뿐, 관련성에 대한 정보는 없습니다. "요약" 탭을 추가하여 t-검정의 결과를 표시하는 것이 좋겠습니다.
2. 모든 계산은 단일 렌더 플롯 함수 내부에서 이루어집니다. 모델을 반응형으로 만들고 출력을 분리하면 더 효율적으로 동작할 것입니다.

이러한 문제를 해결하는 방법을 배우면 이 앱을 훨씬 더 효율적이고 유용하게 만들 수 있습니다.


# sprintf()를 사용하여 t.test() 결과를 간결하게 포맷
sprintf(
  "p value: %0.3f\n[%0.2f, %0.2f]",
  test$p.value, test$conf.int[1], test$conf.int[2]
)
}
```

시뮬레이션된 데이터가 있다면, 이 함수들을 사용하여 두 변수를 비교할 수 있습니다:

```r
x1 <- rnorm(100, mean = 0, sd = 0.5)
x2 <- rnorm(200, mean = 0.15, sd = 0.9)
freqpoly(x1, x2)
cat(t_test(x1, x2))
#> p value: 0.003
#> [-0.38, -0.08]
```

실제 분석에서는 아마 이 함수들을 만들기 전에 탐색을 많이 했을 것입니다. 여기서는 앱에 빨리 도달하기 위해 그 탐색을 건너뛰었습니다. 하지만 명령형 코드를 일반 함수로 추출하는 것은 모든 Shiny 앱에 중요한 기법입니다: 앱에서 더 많은 코드를 추출할수록 이해하기 더 쉬워집니다. 이것은 관심사의 분리(Separation of Concerns)를 돕는 좋은 소프트웨어 엔지니어링입니다: 앱 외부의 함수는 계산에 초점을 맞추고, 앱 내부의 코드는 사용자 행동에 반응하는 데 초점을 맞출 수 있습니다. 이 아이디어는 18장에서 다시 살펴볼 것입니다.

### 앱

이 두 도구를 사용하여 많은 시뮬레이션을 빠르게 탐색하고 싶습니다. Shiny 앱은 이를 수행하는 훌륭한 방법입니다. 지루하게 R 코드를 수정하고 다시 실행하는 것을 피할 수 있기 때문입니다.

다음에서는 조각들을 Shiny 앱으로 감싸서, 입력을 대화형으로 조정할 수 있게 합니다. UI부터 시작하겠습니다. fluidRow()와 column()이 정확히 무엇을 하는지는 "여러 행"에서 다시 살펴볼 것입니다. 하지만 이름만으로 목적을 짐작할 수 있습니다.

첫 번째 행에는 세 개의 열이 있어 입력 컨트롤(분포 1, 분포 2, 플롯 컨트롤)을 배치합니다. 두 번째 행에는 플롯을 위한 넓은 열과 가설 검정을 위한 좁은 열이 있습니다:

```r
ui <- fluidPage(
  fluidRow(
    column(4,
      "Distribution 1",
      numericInput("n1", label = "n", value = 1000,
                   min = 1),
      numericInput("mean1", label = "µ", value = 0,
                   step = 0.1),
      numericInput("sd1", label = "σ", value = 0.5,
                   min = 0.1, step = 0.1)
    ),
    column(4,
      "Distribution 2",
      numericInput("n2", label = "n", value = 1000,
                   min = 1),
      numericInput("mean2", label = "µ", value = 0,
                   step = 0.1),
      numericInput("sd2", label = "σ", value = 0.5,
                   min = 0.1, step = 0.1)
    ),
    column(4,
      "Frequency polygon",
      numericInput("binwidth", label = "Bin width",
                   value = 0.1, step = 0.1),
      sliderInput("range", label = "range",
                  value = c(-3, 3), min = -5, max = 5)
    )
  ),
  fluidRow(
    column(9, plotOutput("hist")),
    column(3, verbatimTextOutput("ttest"))
  )
)
```

서버 함수는 지정된 분포에서 샘플링한 후 freqpoly()와 t_test() 함수를 호출합니다:

```r
server <- function(input, output, session) {
  output$hist <- renderPlot({
    x1 <- rnorm(input$n1, input$mean1, input$sd1)
    x2 <- rnorm(input$n2, input$mean2, input$sd2)
    freqpoly(x1, x2, binwidth = input$binwidth,
             xlim = input$range)
  }, res = 96)
  output$ttest <- renderText({
    x1 <- rnorm(input$n1, input$mean1, input$sd1)
    x2 <- rnorm(input$n2, input$mean2, input$sd2)
    t_test(x1, x2)
  })
}
```

server와 ui의 이 정의는 그림 3-6을 생성합니다. 계속 읽기 전에 라이브 버전을 열고 기본 작동 방식을 빠르게 살펴보는 것을 추천합니다.

그림 3-6. 이 Shiny 앱은 t-검정과 빈도 다각형을 사용하여 두 개의 시뮬레이션된 분포를 비교할 수 있게 합니다. https://hadley.shinyapps.io/ms-case-study-1 에서 실시간으로 확인하세요.

### 반응형 그래프

이 앱의 반응형 그래프를 그리는 것으로 시작합시다. Shiny는 참조하는 입력이 변경될 때만 출력을 업데이트할 만큼 똑똑하지만, 출력 내부의 코드를 선택적으로 실행할 만큼은 똑똑하지 않습니다. 즉, 출력은 원자적입니다: 전체가 실행되거나 실행되지 않습니다.

예를 들어, 서버의 다음 코드 조각을 살펴봅시다:

```r
x1 <- rnorm(input$n1, input$mean1, input$sd1)
x2 <- rnorm(input$n2, input$mean2, input$sd2)
t_test(x1, x2)
```

이 코드를 읽는 사람으로서, n1, mean1, 또는 sd1이 변경될 때만 x1을 업데이트하면 되고, n2, mean2, 또는 sd2가 변경될 때만 x2를 업데이트하면 되는 것을 알 수 있습니다. 하지만 Shiny는 출력을 전체로만 바라보므로, n1, mean1, sd1, n2, mean2, sd2 중 하나가 변경될 때마다 x1과 x2를 모두 업데이트합니다. 이것이 그림 3-7에 표시된 반응형 그래프로 이어집니다:

그림 3-7. 반응형 그래프는 모든 출력이 모든 입력에 의존함을 보여줍니다.

그래프가 매우 밀집되어 있다는 것을 알아챌 것입니다: 거의 모든 입력이 모든 출력에 직접 연결되어 있습니다. 이것이 두 가지 문제를 일으킵니다:

1. 연결이 너무 많아 앱을 이해하기 어렵습니다. 앱을 분리하여 분석할 수 있는 부분이 없습니다.
2. 필요 이상으로 많은 작업을 수행하므로 비효율적입니다. 예를 들어, 플롯의 breaks를 변경하면 데이터가 재계산됩니다. n1의 값을 변경하면 x2가 업데이트됩니다(두 곳에서!).

앱에 또 다른 주요 결점이 있습니다: 빈도 다각형과 t-검정은 별도의 무작위 추출을 사용합니다. 이는 상당히 오해의 소지가 있습니다. 동일한 기본 데이터를 작업할 것으로 예상되기 때문입니다. 다행히도 반응형 표현식을 사용하여 반복된 계산을 추출함으로써 이 모든 문제를 해결할 수 있습니다.

### 그래프 단순화

다음 서버 함수에서 기존 코드를 리팩토링하여 반복된 코드를 두 개의 새로운 반응형 표현식 x1과 x2로 추출합니다. 이 표현식들은 두 분포에서 데이터를 시뮬레이션합니다.

반응형 표현식을 만들려면 reactive()를 호출하고 결과를 변수에 대입합니다. 나중에 표현식을 사용하려면 변수를 함수처럼 호출합니다:

```r
server <- function(input, output, session) {
  x1 <- reactive(rnorm(input$n1, input$mean1, input$sd1))
  x2 <- reactive(rnorm(input$n2, input$mean2, input$sd2))
  output$hist <- renderPlot({
    freqpoly(x1(), x2(), binwidth = input$binwidth,
             xlim = input$range)
  }, res = 96)
  output$ttest <- renderText({
    t_test(x1(), x2())
  })
}
```

이 변환은 그림 3-8에 표시된 상당히 단순한 그래프를 생성합니다.

그림 3-8. 반응형 표현식을 사용하면 그래프가 크게 단순화되어 이해하기 훨씬 쉬워집니다.

이 더 단순한 그래프는 연결된 구성 요소를 분리하여 이해할 수 있으므로 앱을 이해하기 쉽습니다. 분포 매개변수의 값은 x1과 x2를 통해서만 출력에 영향을 줍니다. 이 재작성은 계산을 훨씬 적게 수행하므로 앱을 훨씬 더 효율적으로 만듭니다. 이제 binwidth나 range를 변경하면 기본 데이터가 아닌 플롯만 변경됩니다.

이 모듈성을 강조하기 위해, 그림 3-9는 독립적인 구성 요소 주위에 상자를 그립니다. 이 아이디어는 19장에서 모듈을 논의할 때 다시 살펴볼 것입니다. 모듈은 반복된 코드를 재사용을 위해 추출하면서 앱의 다른 모든 것과 격리되어 있음을 보장합니다. 모듈은 더 복잡한 앱에 매우 유용하고 강력한 기법입니다.

그림 3-9. 모듈은 앱의 부분 간 격리를 강제합니다.

프로그래밍의 "세 번째 규칙(Rule of Three)"에 익숙할 것입니다: 무언가를 세 번 복사하여 붙여넣을 때마다 중복을 줄이는 방법을 찾아야 합니다(일반적으로 함수를 작성하여). 이것은 중요합니다

코드의 중복을 줄여 이해하기 쉽고 요구 사항이 변경될 때 업데이트하기 쉽기 때문입니다. 하지만 Shiny에서는 "한 번의 규칙(Rule of One)"을 고려해야 한다고 생각합니다: 무언가를 한 번 복사하여 붙여넣을 때마다 반복된 코드를 반응형 표현식으로 추출하는 것을 고려해야 합니다. Shiny에서는 반응형 표현식이 사람의 코드 이해를 돕는 것뿐만 아니라 Shiny의 효율적인 코드 재실행 능력도 향상시키기 때문에 규칙이 더 엄격합니다.

### 왜 반응형 표현식이 필요한가?

반응형 코드로 작업을 처음 시작하면 왜 반응형 표현식이 필요한지 궁금할 수 있습니다. 기존 도구(새 변수 만들기와 함수 작성)를 사용하여 코드의 중복을 줄일 수 없는 이유는 무엇일까요?

불행히도, 이 두 기술 모두 반응형 환경에서는 작동하지 않습니다. 변수를 사용하여 중복을 줄이려고 하면 다음과 같은 코드를 작성할 수 있습니다:

```r
server <- function(input, output, session) {
  x1 <- rnorm(input$n1, input$mean1, input$sd1)
  x2 <- rnorm(input$n2, input$mean2, input$sd2)
  output$hist <- renderPlot({
    freqpoly(x1, x2, binwidth = input$binwidth,
             xlim = input$range)
  }, res = 96)
  output$ttest <- renderText({
    t_test(x1, x2)
  })
}
```

이 코드를 실행하면 반응형 컨텍스트 외부에서 입력 값에 접근하려고 하기 때문에 오류가 발생합니다. 그 오류가 발생하지 않더라도 여전히 문제가 있습니다: x1과 x2는 세션이 시작될 때 한 번만 계산되고, 입력이 업데이트될 때마다 다시 계산되지 않습니다.

함수를 사용하려고 하면 앱이 작동할 것입니다:

```r
server <- function(input, output, session) {
  x1 <- function() rnorm(input$n1, input$mean1, input$sd1)
  x2 <- function() rnorm(input$n2, input$mean2, input$sd2)
  output$hist <- renderPlot({
    freqpoly(x1(), x2(), binwidth = input$binwidth,
             xlim = input$range)
  }, res = 96)
  output$ttest <- renderText({
    t_test(x1(), x2())
  })
}
```

하지만 원래 코드와 동일한 문제가 있습니다: 어떤 입력이든 모든 출력이 재계산되고, t-검정과 빈도 다각형은 별도의 샘플에서 실행됩니다.

반응형 표현식은 결과를 자동으로 캐시하고 입력이 변경될 때만 업데이트합니다.9 변수는 값을 한 번만 계산합니다(죽은 죽); 함수는 호출될 때마다 값을 계산합니다(뜨거운 죽); 반응형 표현식은 값이 변경되었을 때만 계산합니다(적당한 온도의 죽).

### 평가 타이밍 제어

이제 반응성의 기본 개념에 익숙해졌으므로, 반응형 표현식이 실행되는 빈도를 늘리거나 줄일 수 있는 두 가지 고급 기법을 논의할 것입니다. 여기서는 기본 기법의 사용법을 보여줄 것이며, 15장에서 기본 구현에 대해 다시 살펴볼 것입니다.

기본 아이디어를 탐색하기 위해 시뮬레이션 앱을 단순화할 것입니다. 하나의 매개변수만 있는 분포를 사용하고 두 샘플이 동일한 n을 공유하도록 강제합니다. 플롯 컨트롤도 제거합니다. 이렇게 하면 더 작은 UI 객체와 서버 함수가 생성됩니다:

```r
ui <- fluidPage(
  fluidRow(
    column(3,
      numericInput("lambda1", label = "lambda1",
                   value = 3),
      numericInput("lambda2", label = "lambda2",
                   value = 5),
      numericInput("n", label = "n", value = 1e4,
                   min = 0)
    ),
    column(9, plotOutput("hist"))
  )
)

server <- function(input, output, session) {
  x1 <- reactive(rpois(input$n, input$lambda1))
  x2 <- reactive(rpois(input$n, input$lambda2))
  output$hist <- renderPlot({
    freqpoly(x1(), x2(), binwidth = 1, xlim = c(0, 40))
  }, res = 96)
}
```

이것은 그림 3-10에 표시된 앱과 그림 3-11에 표시된 반응형 그래프를 생성합니다.

그림 3-10. 이 더 단순한 앱은 두 포아송 분포에서 추출된 무작위 숫자의 빈도 다각형을 표시합니다. https://hadley.shinyapps.io/ms-simulation-2 에서 실시간으로 확인하세요.

그림 3-11. 반응형 그래프.

#### 시간 기반 무효화

시뮬레이션된 데이터라는 사실을 강조하기 위해 데이터를 지속적으로 다시 시뮬레이션하여 정적인 플롯 대신 애니메이션을 보고 싶다고 상상해 봅시다. reactiveTimer()라는 새로운 함수로 업데이트 빈도를 높일 수 있습니다.

reactiveTimer()는 현재 시간이라는 숨겨진 입력에 의존하는 반응형 표현식입니다. 반응형 표현식이 일반적으로 무효화되는 것보다 더 자주 무효화되도록 하려면 reactiveTimer()를 사용할 수 있습니다. 예를 들어, 다음 코드는 500ms 간격을 사용하여 초당 두 번 플롯이 업데이트되도록 합니다. 빠른 변경으로 어지럽히지 않으면서 시뮬레이션을 보고 있다는 것을 상기시키기에 충분히 빠릅니다.

이 변경은 그림 3-12에 표시된 반응형 그래프를 생성합니다:

```r
server <- function(input, output, session) {
  timer <- reactiveTimer(500)
  x1 <- reactive({
    timer()
    rpois(input$n, input$lambda1)
  })
  x2 <- reactive({
    timer()
    rpois(input$n, input$lambda2)
  })
  output$hist <- renderPlot({
    freqpoly(x1(), x2(), binwidth = 1, xlim = c(0, 40))
  }, res = 96)
}
```

그림 3-12. reactiveTimer(500)는 반마다 자동으로 무효화되는 새로운 반응형 입력을 도입합니다.

x1()과 x2()를 계산하는 반응형 표현식에서 timer()를 사용하는 방식에 유의하세요: 값을 사용하지 않고 호출만 합니다. 이렇게 하면 timer가 정확히 어떤 값을 반환하는지 걱정하지 않고 x1과 x2가 timer에 반응형 의존성을 가질 수 있습니다.

#### 클릭 시

앞의 시나리오에서, 시뮬레이션 코드가 실행하는 데 1초가 걸린다면 어떤 일이 일어날지 생각해 보세요. 0.5초마다 시뮬레이션을 수행하므로 Shiny는 점점 더 많은 작업을 하게 되고 결국 따라잡을 수 없게 됩니다. 누군가가 앱에서 빠르게 버튼을 클릭하고 수행하는 계산이 비교적 비용이 높은 경우에도 같은 문제가 발생할 수 있습니다.

Shiny에 대한 큰 작업 대기열이 생성될 수 있으며, 대기열을 처리하는 동안 새로운 이벤트에 응답할 수 없습니다. 이렇게 되면 사용자 경험이 나빠집니다. 앱에서 이 상황이 발생하면, 사용자가 버튼을 클릭하도록 요구하여 비용이 높은 계산을 수행하도록 선택하게 하는 것이 좋습니다. 이것이 actionButton()의 훌륭한 사용 사례입니다:

```r
ui <- fluidPage(
  fluidRow(
    column(3,
      numericInput("lambda1", label = "lambda1",
                   value = 3),
      numericInput("lambda2", label = "lambda2",
                   value = 5),
      numericInput("n", label = "n", value = 1e4,
                   min = 0),
      actionButton("simulate", "Simulate!")
    ),
    column(9, plotOutput("hist"))
  )
)
```

액션 버튼을 사용하려면 새로운 도구를 배워야 합니다. 그 이유를 이해하기 위해, 먼저 이전 접근 방식으로 문제를 해결해 봅시다. 앞에서 했던 것처럼, 값에 반응형 의존성을 갖기 위해 simulate를 값으로 사용하지 않고 참조합니다:

```r
server <- function(input, output, session) {
  x1 <- reactive({
    input$simulate
    rpois(input$n, input$lambda1)
  })
  x2 <- reactive({
    input$simulate
    rpois(input$n, input$lambda2)
  })
  output$hist <- renderPlot({
    freqpoly(x1(), x2(), binwidth = 1, xlim = c(0, 40))
  }, res = 96)
}
```

이것은 그림 3-13의 앱과 그림 3-14의 반응형 그래프를 생성합니다. 이것은 목표를 달성하지 못합니다. 새로운 의존성을 도입했을 뿐이기 때문입니다: x1()과 x2()는 simulate 버튼을 클릭할 때 업데이트되지만, lambda1, lambda2, 또는 n이 변경될 때도 계속 업데이트됩니다. 기존 의존성을 추가하는 대신 교체하고 싶습니다.

그림 3-13. 액션 버튼이 있는 앱. https://hadley.shinyapps.io/ms-action-button 에서 실시간으로 확인하세요.

그림 3-14. 이 반응형 그래프는 목표를 달성하지 못합니다; 기존 의존성을 교체하는 대신 의존성을 추가했습니다.

이 문제를 해결하려면 새로운 도구가 필요합니다: 반응형 의존성을 갖지 않으면서 입력 값을 사용하는 방법입니다. eventReactive()가 필요하며, 두 가지 인수를 가집니다: 첫 번째 인수는 의존성을 갖는 것을 지정하고, 두 번째 인수는 계산할 것을 지정합니다. 이를 통해 이 앱은 simulate가 클릭될 때만 x1()과 x2()를 계산할 수 있습니다:

```r
server <- function(input, output, session) {
  x1 <- eventReactive(input$simulate, {
    rpois(input$n, input$lambda1)
  })
  x2 <- eventReactive(input$simulate, {
    rpois(input$n, input$lambda2)
  })
  output$hist <- renderPlot({
    freqpoly(x1(), x2(), binwidth = 1, xlim = c(0, 40))
  }, res = 96)
}
```

그림 3-15는 새로운 반응형 그래프를 보여줍니다. 원하는 대로, x1과 x2는 이제 lambda1, lambda2, n에 반응형 의존성을 갖지 않는다는 점에 유의하세요:

값을 변경해도 계산이 트리거되지 않습니다. x1과 x2가 값을 계속 사용하지만 더 이상 반응형 의존성을 갖지 않음을 상기시키기 위해 화살표를 매우 연한 회색으로 남겼습니다.

그림 3-15. eventReactive()는 결과를 계산하는 데 사용되는 값(연한 회색 화살표)에서 의존성(검은색 화살표)을 분리할 수 있게 해줍니다.

### 옵저버

지금까지 앱 내부에서 일어나고 있는 일에 초점을 맞추었습니다. 하지만 때로는 앱 외부로 접근하여 세계의 다른 곳에서 부수적인 효과를 일으켜야 할 때가 있습니다. 이는 공유 네트워크 드라이브에 파일을 저장하거나, 웹 API로 데이터를 보내거나, 데이터베이스를 업데이트하거나, (가장 일반적으로) 콘솔에 디버깅 메시지를 출력하는 것일 수 있습니다. 이러한 동작은 앱의 모양에 영향을 주지 않으므로, 출력과 렌더 함수를 사용해서는 안 됩니다. 대신 옵저버(Observer)를 사용해야 합니다.

옵저버를 만드는 방법은 여러 가지가 있으며, "옵저버와 출력"에서 나중에 다시 살펴볼 것입니다. 지금은 Shiny를 처음 배울 때 중요한 디버깅 도구를 제공하는 observeEvent()의 사용법을 보여드리겠습니다.

observeEvent()는 eventReactive()와 매우 유사합니다. 두 가지 중요한 인수가 있습니다: eventExpr과 handlerExpr. 첫 번째 인수는 의존성을 갖는 입력 또는 표현식이고, 두 번째 인수는 실행될 코드입니다. 예를 들어, server()에 대한 다음 수정은 name이 업데이트될 때마다 콘솔에 메시지를 보내도록 합니다:

```r
ui <- fluidPage(
  textInput("name", "What's your name?"),
  textOutput("greeting")
)

server <- function(input, output, session) {
  string <- reactive(paste0("Hello ", input$name, "!"))
  output$greeting <- renderText(string())
  observeEvent(input$name, {
    message("Greeting performed")
  })
}
```

observeEvent()와 eventReactive() 사이에는 두 가지 중요한 차이점이 있습니다:

1. observeEvent()의 결과를 변수에 대입하지 않습니다. 그래서 다른 반응형 소비자에서 참조할 수 없습니다.
2. 옵저버와 출력은 밀접하게 관련되어 있습니다. 출력은 특수한 부수적 효과를 가지고 있다고 생각할 수 있습니다: 사용자의 브라우저에서 HTML을 업데이트합니다. 이 밀접함을 강조하기 위해, 반응형 그래프에서 동일하게 그릴 것입니다.

이것은 그림 3-16에 표시된 반응형 그래프를 생성합니다.


그림 3-16. 반응형 그래프에서 옵저버는 출력과 동일하게 보입니다.

## 요약

이 장은 Shiny 앱의 백엔드, 즉 사용자 행동에 반응하는 server() 코드에 대한 이해를 높여주었을 것입니다. Shiny를 뒷받침하는 반응형 프로그래밍 패러다임을 마스터하기 위한 첫 걸음도 내딛었을 것입니다. 여기서 배운 내용은 여러분을 오래도록 인도할 것입니다. 기반 이론에 대해서는 13장에서 다시 살펴볼 것입니다.

반응성은 매우 강력하지만, 가장 익숙한 R 프로그래밍의 명령형 스타일과 매우 다릅니다. 모든 결과가 완전히 이해되기까지 시간이 걸린다고 해서 놀라지 마세요.

이 장으로 Shiny의 기초에 대한 개요를 마칩니다. 다음 장에서는 데이터 분석을 지원하도록 설계된 더 큰 Shiny 앱을 만들어 지금까지 본 연습을 실습하는 데 도움을 줄 것입니다.

1 Shiny 앱에 대한 모든 연결은 새 세션을 시작합니다. 서로 다른 사람의 연결이든 같은 사람의 여러 탭이든 마찬가지입니다.

2 여러 사용자 간에 공유할 수 있는 작업이 있는 경우가 주요 예외입니다. 예를 들어, 모든 사용자가 동일한 큰 CSV 파일을 볼 수 있으므로 한 번 로드하여 사용자 간에 공유할 수 있습니다. 이 아이디어는 "데이터 처리 예약"에서 다시 살펴볼 것입니다.

3 라이브 앱을 실행 중이라면, 출력이 한 글자씩 업데이트되려면 상당히 천천히 타이핑해야 한다는 점에 유의하세요. Shiny가 업데이트를 보내기 전에 몇 밀리초를 기다리는 디바운싱(Debouncing)이라는 기법을 사용하기 때문입니다. 이는 앱의 응답 시간을 눈에 띄게 줄이지 않으면서 Shiny가 수행해야 하는 작업량을 상당히 줄여줍니다.

4 참고로 이 xkcd 만화를 읽어보세요.

5 ggplot2 범례를 원하는 대로 만드는 데 어려움을 겪은 적이 있다면, 이 문제를 경험했을 것입니다!

6 네, 브라우저에서 볼 수 없으면 Shiny는 출력을 업데이트하지 않습니다! Shiny는 결과를 실제로 볼 수 없으면 작업을 수행하지 않을 만큼 게으릅니다.

7 이 순서의 전문 용어는 위상 정렬(Topological Sort)입니다.

8 빈도 다각형(Frequency Polygon)을 들어본 적이 없다면, 막대 대신 선으로 그려진 히스토그램으로, 동일한 플롯에서 여러 데이터셋을 비교하기 쉽습니다.

9 메모이제이션(Memoization)에 익숙하다면, 유사한 개념입니다.

10 뉴욕 타임즈는 일자리 보고서를 해석하는 방법을 논의하는 기사에서 이 기법을 특히 효과적으로 사용했습니다.

# 4장. 사례 연구: 응급실 부상

## 소개

앞의 세 장에서 많은 새로운 개념을 소개했습니다. 이를 확실히 이해할 수 있도록, 이제 재미있는 데이터셋을 탐색하고 지금까지 본 많은 아이디어를 결합하는 더 풍부한 Shiny 앱을 만들어 보겠습니다. Shiny 외부에서 약간의 데이터 분석을 수행한 다음, 단순하게 시작하여 점차적으로 더 많은 세부 사항을 추가하여 앱으로 전환할 것입니다.

이 장에서는 빠른 파일 읽기를 위한 vroom과 일반적인 데이터 분석을 위한 tidyverse로 Shiny를 보충합니다:

```r
library(shiny)
library(vroom)
library(tidyverse)
```

## 데이터

소비자 제품 안전 위원회(Consumer Product Safety Commission)에서 수집한 전국 전자 부상 감시 시스템(NEISS)의 데이터를 탐색할 것입니다. 이것은 미국 병원의 대표적인 샘플에서 보는 모든 사고를 기록하는 장기 연구입니다. 모든 사람이 이미 이 분야에 익숙하고 각 관찰이 사고 발생 방식을 설명하는 짧은 내러티브가 포함되어 있어 흥미로운 데이터셋입니다.

이 데이터셋에 대한 자세한 내용은 GitHub에서 확인할 수 있습니다. 이 장에서는 2017년 데이터에만 초점을 맞추겠습니다. 이렇게 하면 데이터가 충분히 작게(약 10MB) 유지되어 Git에 쉽게 저장할 수 있습니다(나머지 책과 함께). 빠른 데이터 가져오기 전략에 대해 생각할 필요가 없다는 뜻입니다(이 책의 후반부에서 다시 살펴볼 것입니다).

이 장에 사용된 코드 추출은 GitHub에서 확인할 수 있습니다. 데이터를 자신의 컴퓨터에 가져오려면 다음 코드를 실행하세요:

```r
dir.create("neiss")
download <- function(name) {
  url <- paste0(
    "https://github.com/hadley/masteringshiny/",
    "raw/master/neiss/")
  download.file(paste0(url, name),
                paste0("neiss/", name), quiet = TRUE)
}
download("injuries.tsv.gz")
download("population.tsv")
download("products.tsv")
```

사용할 주요 데이터셋은 약 250,000개의 관찰을 포함하는 injuries입니다:

```r
injuries <- vroom::vroom("neiss/injuries.tsv.gz")
injuries
#> # A tibble: 255,064 x 10
#>    trmt_date  age sex   race  body_part diag  location
#>    <date>     <dbl> <chr> <chr> <chr>     <chr> <chr>
#>  1 2017-01-01    71 male  white Upper Tr… Cont… Other Publ…
#>  2 2017-01-01    16 male  white Lower Arm Burns,… Home
#>  3 2017-01-01    58 male  white Upper Tr… Cont… Home
#>  4 2017-01-01    21 male  white Lower Tr… Stra… Home
#>  5 2017-01-01    54 male  white Head      Inter… Other Publ…
#>  6 2017-01-01    21 male  white Hand      Frac… Home
#> # … with 255,058 more rows, and 3 more variables:
#> #   prod_code <dbl>, weight <dbl>,
#> #   narrative <chr>
```

각 행은 10개의 변수를 가진 단일 사고를 나타냅니다:

- `trmt_date`: 사람이 병원에서 진찰을 받은 날짜(사고가 발생한 날짜가 아님).
- `age`, `sex`, `race`: 사고를 당한 사람에 대한 인구통계학적 정보.
- `body_part`: 신체에서 부상이 발생한 위치(예: 발목 또는 귀). `location`은 사고가 발생한 장소(예: 집 또는 학교).
- `diag`: 부상의 기본 진단(예: 골절 또는 열상).
- `prod_code`: 부상과 관련된 주요 제품.
- `weight`: 이 데이터셋이 미국 전체 인구로 스케일링될 때 이 부상을 입을 것으로 추정되는 사람 수를 나타내는 통계적 가중치.
- `narrative`: 사고 발생 방식에 대한 짧은 이야기.

추가적인 맥락을 위해 두 개의 다른 데이터프레임과 쌍을 이룹니다: products는 제품 코드에서 제품 이름을 조회할 수 있게 해주고, population은 2017년의 각 연령과 성별 조합에 대한 미국 총 인구를 알려줍니다:

```r
products <- vroom::vroom("neiss/products.tsv")
products
#> # A tibble: 38 x 2
#>    prod_code title
#>        <dbl> <chr>
#>  1       464 knives, not elsewhere classified
#>  2       474 tableware and accessories
#>  3       604 desks, chests, bureaus or buffets
#>  4       611 bathtubs or showers
#>  5       649 toilets
#>  6       676 rugs or carpets, not specified
#> # … with 32 more rows

population <- vroom::vroom("neiss/population.tsv")
population
#> # A tibble: 170 x 3
#>      age sex    population
#>    <dbl> <chr>      <dbl>
#>  1     0 female   1924145
#>  2     0 male     2015150
#>  3     1 female   1943534
#>  4     1 male     2031718
#>  5     2 female   1965150
#>  6     2 male     2056625
#> # … with 164 more rows
```

## 탐색

앱을 만들기 전에 데이터를 약간 탐색해 봅시다. 흥미로운 이야기가 있는 제품, 649번 "변기"부터 살펴보겠습니다. 먼저 이 제품과 관련된 부상을 추출합니다:

```r
selected <- injuries %>%
  filter(prod_code == 649)
nrow(selected)
#> [1] 2993
```

다음으로 변기 관련 부상의 위치, 신체 부위, 진단을 살펴보는 몇 가지 기본 요약을 수행합니다. count 변수를 사용하여 개수를 미국 전체의 추정 부상으로 해석할 수 있습니다:

```r
selected %>%
  count(location, wt = weight, sort = TRUE)
#> # A tibble: 6 x 2
#>   location                  n
#>   <chr>                 <dbl>
#> 1 Home                 99603.
#> 2 Other Public Property 18663.
#> 3 Unknown              16267.
#> 4 School                 659.
#> 5 Street Or Highway       16.2
#> 6 Sports Or Recreation…   14.8

selected %>%
  count(body_part, wt = weight, sort = TRUE)
#> # A tibble: 24 x 2
#>    body_part          n
#>    <chr>          <dbl>
#>  1 Head          31370.
#>  2 Lower Trunk   26855.
#>  3 Face          13016.
#>  4 Upper Trunk   12508.
#>  5 Knee           6968.
#>  6 N.S./Unk       6741.
#> # … with 18 more rows

selected %>%
  count(diag, wt = weight, sort = TRUE)
#> # A tibble: 20 x 2
#>    diag                  n
#>    <chr>             <dbl>
#>  1 Other Or Not St… 32897.
#>  2 Contusion Or Ab… 22493.
#>  3 Inter Organ Inj… 21525.
#>  4 Fracture         21497.
#>  5 Laceration       18734.
#>  6 Strain, Sprain    7609.
#> # … with 14 more rows
```

예상대로 변기 관련 부상은 주로 가정에서 발생합니다. 가장 흔한 신체 부위는 낙상(일상적인 변기 사용에서 머리와 얼굴이 관련되지 않기 때문)을 시사하며, 진단은 상당히 다양합니다. 연령과 성별에 따른 패턴도 탐색할 수 있습니다. 충분한 데이터가 있으므로 테이블은 그다지 유용하지 않아, 그림 4-1과 같이 패턴을 더 명확하게 보여주는 플롯을 만듭니다:

```r
summary <- selected %>%
  count(age, sex, wt = weight)
summary
#> # A tibble: 208 x 3
#>      age sex        n
#>    <dbl> <chr>  <dbl>
#>  1     0 female   4.76
#>  2     0 male    14.3
#>  3     1 female 253.
#>  4     1 male   231.
#>  5     2 female 438.
#>  6     2 male   632.
#> # … with 202 more rows

summary %>%
  ggplot(aes(age, n, colour = sex)) +
  geom_line() +
  labs(y = "Estimated number of injuries")
```

그림 4-1. 연령과 성별로 분류된 변기에 의한 추정 부상 수. 3세에 정점을 찍는 소년들의 급증과 중년 무렵부터 시작되는(특히 여성의) 증가, 80세 이후의 점진적인 감소가 보입니다. 정점은 소년들이 일반적으로 서서 변기를 사용하기 때문이고, 여성의 증가는 골다공증 때문이라고 추정됩니다(여성과 남성이 동일한 비율로 부상을 입지만, 골절 위험이 높아 응급실에 더 많이 도달한다는 뜻).

이 패턴을 해석하는 한 가지 문제는 나이 많은 사람이 젊은 사람보다 적다는 것을 알고 있다는 것입니다. 부상을 입을 수 있는 인구가 더 적다는 뜻입니다. 부상당한 사람 수를 총 인구와 비교하고 부상률을 계산하여 이를 제어할 수 있습니다. 여기서는 만 명당 비율을 사용합니다:

```r
summary <- selected %>%
  count(age, sex, wt = weight) %>%
  left_join(population, by = c("age", "sex")) %>%
  mutate(rate = n / population * 1e4)
summary
#> # A tibble: 208 x 5
#>      age sex        n population  rate
#>    <dbl> <chr>  <dbl>      <dbl> <dbl>
#>  1     0 female   4.76   1924145 0.0247
#>  2     0 male    14.3    2015150 0.0708
#>  3     1 female 253.     1943534 1.30
#>  4     1 male   231.     2031718 1.14
#>  5     2 female 438.     1965150 2.23
#>  6     2 male   632.     2056625 3.07
#> # … with 202 more rows
```

그림 4-2에 표시된 대로 비율을 플로팅하면 50세 이후에 놀라울 정도로 다른 추세가 나타납니다: 남성과 여성의 차이가 훨씬 작아지고, 감소가 더 이상 보이지 않습니다. 여성이 남성보다 오래 살 경향이 있어, 나이가 많은 연령대에서는 변기에 부상을 입을 수 있는 여성이 단순히 더 많기 때문입니다:

```r
summary %>%
  ggplot(aes(age, rate, colour = sex)) +
  geom_line(na.rm = TRUE) +
  labs(y = "Injuries per 10,000 people")
```

그림 4-2. 연령과 성별로 분류된 만 명당 추정 부상률. (인구 데이터를 찾을 수 없어 80세까지만 표시됨.)

마지막으로 몇 가지 내러티브를 살펴볼 수 있습니다. 이를 살펴보는 것은 가설을 검증하고 추가 탐색을 위한 새로운 아이디어를 생성하는 비공식적인 방법입니다. 여기서는 무작위로 10개를 추출합니다:

```r
selected %>%
  sample_n(10) %>%
  pull(narrative)
#> [1] "68YOF STRAINED KNEE MOVING FROM TOILET TO
#>      POWER CHAIR AT HOME. DX:..."
#> [2] "97YOM LWR BACK PAIN - MISSED TOILET SEAT,
#>      FELL FLOOR AT NH"
#> [3] "54 YOF DX ALCOHOL INTOXICATION - PT STATES
#>      SHE FELL OFF TOILET."
#> [4] "85YOF-STAFF AT NH STATES PT WAS TRANSITIONIN
#>      TO TOILET FROM WHEELCH..."
#> [5] "FOREHEAD LACERATION. 64 YOM FELL AND HIT HIS
#>      HEAD ON TOILET."
#> [6] "70YOM-STAFF STATES PT FELL OFF TOILET ONTO
#>      CONCRETE FLOOR AT *** AR..."
#> [7] "40YOF WAS INTOXICATED AND FELL OFF THE TOILET
#>      STRUCK HEAD ON THE WA..."
#> [8] "66 Y/O F FELL FROM COMMODE ONTO FLOOR AND
#>      FRACTURED CLAVICLE"
#> [9] "25YOF SYNCOPAL EPS W ON TOILET FELL HIT RS
#>      OF HEAD REPORTLY LOC UNK..."
#> [10] "4 YO M W/LAC TO FOREHEAD SLIPPED IN BATHROOM
#>      HIT ON TOILET FLUSH HA..."
```

하나의 제품에 대해 탐색을 수행했으므로, 코드를 다시 타이핑하지 않고 다른 제품에 대해서도 쉽게 수행할 수 있으면 좋겠습니다. 그래서 Shiny 앱을 만들어 봅시다!

### 프로토타입

복잡한 앱을 구축할 때는 가능한 한 단순하게 시작하여 기본 메커니즘이 작동하는지 확인한 다음 더 복잡한 작업을 시작하는 것을 강력히 추천합니다. 여기서는 하나의 입력(제품 코드), 세 개의 테이블, 하나의 플롯으로 시작합니다.

첫 번째 프로토타입을 설계할 때의 도전은 "가능한 한 단순하게" 만드는 것입니다. 기본 사항을 빠르게 작동시키는 것과 앱의 미래를 계획하는 것 사이에는 긴장이 있습니다. 어느 쪽이든 극단적이면 나쁠 수 있습니다: 너무 좁게 설계하면 나중에 앱을 다시 작업하는 데 많은 시간을 쏟게 되고, 너무 엄격하게 설계하면 나중에 잘려나가는 코드를 작성하는 데 많은 시간을 쏟게 됩니다. 균형을 맞추기 위해, 코드를 작성하기 전에 UI와 반응형 그래프를 빠르게 탐색하기 위해 연필과 종이로 몇 가지 스케치를 자주 합니다.

여기서는 입력(더 많은 입력을 추가할 가능성을 받아들이면서)을 위해 1행, 세 테이블을 위해 1행(각 테이블에 12열 너비의 1/3인 4열을 부여), 플롯을 위해 1행을 사용하기로 결정했습니다:

```r
prod_codes <- setNames(products$prod_code,
                       products$title)

ui <- fluidPage(
  fluidRow(
    column(6,
      selectInput("code", "Product",
                  choices = prod_codes)
    )
  ),
  fluidRow(
    column(4, tableOutput("diag")),
    column(4, tableOutput("body_part")),
    column(4, tableOutput("location"))
  ),
  fluidRow(
    column(12, plotOutput("age_sex"))
  )
)
```

fluidRow()와 column()에 대해 아직 이야기하지 않았지만, 맥락에서 무엇을 하는지 짐작할 수 있을 것이며, "여러 행"에서 다시 이야기할 것입니다. selectInput() choices에서 setNames()의 사용에도 유의하세요: 이는 UI에 제품 이름을 표시하고 서버에 제품 코드를 반환합니다.

서버 함수는 비교적 간단합니다. 먼저 정적 변수인 selected와 summary를 반응형 표현식으로 변환합니다.これは 합리적인 일반적인 패턴입니다: 데이터 분석에서 변수를 만들어 분석을 단계로 분해하고多次 계산을 피하지만, Shiny 앱에서 반응형 표현식은 동일한 역할을 합니다. Shiny 앱을 시작하기 전에 분석 코드를 약간 정리하는 것이 좋은 아이디어인 경우가 많습니다. 이렇게 하면 반응성의 추가 복잡성을 더하기 전에 일반 R 코드로 이러한 문제를 생각할 수 있습니다:

```r
server <- function(input, output, session) {
  selected <- reactive(
    injuries %>% filter(prod_code == input$code)
  )

  output$diag <- renderTable(
    selected() %>%
      count(diag, wt = weight, sort = TRUE)
  )

  output$body_part <- renderTable(
    selected() %>%
      count(body_part, wt = weight, sort = TRUE)
  )

  output$location <- renderTable(
    selected() %>%
      count(location, wt = weight, sort = TRUE)
  )

  summary <- reactive({
    selected() %>%
      count(age, sex, wt = weight) %>%
      left_join(population, by = c("age", "sex")) %>%
      mutate(rate = n / population * 1e4)
  })

  output$age_sex <- renderPlot({
    summary() %>%
      ggplot(aes(age, n, colour = sex)) +
      geom_line() +
      labs(y = "Estimated number of injuries")
  }, res = 96)
}
```

summary 반응형 표현식은 단일 반응형 소비자에 의존하기 때문에 엄밀히는 필요하지 않지만, 계산과 플로팅을 분리하는 것은 좋은 관행입니다. 앱의 흐름을 이해하기 쉽고 나중에 일반화하기도 쉽기 때문입니다.

결과 앱의 스크린샷은 그림 4-3에 나와 있습니다. GitHub에서 소스 코드를 볼 수 있습니다.

그림 4-3. NEISS 탐색 앱의 첫 번째 프로토타입. https://hadley.shinyapps.io/msprototype 에서 실시간으로 확인하세요.

### 테이블 다듬기

기본 구성 요소가 준비되어 작동하고 있으므로, 앱을 점진적으로 개선할 수 있습니다. 이 앱의 첫 번째 문제는 테이블에 많은 정보를 표시한다는 것입니다. 하이라이트만 원할 가능성이 높습니다. 이를 해결하려면 먼저 테이블을 잘라내는 방법을 알아내야 합니다. forcats 함수의 조합으로 해결하기로 했습니다: 변수를 팩터로 변환하고, 레벨의 빈도순으로 정렬한 다음, 상위 5개 이후의 모든 레벨을 하나로 묶습니다:

```r
injuries %>%
  mutate(diag = fct_lump(fct_infreq(diag), n = 5)) %>%
  group_by(diag) %>%
  summarise(n = as.integer(sum(weight)))
#> # A tibble: 6 x 2
#>   diag                      n
#>   <fct>                 <int>
#> 1 Other Or Not Stated 1806436
#> 2 Fracture            1558961
#> 3 Laceration          1432407
#> 4 Strain, Sprain      1432556
#> 5 Contusion Or Abr…   1451987
#> 6 Other               1929147
```

방법을 알고 있었기 때문에, 모든 변수에 대해 이를 자동화하는 작은 함수를 작성했습니다. 세부 사항은 여기서 중요하지 않지만 12장에서 다시 살펴볼 것입니다. 복사하여 붙여넣어도 문제를 해결할 수 있으므로, 코드가 전혀 생소해 보여도 걱정하지 마세요:

```r
count_top <- function(df, var, n = 5) {
  df %>%
    mutate({{ var }} := fct_lump(fct_infreq({{ var }}),
                                 n = n)) %>%
    group_by({{ var }}) %>%
    summarise(n = as.integer(sum(weight)))
}
```

서버 함수에서 이를 사용합니다:

```r
output$diag <- renderTable(
  count_top(selected(), diag), width = "100%")
output$body_part <- renderTable(
  count_top(selected(), body_part), width = "100%")
output$location <- renderTable(
  count_top(selected(), location), width = "100%")
```

앱의 미학을 개선하기 위해 한 가지 변경을 더 했습니다: 모든 테이블이 최대 너비(즉, 나타나는 열을 채우도록)를 차지하도록 강제했습니다. 이는 우연한 변동을 줄여 출력을 더 미적으로 만족스럽게 만듭니다.

결과 앱의 스크린샷은 그림 4-4에 나와 있습니다. GitHub에서 소스 코드를 볼 수 있습니다.


그림 4-4. 앱의 두 번째 반복은 요약 테이블에서 가장 빈번한 행만 표시하여 표시를 개선합니다. https://hadley.shinyapps.io/ms-polish-tables 에서 실시간으로 확인하세요.

### 비율 대 개수

지금까지는 단일 플롯만 표시했지만, 사용자에게 부상 수와 인구 표준화 비율 중 선택할 수 있는 옵션을 제공하고 싶습니다. 먼저 UI에 컨트롤을 추가합니다. 여기서는 두 가지 상태를 명확하게 만들고 나중에 새로운 상태를 쉽게 추가할 수 있으므로 selectInput()을 사용하기로 했습니다:

```r
fluidRow(
  column(8,
    selectInput("code", "Product",
      choices = setNames(products$prod_code,
                         products$title),
      width = "100%"
    )
  ),
  column(2,
    selectInput("y", "Y axis",
                c("rate", "count")))
),
```

(인구 분포를 올바르게 해석할 필요가 없으므로 기본값을 비율로 설정했습니다.)

플롯을 생성할 때 해당 입력에 따라 조건을 둡니다:

```r
output$age_sex <- renderPlot({
  if (input$y == "count") {
    summary() %>%
      ggplot(aes(age, n, colour = sex)) +
      geom_line() +
      labs(y = "Estimated number of injuries")
  } else {
    summary() %>%
      ggplot(aes(age, rate, colour = sex)) +
      geom_line(na.rm = TRUE) +
      labs(y = "Injuries per 10,000 people")
  }
}, res = 96)
```

결과 앱의 스크린샷은 그림 4-5에 나와 있습니다. GitHub에서 소스 코드를 볼 수 있습니다.

그림 4-5. 이번 반복에서는 y축에 개수와 인구 표준화 비율 간 전환 기능을 사용자에게 제공합니다. https://hadley.shinyapps.io/ms-rate-vs-count 에서 실시간으로 확인하세요.

### 내러티브

내러티브는 매우 흥미롭고, 플롯을 볼 때 떠올린 가설을 비공식적으로 교차 검증할 수 있는 방법을 제공하기 때문에 접근할 수 있는 방법을 제공하고 싶습니다. R 코드에서는 한 번에 여러 내러티브를 샘플링했지만, 대화형으로 탐색할 수 있는 앱에서는 그럴 이유가 없습니다. 해결책은 두 부분으로 구성됩니다.

먼저 UI 하단에 새 행을 추가합니다. 새로운 이야기를 트리거하는 데 액션 버튼을 사용하고, 내러티브를 textOutput()에 넣습니다:

```r
fluidRow(
  column(2, actionButton("story",
                         "Tell me a story")),
  column(10, textOutput("narrative"))
)
```

결과 앱의 스크린샷은 그림 4-6에 나와 있습니다. GitHub에서 소스 코드를 볼 수 있습니다.

그림 4-6. 마지막 반복은 선택된 행에서 무작위 내러티브를 추출하는 기능을 추가합니다. https://hadley.shinyapps.io/ms-narrative 에서 실시간으로 확인하세요.

그런 다음 eventReactive()를 사용하여 버튼이 클릭되거나 기본 데이터가 변경될 때만 업데이트되는 반응형을 만듭니다:

```r
narrative_sample <- eventReactive(
  list(input$story, selected()),
  selected() %>%
    pull(narrative) %>%
    sample(1)
)
output$narrative <- renderText(narrative_sample())
```

## 연습 문제

1. 각 앱에 대한 반응형 그래프를 그려보세요.

2. 요약 테이블을 줄이는 코드에서 fct_infreq()와 fct_lump()를 뒤집으면 어떻게 되나요?

3. 요약 테이블에 표시할 행 수를 사용자가 결정할 수 있는 입력 컨트롤을 추가하세요.

4. 앞뒤 버튼으로 모든 내러티브를 체계적으로 살펴볼 수 있는 방법을 제공하세요. 고급: 내러티브 목록을 "원형"으로 만들어 마지막 내러티브에서 앞으로 가면 첫 번째 내러티브로 이동하도록 하세요.

## 요약

이제 Shiny 앱의 기본기를 익혔으므로, 다음 일곱 장에서 중요한 기법들을 살펴볼 것입니다. 워크플로우에 대한 다음 장을 읽은 후, 나머지 장을 훑어보아 어떤 내용을 다루는지 파악하고, 앱에 기법이 필요할 때 다시 살펴보는 것을 추천합니다.

# 2부. Shiny 활용

다음 장에서는 유용한 기법들을 살펴봅니다. 모든 사람이 5장부터 시작하는 것이 좋습니다. 앱 개발과 디버깅, 막혔을 때 도움을 구하는 데 중요한 도구를 제공하기 때문입니다. 그 후에는 정해진 순서가 없고 장 간의 연결도 비교적 적습니다: 빠르게 훑어보아 전체적인 그림을 파악하고(나중에 관련 문제가 발생했을 때 이 도구들을 기억할 수 있도록) 현재 필요한 부분만 깊이 읽는 것을 추천합니다. 주요 주제에 대한 빠른 요약입니다:

- 6장에서는 페이지에 입력 및 출력 컴포넌트를 레이아웃하는 다양한 방법과 테마로 외형을 커스터마이징하는 방법을 자세히 설명합니다.
- 7장에서는 플롯에 직접적인 상호작용을 추가하는 방법과 다른 방식으로 생성된 이미지를 표시하는 방법을 보여줍니다.
- 8장에서는 앱 실행 중 사용자에게 피드백을 제공하는 기법(인라인 오류, 알림, 진행률 표시줄, 대화 상자 등)을 다룹니다.
- 9장에서는 앱과 파일을 주고받는 방법을 논의합니다.
- 10장에서는 앱이 실행되는 동안 사용자 인터페이스를 동적으로 수정하는 방법을 보여줍니다.
- 11장에서는 사용자가 북마크할 수 있도록 앱 상태를 기록하는 방법을 보여줍니다.
- 12장에서는 tidyverse 패키지를 사용할 때 사용자가 변수를 선택할 수 있게 하는 방법을 보여줍니다.

앱 개발 워크플로우를 개선하는 것으로 시작합시다.

# 5장. 워크플로우

많은 Shiny 앱을 작성할 계획이라면(이 책을 읽고 있으니 그럴 것이라 기대합니다!), 기본 워크플로우에 시간을 투자할 가치가 있습니다. 워크플로우 개선은 장기적으로 큰 수익을 가져다주는 투자처입니다. R 코드를 작성하는 시간의 비율을 높여줄 뿐만 아니라, 결과를 더 빠르게 볼 수 있게 하여 Shiny 앱 작성 과정을 더 즐겁게 만들고 기술이 더 빠르게 향상되도록 도와줍니다.

이 장의 목표는 세 가지 중요한 Shiny 워크플로우를 개선하는 데 도움을 주는 것입니다:

1. 앱을 만들고, 변경하고, 결과를 실험하는 기본 개발 주기.
2. 코드를 수정하고 결과를 시도하는 디버깅 워크플로우.
3. 문제를 설명하는 자체 포함 코드 청크인 reprex 작성.

## 개발 워크플로우

개발 워크플로우를 최적화하는 목표는 변경과 결과를 보는 사이의 시간을 줄이는 것입니다. 반복할수록 빠르게 실험하고 더 나은 Shiny 개발자가 될 수 있습니다. 여기서 최적화할 두 가지 주요 워크플로우가 있습니다: 첫 번째 앱 만들기와 코드 수정 및 결과 시도의 반복 주기 가속화.

### 앱 만들기

모든 앱은 동일한 여섯 줄의 R 코드로 시작합니다:

```r
library(shiny)

ui <- fluidPage(
)

server <- function(input, output, session) {
}

shinyApp(ui, server)
```

그 코드를 타이핑하는 데 빨리 질릴 것이므로, RStudio는 몇 가지 단축키를 제공합니다:

- 이미 app.R을 열었다면 shinyapp을 타이핑한 다음 Shift+Tab을 눌러 Shiny 앱 스니펫을 삽입합니다.1
- 새 프로젝트를 시작하려면2 File 메뉴에서 New Project를 선택한 다음 Shiny Web Application을 선택합니다(그림 5-1 참조).

그림 5-1. RStudio에서 새 Shiny 앱을 만들려면 프로젝트 유형으로 Shiny Web Application을 선택합니다.

하루에 앱을 한두 개만 만들 것 같아서 이 단축키를 배울 가치가 없다고 생각할 수 있지만, 단순한 앱을 만드는 것은 큰 프로젝트를 시작하기 전에 기본 개념을 확인하는 훌륭한 방법이며, 디버깅에도 훌륭한 도구입니다.

### 변경 사항 보기

하루에 몇 개의 앱을 만들겠지만, 앱을 수백 번 실행할 것이므로 개발 워크플로우를 마스터하는 것이 특히 중요합니다. 반복 시간을 줄이는 첫 번째 방법은 Run App 버튼을 클릭하는 대신 Cmd/Ctrl+Shift+Enter 키보드 단축키를 배우는 것입니다. 이를 통해 다음과 같은 개발 워크플로우를 얻을 수 있습니다:

1. 코드를 작성합니다.
2. Cmd/Ctrl+Shift+Enter로 앱을 시작합니다.
3. 앱을 대화형으로 실험합니다.
4. 앱을 닫습니다.
5. 1로 돌아갑니다.

반복 속도를 더 높이는 또 다른 방법은 autoreload를 켜고 백그라운드 작업으로 앱을 실행하는 것입니다. 이 워크플로우에서는 파일을 저장하자마자 앱이 다시 시작됩니다: 닫고 다시 시작할 필요가 없습니다. 이를 통해 더 빠른 워크플로우를 얻을 수 있습니다:

1. 코드를 작성하고 Cmd/Ctrl+S를 눌러 파일을 저장합니다.
2. 대화형으로 실험합니다.
3. 1로 돌아갑니다.

이 기법의 주요 단점은 앱이 별도의 프로세스에서 실행되기 때문에 디버깅이 상당히 어렵다는 것입니다. 앱이 점점 커질수록, "대화형으로 실험" 단계가 부담스러워지기 시작할 것입니다. 변경으로 영향을 받았을 수 있는 앱의 모든 구성 요소를 다시 확인하는 것을 기억하기 너무 어렵습니다.

나중에 21장에서 자동 테스트 도구를 배우게 되면, 대화형 실험을 자동화된 코드로 전환할 수 있습니다. 이를 통해 테스트를 더 빠르게 실행할 수 있고(자동화되어 있으므로) 중요한 테스트를 잊어버릴 수 없습니다. 테스트를 개발하는 데 초기 투자가 필요하지만, 대형 앱에서는 투자가 풍부하게 보상됩니다.

### 보기 제어

기본적으로 앱을 실행하면 팝업 창에 나타납니다. Run App 드롭다운에서 선택할 수 있는 두 가지 다른 옵션이 있습니다(그림 5-2 참조):

- **Run in Viewer Pane**: 뷰어 창(일반적으로 IDE의 오른쪽)에 앱을 엽니다. 앱 코드를 실행하는 동시에 앱을 볼 수 있으므로较小 앱에 유용합니다.
- **Run External**: 일반 웹 브라우저에서 앱을 엽니다.较大 앱과 대부분의 사용자가 경험할 맥락에서 앱이 어떻게 보이는지 확인하고 싶을 때 유용합니다.

그림 5-2. Run App 버튼은 실행 중인 앱이 어떻게 표시될지를 선택할 수 있게 해줍니다.

## 디버깅

앱 작성을 시작하면 문제가 발생할 것이 거의 확실합니다. 대부분의 버그의 원인은 Shiny에 대한 여러분의 정신 모델과 Shiny가 실제로 하는 일 사이의 불일치입니다. 이 책을 읽으면서 정신 모델이 개선되어 실수가 줄어들겠지만, 실수가 발생하면 문제를 더 쉽게 발견할 수 있습니다. 하지만 어떤 언어로든 작동하는 코드를 첫 시도에 확실하게 작성하려면 수 년의 경험이 필요합니다. 이는 실수를 식별하고 수정하는 강건한 워크플로우를 개발해야 함을 의미합니다.

여기서는 Shiny 앱에 특화된 과제에 초점을 맞추겠습니다. R에서의 디버깅이 새로운 경우 rstudio::conf(2020)에서 Jenny Bryan이 발표한 "Object of type closure is not subsettable" 기조연설부터 시청하는 것을 추천합니다.

세 가지 주요 유형의 문제를 논의할 것입니다:

1. **예상치 못한 오류가 발생하는 경우**: 가장 쉬운 경우입니다. 오류가 정확히 어디서 발생했는지 파악할 수 있는 트레이스백(Traceback)이 제공되기 때문입니다. 문제를 식별하면 기대와 현실 사이의 차이를 발견할 때까지 가정을 체계적으로 테스트해야 합니다. 대화형 디버거가 이 과정에서 강력한 조력자입니다.

2. **오류는 없지만 값이 잘못된 경우**: 여기서는 대화형 디버거와 조사 기술을 사용하여 근본 원인을 추적해야 합니다.

3. **모든 값이 올바르지만 예상한 때에 업데이트되지 않는 경우**: Shiny에 고유한 가장 도전적인 문제입니다. 이러한 상황이 발생하면 좌절스럽겠지만, 디버깅 기술을 연습할 기회로 전환할 수 있습니다.

다음 섹션에서 또 다른 중요한 기법—최소한의 재현 가능한 예시 만들기—에 대해 다시 살펴볼 것입니다. 막혀서 다른 사람의 도움을 받아야 하는 경우 최소한의 예시를 만드는 것이 중요합니다. 하지만 자신의 코드를 디버깅할 때도 최소한의 예시를 만드는 것은 매우 중요한 기술입니다. 일반적으로 많은 코드가 잘 작동하고 문제를 일으키는 코드는 매우 적습니다. 작동하는 코드를 제거하여 문제 코드를 좁힐 수 있다면, 솔루션을 훨씬 더 빠르게 반복할 수 있습니다. 매일 사용하는 기법입니다.

### 트레이스백 읽기

R에서는 모든 오류와 함께 트레이스백(Call Stack)이 제공됩니다. 이는 오류로 이어진 호출 시퀀스를 문자 그대로 추적합니다. 예를 들어, 다음 간단한 호출 시퀀스를 살펴봅시다:

f()는 g()를 호출하고, g()는 h()를 호출하며, h()는 곱셈 연산자를 호출합니다:

```r
f <- function(x) g(x)
g <- function(x) h(x)
h <- function(x) x * 2
```

이 코드에 오류가 발생하면 다음과 같습니다:

```r
f("a")
#> Error in x * 2: non-numeric argument to
#>   binary operator
```

traceback()을 호출하여 문제로 이어진 호출 시퀀스를 찾을 수 있습니다:

```r
traceback()
#> 3: h(x)
#> 2: g(x)
#> 1: f("a")
```

트레이스백을 뒤집으면 이해하기 가장 쉽습니다:

```
1: f("a")
2: g(x)
3: h(x)
```

이제 오류로 이어진 호출 시퀀스를 보여줍니다: f()가 g()를 호출했고, g()가 h()를 호출했으며(여기서 오류 발생), h()가 곱셈 연산자를 호출했습니다.

### Shiny에서의 트레이스백

불행히도, Shiny에서는 앱이 실행되는 동안 코드를 실행할 수 없으므로 traceback()을 사용할 수 없습니다. 대신 Shiny가 자동으로 트레이스백을 출력합니다. 예를 들어, 앞에서 정의한 f() 함수를 사용하는 다음 앱을 살펴봅시다:

```r
library(shiny)
f <- function(x) g(x)
g <- function(x) h(x)
h <- function(x) x * 2

ui <- fluidPage(
  selectInput("n", "N", 1:10),
  plotOutput("plot")
)
server <- function(input, output, session) {
  output$plot <- renderPlot({
    n <- f(input$n)
    plot(head(cars, n))
  }, res = 96)
}
shinyApp(ui, server)
```

이 앱을 실행하면 앱에 오류 메시지가 표시되고 콘솔에 트레이스백이 표시됩니다:

```
Error in *: non-numeric argument to binary operator
169: g [app.R#4]
168: f [app.R#3]
167: renderPlot [app.R#13]
165: func
125: drawPlot
111: <reactive:plotObj>
95: drawReactive
82: renderFunc
81: output$plot
1: runApp
```

무슨 일이 일어나고 있는지 이해하기 위해, 다시 뒤집어서 나타나는 순서대로 호출 시퀀스를 볼 수 있습니다:

```
Error in *: non-numeric argument to binary operator
1: runApp
81: output$plot
82: renderFunc
95: drawReactive
111: <reactive:plotObj>
125: drawPlot
165: func
167: renderPlot [app.R#13]
168: f [app.R#3]
169: g [app.R#4]
```

호출 스택에는 세 가지 기본 부분이 있습니다:

1. **시작 부분**: 첫 번째 몇 개의 호출이 앱을 시작합니다. 이 경우 runApp()만 보이지만, 앱을 시작하는 방식에 따라 더 복잡한 것이 보일 수 있습니다. 예를 들어, source()를 호출하여 앱을 실행한 경우 이것이 보일 수 있습니다:

```
1: source
3: print.shiny.appobj
5: runApp
```

일반적으로 첫 번째 runApp() 이전의 모든 것은 무시할 수 있습니다. 이는 앱을 실행하기 위한 설정 코드일 뿐입니다.

2. **중간 부분**: 반응형 표현식을 호출하는 Shiny 내부 코드가 나타납니다:

```
81: output$plot
82: renderFunc
95: drawReactive
111: <reactive:plotObj>
125: drawPlot
165: func
```

여기서 output$plot을 발견하는 것이 정말 중요합니다: 어떤 반응형(plot)이 오류를 일으키는지 알려줍니다. 다음 함수들은 내부 것이므로 무시할 수 있습니다.

3. **마지막 부분**: 여러분이 작성한 코드가 나타납니다:

```
167: renderPlot [app.R#13]
168: f [app.R#3]
169: g [app.R#4]
```

renderPlot() 내부에서 호출된 코드입니다. 파일 경로와 줄 번호가 있으므로 여기에 주의를 기울여야 한다는 것을 알 수 있습니다.

앱에서 오류가 발생했지만 트레이스백이 보이지 않는 경우, Cmd/Ctrl+Shift+Enter로(또는 RStudio를 사용하지 않는 경우 runApp()을 호출하여) 앱을 실행하고 있고 실행 중인 파일을 저장했는지 확인하세요. 앱을 실행하는 다른 방법은 트레이스백을 만드는 데 필요한 정보를 항상 캡처하지 않습니다.

### 대화형 디버거

오류의 소스를 찾았고 원인을 파악하고 싶을 때, 사용할 수 있는 가장 강력한 도구는 대화형 디버거(Interactive Debugger)입니다. 디버거는 실행을 일시 중지하고 문제가 무엇인지 파악하기 위해 모든 코드를 실행할 수 있는 대화형 R 콘솔을 제공합니다.

디버거를 시작하는 두 가지 방법이 있습니다:

1. **소스 코드에 browser() 호출을 추가합니다**: 이것이 대화형 디버거를 시작하는 표준 R 방식이며, Shiny를 어떤 방식으로든 실행하든 작동합니다. browser()의 또 다른 장점은 R 코드이므로 if 문과 결합하여 조건부로 만들 수 있다는 것입니다. 이를 통해 문제가 있는 입력에 대해서만 디버거를 시작할 수 있습니다:

```r
if (input$value == "a") {
  browser()
}
```


# 또는 아마도
if (my_reactive() < 0) {
  browser()
}
```

RStudio에서 중단점을 추가하려면 줄 번호 왼쪽을 클릭합니다. 빨간색 원을 클릭하여 중단점을 제거할 수 있습니다.

중단점의 장점은 코드가 아니기 때문에 버전 관리 시스템에 실수로 커밋할 걱정이 없다는 것입니다. RStudio를 사용하고 있다면, 그림 5-3과 같은 툴바가 디버깅 중에 콘솔 상단에 나타납니다. 툴바는 이제 사용할 수 있는 디버깅 명령을 기억하기 쉬운 방법입니다. RStudio 외부에서도 사용할 수 있습니다. 단일 문자 명령을 기억해야 합니다. 가장 유용한 세 가지 명령은:

- **Next (n 누름)**: 함수의 다음 단계를 실행합니다. 변수 이름이 n인 경우 print(n)을 사용하여 값을 표시해야 합니다.
- **Continue (c 누름)**: 대화형 디버깅을 종료하고 함수의 일반 실행을 계속합니다. 잘못된 상태를 수정하고 함수가 올바르게 진행되는지 확인하고 싶을 때 유용합니다.
- **Stop (Q 누름)**: 디버깅을 중지하고, 함수를 종료하고, 전역 작업 공간으로 돌아갑니다. 문제가 어디 있는지 파악하고 수정할 준비가 되면 사용하세요.

그림 5-3. RStudio의 디버깅 툴바.

이러한 도구를 사용하여 코드를 한 줄씩 진행하는 것 외에도, 문제가 무엇인지 추적하기 위해 많은 대화형 코드를 작성하고 실행하게 됩니다. 디버깅은 기대와 현실을 체계적으로 비교하여 불일치를 찾는 과정입니다. R에서의 디버깅이 새로운 경우, "Advanced R"의 디버깅 장을 읽어 일반적인 기법을 배우는 것이 좋습니다.

### 사례 연구

"불가능한 것을 제거하면, 남아 있는 것은 아무리 믿기 어려워도 진실일 수밖에 없다."
— 아서 코난 도일의 <네 개의 서명>에서 셜록 홈즈

기본적인 디버깅 접근 방식을 시연하기 위해, "계층적 선택 상자"를 작성할 때 encounters한 작은 문제를 보여드리겠습니다. 먼저 기본 맥락을 보여준 다음, 대화형 디버깅 도구 없이 해결한 문제와 대화형 디버깅이 필요한 문제를 살펴보고, 마지막 놀라운 사실을 발견하게 됩니다.

초기 목표는 꽤 단순합니다: 판매 데이터셋이 있고 영역별로 필터링하고 싶습니다. 데이터는 다음과 같습니다:

```r
sales <- readr::read_csv(
  "sales-dashboard/sales_data_sample.csv")
sales <- sales[c(
  "TERRITORY", "ORDERDATE", "ORDERNUMBER",
  "PRODUCTCODE", "QUANTITY ORDERED", "PRICEEACH"
)]
sales
#> # A tibble: 2,823 x 6
#>    TERRITORY ORDERDATE  ORDERNUMBER PRODUCTCODE
#>    <chr>     <chr>          <dbl> <chr>
#>  1 <NA>      2/24/2003…    10107 S10_1678
#>  2 EMEA      5/7/2003 …    10121 S10_1678
#>  3 EMEA      7/1/2003 …    10134 S10_1678
#>  4 <NA>      8/25/2003…    10145 S10_1678
#> # … with 2,819 more rows
```

영역은 다음과 같습니다:

```r
unique(sales$TERRITORY)
#> [1] NA     "EMEA"  "APAC"  "Japan"
```

이 문제를 처음 시작했을 때, 다른 조사 없이 앱을 작성할 만큼 단순하다고 생각했습니다:

```r
ui <- fluidPage(
  selectInput("territory", "territory",
              choices = unique(sales$TERRITORY)),
  tableOutput("selected")
)
server <- function(input, output, session) {
  selected <- reactive(
    sales[sales$TERRITORY == input$territory, ]
  )
  output$selected <- renderTable(head(selected(), 10))
}
```

8줄짜리 앱—어떻게 잘못될 수 있겠는가? 앱을 열었을 때, 어떤 영역을 선택하든 결측값이 많이 보였습니다. 문제가 될 가능성이 가장 높은 코드는 데이터를 선택하여 표시하는 반응형이었습니다: `sales[sales$TERRITORY == input$territory, ]`. 그래서 앱을 중지하고 부분 집합이 예상한 대로 작동하는지 빠르게 확인했습니다:

```r
sales[sales$TERRITORY == "EMEA", ]
#> # A tibble: 2,481 x 6
#>    TERRITORY ORDERDATE  ORDERNUMBER PRODUCTCODE
#>    <chr>     <chr>          <dbl> <chr>
#>  1 <NA>      <NA>              NA <NA>
#>  2 EMEA      5/7/2003 …    10121 S10_1678
#>  3 EMEA      7/1/2003 …    10134 S10_1678
#>  4 <NA>      <NA>              NA <NA>
#> # … with 2,477 more rows
```

이런! TERRITORY에 많은 결측값이 포함되어 있다는 것을 잊고 있었습니다. 이는 `sales$TERRITORY == "EMEA"`가 많은 결측값을 포함한다는 뜻입니다:

```r
head(sales$TERRITORY == "EMEA", 25)
#> [1] NA  TRUE  TRUE  NA  NA  NA  TRUE  TRUE
#> [9]  NA  TRUE FALSE  NA  NA  NA  TRUE  NA
#> [17] TRUE TRUE  NA  NA  TRUE FALSE  TRUE  NA
#> [25] TRUE
```

이 결측값은 결측 행이 되고, [,를 사용하여 sales 데이터프레임의 부분 집합을 만들 때 입력의 결측값은 출력에서 유지됩니다. 이 문제를 해결하는 방법은 많지만, subset()을 사용하기로 했습니다.3 자동으로 결측값을 제거하고 sales를 타이핑하는 횟수를 줄여주기 때문입니다. 그런 다음 이것이 실제로 작동하는지 다시 확인했습니다:

```r
subset(sales, TERRITORY == "EMEA")
#> # A tibble: 1,407 x 6
#>    TERRITORY ORDERDATE  ORDERNUMBER PRODUCTCODE
#>    <chr>     <chr>          <dbl> <chr>
#>  1 EMEA      5/7/2003 …    10121 S10_1678
#>  2 EMEA      7/1/2003 …    10134 S10_1678
#>  3 EMEA      11/11/200…   10180 S10_1678
#>  4 EMEA      11/18/200…   10188 S10_1678
#> # … with 1,403 more rows
```

이것이 대부분의 문제를 해결했지만, 영역 드롭다운에서 NA를 선택할 때 여전히 문제가 있었습니다: 행이 나타나지 않았습니다. 그래서 다시 콘솔에서 확인했습니다:

```r
subset(sales, TERRITORY == NA)
#> # A tibble: 0 x 6
#> # … with 6 variables: TERRITORY <chr>,
#> #   ORDERDATE <chr>, ORDERNUMBER <dbl>,
#> #   PRODUCTCODE <chr>, QUANTITY ORDERED <dbl>,
#> #   PRICEEACH <dbl>
```

그런 다음 결측값은 전염성이 있기 때문에 이것이 작동하지 않는다는 것을 기억했습니다:

```r
head(sales$TERRITORY == NA, 25)
#> [1] NA NA NA NA NA NA NA NA NA NA NA NA NA
#> [14] NA NA NA NA NA NA NA NA NA NA NA NA
```

이 문제를 해결하기 위해 사용할 수 있는 또 다른 트릭이 있습니다: ==를 %in%로 전환합니다:

```r
head(sales$TERRITORY %in% NA, 25)
#> [1] TRUE FALSE FALSE TRUE TRUE TRUE FALSE
#> [8] FALSE TRUE FALSE FALSE TRUE TRUE TRUE
#> [15] FALSE TRUE FALSE FALSE TRUE TRUE FALSE
#> [22] FALSE FALSE FALSE TRUE FALSE

subset(sales, TERRITORY %in% NA)
#> # A tibble: 1,074 x 6
#>    TERRITORY ORDERDATE  ORDERNUMBER PRODUCTCODE
#>    <chr>     <chr>          <dbl> <chr>
#>  1 <NA>      2/24/2003…    10107 S10_1678
#>  2 <NA>      8/25/2003…    10145 S10_1678
#>  3 <NA>      10/10/200…   10159 S10_1678
#>  4 <NA>      10/28/200…   10168 S10_1678
#> # … with 1,070 more rows
```

앱을 업데이트하고 다시 시도했지만 여전히 작동하지 않았습니다! 드롭다운에서 "NA"를 선택했을 때 어떤 행도 보이지 않았습니다. 이 시점에서 콘솔에서 할 수 있는 모든 것을 다했다고 판단했고, Shiny 내부의 코드가 예상한 대로 작동하지 않는 이유를 알아내기 위해 실험이 필요했습니다.

문제의 가장 유력한 원인이 selected 반응형에 있을 것이라고 추론했고, 거기에 browser() 문을 추가했습니다. 이렇게 하면 반응형이 두 줄이 되어 {}로 감싸야 했습니다:

```r
server <- function(input, output, session) {
  selected <- reactive({
    browser()
    subset(sales, TERRITORY %in% input$territory)
  })
  output$selected <- renderTable(head(selected(), 10))
}
```

이제 앱이 실행되면 즉시 대화형 콘솔로 던져집니다. 첫 번째 단계는 문제가 있는 상황에 있는지 확인하는 것이었으므로 subset(sales, TERRITORY %in% input$territory)을 실행했습니다. 빈 데이터프레임을 반환하여 원하는 위치에 있다는 것을 알 수 있었습니다. 문제를 보지 못했다면 c를 타이핑하여 앱이 계속 실행되도록 하고, 실패 상태로 만들기 위해 앱과 더 상호작용했을 것입니다.

그런 다음 subset()에 대한 입력이 예상한 대로인지 확인했습니다. 먼저 sales 데이터셋이 괜찮은지 다시 확인했습니다. 앱에서 건드리는 것이 없기 때문에 손상되었을 것이라고는 기대하지 않았지만, 모든 가정을 주의 깊게 확인하는 것이 가장 안전합니다. sales는 괜찮아 보였으므로, 문제는 TERRITORY %in% input$territory에 있어야 합니다.

TERRITORY가 sales의 일부이므로 input$territory를 살펴보는 것으로 시작했습니다:

```r
input$territory
#> [1] "NA"
```

잠시 이 코드를 응시했습니다. 이것도 괜찮아 보였기 때문입니다. 그런 다음 생각했습니다: NA이기를 기대했지만 실제로는 "NA"입니다! 이제 Shiny 앱 외부에서 문제를 재현할 수 있습니다:

```r
subset(sales, TERRITORY %in% "NA")
#> # A tibble: 0 x 6
#> # … with 6 variables: TERRITORY <chr>,
#> #   ORDERDATE <chr>, ORDERNUMBER <dbl>,
#> #   PRODUCTCODE <chr>, QUANTITY ORDERED <dbl>,
#> #   PRICEEACH <dbl>
```

그런 다음 간단한 수정 방법을 찾아 서버에 적용하고 앱을 다시 실행했습니다:

```r
server <- function(input, output, session) {
  selected <- reactive({
    if (input$territory == "NA") {
      subset(sales, is.na(TERRITORY))
    } else {
      subset(sales, TERRITORY == input$territory)
    }
  })
  output$selected <- renderTable(head(selected(), 10))
}
```

성공! 문제가 해결되었습니다! 하지만 이것은 꽤 놀라웠습니다: Shiny가 조용히 NA를 "NA"로 변환했기 때문에, 버그 보고서도 제출했습니다.

몇 주 후에 이 예시를 다시 살펴보고 다양한 영역에 대해 생각하기 시작했습니다. 유럽, 중동, 아프리카(EMEA)와 아시아-태평양(APAC)이 있습니다. 북미는 어디에 있을까요? 그런 다음 깨달았습니다: 원본 데이터에서 약어 NA를 사용했고 R이 이를 결측값으로 읽어들였을 것입니다. 그래서 실제 수정은 데이터 로딩 중에 이루어져야 합니다:

```r
sales <- readr::read_csv(
  "sales-dashboard/sales_data_sample.csv", na = "")
unique(sales$TERRITORY)
#> [1] "NA"    "EMEA"  "APAC"  "Japan"
```

이것은 삶을 훨씬 더 단순하게 만들었습니다!

이것은 디버깅과 관련된 일반적인 패턴입니다: 문제의 원인을 완전히 이해하기 전에 양파의 여러 층을 벗겨내야 하는 경우가 많습니다.

### 반응성 디버깅

디버깅하기 가장 어려운 유형의 문제는 반응형이 예상하지 못한 순서로 실행될 때입니다. 이 책의 이 시점에서는 이 문제를 디버깅하는 데 도움이 되는 도구가 비교적 적습니다. 다음 섹션에서 최소한의 reprex를 만드는 방법을 배우게 되며, 이 유형의 문제에 매우 중요합니다. 나중에这本书에서 기본 이론과 reactive log와 같은 도구에 대해 더 배우게 될 것입니다.

하지만 지금은 여기서 유용한 고전적인 기법에 초점을 맞추겠습니다: "print" 디버깅. print 디버깅의 기본 아이디어는 코드의 특정 부분이 언제 평가되는지 이해해야 할 때 print()를 호출하고 중요한 변수의 값을 표시하는 것입니다. 이를 "print" 디버깅이라고 부르지만(대부분의 언어에서는 print 함수를 사용하므로), R에서는 message()를 사용하는 것이 더 적합합니다:

- `print()`: 데이터 벡터를 표시하도록 설계되어 문자열에 따옴표를 넣고 첫 번째 줄을 [1]로 시작합니다.
- `message()`: 결과를 "표준 출력"이 아닌 "표준 에러"로 보냅니다.这些都是 출력 스트림을 설명하는 전문 용어로, 대화형으로 실행할 때는 동일한 방식으로 표시되기 때문에 일반적으로 눈에 띄지 않습니다. 하지만 앱이 다른 곳에서 호스팅되면 "표준 에러"로 전송된 출력이 로그에 기록됩니다.

message()를 glue::glue()와 결합하는 것도 추천합니다. 이렇게 하면 메시지에서 텍스트와 값을 쉽게 교차시킬 수 있습니다. glue를 본 적이 없다면, {} 안의 모든 것이 평가되어 출력에 삽입된다는 기본 아이디어입니다:

```r
library(glue)
name <- "Hadley"
message(glue("Hello {name}"))
#> Hello Hadley
```

마지막으로 유용한 도구는 str()로, 모든 객체의 상세한 구조를 출력합니다. 예상한 유형의 객체를 가지고 있는지 다시 확인해야 할 때 특히 유용합니다.

다음은 몇 가지 기본 아이디어를 보여주는 간단한 앱입니다. 반응형 내부에서 message()를 사용하는 방식에 유의하세요: 계산을 수행하고, 메시지를 보낸 다음, 이전에 계산된 값을 반환해야 합니다:

```r
ui <- fluidPage(
  sliderInput("x", "x", value = 1,
              min = 0, max = 10),
  sliderInput("y", "y", value = 2,
              min = 0, max = 10),
  sliderInput("z", "z", value = 3,
              min = 0, max = 10),
  textOutput("total")
)
server <- function(input, output, session) {
  observeEvent(input$x, {
    message(glue(
      "Updating y from {input$y} to {input$x * 2}"))
    updateSliderInput(session, "y",
                      value = input$x * 2)
  })
  total <- reactive({
    total <- input$x + input$y + input$z
    message(glue("New total is {total}"))
    total
  })
  output$total <- renderText({
    total()
  })
}
```

앱을 시작하면 콘솔에 다음과 같이 표시됩니다:

```
Updating y from 2 to 2
New total is 6
```

x 슬라이더를 3으로 드래그하면 다음이 표시됩니다:

```
Updating y from 2 to 6
New total is 8
New total is 12
```

결과가 약간 놀라울 수 있습니다. 8장과 "반응형 그래프"에서 무슨 일이 일어나고 있는지 더 자세히 알아보게 될 것입니다.

## 도움 구하기

이러한 기법을 시도한 후에도 여전히 막혀 있다면, 다른 사람에게 물어볼 때입니다. 도움을 얻을 수 있는 좋은 장소는 RStudio 커뮤니티 사이트입니다. 이 사이트는 많은 Shiny 사용자와 Shiny 패키지 자체의 개발자가 읽습니다. 또한 다른 사람을 도움으로써 Shiny 기술을 향상시키고 싶을 때 방문하기 좋은 장소입니다.

가장 유용한 도움을 가능한 한 빨리 얻으려면 reprex, 즉 재현 가능한 예시를 만들어야 합니다. reprex의 목표는 문제를 설명하고 다른 컴퓨터에서 쉽게 실행할 수 있는 가장 작은 R 코드 스니펫을 제공하는 것입니다.

reprex를 만드는 것은 예의 바른 것입니다. 다른 사람이 작업을 수행할 수 있는 형태로 문제의 본질적인 요소를 캡처하기 때문입니다. 도움을 받으려면 가능한 한 쉽게 만들어야 합니다!

reprex는 누군가가 당신을 돕고 싶어 할 때, 정확히 문제가 무엇인지 빠르게 파악하고 가능한 솔루션을 쉽게 실험할 수 있도록 합니다.

### reprex 기초

reprex는 다른 컴퓨터의 R 세션에 복사하여 붙여넣었을 때 작동하는 R 코드입니다. 다음은 간단한 Shiny 앱 reprex입니다:

```r
library(shiny)
ui <- fluidPage(
  selectInput("n", "N", 1:10),
  plotOutput("plot")
)
server <- function(input, output, session) {
  output$plot <- renderPlot({
    n <- input$n * 2
    plot(head(cars, n))
  })
}
shinyApp(ui, server)
```

이 코드는 실행 중인 컴퓨터에 대해 어떤 가정도 하지 않습니다(Shiny가 설치되어 있다는 것만 제외하면). 그래서 누구나 이 코드를 실행하고 문제를 볼 수 있습니다: 이진 연산자에 대한 비숫자 인수라는 오류가 발생합니다.

문제를 명확히 설명하는 것이 도움을 얻는 첫 번째 단계이며, 누구나 코드를 복사하여 붙여넣으면 문제를 재현할 수 있으므로, 코드를 쉽게 탐색하고 가능한 솔루션을 테스트할 수 있습니다. (이 경우 selectInput()이 input$n에서 문자열을 만들므로 as.numeric(input$n)이 필요합니다.)

### reprex 만들기

reprex를 만드는 첫 번째 단계는 코드를 실행하는 데 필요한 모든 것을 포함하는 단일 자체 포함 파일을 만드는 것입니다. 새로운 R 세션을 시작한 다음 코드를 실행하여 작동하는지 확인해야 합니다. 앱을 작동시키는 데 필요한 패키지를4 로드하는 것을 잊지 않았는지 확인하세요.

일반적으로 앱이 다른 사람의 컴퓨터에서 작동하게 만드는 가장 어려운 부분은 자신의 컴퓨터에만 저장된 데이터의 사용을 제거하는 것입니다. 세 가지 유용한 패턴이 있습니다:

1. **데이터가 문제와 직접 관련이 없는 경우**: mtcars나 iris와 같은 내장 데이터셋을 대신 사용할 수 있습니다.
2. **문제를 설명하는 데이터셋을 만드는 R 코드를 작성할 수 있는 경우**:

```r
mydata <- data.frame(
  x = 1:5,
  y = c("a", "b", "c", "d", "e")
)
```

3. **위의 두 기법이 모두 실패하는 경우**: dput()을 사용하여 데이터를 코드로 변환할 수 있습니다. 예를 들어 dput(mydata)는 mydata를 재생성하는 코드를 생성합니다:

```r
dput(mydata)
#> structure(list(x = 1:5,
#>   y = c("a", "b", "c", "d", "e")),
#>   class = "data.frame",
#>   row.names = c(NA, -5L))
```

그 코드를 얻으면 reprex에 넣어 mydata를 생성할 수 있습니다:

```r
mydata <- structure(list(x = 1:5,
  y = structure(1:5,
    .Label = c("a", "b", "c", "d", "e"),
    class = "factor")),
  class = "data.frame",
  row.names = c(NA, -5L))
```

원본 데이터에서 dput()을 실행하면 종종 막대한 양의 코드가 생성되므로, 문제를 설명하는 데이터의 하위 집합을 찾으세요. 제공하는 데이터셋이 작을수록 다른 사람이 문제를 해결하는 데 도움을 받기 쉽습니다. 디스크에서 읽는 것이 문제의 줄일 수 없는 부분으로 보이는 경우, 최후의 전략은 app.R과 필요한 데이터 파일을 모두 포함하는 완전한 프로젝트를 제공하는 것입니다. 이를 제공하는 가장 좋은 방법은 GitHub에서 호스팅되는 RStudio 프로젝트이지만, 그렇지 않으면 로컬에서 실행할 수 있는 zip 파일을 주의 깊게 만들 수 있습니다. 상대 경로(즉, `read.csv("c:\\my-username\\files\\my-data.csv")`가 아닌 `read.csv("my-data.csv")`)를 사용하여 다른 컴퓨터에서 실행할 때도 코드가 작동하도록 하세요.

reader를 고려하고 코드를 약간 포맷팅하여 읽기 쉽게 하는 것도 고려해야 합니다. tidyverse 스타일 가이드를 채택한다면 코드를 자동으로 포맷팅할 수 있습니다.


styler 패키지를 사용하여 자동으로 코드를 다시 포맷팅할 수 있습니다. 이렇게 하면 코드를 빠르게 읽기 쉽게 만들 수 있습니다.

### 최소한의 reprex 만들기

재현 가능한 예시를 만드는 것은 훌륭한 첫 번째 단계입니다. 다른 사람이 정확히 문제를 재현할 수 있게 해주기 때문입니다. 하지만 문제 코드는 종종 잘 작동하는 코드 사이에 묻혀 있어, 괜찮은 코드를 잘라내면 도움을 주는 사람의 삶을 훨씬 더 쉽게 만들 수 있습니다.

Shiny 앱은 종종 복잡하기 때문에 가능한 한 작은 reprex를 만드는 것이 특히 중요합니다. 앱에서 어려움을 겪고 있는 정확한 부분을 추출할 수 있다면, 잠재적인 조력자가 전체 앱을 이해하도록 강요하는 것보다 더 빠르고 고품질의 도움을 받을 수 있습니다. 추가적인 이점으로, 이 과정은 종종 문제의 원인을 발견하게 만들어 다른 사람의 도움을 기다릴 필요가 없게 합니다!

많은 코드를 본질적인 문제로 줄이는 것은 기술이며, 처음에는 그다지 능숙하지 않을 수 있습니다. 괜찮습니다! 코드 복잡성을 가장 조금 줄이는 것도 도움을 주는 사람에게 도움이 되며, 시간이 지남에 따라 reprex 축소 기술이 향상될 것입니다.

코드의 어떤 부분이 문제를 유발하는지 모르는 경우, 좋은 방법은 응용 프로그램의 코드를 한 조각씩 제거하여 문제가 사라질 때까지 제거하는 것입니다. 특정 코드 조각을 제거하면 문제가 멈추면, 그 코드가 문제와 관련되어 있을 가능성이 높습니다. 또는 문제가 다시 나타날 때까지 새로 만든 빈 앱에서 점진적으로 구축하는 것이 더 쉬운 경우도 있습니다.

앱을 문제를 보여주도록 단순화했다면, 마지막으로 확인할 가치가 있습니다:

- UI의 모든 입력과 출력이 문제와 관련이 있는가?
- 앱의 복잡한 레이아웃을 현재 문제에 집중할 수 있도록 단순화할 수 있는가?
- 앱을 예쁘게 보이게 하지만 문제와 관련이 없는 모든 UI 커스터마이징을 제거했는가?
- server()에서 이제 제거할 수 있는 반응형이 있는가?
- 문제를 해결하기 위해 여러 방법을 시도한 경우, 작동하지 않은 시도의 모든 흔적을 제거했는가?
- 문제를 설명하는 데 필요한 모든 패키지를 로드하고 있는가?
- 더미 코드로 함수를 대체하여 패키지를 제거할 수 있는가?

많은 작업이 될 수 있지만, 보상은 큽니다: reprex를 만드는 동안 솔루션을 발견하는 경우가 많고, 그렇지 않더라도 도움을 받기가 훨씬 더 쉬워집니다.

### 사례 연구

최고 수준의 reprex를 만드는 과정을 설명하기 위해, RStudio 커뮤니티에 게시된 Scott Novogoratz의 예시를 사용하겠습니다. 초기 코드는 reprex에 가까웠지만, 두 개의 패키지를 로드하는 것을 잊어 재현 가능하지 않았습니다. 출발점으로 다음을 수행했습니다:

1. 누락된 library(lubridate)와 library(xts)를 추가했습니다.
2. ui와 server를 별도의 객체로 분리했습니다.
3. styler::style_selection()으로 코드를 다시 포맷팅했습니다.

이로써 다음 reprex가 생성되었습니다:

```r
library(xts)
library(lubridate)
library(shiny)

ui <- fluidPage(
  uiOutput("interaction_slider"),
  verbatimTextOutput("breaks")
)

server <- function(input, output, session) {
  df <- data.frame(
    dateTime = c(
      "2019-08-20 16:00:00",
      "2019-08-20 16:00:01",
      "2019-08-20 16:00:02",
      "2019-08-20 16:00:03",
      "2019-08-20 16:00:04",
      "2019-08-20 16:00:05"
    ),
    var1 = c(9, 8, 11, 14, 16, 1),
    var2 = c(3, 4, 15, 12, 11, 19),
    var3 = c(2, 11, 9, 7, 14, 1)
  )

  timeSeries <- as.xts(df[, 2:4],
    order.by = strptime(df[, 1],
      format = "%Y-%m-%d %H:%M:%S"))

  print(paste(min(time(timeSeries)),
    is.POSIXt(min(time(timeSeries))), sep = " "))
  print(paste(max(time(timeSeries)),
    is.POSIXt(max(time(timeSeries))), sep = " "))

  output$interaction_slider <- renderUI({
    sliderInput("slider", "Select Range:",
      min = min(time(timeSeries)),
      max = max(time(timeSeries)),
      value = c(min, max))
  })

  brks <- reactive({
    req(input$slider)
    seq(input$slider[1], input$slider[2],
        length.out = 10)
  })

  output$breaks <- brks
}

shinyApp(ui, server)
```

이 reprex를 실행하면 원래 게시물과 동일한 문제가 나타납니다: "Type mismatch for min, max, and value. Each must be Date, POSIXt, or number."라는 오류입니다. 이는 탄탄한 reprex입니다: 제 컴퓨터에서 쉽게 실행할 수 있고 즉시 문제를 설명합니다. 하지만 약간 길어서 문제가 무엇을 유발하는지 명확하지 않습니다.

이 reprex를 더 단순화하려면, 코드의 각 줄을 주의 깊게 살펴보고 중요한지 확인해야 합니다. 그 과정에서 다음을 발견했습니다:

1. print()로 시작하는 두 줄을 제거해도 오류에 영향을 주지 않았습니다. 그 두 줄은 lubridate::is.POSIXt()를 사용했으며, 이는 lubridate의 유일한 사용이었으므로, 제거하면 lubridate를 로드할 필요가 없어졌습니다.

2. df는 timeSeries라는 xts 데이터프레임으로 변환되는 데이터프레임입니다. 하지만 timeSeries는 time(timeSeries)를 통해서만 사용되며, 이는 날짜/시간을 반환합니다. 그래서 더미 날짜/시간 데이터를 포함하는 새로운 변수 datetime을 만들었습니다. 이것이 여전히 동일한 오류를 생성했으므로, timeSeries와 df를 제거했고, xts의 유일한 사용이었으므로 library(xts)도 제거했습니다.

이러한 변경으로 다음과 같은 새로운 server()가 생성되었습니다:

```r
datetime <- Sys.time() + (86400 * 0:10)

server <- function(input, output, session) {
  output$interaction_slider <- renderUI({
    sliderInput("slider", "Select Range:",
      min = min(datetime),
      max = max(datetime),
      value = c(min, max))
  })

  brks <- reactive({
    req(input$slider)
    seq(input$slider[1], input$slider[2],
        length.out = 10)
  })

  output$breaks <- brks
}
```

그런 다음 이 예시가 서버 함수에서 UI를 생성하는 비교적 정교한 Shiny 기법을 사용한다는 것을 알아챘습니다. 하지만 여기서 renderUI()는 반응형 입력을 사용하지 않으므로, 서버 함수에서 UI로 옮겨도 동일하게 작동해야 합니다.

이렇게 하면 앱을 시작하기 전에 오류가 더 일찍 발생하는 특히 좋은 결과가 나왔습니다:

```r
ui <- fluidPage(
  sliderInput("slider", "Select Range:",
    min = min(datetime),
    max = max(datetime),
    value = c(min, max)),
  verbatimTextOutput("breaks")
)
#> Error: Type mismatch for min, max, and value.
#> i All values must have same type: either
#>   numeric, Date, or POSIXt.
```

이제 오류 메시지에서 힌트를 얻어 min, max, value에 전달하는 각 입력을 살펴보고 문제가 어디 있는지 확인할 수 있습니다:

```r
min(datetime)
#> [1] "2021-03-05 16:38:02 CST"
max(datetime)
#> [1] "2021-03-15 17:38:02 CDT"
c(min, max)
#> [[1]]
#> function (..., na.rm = FALSE).Primitive("min")
#>
#> [[2]]
#> function (..., na.rm = FALSE).Primitive("max")
```

이제 문제가 명확합니다: min과 max 변수를 할당하지 않았기 때문에, 실수로 min()과 max() 함수를 sliderInput()에 전달하고 있습니다. 이 문제를 해결하는 한 가지 방법은 range()를 대신 사용하는 것입니다:

```r
ui <- fluidPage(
  sliderInput("slider", "Select Range:",
    min = min(datetime),
    max = max(datetime),
    value = range(datetime)),
  verbatimTextOutput("breaks")
)
```

이것은 reprex를 만드는 과정에서 나오는 비교적 일반적인 결과입니다: 문제를 핵심 구성 요소로 단순화하면 솔루션이 명확해집니다. 좋은 reprex를 만드는 것은 놀라울 정도로 강력한 디버깅 기법입니다.

이 reprex를 단순화하기 위해, 익숙하지 않은 함수에 대한 많은 실험과 조사가 필요했습니다.5 이것이 당신의 reprex라면 보통 훨씬 쉽습니다. 코드의 의도를 이미 알고 있기 때문입니다. 하지만 여전히 문제가 정확히 어디에서 오는지 알아내기 위해 많은 실험이 필요할 수 있습니다. 좌절스럽고 시간이 많이 걸릴 수 있지만, 여러 가지 이점이 있습니다:

1. Shiny를 아는 사람 누구나 접근할 수 있는 문제 설명을 만들 수 있게 해줍니다(특정 분야를 아는 사람만이 아니라).
2. 코드가 어떻게 작동하는지 더 나은 정신 모델을 구축하게 되어, 미래에 동일하거나 유사한 실수를 할 가능성이 줄어듭니다.
3. 시간이 지남에 따라 reprex를 만드는 속도가 빨라지고, 디버깅할 때 주요 기법 중 하나가 될 것입니다.
4. 완벽한 reprex를 만들지 않더라도, reprex를 개선하기 위해 할 수 있는任何 작업은 다른 사람의 작업량을 줄여줍니다. 이것은 패키지 개발자에게 도움을 요청하려고 할 때 특히 중요합니다. 보통 시간에 대한 수요가 많기 때문입니다.

RStudio 커뮤니티에서 누군가의 앱을 돕고 싶을 때, reprex를 만드는 것이 항상 첫 번째 단계입니다. 이것은 돕고 싶지 않은 사람들을 떼어내기 위한 꾸며진 작업이 아닙니다: 이것이 정확히 제가 시작하는 곳입니다!

## 요약

이 장은 앱 개발, 문제 디버깅, 도움 구하기를 위한 유용한 워크플로우를 제공했습니다. 이러한 워크플로우는 개별 앱을 구체적으로 개선하지 않기 때문에 약간 추상적이고 무시하기 쉬울 수 있습니다. 하지만 워크플로우를 나의 "비밀" 무기 중 하나로 생각합니다: 내가 많은 성과를 거둘 수 있었던 이유 중 하나는 워크플로우를 분석하고 개선하는 데 시간을 투자하기 때문입니다. 여러분도 같은 일을 하시기를 강력히 권장합니다!

레이아웃과 테마에 대한 다음 장은 유용한 기법들의 첫 번째 장입니다. 순서대로 읽을 필요가 없습니다. 현재 앱에 필요한 장으로 자유롭게 건너뛰세요.

1 스니펫은 일반적인 코드 조각을 삽입할 수 있는 텍스트 매크로입니다. 스니펫 사용을 즐긴다면 ThinkR이 정리한 Shiny 전용 스니펫 모음을 확인하세요.

2 프로젝트는 작업 중인 다른 프로젝트와 격리된 자체 포함 디렉토리입니다. RStudio를 사용하지만 현재 프로젝트를 사용하지 않는다면, 프로젝트 지향적 라이프스타일에 대해 읽어보시기를 강력히 추천합니다.

3 앱에서 다른 패키지를 필요로 하지 않도록 subset()을 사용하고 있습니다. 더 큰 앱에서는 dplyr::filter()를 선호할 것입니다. 동작이 더 익숙하기 때문입니다.

4 평소에 패키지를 로드하는 방식에 관계없이, 여러 번의 library() 호출을 사용하는 것을 강력히 추천합니다. 사용하는 도구에 익숙하지 않은 사람들에게 잠재적인 혼동의 원인을 제거합니다.

5 예를 들어, is.POSIXt()가 lubridate 패키지의 일부라는 것을 전혀 몰랐습니다!

# 6장. 레이아웃, 테마, HTML

## 소개

이 장에서는 앱의 전반적인 외형을 제어하는 새로운 도구를 열게 될 것입니다. 입력과 출력을 구성할 수 있는 페이지 레이아웃(단일 및 "다중")에 대해 이야기하는 것으로 시작합니다. 그런 다음 Shiny가 사용하는 CSS 툴킷인 Bootstrap과 테마로 전반적인 시각적 외형을 커스터마이징하는 방법을 배웁니다. 마지막으로 내부에서 무슨 일이 일어나고 있는지에 대한 간략한 논의로 마무리하여, HTML과 CSS를 알고 있다면 Shiny 앱을 더 추가로 커스터마이징할 수 있게 합니다.

예상대로 shiny를 로드하는 것으로 시작합니다:

```r
library(shiny)
```

### 단일 페이지 레이아웃

2장에서 앱의 대화형 컴포넌트를 구성하는 입력과 출력에 대해 배웠습니다. 하지만 페이지에 어떻게 레이아웃하는지에 대해서는 이야기하지 않았고, 대신 fluidPage()를 사용하여 가능한 한 빨리 함께 던져 넣었습니다. Shiny를 배우는 데는 괜찮지만, 사용 가능하거나 시각적으로 매력적인 앱을 만들지는 못하므로, 이제 몇 가지 더 많은 레이아웃 함수를 배울 때입니다.

레이아웃 함수는 앱의 고수준 시각적 구조를 제공합니다. 레이아웃은 함수 호출의 계층 구조로 만들어지며, R의 계층 구조는 생성된 HTML의 계층 구조와 일치합니다. 이를 통해 레이아웃 코드를 이해하는 데 도움이 됩니다. 예를 들어, 다음과 같은 레이아웃 코드를 보면:

```r
fluidPage(
  titlePanel("Hello Shiny!"),
  sidebarLayout(
    sidebarPanel(
      sliderInput("obs", "Observations:",
        min = 0, max = 1000, value = 500)
    ),
    mainPanel(
      plotOutput("distPlot")
    )
  )
)
```

함수 호출의 계층 구조에 집중하세요:

```
fluidPage(
  titlePanel(),
  sidebarLayout(
    sidebarPanel(),
    mainPanel()
  )
)
```

이 함수들은 아직 없지만, 이름을 읽어보면 무슨 일이 일어나고 있는지 짐작할 수 있습니다. 이 코드가 고전적인 앱 디자인을 생성할 것이라고 상상할 수 있습니다: 상단의 제목 표시줄, 그 다음 사이드바(슬라이더 포함)와 메인 패널(플롯 포함).

縮進을 통해 계층 구조를 쉽게 볼 수 있는 것은 일관된 스타일을 사용하는 것이 좋은 이유 중 하나입니다.

이 섹션의 나머지 부분에서는 단일 페이지 앱을 설계하는 데 도움이 되는 함수를 논의한 다음, 다음 섹션에서 다중 페이지 앱으로 이동합니다. Shiny Application 레이아웃 가이드도 확인하는 것을 추천합니다. 약간 오래되었지만 유용한 보석들이 포함되어 있습니다.

#### 페이지 함수

가장 중요하지만 가장 흥미롭지 않은 레이아웃 함수는 fluidPage()로, 거의 모든 예시에서 보았습니다. 하지만 무엇을 하는지, 그리고 혼자 사용하면 어떻게 되는지 살펴봅시다. 그림 6-1이 결과를 보여줍니다: 매우 지루한 앱처럼 보이지만, fluidPage()가 Shiny에 필요한 모든 HTML, CSS, JavaScript를 설정하기 때문에 백그라운드에서 많은 일이 일어나고 있습니다.

fluidPage() 외에도, Shiny는 더 전문적인 상황에서 유용할 수 있는 몇 가지 다른 페이지 함수를 제공합니다: fixedPage()와 fillPage()입니다.

- `fixedPage()`: fluidPage()처럼 작동하지만 고정된 최대 너비를 가집니다.较大的 화면에서 앱이 비합리적으로 넓어지는 것을 방지합니다.
- `fillPage()`: 브라우저의 전체 높이를 채웁니다. 전체 화면을 차지하는 플롯을 만들고 싶을 때 유용합니다.

자세한 내용은 문서에서 확인할 수 있습니다.

그림 6-1. fluidPage()로만 구성된 UI.

#### 사이드바가 있는 페이지

더 복잡한 레이아웃을 만들려면 fluidPage() 내부에서 레이아웃 함수를 호출해야 합니다. 예를 들어, 왼쪽에 입력과 오른쪽에 출력이 있는 두 열 레이아웃을 만들려면 sidebarLayout()과 그 친구들인 titlePanel(), sidebarPanel(), mainPanel()을 사용할 수 있습니다. 다음 코드는 그림 6-2를 생성하는 기본 구조를 보여줍니다:

```r
fluidPage(
  titlePanel(
    # 앱 제목/설명
  ),
  sidebarLayout(
    sidebarPanel(
      # 입력
    ),
    mainPanel(
      # 출력
    )
  )
)
```


# 출력
)
)
```

그림 6-2. 사이드바가 있는 기본 앱의 구조. 더 사실적으로 만들기 위해, 중앙극한정리(Central Limit Theorem)를 시연하는 매우 간단한 앱을 만들기 위해 입력과 출력을 추가합니다(그림 6-3 참조). 이 앱을 직접 실행하면 샘플 수를 늘려 분포가 더 정규에 가까워지는 것을 볼 수 있습니다:

```r
ui <- fluidPage(
  titlePanel("Central limit theorem"),
  sidebarLayout(
    sidebarPanel(
      numericInput("m", "Number of samples:", 2,
                   min = 1, max = 100)
    ),
    mainPanel(
      plotOutput("hist")
    )
  )
)
server <- function(input, output, session) {
  output$hist <- renderPlot({
    means <- replicate(1e4, mean(runif(input$m)))
    hist(means, breaks = 20)
  }, res = 96)
}
```

그림 6-3. 일반적인 앱 디자인은 컨트롤을 사이드바에 배치하고 결과를 메인 패널에 표시합니다.

### 여러 행

내부적으로 sidebarLayout()은 유연한 여러 행 레이아웃 위에 구축되어 있으며, 이를 직접 사용하여 더 시각적으로 복잡한 앱을 만들 수 있습니다. fluidPage()로 시작한 다음 fluidRow()로 행을 만들고 column()으로 열을 만듭니다. 다음 템플릿은 그림 6-4에 표시된 구조를 생성합니다:

```r
fluidPage(
  fluidRow(
    column(4, ...),
    column(8, ...)
  ),
  fluidRow(
    column(6, ...),
    column(6, ...)
  )
)
```

그림 6-4. 간단한 여러 행 앱의 기본 구조.

각 행은 12개의 열로 구성되며, column()의 첫 번째 인수는 해당 열 중 몇 개를 차지할지 지정합니다. 12열 레이아웃은 2열, 3열, 4열 레이아웃을 쉽게 만들거나 좁은 열을 사용하여 스페이서를 만들 수 있으므로 상당한 유연성을 제공합니다. 이 레이아웃의 예시는 그림 4-3에서 볼 수 있습니다.

그리드 시스템을 사용한 설계에 대해 더 배우고 싶다면, Josef Müller-Brockmann의 고전적인 텍스트 Grid Systems in Graphic Design를 강력히 추천합니다.

## 연습 문제

1. sidebarLayout()의 문서를 읽어서 사이드바와 메인 패널의 너비(열 수)를 결정하세요. fluidRow()와 column()으로 외형을 재현할 수 있나요? 무엇이 빠졌나요?

2. 중앙극한정리 앱을 수정하여 사이드바를 왼쪽이 아닌 오른쪽에 배치하세요.

3. 각각 절반의 너비를 차지하는 두 개의 플롯을 포함하는 앱을 만드세요. 플롯 아래의 전체 너비 컨테이너에 컨트롤을 배치하세요.

### 다중 페이지 레이아웃

앱이 복잡해지면 모든 것을 단일 페이지에 맞추는 것이 불가능해질 수 있습니다. 이 섹션에서는 여러 페이지의 환상을 만드는 tabPanel()의 다양한 사용법을 배웁니다. 여전히 단일 기본 HTML 파일을 가진 단일 앱이지만, 조각으로 분해되어 한 번에 하나의 조각만 보이기 때문에 이는 환상입니다.

다중 페이지 앱은 19장에서 배우게 될 모듈과 특히 잘 어울립니다. 모듈은 사용자 인터페이스를 분리하는 것과 동일한 방식으로 서버 함수를 분리할 수 있게 하여, 잘 정의된 연결을 통해서만 상호작용하는 독립적인 컴포넌트를 만듭니다.

#### 탭셋

페이지를 조각으로 나누는 간단한 방법은 tabsetPanel()과 그 가까운 친구 tabPanel()을 사용하는 것입니다. 다음 코드에서 볼 수 있듯이, tabsetPanel()은 임의의 수의 tabPanel()을 위한 컨테이너를 만들며, 각 tabPanel()은 임의의 다른 HTML 컴포넌트를 포함할 수 있습니다. 그림 6-5는 간단한 예시를 보여줍니다:

```r
ui <- fluidPage(
  tabsetPanel(
    tabPanel("Import data",
      fileInput("file", "Data",
                buttonLabel = "Upload..."),
      textInput("delim",
                "Delimiter (leave blank to guess)",
                ""),
      numericInput("skip", "Rows to skip", 0,
                   min = 0),
      numericInput("rows", "Rows to preview", 10,
                   min = 1)
    ),
    tabPanel("Set parameters"),
    tabPanel("Visualise results")
  )
)
```

그림 6-5. tabsetPanel()은 사용자가 하나의 tabPanel()을 선택하여 보게 합니다.

사용자가 어떤 탭을 선택했는지 알고 싶다면, tabsetPanel()에 id 인수를 제공하면 입력이 됩니다. 그림 6-6이 이를 보여줍니다:

```r
ui <- fluidPage(
  sidebarLayout(
    sidebarPanel(
      textOutput("panel")
    ),
    mainPanel(
      tabsetPanel(
        id = "tabset",
        tabPanel("panel 1", "one"),
        tabPanel("panel 2", "two"),
        tabPanel("panel 3", "three")
      )
    )
  )
)
server <- function(input, output, session) {
  output$panel <- renderText({
    paste("Current panel: ", input$tabset)
  })
}
```

그림 6-6. id 인수를 사용하면 탭셋이 입력이 됩니다. 이를 통해 현재 보이는 탭에 따라 앱이 다르게 작동하도록 할 수 있습니다.

tabsetPanel()은 앱의 어디에서든 사용할 수 있습니다. 필요한 경우 다른 컴포넌트(탭셋 포함!) 내부에 탭셋을 중첩하는 것도 완전히 괜찮습니다.

#### 네비게이션 리스트와 네비게이션 바

탭이 수평으로 표시되므로, 사용할 수 있는 탭의 수에는 근본적인 한계가 있습니다. 특히 긴 제목을 가진 탭인 경우에 그렇습니다.

navbarPage()와 navbarMenu()는 더 긴 제목으로 더 많은 탭을 사용할 수 있는 두 가지 대안적인 레이아웃을 제공합니다.

navlistPanel()은 tabsetPanel()과 유사하지만, 탭 제목을 수평이 아닌 사이드바에 수직으로 표시합니다. 다음 코드가 보여주는 것처럼, 일반 문자열로 제목을 추가할 수도 있으며, 이는 그림 6-7을 생성합니다:

```r
ui <- fluidPage(
  navlistPanel(
    id = "tabset",
    "Heading 1",
    tabPanel("panel 1", "Panel one contents"),
    "Heading 2",
    tabPanel("panel 2", "Panel two contents"),
    tabPanel("panel 3", "Panel three contents")
  )
)
```

그림 6-7. navlistPanel()은 탭 제목을 수평이 아닌 수직으로 표시합니다.

다른 접근 방식은 navbarPage()를 사용하는 것입니다: 여전히 탭 제목을 수평으로 표시하지만, navbarMenu()를 사용하여 추가 계층 수준의 드롭다운 메뉴를 추가할 수 있습니다. 그림 6-8은 간단한 예시를 보여줍니다:

```r
ui <- navbarPage(
  "Page title",
  tabPanel("panel 1", "one"),
  tabPanel("panel 2", "two"),
  tabPanel("panel 3", "three"),
  navbarMenu("subpanels",
    tabPanel("panel 4a", "four-a"),
    tabPanel("panel 4b", "four-b"),
    tabPanel("panel 4c", "four-c")
  )
)
```

그림 6-8. navbarPage()는 페이지 상단에 수평 네비게이션 바를 설정합니다.

이러한 레이아웃은 풍부하고 만족스러운 앱을 만들 수 있는 상당한 능력을 제공합니다. 더 나아가려면 기본 설계 시스템에 대해 더 배워야 합니다.

## Bootstrap

앱 커스터마이징 여정을 계속하려면 Shiny가 사용하는 Bootstrap 프레임워크에 대해 조금 더 배워야 합니다. Bootstrap은 HTML 관례, CSS 스타일, JavaScript 스니펫을 편리한 형태로 번들링한 모음입니다. Bootstrap은 원래 Twitter를 위해 개발된 프레임워크에서 성장했으며, 지난 10년 동안 웹에서 가장 인기 있는 CSS 프레임워크 중 하나가 되었습니다.

Bootstrap은 R에서도 인기가 있습니다. rmarkdown::html_document()로 만든 많은 문서와 pkgdown으로 만든 많은 패키지 웹사이트를 보았을 것이며, 둘 다 Bootstrap을 사용합니다. Shiny 개발자로서 Bootstrap에 대해 너무 많이 생각할 필요는 없습니다. Shiny 함수가 자동으로 Bootstrap 호환 HTML을 생성하기 때문입니다. 하지만 Bootstrap이 존재한다는 것을 아는 것이 좋습니다. 그렇게 하면:

1. "테마"에서 논의된 대로 bslib::bs_theme()를 사용하여 코드의 시각적 외형을 커스터마이징할 수 있습니다.
2. "액션 버튼"에서 본 것처럼 class 인수를 사용하여 Bootstrap 클래스 이름으로 레이아웃, 입력, 출력을 커스터마이징할 수 있습니다.
3. bslib 문서의 유틸리티 클래스 섹션에서 설명하는 것처럼 Shiny가 제공하지 않는 Bootstrap 컴포넌트를 생성하는 자체 함수를 만들 수 있습니다.

완전히 다른 CSS 프레임워크를 사용하는 것도 가능합니다. 여러 기존 R 패키지가 Bootstrap의 인기 있는 대안을 래핑하여 이를 쉽게 만듭니다:

- **shiny.semantic**: Appsilon의 이 패키지는 Fomantic-UI 위에 구축됩니다.
- **shinyMobile**: RinteRface의 이 패키지는 Framework7 위에 구축되며 모바일 앱용으로 특별히 설계되었습니다.
- **shinymaterial**: Eric Anderson의 이 패키지는 Google의 Material 디자인 프레임워크 위에 구축됩니다.
- **shinydashboard**: RStudio의 이 패키지는 대시보드를 만들도록 설계된 레이아웃 시스템을 제공합니다.

GitHub에서 더 완전하고 활발히 유지 관리되는 목록을 찾을 수 있습니다.

### 테마

Bootstrap은 R 커뮤니티 내에서 너무 보편적이어서 스타일 피로를 얻기 쉽습니다: 잠시 후, 모든 Shiny 앱과 Rmd가 동일해 보이기 시작합니다. 해결책은 bslib 패키지를 사용한 테마입니다. bslib는 Bootstrap 기본값을 재정의하여 자신만의 외형을 만들 수 있게 하는 비교적 새로운 패키지입니다. 이 글을 쓰는 시점에서 bslib는 주로 Shiny에만 적용되지만, 향상된 테마 기능을 RMarkdown, pkgdown 등으로 가져오는 작업이 진행 중입니다.

회사용 앱을 만드는 경우, 테마에 시간을 투자하는 것을 강력히 추천합니다. 회사 스타일 가이드에 맞게 앱에 테마를 적용하면 자신을 좋게 보이게 만드는 쉬운 방법입니다.

#### 시작하기

bslib::bs_theme()로 테마를 만든 다음, 페이지 레이아웃 함수의 theme 인수로 앱에 적용합니다:

```r
fluidPage(
  theme = bslib::bs_theme(...)
)
```

지정하지 않으면 Shiny는 기본적으로 만들어진 이후로 사용해온 클래식 Bootstrap v3 테마를 사용합니다. 기본적으로 bslib::bs_theme()는 Bootstrap v4를 사용합니다. 내장 컴포넌트만 사용하는 경우 v4를 사용해도 문제가 발생하지 않습니다. 사용자 지정 HTML을 사용한 경우 문제가 발생할 수 있으므로, version = 3으로 v3를 유지하도록 강제할 수 있습니다.

#### Shiny 테마

앱의 전반적인 외형을 변경하는 가장 쉬운 방법은 bslib::bs_theme()의 bootswatch 인수를 사용하여 미리 만들어진 "bootswatch" 테마를 선택하는 것입니다. 그림 6-9는 다음 코드의 결과를 보여주며, "darkly"를 다른 테마로 전환합니다:

```r
ui <- fluidPage(
  theme = bslib::bs_theme(bootswatch = "darkly"),
  sidebarLayout(
    sidebarPanel(
      textInput("txt", "Text input:", "text here"),
      sliderInput("slider", "Slider input:",
                  1, 100, 30)
    ),
    mainPanel(
      h1(paste0("Theme: darkly")),
      h2("Header 2"),
      p("Some text")
    )
  )
)
```

그림 6-9. 동일한 앱에 네 가지 bootswatch 테마를 적용한 결과: darkly, flatly, sandstone, united.

또는 bs_theme()의 다른 인수를 사용하여 자체 테마를 구성할 수 있습니다. bg(배경색), fg(전경색), base_font 등이 있습니다:

```r
theme <- bslib::bs_theme(
  bg = "#0b3d91",
  fg = "white",
  base_font = "Source Sans Pro"
)
```

테마를 미리 보고 커스터마이징하는 쉬운 방법은 bslib::bs_theme_preview(theme)를 사용하는 것입니다. 표준 컨트롤이 많이 적용되었을 때 테마가 어떻게 보이는지 보여주는 Shiny 앱을 열고, 가장 중요한 매개변수를 커스터마이징하기 위한 대화형 컨트롤도 제공합니다.

#### 플롯 테마

앱의 스타일을 크게 커스터마이징한 경우, 플롯도 일치하도록 커스터마이징하고 싶을 수 있습니다. 다행히도 thematic 패키지 덕분에 이 작업은 정말 쉽습니다. thematic 패키지는 ggplot2, lattice, 기본 플롯에 자동으로 테마를 적용합니다. 서버 함수에서 thematic_shiny()를 호출하기만 하면 됩니다. 이렇게 하면 앱 테마에서 모든 설정이 자동으로 결정됩니다(그림 6-10 참조):

```r
library(ggplot2)
ui <- fluidPage(
  theme = bslib::bs_theme(bootswatch = "darkly"),
  titlePanel("A themed plot"),
  plotOutput("plot"),
)
server <- function(input, output, session) {
  thematic::thematic_shiny()
  output$plot <- renderPlot({
    ggplot(mtcars, aes(wt, mpg)) +
      geom_point() +
      geom_smooth()
  }, res = 96)
}
```

그림 6-10. thematic::thematic_shiny()는 ggplot2가 앱 테마와 자동으로 일치하도록 보장합니다.

## 연습 문제

1. bslib::bs_theme_preview()를 사용하여 만들 수 있는 가장 추악한 테마를 만드세요.

### 내부 구조

Shiny는 R 사용자로서 HTML의 세부 사항을 배울 필요가 없도록 설계되었습니다. 하지만 HTML과 CSS를 알고 있다면, Shiny를 더 추가로 커스터마이징할 수 있습니다. 불행히도, HTML과 CSS를 가르치는 것은 이 책의 범위 밖이지만, MDN의 HTML 및 CSS 기초 자습서가 좋은 시작점입니다.

가장 중요한 것은 모든 입력, 출력, 레이아웃 함수 뒤에 마법이 없다는 것입니다: 단지 HTML을 생성할 뿐입니다.2 콘솔에서 UI 함수를 직접 실행하여 HTML을 볼 수 있습니다:

```r
fluidPage(
  textInput("name", "What's your name?")
)
#> <div class="container-fluid">
#>   <div class="form-group shiny-input-container">
#>     <label for="name">What's your name?</label>
#>     <input id="name" type="text"
#>            class="form-control" value=""/>
#>   </div>
#> </div>
```

이것은 `<body>` 태그의 내용이라는 점에 유의하세요. Shiny의 다른 부분이 `<head>` 생성을 담당합니다.

추가적인 CSS 또는 JavaScript 의존성을 포함하려면 htmltools::htmlDependency()를 배워야 합니다. 좋은 시작점은 R-hub 블로그 게시물 "JavaScript for the R Package Developer"와 Outstanding User Interfaces with Shiny의 4장입니다.

ui에 자체 HTML을 추가하는 것도 가능합니다. 한 가지 방법은 HTML() 함수로 리터럴 HTML을 포함하는 것입니다. 다음 예시에서는 문자열에 따옴표를 쉽게 포함하기 위해 "원시 문자열 상수"3 r"()"를 사용합니다:

```r
ui <- fluidPage(
  HTML(r"(
    <h1>This is a heading</h1>
    <p class="my-class">This is some text!</p>
    <ul>
      <li>First bullet</li>
      <li>Second bullet</li>
    </ul>
  )")
)
```

HTML/CSS 전문가라면 fluidPage()를 완전히 건너뛰고 원시 HTML을 제공하는 것에 관심이 있을 수 있습니다. 자세한 내용은 "HTML로 전체 UI 구축"을 참조하세요.

또는 Shiny가 제공하는 HTML 헬퍼를 사용할 수 있습니다. h1()과 p()와 같은 가장 중요한 요소에 대한 일반 함수가 있고, 나머지는 tags 헬퍼를 통해 접근할 수 있습니다. 명명된 인수는 속성이 되고, 명명되지 않은 인수는 자식이 됩니다. 따라서 앞의 HTML을 다음과 같이 재현할 수 있습니다:

```r
ui <- fluidPage(
  h1("This is a heading"),
  p("This is some text", class = "my-class"),
  tags$ul(
    tags$li("First bullet"),
    tags$li("Second bullet")
  )
)
```

코드로 HTML을 생성하는 한 가지 장점은 기존 Shiny 컴포넌트를 사용자 지정 구조에 짜 넣을 수 있다는 것입니다. 예를 들어, 다음 코드는 두 개의 출력을 포함하는 단락을 만들며, 하나는굵게 표시됩니다:

```r
tags$p(
  "You made ",
  tags$b("$", textOutput("amount", inline = TRUE)),
  " in the last ",
  textOutput("days", inline = TRUE),
  " days "
)
```

inline = TRUE의 사용에 유의하세요. textOutput()의 기본값은 완전한 단락을 만드는 것입니다. HTML, CSS, JavaScript를 사용하여 매력적인 사용자 인터페이스를 만드는 방법에 대해 더 배우고 싶다면, David Granjon의 Outstanding User Interfaces with Shiny를 강력히 추천합니다.

## 요약

이 장은 복잡하고 매력적인 Shiny 앱을 만드는 데 필요한 도구를 제공했습니다. 단일 및 다중 페이지 앱을 레이아웃할 수 있는 Shiny 함수(예: fluidPage()와 tabsetPanel())와 테마로 전반적인 시각적 외형을 커스터마이징하는 방법을 배웠습니다. 또한 내부에서 무슨 일이 일어나고 있는지에 대해 조금 배웠습니다: Shiny가 Bootstrap을 사용하고 입력 및 출력 함수가 HTML을 반환하며, 이것을 직접 만들 수도 있다는 것을 알게 되었습니다.

다음 장에서는 앱의 또 다른 중요한 시각적 컴포넌트에 대해 더 배우게 될 것입니다: 그래픽.

1 폰트는 색상보다 약간 까다롭습니다. 앱 뷰어에도 해당 폰트가 있는지 확인해야 하기 때문입니다. 모든 세부 사항은 bs_theme() 문서를 참조하세요.

2 입력과 출력을 R에 연결하는 마법은 다른 곳(JavaScript를 통해)에서 일어나지만, 이것은 이 책의 범위를 훨씬 벗어납니다.

3 R 4.0.0에서 도입되었습니다.

# 7장. 그래픽

## 소개

2장에서 renderPlot()에 대해 간략하게 이야기했습니다. 앱에서 그래픽을 표시하는 강력한 도구입니다. 이 장에서는 마우스 이벤트에 반응하는 대화형 플롯을 만드는 데 이를 최대한 활용하는 방법을 보여줄 것입니다. 동적 너비와 높이로 플롯을 만들고 renderImage()로 이미지를 표시하는 것과 같은 다른 유용한 기법도 배우게 됩니다.

이 장에서는 그래픽의 대부분을 ggplot2를 사용할 것이므로 shiny와 함께 ggplot2가 필요합니다:

```r
library(shiny)
library(ggplot2)
```

### 대화형 기능

plotOutput()의 가장 멋진 점 중 하나는 플롯을 표시하는 출력일 뿐만 아니라 포인터 이벤트에 반응하는 입력도 될 수 있다는 것입니다. 이를 통해 사용자가 플롯의 데이터와 직접 상호작용하는 대화형 그래픽을 만들 수 있습니다. 대화형 그래픽은 폭넓은 응용 범위를 가진 강력한 도구입니다. 모든 가능성을 보여줄 공간이 없으므로, 여기서는 기본 사항에 초점을 맞추고 더 배울 수 있는 리소스를 안내하겠습니다.

#### 기초

플롯은 네 가지 다른 마우스1 이벤트에 반응할 수 있습니다: click, dblclick(더블클릭), hover(마우스가 같은 위치에 잠시 머무를 때), brush(사각형 선택 도구). 이 이벤트를 Shiny 입력으로 변환하려면 해당 plotOutput() 인수에 문자열을 제공합니다. 예를 들어 `plotOutput("plot", click = "plot_click")`. 이렇게 하면 플롯에서의 마우스 클릭을 처리하는 데 사용할 수 있는 input$plot_click이 생성됩니다.

다음은 마우스 클릭을 처리하는 매우 간단한 예시입니다. plot_click 입력을 등록한 다음 이를 사용하여 마우스 클릭 좌표로 출력을 업데이트합니다. 그림 7-1이 결과를 보여줍니다:

```r
ui <- fluidPage(
  plotOutput("plot", click = "plot_click"),
  verbatimTextOutput("info")
)
server <- function(input, output) {
  output$plot <- renderPlot({
    plot(mtcars$wt, mtcars$mpg)
  }, res = 96)
  output$info <- renderPrint({
    req(input$plot_click)
    x <- round(input$plot_click$x, 2)
    y <- round(input$plot_click$y, 2)
    cat("[", x, ", ", y, "]", sep = "")
  })
}
```

그림 7-1. 왼쪽 위 점을 클릭하면 인쇄된 좌표가 업데이트됩니다. https://hadley.shinyapps.io/ms-click 에서 실시간으로 확인하세요. (req()를 사용하여 첫 번째 클릭 전까지 앱이 아무것도 하지 않도록 하고, 좌표가 기본 wt와 mpg 변수 기준임에 유의하세요.)

다음 섹션에서는 이벤트를 더 자세히 설명합니다. click 이벤트부터 시작한 다음 밀접하게 관련된 dblclick과 hover에 대해 간략하게 논의합니다. 그런 다음 네 변(xmin, xmax, ymin, ymax)으로 정의된 사각형 "브러시"를 제공하는 brush 이벤트에 대해 배웁니다. 그런 다음 동작의 결과로 플롯을 업데이트하는 몇 가지 예시를 제시한 다음, Shiny에서의 대화형 그래픽의 일부 제한 사항을 논의합니다.

#### 클릭

포인트 이벤트는 많은 정보를 포함하는 비교적 풍부한 리스트를 반환합니다. 가장 중요한 구성 요소는 x와 y로, 이벤트의 데이터 좌표 위치를 제공합니다. 이 데이터 구조에 대해서는 이야기하지 않겠습니다. 비교적 드문 상황에서만 필요하기 때문입니다. (세부 사항을 원한다면 Shiny 갤러리의 이 앱을 사용하세요.)

대신 nearPoints() 헬퍼를 사용하게 됩니다. 이 헬퍼는 클릭 근처의 행을 포함하는 데이터프레임을 반환하고, 많은 까다로운 세부 사항을 처리합니다. 다음은 nearPoints()가 작동하는 간단한 예시로, 이벤트 근처의 점에 대한 데이터 테이블을 보여줍니다. 그림 7-2는 앱의 스크린샷입니다:

```r
ui <- fluidPage(
  plotOutput("plot", click = "plot_click"),
  tableOutput("data")
)
server <- function(input, output, session) {
  output$plot <- renderPlot({
    plot(mtcars$wt, mtcars$mpg)
  }, res = 96)
  output$data <- renderTable({
    nearPoints(mtcars, input$plot_click,
               xvar = "wt", yvar = "mpg")
  })
}
```


그림 7-2. nearPoints()는 플롯 좌표를 데이터 행으로 변환하여 클릭한 점의 기본 데이터를 쉽게 보여줍니다. https://hadley.shinyapps.io/ms-nearPoints 에서 실시간으로 확인하세요.

여기서 nearPoints()에 네 개의 인수를 제공합니다: 플롯의 기본이 되는 데이터프레임, 입력 이벤트, 축의 변수 이름입니다. ggplot2를 사용하는 경우 xvar와 yvar가 플롯 데이터 구조에서 자동으로 채워지므로 처음 두 인수만 제공하면 됩니다. 그래서 이 장의 나머지 부분에서는 ggplot2를 사용할 것입니다. 이전 예시를 ggplot2로 재구현하면 다음과 같습니다:

```r
ui <- fluidPage(
  plotOutput("plot", click = "plot_click"),
  tableOutput("data")
)
server <- function(input, output, session) {
  output$plot <- renderPlot({
    ggplot(mtcars, aes(wt, mpg)) +
      geom_point()
  }, res = 96)
  output$data <- renderTable({
    req(input$plot_click)
    nearPoints(mtcars, input$plot_click)
  })
}
```

nearPoints()가 정확히 무엇을 반환하는지 궁금할 수 있습니다. "대화형 디버거"에서 논의한 browser()를 사용하기 좋은 곳입니다:

```r
output$data <- renderTable({
  req(input$plot_click)
  browser()
  nearPoints(mtcars, input$plot_click)
})
```

앱을 시작하고 점을 클릭하면 대화형 디버거로 던져집니다. 여기서 nearPoints()를 실행하여 반환 값을 볼 수 있습니다:

```r
nearPoints(mtcars, input$plot_click)
#>                mpg cyl disp  hp drat    wt  qsec vs am gear carb
#> Datsun 710    22.8   4  108  93 3.85 2.320 18.61  1  1    4    1
```

nearPoints()를 사용하는 또 다른 방법은 allRows = TRUE와 addDist = TRUE를 사용하는 것입니다. 이렇게 하면 두 개의 새로운 열이 포함된 원래 데이터프레임이 반환됩니다:

- `dist_`: 행과 이벤트 사이의 거리(픽셀 단위).
- `selected_`: 클릭 이벤트 근처에 있는지 여부(즉, allRows = FALSE일 때 반환될 행인지 여부).

나중에 그 예시를 보게 될 것입니다.

### 다른 포인트 이벤트

동일한 접근 방식은 click, dblclick, hover에서도 동일하게 작동합니다: 인수 이름만 변경하면 됩니다. 필요하다면 clickOpts(), dblclickOpts(), 또는 hoverOpts()를 문자열 대신 제공하여 이벤트를 추가로 제어할 수 있습니다. 이들은 드물게 필요하므로 여기서는 논의하지 않겠습니다. 자세한 내용은 문서를 참조하세요.

하나의 플롯에서 여러 유형의 상호작용을 사용할 수 있습니다. 사용자에게 할 수 있는 일을 설명하기만 하면 됩니다: 마우스 이벤트를 사용하여 앱과 상호작용하는 한 가지 단점은 즉시 발견할 수 없다는 것입니다.3

### 브러싱

플롯에서 점을 선택하는 또 다른 방법은 브러시를 사용하는 것입니다. 네 변으로 정의된 사각형 선택입니다. Shiny에서 브러시를 사용하는 것은 click과 nearPoints()를 마스터하면 간단합니다: brush 인수와 brushedPoints() 헬퍼로 전환하면 됩니다.

다음은 브러시로 선택된 점을 보여주는 또 다른 간단한 예시입니다. 그림 7-3이 결과를 보여줍니다:

```r
ui <- fluidPage(
  plotOutput("plot", brush = "plot_brush"),
  tableOutput("data")
)
server <- function(input, output, session) {
  output$plot <- renderPlot({
    ggplot(mtcars, aes(wt, mpg)) +
      geom_point()
  }, res = 96)
  output$data <- renderTable({
    brushedPoints(mtcars, input$plot_brush)
  })
}
```

그림 7-3. brush 인수를 설정하면 사용자에게 드래그 가능한 "브러시"를 제공합니다. 이 앱에서는 브러시 아래의 점이 테이블에 표시됩니다. https://hadley.shinyapps.io/ms-brushedPoints 에서 실시간으로 확인하세요.

brushOpts()를 사용하여 색상(채우기와 테두리)을 제어하거나, direction = "x" 또는 "y"로 브러싱을 단일 차원으로 제한할 수 있습니다(예: 시계열 브러싱에 유용).

### 플롯 수정

지금까지는 상호작용의 결과를 다른 출력에 표시했습니다. 하지만 대화형 기능의 진정한 아름다움은 상호작용하는 동일한 플롯에 변경 사항을 표시할 때 나타납니다. 안타깝게도, 이는 아직 배우지 않은 고급 반응성 기법이 필요합니다: reactiveVal(). 16장에서 다시 살펴볼 것이지만, 여기서는 너무 유용한 기법이기 때문에 보여주고 싶었습니다. 그 장을 읽은 후에 이 섹션을 다시 읽어야 할 수 있지만, 모든 이론 없이도 잠재적인 응용에 대한 감을 잡을 수 있을 것입니다.

이름에서 짐작할 수 있듯이, reactiveVal()은 reactive()와 상당히 유사합니다. reactiveVal()을 초기 값과 함께 호출하여 반응형 값을 만들고, reactive와 같은 방식으로 값을 검색합니다:

```r
val <- reactiveVal(10)
val()
#> [1] 10
```

큰 차이점은 반응형 값을 업데이트할 수도 있고, 이를 참조하는 모든 반응형 소비자가 재계산된다는 것입니다. 반응형 값은 업데이트에 특수 구문을 사용합니다: 함수처럼 호출하고 첫 번째 인수가 새 값입니다:

```r
val(20)
val()
#> [1] 20
```

현재 값을 사용하여 반응형 값을 업데이트하려면 다음과 같습니다:

```r
val(val() + 1)
val()
#> [1] 21
```

불행히도, 실제로 이 코드를 콘솔에서 실행하려고 하면 오류가 발생합니다. 반응형 환경에서 실행되어야 하기 때문입니다. 이를 통해 실험과 디버깅이 더 어려워집니다. browser()와 유사한 것을 사용하여 shinyApp() 호출 내부에서 실행을 일시 중지해야 하기 때문입니다. 이것은 16장에서 다시 돌아올 과제 중 하나입니다.

지금은 reactiveVal()을 배우는 도전을 제쳐두고, 왜 신경 쓸 가치가 있는지 보여드리겠습니다. 클릭과 플롯의 점 사이의 거리를 시각화하고 싶다고 상상해 보세요. 다음 앱에서는 이러한 거리를 저장하기 위해 반응형 값을 만드는 것으로 시작합니다. 아무것도 클릭하기 전에 사용되는 상수로 초기화합니다. 그런 다음 observeEvent()를 사용하여 마우스가 클릭될 때 반응형 값을 업데이트하고, 점 크기로 거리를 시각화하는 ggplot을 만듭니다. 전체적으로 다음과 같은 코드이며 그림 7-4의 결과를 생성합니다:

```r
set.seed(1014)
df <- data.frame(x = rnorm(100), y = rnorm(100))

ui <- fluidPage(
  plotOutput("plot", click = "plot_click")
)

server <- function(input, output, session) {
  dist <- reactiveVal(rep(1, nrow(df)))

  observeEvent(input$plot_click,
    dist(nearPoints(df, input$plot_click,
                    allRows = TRUE,
                    addDist = TRUE)$dist_))

  output$plot <- renderPlot({
    df$dist <- dist()
    ggplot(df, aes(x, y, size = dist)) +
      geom_point() +
      scale_size_area(limits = c(0, 1000),
                      max_size = 10, guide = NULL)
  }, res = 96)
}
```

그림 7-4. 이 앱은 reactiveVal()을 사용하여 마지막으로 클릭한 점까지의 거리를 저장하며, 이것이 점 크기에 매핑됩니다. 여기서는 왼쪽 끝에 있는 점을 클릭한 결과를 보여줍니다. https://hadley.shinyapps.io/ms-modifying-size 에서 실시간으로 확인하세요.

여기서 주목해야 할 두 가지 중요한 ggplot2 기법이 있습니다:

1. 플로팅하기 전에 데이터프레임에 거리를 추가합니다. 관련 변수를 시각화하기 전에 데이터프레임에 함께 넣는 것이 좋은 관행이라고 생각합니다.
2. scale_size_area()의 limits를 설정하여 클릭 간에 크기가 비교 가능하도록 합니다. 올바른 범위를 찾기 위해 약간의 대화형 실험을 했지만, 필요하다면 정확한 세부 사항을 계산할 수 있습니다(장 끝의 연습 문제를 참조).

더 복잡한 아이디어가 있습니다. 브러시를 사용하여 선택에 점을 점진적으로 추가하고 싶습니다. 여기서는 다른 색상으로 선택을 표시하지만, 다른 많은 응용을 상상할 수 있습니다. 이를 작동시키려면 reactiveVal()을 FALSE 벡터로 초기화한 다음, brushedPoints()와 |를 사용하여 브러시 아래의 모든 점을 선택에 추가합니다. 사용자가 새로 시작할 수 있는 방법을 제공하기 위해 더블클릭으로 선택을 재설정합니다. 그림 7-5는 실행 중인 앱의 두 가지 스크린샷을 보여줍니다:

```r
ui <- fluidPage(
  plotOutput("plot", brush = "plot_brush",
             dblclick = "plot_reset")
)
server <- function(input, output, session) {
  selected <- reactiveVal(rep(FALSE, nrow(mtcars)))

  observeEvent(input$plot_brush, {
    brushed <- brushedPoints(mtcars,
      input$plot_brush, allRows = TRUE)$selected_
    selected(brushed | selected())
  })

  observeEvent(input$plot_reset, {
    selected(rep(FALSE, nrow(mtcars)))
  })

  output$plot <- renderPlot({
    mtcars$sel <- selected()
    ggplot(mtcars, aes(wt, mpg)) +
      geom_point(aes(colour = sel)) +
      scale_colour_discrete(limits = c("TRUE", "FALSE"))
  }, res = 96)
}
```

그림 7-5. 이 앱은 브러시를 "영구적"으로 만들어 드래그하면 현재 선택에 추가됩니다. 다시 한번, 범례(및 색상)가 첫 번째 클릭 후에 변경되지 않도록 스케일의 limits를 설정했습니다.

### 대화형 기능의 한계

진행하기 전에, 대화형 플롯의 한계를 이해하기 위해 기본 데이터 흐름을 이해하는 것이 중요합니다. 기본 흐름은 다음과 같습니다:

1. JavaScript가 마우스 이벤트를 캡처합니다.
2. Shiny가 마우스 이벤트 데이터를 R로 다시 보내고, 앱에 입력이 이제 오래되었다고 알립니다.
3. 모든 하위 반응형 소비자가 재계산됩니다.
4. plotOutput()이 새로운 PNG를 생성하고 브라우저로 보냅니다.

로컬 앱의 경우 병목 현상은 플롯을 그리는 데 걸리는 시간인 경향이 있습니다. 플롯의 복잡성에 따라 상당한 시간이 걸릴 수 있습니다. 하지만 호스팅되는 앱의 경우, 이벤트를 브라우저에서 R로 전송하는 시간과 렌더링된 플롯을 R에서 브라우저로 다시 전송하는 시간도 고려해야 합니다.

일반적으로, 이는 동작과 응답이 즉시 인식되는 Shiny 앱(즉, 플롯이 당신의 동작과 동시에 업데이트되는 것처럼 보이는)을 만드는 것이 불가능하다는 것을 의미합니다. 그 수준의 속도가 필요하다면, JavaScript에서 더 많은 계산을 수행해야 합니다. 한 가지 방법은 JavaScript 그래픽 라이브러리를 래핑하는 R 패키지를 사용하는 것입니다. 지금 이 책을 쓰는 시점에서 Carson Sievert의 Interactive Web-Based Data Visualization with R, Plotly, and Shiny에 문서화된 대로 plotly 패키지가 가장 좋은 경험을 제공할 것이라고 생각합니다.

### 동적 높이와 너비

이 장의 나머지 부분은 대화형 그래픽만큼 흥미롭지는 않지만, 어디선가는 다뤄야 할 중요한 내용을 포함하고 있습니다.

먼저, 사용자 동작에 대응하여 너비와 높이가 변경되도록 플롯 크기를 반응형으로 만들 수 있습니다. 이를 위해 renderPlot()의 width와 height 인수에 인수가 없는 함수를 제공합니다. 이제 이것들은 UI가 아닌 서버에서 정의되어야 합니다(변경될 수 있기 때문입니다). 이 함수는 인수가 없어야 하며 원하는 크기를 픽셀 단위로 반환해야 합니다. 반응형 환경에서 평가되므로 플롯의 크기를 동적으로 만들 수 있습니다.

다음 앱은 기본 아이디어를 설명합니다. 플롯의 크기를 직접 제어하는 두 개의 슬라이더를 제공합니다. 그림 7-6에 몇 가지 샘플 스크린샷이 나와 있습니다. 플롯의 크기를 조정할 때 데이터는 동일하게 유지됩니다: 새로운 무작위 숫자를 얻지 않습니다:

```r
ui <- fluidPage(
  sliderInput("height", "height",
              min = 100, max = 500, value = 250),
  sliderInput("width", "width",
              min = 100, max = 500, value = 250),
  plotOutput("plot", width = 250, height = 250)
)
server <- function(input, output, session) {
  output$plot <- renderPlot(
    width = function() input$width,
    height = function() input$height,
    res = 96,
    { plot(rnorm(20), rnorm(20)) }
  )
}
```

그림 7-6. 플롯 크기를 동적으로 만들어 사용자 동작에 반응하도록 할 수 있습니다. 이 그림은 너비 변경의 효과를 보여줍니다. https://hadley.shinyapps.io/ms-resize 에서 실시간으로 확인하세요.

실제 앱에서는 width와 height 함수에서 더 복잡한 표현식을 사용하게 됩니다. 예를 들어 ggplot2에서 패싯 플롯을 사용하는 경우, 개별 패싯4 크기를 대략 동일하게 유지하기 위해 플롯 크기를 늘리는 데 사용할 수 있습니다.

### 이미지

기존 이미지(플롯이 아닌)를 표시하려면 renderImage()를 사용할 수 있습니다. 예를 들어, 사용자에게 보여주고 싶은 사진 디렉토리가 있을 수 있습니다. 다음 앱은 귀여운 강아지 사진(그림 7-7)을 표시하여 renderImage()의 기본 사항을 설명합니다. 사진은 저의 가장 좋아하는 로열티 프리 스톡 사진 소스인 Unsplash에서 가져왔습니다:

```r
puppies <- tibble::tribble(
  ~breed,       ~id,            ~author,
  "corgi",      "eoqnr8ikwFE",  "alvannee",
  "labrador",   "KCdYn0xu2fU",  "shaneguymon",
  "spaniel",    "TzjMd7i5WQI",  "_redo_"
)
ui <- fluidPage(
  selectInput("id", "Pick a breed",
    choices = setNames(puppies$id, puppies$breed)),
  htmlOutput("source"),
  imageOutput("photo")
)
server <- function(input, output, session) {
  output$photo <- renderImage({
    list(
      src = file.path("puppy-photos",
                      paste0(input$id, ".jpg")),
      contentType = "image/jpeg",
      width = 500,
      height = 650
    )
  }, deleteFile = FALSE)

  output$source <- renderUI({
    info <- puppies[puppies$id == input$id,,
                    drop = FALSE]
    HTML(glue::glue("<p>
      <a href='https://unsplash.com/photos/{info$id}'>
        original
      </a> by
      <a href='https://unsplash.com/@{info$author}'>
        {info$author}
      </a>
    </p>"))
  })
}
```

renderImage()는 리스트를 반환해야 합니다. 가장 중요한 인수는 src로, 이미지 파일의 로컬 경로입니다. 추가로 다음을 제공할 수 있습니다:

- `contentType`: 이미지의 MIME 유형을 정의합니다. 제공하지 않으면 Shiny가 파일 확장자에서 추측하므로, 이미지에 확장자가 없는 경우에만 제공하면 됩니다.
- `width`와 `height`: 이미지의 크기를 알고 있는 경우.
- 기타 모든 인수(예: class 또는 alt)는 HTML의 `<img>` 태그에 속성으로 추가됩니다.

그림 7-7. renderImage()를 사용하여 귀여운 강아지 사진을 표시하는 앱. https://hadley.shinyapps.io/ms-puppies 에서 실시간으로 확인하세요.

deleteFile 인수도 반드시 제공해야 합니다. 불행히도 renderImage()는 원래 임시 파일과 작동하도록 설계되었기 때문에 렌더링 후 이미지를 자동으로 삭제했습니다. 이것은 명백히 매우 위험했으므로 Shiny 1.5.0에서 동작이 변경되었습니다. 이제 Shiny는 이미지를 삭제하지 않고 대신 어떤 동작을 원하는지 명시적으로 선택하도록 강제합니다. Shiny 웹사이트에서 renderImage()에 대해 더 배우고 다른 사용법을 볼 수 있습니다.

## 요약

시각화는 데이터를 전달하는 엄청나게 강력한 방법이며, 이 장은 Shiny 앱을 강화하기 위한 몇 가지 고급 기법을 제공했습니다. 다음으로, 앱에서 무슨 일이 일어나고 있는지에 대한 사용자 피드백을 제공하는 기법을 배우게 되며, 이는 상당한 시간이 걸리는 동작에 특히 중요합니다.

1 이 장을 쓸 때 Shiny는 터치 이벤트를 지원하지 않았으며, 이는 플롯의 대화형 기능이 모바일 기기에서 작동하지 않음을 의미합니다. 이 책을 읽을 때쯤이면 이 이벤트를 지원할 수 있기를 바랍니다.

2 nearestPoints()가 아니라 nearPoints()라는 점에 유의하세요. 기존 데이터 포인트 근처를 클릭하지 않으면 아무것도 반환하지 않습니다.

3 일반적으로 설명 텍스트를 추가하면 인터페이스가 너무 복잡하다는 것을 의미하므로, 가능한 한 피하는 것이 좋습니다.这就是 Don Norman의 The Design of Everyday Things에서 소개한 어포던스(Affordances), 즉 물체가 어떻게 상호작용해야 하는지를 자연스럽게 제안해야 한다는 아이디어의 핵심입니다.

4 안타깝게도, 플롯 테두리 주변의 고정 요소의 크기를 현재 알 수 없으므로 정확히 동일하게 유지하는 쉬운 방법이 없습니다.

# 8장. 사용자 피드백

## 소개

사용자에게 무슨 일이 일어나고 있는지에 대한 통찰력을 제공하면 앱을 더 사용하기 쉽게 만들 수 있습니다. 입력이 의미가 없을 때 더 나은 메시지 형태이거나, 오래 걸리는 작업에 대한 진행률 표시줄 형태일 수 있습니다. 일부 피드백은 출력을 통해 자연스럽게 발생하며, 이미 사용하는 방법을 알고 있지만, 종종 다른 것이 필요합니다.

이 장의 목표는 사용 가능한 다른 옵션 중 일부를 보여주는 것입니다. 먼저 유효성 검사 기법으로 시작하여, 입력(또는 입력의 조합)이 유효하지 않은 상태일 때 사용자에게 알립니다. 그런 다음 알림, 즉 사용자에게 일반적인 메시지를 보내는 것과许多 단계로 구성된 시간 소모적인 작업에 대한 세부 정보를 제공하는 진행률 표시줄로 계속합니다. 마지막으로 위험한 동작과 확인 대화 상자 또는 동작 실행 취소 기능으로 사용자에게 안심을 주는 방법을 논의하며 마무리합니다.

이 장에서는 Andy Merlino의 shinyFeedback과 John Coene의 waiter를 사용합니다. 또한 현재 개발 중인 Joe Cheng의 shinyvalidate 패키지에도 주목해야 합니다.

shiny를 로드하는 것으로 시작합니다:

```r
library(shiny)
```

### 유효성 검사

사용자에게 줄 수 있는 첫 번째이자 가장 중요한 피드백은 잘못된 입력을 받았다는 것입니다. 이것은 R에서 좋은 함수를 작성하는 것과 유사합니다: 사용자 친화적인 함수는 예상되는 입력이 무엇이고 어떻게 기대를 위반했는지 설명하는 명확한 오류 메시지를 제공합니다.

사용자가 앱을 어떻게 잘못 사용할 수 있는지 생각해 보면, R 코드로 오류가 흘러들어가 정보 없는 오류를 생성하는 대신 UI에서 유용한 메시지를 제공할 수 있습니다.

#### 입력 유효성 검사

사용자에게 추가 피드백을 제공하는 좋은 방법은 shinyFeedback 패키지를 사용하는 것입니다. 사용법은 두 단계입니다. 먼저 ui에 useShinyFeedback()을 추가합니다. 이렇게 하면 매력적인 오류 메시지 표시에 필요한 HTML과 JavaScript가 설정됩니다:

```r
ui <- fluidPage(
  shinyFeedback::useShinyFeedback(),
  numericInput("n", "n", value = 10),
  textOutput("half")
)
```

서버 함수에서 feedback(), feedbackWarning(), feedbackDanger(), 또는 feedbackSuccess() 중 하나를 호출합니다. 이들 모두 세 가지 핵심 인수를 가집니다:

- `inputId`: 피드백을 배치할 입력의 ID.
- `show`: 피드백을 표시할지 여부를 결정하는 논리값.
- `text`: 표시할 텍스트.

외형을 추가로 커스터마이징할 수 있는 color와 icon 인수도 있습니다. 자세한 내용은 문서를 참조하세요.

실제 예시에서 이것이 어떻게 함께 작동하는지 살펴봅시다. 짝수만 허용한다고 가정합니다:

```r
server <- function(input, output, session) {
  half <- reactive({
    even <- input$n %% 2 == 0
    shinyFeedback::feedbackWarning(
      "n", !even, "Please select an even number")
    input$n / 2
  })
  output$half <- renderText(half())
}
```

그림 8-1이 결과를 보여줍니다.

그림 8-1. feedbackWarning()은 유효하지 않은 입력에 대한 경고를 표시합니다. 왼쪽의 앱은 유효한 입력을, 오른쪽의 앱은 경고 메시지가 포함된 유효하지 않은(홀수) 입력을 보여줍니다. https://hadley.shinyapps.io/ms-feedback 에서 실시간으로 확인하세요.

오류 메시지가 표시되지만 출력이 여전히 업데이트된다는 점에 유의하세요. 일반적으로 원하지 않습니다. 유효하지 않은 입력은 사용자에게 보여주고 싶지 않은 정보 없는 R 오류를 유발할 가능성이 높기 때문입니다. 입력이 반응형 변경을 트리거하지 않도록 하려면 새로운 도구가 필요합니다: "required"의 약자인 req()입니다. 다음과 같습니다:

```r
server <- function(input, output, session) {
  half <- reactive({
    even <- input$n %% 2 == 0
    shinyFeedback::feedbackWarning(
      "n", !even, "Please select an even number")
    req(even)
    input$n / 2
  })
  output$half <- renderText(half())
}
```

req()에 대한 입력이 true가 아니면, Shiny에게 반응형이 필요한 모든 입력을 가지고 있지 않으므로 중단해야 한다고 특수 신호를 보냅니다.


req()로 실행 취소

req()는 유효성 검사 외부에서 시작하는 것이 가장 이해하기 쉽습니다. 앱을 시작하면 사용자가 아무것도 하기 전에 전체 반응형 그래프가 계산된다는 것을 눈치챘을 수 있습니다. 입력에 의미 있는 기본값을 선택할 수 있을 때는 잘 작동합니다. 하지만 항상 그런 것은 아니며, 때로는 사용자가 실제로 무언가를 할 때까지 기다리고 싶을 때가 있습니다. 이는 세 가지 컨트롤에서 자주 발생합니다:

1. `textInput()`: value = ""를 사용하고 사용자가 무언가를 타이핑할 때까지 아무것도 하지 않으려는 경우.
2. `selectInput()`: 빈 선택 ""를 제공하고 사용자가 선택할 때까지 아무것도 하지 않으려는 경우.
3. `fileInput()`: 사용자가 업로드하기 전에 결과가 비어 있는 경우. 9장의 그림 9-1에서 다시 살펴볼 것입니다.

어떤 조건이 true가 될 때까지 반응형을 "일시 중지"하는 방법이 필요합니다. 이것이 req()의 역할이며, 반응형 생산자가 계속하기 전에 필요한 값을 확인합니다.

예를 들어, 다음 앱을 살펴봅시다. 이 앱은 영어 또는 마오리어로 인사를 생성합니다. 이 앱을 실행하면 그림 8-2와 같은 오류가 발생합니다. 기본 선택인 ""에 해당하는 greetings 벡터의 항목이 없기 때문입니다:

```r
ui <- fluidPage(
  selectInput("language", "Language",
              choices = c("", "English", "Maori")),
  textInput("name", "Name"),
  textOutput("greeting")
)
server <- function(input, output, session) {
  greetings <- c(
    English = "Hello",
    Maori = "Ki ora"
  )
  output$greeting <- renderText({
    paste0(greetings[[input$language]], " ",
           input$name, "!")
  })
}
```

그림 8-2. 언어가 선택되지 않았기 때문에 앱이 로드될 때 정보 없는 오류를 표시합니다.

req()를 사용하여 이 문제를 해결할 수 있습니다. 이제 사용자가 language와 name 모두에 값을 제공할 때까지 아무것도 표시되지 않습니다. 다음 코드와 그림 8-3을 참조하세요:

```r
server <- function(input, output, session) {
  greetings <- c(
    English = "Hello",
    Maori = "Ki ora"
  )
  output$greeting <- renderText({
    req(input$language, input$name)
    paste0(greetings[[input$language]], " ",
           input$name, "!")
  })
}
```

그림 8-3. req()를 사용하여 language와 name이 모두 제공된 후에만 출력이 표시됩니다. https://hadley.shinyapps.io/ms-require-simple2 에서 실시간으로 확인하세요.

1 req()는 특수 조건을 신호하여 작동합니다. 이 특수 조건은 모든 하위 반응형과 출력이 실행을 중지하도록 합니다. 기술적으로, 모든 하위 반응형 소비자를 무효화된 상태로 둡니다. 이 용어는 16장에서 다시 살펴볼 것입니다.

2 req()는 입력 컨트롤 유형에 관계없이 사용자가2 값을 제공한 경우에만 진행하도록 설계되었습니다. 필요하다면 own 논리적 진술과 함께 req()를 사용할 수 있습니다. 예를 들어, req(input$a > 0)은 a가 0보다 클 때 계산이 진행되도록 허용합니다. 이것은 일반적으로 유효성 검사를 수행할 때 사용하는 형태이며, 다음에서 볼 것입니다.

### req()와 유효성 검사

req()와 shinyFeedback을 결합하여 더 도전적인 문제를 해결해 봅시다. 1장에서 만든 단순한 앱으로 돌아갈 것입니다. 이 앱은 내장 데이터셋을 선택하고 내용을 볼 수 있게 해주었습니다. textInput()을 selectInput() 대신 사용하여 더 일반적이고 복잡하게 만들 것입니다. UI는 거의 변경되지 않습니다:

```r
ui <- fluidPage(
  shinyFeedback::useShinyFeedback(),
  textInput("dataset", "Dataset name"),
  tableOutput("data")
)
```

하지만 서버 함수는 약간 더 복잡해져야 합니다. req()를 두 가지 방식으로 사용합니다: 사용자가 값을 입력한 경우에만 계산을 진행하고 싶으므로 req(input$dataset)을 수행합니다. 그런 다음 제공된 이름이 실제로 존재하는지 확인합니다. 존재하지 않으면 오류 메시지를 표시한 다음 req()를 사용하여 계산을 취소합니다. cancelOutput = TRUE의 사용에 유의하세요: 일반적으로 반응형을 취소하면 모든 하위 출력이 재설정됩니다. cancelOutput = TRUE를 사용하면 마지막으로 정상적인 값을 표시한 채로 유지됩니다. 이것은 텍스트를 입력하는 동안 업데이트를 트리거할 수 있는 textInput()에 중요합니다. 결과는 그림 8-4에 나와 있습니다.

```r
server <- function(input, output, session) {
  data <- reactive({
    req(input$dataset)
    exists <- exists(input$dataset, "package:datasets")
    shinyFeedback::feedbackDanger(
      "dataset", !exists, "Unknown dataset")
    req(exists, cancelOutput = TRUE)
    get(input$dataset, "package:datasets")
  })
  output$data <- renderTable({
    head(data())
  })
}
```

그림 8-4. 로드 시 데이터셋 이름이 비어 있으므로 테이블이 비어 있습니다. 유효한 데이터셋 이름(iris)을 입력한 후 데이터가 표시되며, 새로운 데이터셋 이름을 입력하기 위해 Backspace를 누를 때까지 계속 표시됩니다. https://hadley.shinyapps.io/ms-require-cancel 에서 실시간으로 확인하세요.

#### 출력 유효성 검사

shinyFeedback은 문제가 단일 입력과 관련될 때 훌륭합니다. 하지만 때로는 유효하지 않은 상태가 입력의 조합으로 인해 발생합니다. 이 경우 입력 옆에 오류를 넣는 것은 의미가 없습니다(어떤 입력 옆에 넣을까요?), 대신 출력에 넣는 것이 더 의미가 있습니다. Shiny에 내장된 도구인 validate()로 이를 수행할 수 있습니다.

반응형 또는 출력 내부에서 validate(message)가 호출되면, 나머지 코드의 실행을 중단하고 대신 모든 하위 출력에 message를 표시합니다. 다음 코드는 음수 값의 로그 또는 제곱근을 계산하지 않는 간단한 예시를 보여줍니다:

```r
ui <- fluidPage(
  numericInput("x", "x", value = 0),
  selectInput("trans", "transformation",
    choices = c("square", "log", "square-root")),
  textOutput("out")
)
server <- function(input, output, server) {
  output$out <- renderText({
    if (input$x < 0 &&
        input$trans %in% c("log", "square-root")) {
      validate(
        "x can not be negative for this transformation")
    }
    switch(input$trans,
      square = input$x ^ 2,
      "square-root" = sqrt(input$x),
      log = log(input$x)
    )
  })
}
```

결과는 그림 8-5에서 볼 수 있습니다.

그림 8-5. 입력이 유효하면 출력은 변환을 표시합니다. 입력의 조합이 유효하지 않으면 출력이 유용한 메시지로 대체됩니다.

### 알림

문제가 없고 사용자에게 무슨 일이 일어나고 있는지 알리고 싶을 때는 알림을 사용합니다. Shiny에서 알림은 showNotification()으로 만들어지며 페이지의 오른쪽 아래에 쌓입니다. showNotification()을 사용하는 세 가지 기본 방법이 있습니다:

1. 고정된 시간 후 자동으로 사라지는 일시적 알림 표시
2. 프로세스가 시작될 때 알림을 표시하고 프로세스가 끝나면 제거
3. 점진적 업데이트로 단일 알림 업데이트

이 세 가지 기법은 다음 섹션에서 논의됩니다.

#### 일시적 알림

showNotification()을 사용하는 가장 간단한 방법은 단일 인수, 즉 표시하려는 메시지를 사용하여 호출하는 것입니다. 이 동작을 스크린샷으로 캡처하기 어렵습니다. 라이브 앱에서 확인하세요:

```r
ui <- fluidPage(
  actionButton("goodnight", "Good night")
)
server <- function(input, output, session) {
  observeEvent(input$goodnight, {
    showNotification("So long")
    Sys.sleep(1)
    showNotification("Farewell")
    Sys.sleep(1)
    showNotification("Auf Wiedersehen")
    Sys.sleep(1)
    showNotification("Adieu")
  })
}
```

기본적으로 메시지는 5초 후에 사라지지만, duration을 설정하여 재정의할 수 있으며, 사용자는 닫기 버튼을 클릭하여 더 일찍 제거할 수 있습니다. 알림을 더 눈에 띄게 만들려면 type 인수를 "message", "warning", 또는 "error" 중 하나로 설정할 수 있습니다:

```r
server <- function(input, output, session) {
  observeEvent(input$goodnight, {
    showNotification("So long")
    Sys.sleep(1)
    showNotification("Farewell", type = "message")
    Sys.sleep(1)
    showNotification("Auf Wiedersehen",
                     type = "warning")
    Sys.sleep(1)
    showNotification("Adieu", type = "error")
  })
}
```

그림 8-6이 이것이 어떻게 보이는지 보여줍니다.

그림 8-6. "Good night"을 클릭한 후 알림의 진행: 첫 번째 알림이 나타나고, 3초 후 모든 알림이 표시된 다음, 알림이 사라지기 시작합니다. https://hadley.shinyapps.io/ms-notify-persistent 에서 실시간으로 확인하세요.

#### 완료 시 제거

알림의 존재를 오래 걸리는 작업에 연결하는 것이 유용한 경우가 많습니다. 이 경우 작업이 시작될 때 알림을 표시하고 작업이 완료되면 알림을 제거하고 싶습니다. 이를 위해 다음이 필요합니다:

1. 작업이 완료될 때까지 알림이 보이도록 duration = NULL과 closeButton = FALSE를 설정합니다.
2. showNotification()이 반환하는 id를 저장한 다음 이 값을 removeNotification()에 전달합니다.

이를 수행하는 가장 신뢰할 수 있는 방법은 on.exit()를 사용하는 것입니다. on.exit()는 작업이 어떻게 완료되든(성공적이든 오류가 발생하든) 알림이 제거되도록 보장합니다. on.exit()에 대해 더 배우려면 "상태 변경 및 복원" 문서를 읽으세요.

다음 예시는 대용량 CSV 파일을 읽을 때 사용자에게 최신 상태를 유지하는 방법을 보여줍니다:3

```r
server <- function(input, output, session) {
  data <- reactive({
    id <- showNotification("Reading data...",
      duration = NULL, closeButton = FALSE)
    on.exit(removeNotification(id), add = TRUE)
    read.csv(input$file$datapath)
  })
}
```

일반적으로 이러한 유형의 알림은 반응형에 있습니다. 오래 걸리는 계산이 필요할 때만 다시 실행되도록 보장하기 때문입니다.

#### 점진적 업데이트

첫 번째 예시에서 보았듯이, showNotification()에 대한 여러 호출은 일반적으로 여러 알림을 만듭니다. 대신 첫 번째 호출에서 id를 캡처하고 후속 호출에서 사용하여 단일 알림을 업데이트할 수 있습니다:

```r
ui <- fluidPage(
  tableOutput("data")
)
server <- function(input, output, session) {
  notify <- function(msg, id = NULL) {
    showNotification(msg, id = id,
      duration = NULL, closeButton = FALSE)
  }
  data <- reactive({
    id <- notify("Reading data...")
    on.exit(removeNotification(id), add = TRUE)
    Sys.sleep(1)
    notify("Reticulating splines...", id = id)
    Sys.sleep(1)
    notify("Herding llamas...", id = id)
    Sys.sleep(1)
    notify("Orthogonalizing matrices...", id = id)
    Sys.sleep(1)
    mtcars
  })
  output$data <- renderTable(head(data()))
}
```

오래 걸리는 작업에 여러 하위 구성 요소가 있는 경우에 유용합니다. 라이브 앱에서 결과를 볼 수 있습니다.

### 진행률 표시줄

오래 걸리는 작업에 대한 가장 좋은 유형의 피드백은 진행률 표시줄입니다. 프로세스에서 어느 위치에 있는지 알려줄 뿐만 아니라, 얼마나 더 걸릴지 추정하는 데도 도움을 줍니다: 깊은 숨을 쉬어야 할까요, 커피를 마시러 가야 할까요, 아니면 내일 다시 올까요?

이 섹션에서는 진행률 표시줄을 표시하는 두 가지 기법을 보여줄 것입니다. 하나는 Shiny에 내장된 것이고, 다른 하나는 John Coene이 개발한 waiter 패키지의 것입니다.

불행히도, 두 기법 모두 동일한 주요 단점을 가지고 있습니다: 진행률 표시줄을 사용하려면 큰 작업을 대략 동일한 시간이 걸리는 알려진 수의 작은 부분으로 나눌 수 있어야 합니다. 이것은 종종 어렵습니다. 특히 기본 코드가 C로 작성되어 진행률 업데이트를 통신할 방법이 없기 때문입니다. dplyr, readr, vroom과 같은 패키지가有一天 Shiny로 쉽게 전달할 수 있는 진행률 표시줄을 생성하도록 progress 패키지의 도구를 작업 중입니다.

#### Shiny

Shiny로 진행률 표시줄을 만들려면 withProgress()와 incProgress()를 사용해야 합니다.4 다음과 같이 느리게 실행되는 코드가 있다고 상상해 보세요:

```r
for (i in seq_len(step)) {
  x <- function_that_takes_a_long_time(x)
}
```

withProgress()로 감싸는 것으로 시작합니다. 코드가 시작될 때 진행률 표시줄을 표시하고 완료되면 자동으로 제거합니다:

```r
withProgress({
  for (i in seq_len(step)) {
    x <- function_that_takes_a_long_time(x)
  }
})
```

각 단계 후에 incProgress()를 호출합니다:

```r
withProgress({
  for (i in seq_len(step)) {
    x <- function_that_takes_a_long_time(x)
    incProgress(1 / length(step))
  }
})
```

incProgress()의 첫 번째 인수는 진행률 표시줄을 증가시킬 양입니다. 기본적으로 진행률 표시줄은 0에서 시작하여 1에서 끝나므로, 단계 수로 나눈 값만큼 증가하면 루프가 끝날 때 진행률 표시줄이 완료됩니다. 이것이 완전한 Shiny 앱에서 어떻게 보이는지, 그림 8-7을 참조하세요:

```r
ui <- fluidPage(
  numericInput("steps", "How many steps?", 10),
  actionButton("go", "go"),
  textOutput("result")
)
server <- function(input, output, session) {
  data <- eventReactive(input$go, {
    withProgress(
      message = "Computing random number",
      {
        for (i in seq_len(input$steps)) {
          Sys.sleep(0.5)
          incProgress(1 / input$steps)
        }
        runif(1)
      })
  })
  output$result <- renderText(round(data(), 2))
}
```

그림 8-7. 진행률 표시줄은 계산이 얼마나 더 걸리는지 나타내는 데 도움이 됩니다. https://hadley.shinyapps.io/ms-progress 에서 실시간으로 확인하세요.

유의할 점 몇 가지:

1. 진행률 표시줄에 설명 텍스트를 추가하기 위해 선택적 message 인수를 사용했습니다.
2. Sys.sleep()을 사용하여 오래 걸리는 작업을 시뮬레이션했습니다. 코드에서는 이것이 느린 함수일 것입니다.
3. 버튼과 eventReactive()를 결합하여 사용자가 이벤트가 언제 시작되는지 제어할 수 있게 했습니다. 이것은 진행률 표시줄이 필요한 모든 작업에 좋은 관행입니다.

#### Waiter

내장된 진행률 표시줄은 기본 사항에 훌륭하지만, 더 많은 시각적 옵션을 제공하는 것이 필요하다면 waiter 패키지를 사용해 볼 수 있습니다. 앞의 코드를 waiter와 작동하도록 수정하는 것은 간단합니다. UI에 use_waitress()를 추가합니다:

```r
ui <- fluidPage(
  waiter::use_waitress(),
  numericInput("steps", "How many steps?", 10),
  actionButton("go", "go"),
  textOutput("result")
)
```

waiter의 진행률 표시줄 인터페이스는 약간 다릅니다. waiter 패키지는 R6 객체를 사용하여 진행률 관련 함수를 단일 객체로 번들링합니다. R6 객체를 사용해 본 적이 없다면 세부 사항에 너무 걱정하지 마세요. 이 템플릿을 복사하여 붙여넣기만 하면 됩니다. 기본 수명 주기는 다음과 같습니다:

```r
# 새로운 진행률 표시줄 만들기
waitress <- waiter::Waitress$new(
  max = input$steps)
# 완료되면 자동으로 닫기
on.exit(waitress$close())
for (i in seq_len(input$steps)) {
  Sys.sleep(0.5)
  waitress$inc(1)
}
```

waiter의 장점은 사용자 정의할 수 있다는 것입니다. 진행률 표시줄의 모양을 변경하고 브라우저의 오른쪽 상단이 아닌 다른 위치에 표시할 수 있습니다. waiter 문서에서 자세한 내용을 알아보세요.


# 한 단계 증가
waitress$inc(1)
```

Shiny 앱에서 다음과 같이 사용할 수 있습니다:

```r
server <- function(input, output, session) {
  data <- eventReactive(input$go, {
    waitress <- waiter::Waitress$new(
      max = input$steps)
    on.exit(waitress$close())
    for (i in seq_len(input$steps)) {
      Sys.sleep(0.5)
      waitress$inc(1)
    }
    runif(1)
  })
  output$result <- renderText(round(data(), 2))
}
```

기본 표시는 페이지 상단의 얇은 진행률 표시줄입니다. 라이브 앱에서 확인할 수 있지만, 출력을 커스터마이징하는 여러 가지 방법이 있습니다:

기본 테마를 다음 중 하나로 재정의할 수 있습니다:

- `overlay`: 전체 페이지를 숨기는 불투명 진행률 표시줄.
- `overlay-opacity`: 전체 페이지를 덮는 반투명 진행률 표시줄.
- `overlay-percent`: 숫자 백분율도 표시하는 불투명 진행률 표시줄.

전체 페이지에 대한 진행률 표시줄을 표시하는 대신, selector 매개변수를 설정하여 기존 입력 또는 출력 위에 겹쳐서 표시할 수 있습니다. 예를 들어:

```r
waitress <- Waitress$new(selector = "#steps",
                         theme = "overlay")
```

### 스피너

작업에 얼마나 걸릴지 정확히 모르고, 무언가가 일어나고 있다는 것을 사용자에게 안심시키는 애니메이션 스피너를 표시하고 싶을 때가 있습니다. waiter 패키지를 이 작업에도 사용할 수 있습니다. Waitress 대신 Waiter를 사용하기만 하면 됩니다:

```r
ui <- fluidPage(
  waiter::use_waiter(),
  actionButton("go", "go"),
  textOutput("result")
)
server <- function(input, output, session) {
  data <- eventReactive(input$go, {
    waiter <- waiter::Waiter$new()
    waiter$show()
    on.exit(waiter$hide())
    Sys.sleep(sample(5, 1))
    runif(1)
  })
  output$result <- renderText(round(data(), 2))
}
```

그림 8-8은 이것이 앱에서 어떻게 나타나는지 보여줍니다.

그림 8-8. waiter는 무언가가 일어나는 동안 전체 앱 스피너를 표시합니다. https://hadley.shinyapps.io/ms-spinner-1 에서 실시간으로 확인하세요.

Waitress처럼, Waiter를 특정 출력에 대해서도 사용할 수 있습니다. These waiters는 출력이 업데이트될 때 스피너를 자동으로 제거할 수 있으므로 코드가 더 간단합니다:

```r
ui <- fluidPage(
  waiter::use_waiter(),
  actionButton("go", "go"),
  plotOutput("plot"),
)
server <- function(input, output, session) {
  data <- eventReactive(input$go, {
    waiter::Waiter$new(id = "plot")$show()
    Sys.sleep(3)
    data.frame(x = runif(50), y = runif(50))
  })
  output$plot <- renderPlot(plot(data()), res = 96)
}
```

그림 8-9가 결과를 보여줍니다.

그림 8-9. 대신 단일 출력에 대한 스피너를 표시할 수 있습니다. https://hadley.shinyapps.io/ms-spinner-2 에서 실시간으로 확인하세요.

waiter 패키지는 선택할 수 있는 다양한 스피너를 제공합니다. `?waiter::spinners`에서 옵션을 확인한 다음 (예를 들어) Waiter$new(html = spin_ripple())과 같은 것을 선택하세요.

더 간단한 대안은 Dean Attali의 shinycssloaders 패키지를 사용하는 것입니다. JavaScript를 사용하여 Shiny 이벤트를 수신하므로 서버 측 코드가 전혀 필요하지 않습니다. 대신 shinycssloaders::withSpinner()을 사용하여 무효화되었을 때 자동으로 스피너를 얻고 싶은 출력을 감싸기만 하면 됩니다:

```r
library(shinycssloaders)
ui <- fluidPage(
  actionButton("go", "go"),
  withSpinner(plotOutput("plot")),
)
server <- function(input, output, session) {
  data <- eventReactive(input$go, {
    Sys.sleep(3)
    data.frame(x = runif(50), y = runif(50))
  })
  output$plot <- renderPlot(plot(data()), res = 96)
}
```

### 확인 및 실행 취소

때로는 동작이 잠재적으로 위험할 수 있으며, 사용자가 정말로 그것을 원하는지 확인하고 싶거나, 너무 늦기 전에 실행 취소할 수 있는 능력을 제공하고 싶을 때가 있습니다. 이 섹션의 세 가지 기법은 기본 옵션을 설명하고 앱에서 어떻게 구현할 수 있는지에 대한 팁을 제공합니다.

#### 명시적 확인

사용자가 실수로 위험한 동작을 수행하는 것을 보호하는 가장 간단한 접근 방식은 명시적 확인을 요구하는 것입니다. 가장 쉬운 방법은 대화 상자를 사용하는 것입니다. 대화 상자는 사용자가 소수의 동작 중에서 선택하도록 강제합니다. Shiny에서 modalDialog()로 대화 상자를 만듭니다. 이것은 상호작용의 새로운 "모드"를 만들기 때문에 "모달" 대화 상자라고 불립니다. 대화 상자를 처리할 때까지 메인 애플리케이션과 상호작용할 수 없습니다.

디렉토리에서 파일을 삭제하는(또는 데이터베이스의 행을 삭제하는 등) Shiny 앱이 있다고 상상해 보세요. 이것은 실행 취소하기 어렵기 때문에, 사용자가 정말 확실히 하도록 하고 싶을 것입니다. 그림 8-10과 같이 명시적 확인을 요구하는 대화 상자를 만들 수 있습니다:

```r
modal_confirm <- modalDialog(
  "Are you sure you want to continue?",
  title = "Deleting files",
  footer = tagList(
    actionButton("cancel", "Cancel"),
    actionButton("ok", "Delete",
                 class = "btn btn-danger")
  )
)
```

그림 8-10. 대화 상자는 파일을 삭제할지 여부를 확인합니다.

대화 상자를 만들 때 고려해야 할 몇 가지 작지만 중요한 세부 사항이 있습니다:

1. **버튼에 무엇이라고 할 것인가?** 설명적인 것이 좋으므로 yes/no 또는 continue/cancel 대신 핵심 동사를 반복하는 것이 좋습니다.
2. **버튼을 어떻게 순서대로 놓을 것인가?** cancel을 먼저 놓을까요(Mac처럼), continue를 먼저 놓을까요(Windows처럼)? 가장 좋은 옵션은 대부분의 사람이 사용할 것으로 생각하는 플랫폼을 미러링하는 것입니다.
3. **위험한 옵션을 더 눈에 띄게 할 수 있는가?** 여기서는 class = "btn btn-danger"를 사용하여 버튼을 두드러지게 스타일링했습니다. Jakob Nielsen의 "OK-Cancel or Cancel-OK? The Trouble With Buttons" 기사에서 더 많은 좋은 조언을 찾을 수 있습니다.

이 대화 상자를 실제(비록 매우 단순하지만) 앱에서 사용해 봅시다. UI는 "모든 파일 삭제"를 위한 단일 버튼을 노출합니다:

```r
ui <- fluidPage(
  actionButton("delete", "Delete all files?")
)
```

server()에는 두 가지 새로운 아이디어가 있습니다:

1. showModal()과 removeModal()을 사용하여 대화 상자를 표시하고 숨깁니다.
2. modal_confirm에서 UI가 생성한 이벤트를 관찰합니다.

이러한 객체는 ui에서 정적으로 만들어지지 않고 대신 showModal()로 server()에서 동적으로 추가됩니다:

```r
server <- function(input, output, session) {
  observeEvent(input$delete, {
    showModal(modal_confirm)
  })
  observeEvent(input$ok, {
    showNotification("Files deleted")
    removeModal()
  })
  observeEvent(input$cancel, {
    removeModal()
  })
}
```

이 아이디어는 10장에서 훨씬 더 자세히 볼 수 있습니다.

#### 동작 실행 취소

명시적 확인은 드물게 수행되는 파괴적인 동작에 가장 유용합니다. 빈번한 동작의 오류를 줄이고 싶다면 이를 피해야 합니다. 예를 들어, 이 기법은 Twitter에서는 작동하지 않습니다. "이 트윗을 정말로 올리시겠습니까?"라고 말하는 대화 상자가 있다면, 자동으로 yes를 클릭하게 될 것이고, 트윗 10초 후에 오타를 발견했을 때 여전히 같은 후회감을 느낄 것입니다. 이 상황에서는 실제 동작을 수행하기 전에 몇 초를 기다려 사용자가 문제를 발견하고 실행 취소할 수 있는 기회를 주는 것이 더 나은 접근 방식입니다. 이것은 정말 실행취소가 아닙니다(실제로 아무것도 하지 않기 때문이지만), 사용자가 이해할 수 있는 상기시키는 단어입니다.

이 기법을 개인적으로 실행취소 버튼이 있으면 좋겠다고 생각하는 웹사이트, Twitter로 설명합니다. Twitter UI의 본질은 매우 단순합니다: 트윗을 작성할 텍스트 영역과 전송 버튼이 있습니다:

```r
ui <- fluidPage(
  textAreaInput("message", label = NULL,
    placeholder = "What's happening?", rows = 3),
  actionButton("tweet", "Tweet")
)
```

서버 함수는 상당히 복잡하며 아직 이야기하지 않은 기법이 필요합니다. 코드를 이해하는 것에 너무 걱정하지 마세요. 기본 아이디어에 집중하세요: observeEvent()의 특수 인수를 사용하여 몇 초 후에 코드를 실행합니다.

큰 새로운 아이디어는 observeEvent()의 결과를 캡처하여 변수에 저장한다는 것입니다. 이를 통해 옵저버를 파괴할 수 있어 실제로 트윗을 보내는 코드가 결코 실행되지 않습니다:

```r
runLater <- function(action, seconds = 3) {
  observeEvent(
    invalidateLater(seconds * 1000),
    action,
    ignoreInit = TRUE,
    once = TRUE,
    ignoreNULL = FALSE,
    autoDestroy = FALSE
  )
}

server <- function(input, output, session) {
  waiting <- NULL
  last_message <- NULL

  observeEvent(input$tweet, {
    notification <- glue::glue(
      "Tweeted '{input$message}'")
    last_message <<- input$message
    updateTextAreaInput(session, "message", value = "")
    showNotification(
      notification,
      action = actionButton("undo", "Undo?"),
      duration = NULL,
      closeButton = FALSE,
      id = "tweeted",
      type = "warning"
    )
    waiting <<- runLater({
      cat("Actually sending tweet...\n")
      removeNotification("tweeted")
    })
  })

  observeEvent(input$undo, {
    waiting$destroy()
    showNotification("Tweet retracted", id = "tweeted")
    updateTextAreaInput(session, "message",
                        value = last_message)
  })
}
```

라이브 앱에서 실제로 어떻게 작동하는지 확인하세요.

#### 휴지통

며칠 후에 후회할 수 있는 동작에 대한 더 정교한 패턴은 컴퓨터의 휴지통 또는.recycle bin과 같은 것을 구현하는 것입니다. 파일을 삭제하면 영구적으로 삭제되지 않고 대신 보관 장소로 이동되며, 비우려면 별도의 동작이 필요합니다. 이것은 스테로이드를 사용한 "실행취소"와 같습니다. 동작을 후회할 충분한 시간이 있습니다. 확인과도 약간 비슷합니다: 삭제를 영구적으로 만들려면 두 가지 별도의 동작을 수행해야 합니다.

이 기법의 주요 단점은 구현이 상당히 더 복잡하다는 것입니다(실행 취소에 필요한 정보를 저장하는 별도의 "보관 장소"가 필요함) và 축적을 피하기 위해 사용자의 정기적인 개입이 필요하다는 것입니다. 그래서 가장 복잡한 Shiny 앱을 제외하고는 범위 밖이라고 생각하여 여기서 구현을 보여주지 않겠습니다.

## 요약

이 장은 앱에서 무슨 일이 일어나고 있는지 사용자와 소통하는 데 도움이 되는 여러 도구를 제공했습니다. 어떤 의미에서 이러한 기법은 대부분 선택 사항입니다. 하지만 기법 없이도 앱은 작동하지만, 신중한 적용은 사용자 경험의 질에 큰 영향을 줄 수 있습니다.

앱의 유일한 사용자인 경우 피드백을 생략하는 경우가 많지만, 더 많은 사람이 사용할수록 신중한 알림이 보상을 가져올 것입니다.

다음 장에서는 사용자와 파일을 주고받는 방법을 배우게 됩니다.

1 Condition은 오류, 경고, 메시지를 포함하는 전문 용어입니다. 관심이 있다면 Advanced R의 8장에서 R의 condition 시스템에 대한 세부 사항을 더 배울 수 있습니다.

2 더 정확히는, req()는 입력이 truthy, 즉 FALSE, NULL, "", 또는 `?isTruthy`에 설명된 몇 가지 다른 특수한 경우를 제외한 모든 값인 경우에만 진행합니다.

3 CSV 파일 읽기가 응용 프로그램의 병목 현상인 경우, data.table::fread()와 vroom::vroom()을 대신 사용하는 것을 고려해야 합니다. read.csv()보다 몇 배나 빠를 수 있습니다.

4 코드에 for 루프 또는 apply/map 함수가 포함되지 않으면, 진행률 표시줄을 만들기가 매우 어려울 것입니다.

# 9장. 업로드와 다운로드

## 소개

사용자와 파일을 주고받는 것은 앱의 일반적인 기능입니다. 이를 사용하여 분석을 위해 데이터를 업로드하거나 결과를 데이터셋 또는 보고서로 다운로드할 수 있습니다. 이 장은 앱으로 파일을 가져오고 보내는 데 필요한 UI와 서버 컴포넌트를 보여줍니다.

shiny를 로드하는 것으로 시작합니다:

```r
library(shiny)
```

### 업로드

먼저 파일 업로드에 대해 논의하겠습니다. 기본 UI와 서버 컴포넌트를 보여준 다음, 간단한 앱에서 어떻게 함께 작동하는지 보여줄 것입니다.

#### UI

파일 업로드를 지원하는 UI는 간단합니다: UI에 fileInput()을 추가하기만 하면 됩니다:

```r
ui <- fluidPage(
  fileInput("upload", "Upload a file")
)
```

대부분의 다른 UI 컴포넌트와 마찬가지로, 필수 인수는 id와 label 두 개뿐입니다. width, buttonLabel, placeholder 인수는 다른 방식으로 외형을 조정할 수 있게 해줍니다. 여기서는 논의하지 않겠지만, `?fileInput`에서 더 읽을 수 있습니다.

#### 서버

fileInput()을 서버에서 처리하는 것은 다른 입력보다 약간 더 복잡합니다. 대부분의 입력은 단순한 벡터를 반환하지만, fileInput()은 네 개의 열을 가진 데이터프레임을 반환합니다:

- `name`: 사용자의 컴퓨터에 있는 원본 파일 이름.
- `size`: 파일 크기(바이트 단위). 기본적으로 사용자는 최대 5MB까지만 업로드할 수 있습니다. shiny.maxRequestSize 옵션을 설정하여 이 한도를 늘릴 수 있습니다. 예를 들어 최대 10MB를 허용하려면 `options(shiny.maxRequestSize = 10 * 1024^2)`를 실행하세요.
- `type`: 파일의 "MIME 유형".1 이것은 파일 유형의 공식 사양으로, 일반적으로 확장자에서 파생되며 Shiny 앱에서는 거의 필요하지 않습니다.
- `datapath`: 서버에서 데이터가 업로드된 경로. 이 경로를 임시로 취급하세요: 사용자가 더 많은 파일을 업로드하면 이 파일이 삭제될 수 있습니다. 데이터는 항상 임시 디렉토리에 저장되고 임시 이름이 부여됩니다.

이 데이터 구조를 이해하는 가장 쉬운 방법은 간단한 앱을 만드는 것입니다. 다음 코드를 실행하고 몇 가지 파일을 업로드하여 Shiny가 어떤 데이터를 제공하는지 확인하세요:

```r
ui <- fluidPage(
  fileInput("upload", NULL,
    buttonLabel = "Upload...",
    multiple = TRUE),
  tableOutput("files")
)
server <- function(input, output, session) {
  output$files <- renderTable(input$upload)
}
```

강아지 사진 몇 장을 업로드한 후 결과는 그림 9-1에서 볼 수 있습니다.

그림 9-1. 이 간단한 앱은 업로드된 파일에 대해 Shiny가 제공하는 데이터를 정확히 볼 수 있게 해줍니다. https://hadley.shinyapps.io/ms-upload 에서 실시간으로 확인하세요.

label과 buttonLabel 인수를 사용하여 외형을 약간 커스터마이징하고, multiple = TRUE를 사용하여 사용자가 여러 파일을 업로드할 수 있게 한 점에 유의하세요.

#### 데이터 업로드

사용자가 데이터셋을 업로드하는 경우, 두 가지 세부 사항을 인식해야 합니다:

1. `input$upload`는 페이지 로드 시 NULL로 초기화되므로, 첫 번째 파일이 업로드될 때까지 코드가 기다리도록 req(input$file)이 필요합니다.
2. `accept` 인수는 가능한 입력을 제한할 수 있게 해줍니다. 가장 쉬운 방법은 `accept = ".csv"`와 같은 파일 확장자의 문자열 벡터를 제공하는 것입니다. 하지만 accept 인수는 브라우저에 대한 제안일 뿐이고 항상 강제되지 않으므로, 직접 유효성 검사(예: "유효성 검사")를 수행하는 것이 좋은 관행입니다.

R에서 파일 확장자를 얻는 가장 쉬운 방법은 tools::file_ext()이지만, 확장자에서 앞의 .를 제거한다는 점에 유의하세요.

이 모든 아이디어를 결합하면 다음을 얻을 수 있습니다. .csv 또는 .tsv 파일을 업로드하고 처음 n 행을 볼 수 있습니다:

```r
ui <- fluidPage(
  fileInput("file", NULL, accept = c(".csv", ".tsv")),
  numericInput("n", "Rows", value = 5, min = 1, step = 1),
  tableOutput("head")
)
server <- function(input, output, session) {
  data <- reactive({
    req(input$file)
    ext <- tools::file_ext(input$file$name)
    switch(ext,
      csv = vroom::vroom(input$file$datapath,
                         delim = ","),
      tsv = vroom::vroom(input$file$datapath,
                         delim = "\t"),
      validate(
        "Invalid file; Please upload a .csv or .tsv file")
    )
  })
  output$head <- renderTable({
    head(data(), input$n)
  })
}
```

라이브 앱에서 실제로 어떻게 작동하는지 확인하세요. multiple = FALSE(기본값)이므로 input$file은 단일 행 데이터프레임이고, input$file$name과 input$file$datapath는 길이가 1인 문자열 벡터라는 점에 유의하세요.

### 다운로드

다음으로 파일 다운로드를 살펴보겠습니다. 기본 UI와 서버 컴포넌트를 보여준 다음, 사용자가 데이터나 보고서를 다운로드할 수 있도록 사용하는 방법을 시연하겠습니다.

#### 기초

다시 한번, UI는 간단합니다: downloadButton(id) 또는 downloadLink(id)를 사용하여 사용자가 파일을 다운로드할 수 있는 것을 클릭하게 합니다:

```r
ui <- fluidPage(
  downloadButton("download1"),
  downloadLink("download2")
)
```

결과는 그림 9-2에 나와 있습니다.

그림 9-2. 다운로드 버튼과 다운로드 링크.

actionButtons()과 동일한 class와 icon 인수를 사용하여 외형을 커스터마이징할 수 있습니다. "액션 버튼"에서 설명한 대로입니다.

다른 출력과 달리, downloadButton()은 렌더 함수와 쌍을 이룹니다. 대신 downloadHandler()를 사용하며, 다음과 같습니다:

```r
output$download <- downloadHandler(
  filename = function() {
    paste0(input$dataset, ".csv")
  },
  content = function(file) {
    write.csv(data(), file)
  }
)
```

downloadHandler()는 두 가지 인수, 모두 함수입니다:

- `filename`: 파일 이름(문자열로)을 반환하는 인수가 없는 함수. 이 함수의 역할은 다운로드 대화 상자에서 사용자에게 표시될 이름을 만드는 것입니다.
- `content`: 파일을 저장할 경로인 file이라는 인수를 가진 함수. 이 함수의 역할은 Shiny가 알 수 있는 곳에 파일을 저장하는 것입니다. 이렇게 하면 파일 내용을 계속 제어하면서 Shiny가 파일을 어디에 저장할지 제어할 수 있습니다(보안 위치에 배치될 수 있도록). 이는 일반적인 인터페이스이지만 작동합니다.

다음으로 이 조각들을 결합하여 데이터 파일이나 보고서를 사용자에게 전송하는 방법을 보여줄 것입니다.

#### 데이터 다운로드

다음 앱은 datasets 패키지의 모든 데이터셋을 탭으로 구분된 파일로 다운로드할 수 있게 하여 데이터 다운로드의 기본 사항을 보여줍니다(그림 9-3). 유럽의 많은 국가가 숫자의 정수 부분과 소수 부분을 구분하기 위해 쉼표를 사용하기 때문에(예: 1,23 대 1.23) .csv(쉼표로 구분된 값) 대신 .tsv(탭으로 구분된 값)를 사용하는 것을 추천합니다.这意味着 그들은 필드를 구분하기 위해 쉼표를 사용할 수 없으므로 이른바 CSV 파일에서 세미콜론을 대신 사용합니다! 탭으로 구분된 파일을 사용하면 어느 곳에서나 동일하게 작동하므로 이 복잡성을 피할 수 있습니다:

```r
ui <- fluidPage(
  selectInput("dataset", "Pick a dataset",
              ls("package:datasets")),
  tableOutput("preview"),
  downloadButton("download", "Download .tsv")
)
server <- function(input, output, session) {
  data <- reactive({
    out <- get(input$dataset, "package:datasets")
    if (!is.data.frame(out)) {
      validate(paste0("'", input$dataset,
                      "' is not a data frame"))
    }
    out
  })
  output$preview <- renderTable({
    head(data())
  })
  output$download <- downloadHandler(
    filename = function() {
      paste0(input$dataset, ".tsv")
    },
    content = function(file) {
      vroom::vroom_write(data(), file)
    }
  )
}
```

그림 9-3. 이 더 풍부한 앱은 내장 데이터셋을 선택하고 다운로드하기 전에 미리 볼 수 있게 해줍니다. https://hadley.shinyapps.io/ms-download-data 에서 실시간으로 확인하세요.

데이터프레임인 데이터셋만 다운로드할 수 있도록 validate()를 사용하는 점에 유의하세요. 더 나은 접근 방식은 목록을 사전 필터링하는 것이지만, validate()의 또 다른 적용 사례를 볼 수 있습니다.

#### 보고서 다운로드


데이터를 다운로드하는 것 외에도, 앱 사용자가 Shiny 앱에서의 대화형 탐색 결과를 요약하는 보고서를 다운로드할 수 있게 하려고 할 수 있습니다. 이는 상당한 작업입니다. 동일한 정보를 다른 형식으로도 표시해야 하기 때문이지만, 고위험 앱에는 매우 유용합니다. 이러한 보고서를 생성하는 강력한 방법은 매개변수화된 RMarkdown 문서를 사용하는 것입니다.

매개변수화된 RMarkdown 파일에는 YAML 메타데이터에 params 필드가 있습니다:

```yaml
title: My Document
output: html_document
params:
  year: 2018
  region: Europe
  printcode: TRUE
  data: file.csv
```

문서 내부에서는 params 리스트의 요소를 사용하여 이러한 값에 접근할 수 있습니다(예: params$year, params$region). YAML 메타데이터의 값은 기본값입니다. 일반적으로 rmarkdown::render() 호출에서 params 인수를 제공하여 이를 재정의합니다. 이를 통해 동일한 .Rmd에서 많은 다른 보고서를 쉽게 생성할 수 있습니다.

다음은 "다운로드 가능한 보고서 생성"에서 채택한 간단한 예시로, 이 기법에 대해 더 자세히 설명합니다. 핵심 아이디어는 downloadHandler()의 content 인수에서 rmarkdown::render()를 호출하는 것입니다:

```r
ui <- fluidPage(
  sliderInput("n", "Number of points", 1, 100, 50),
  downloadButton("report", "Generate report")
)
server <- function(input, output, session) {
  output$report <- downloadHandler(
    filename = "report.html",
    content = function(file) {
      params <- list(n = input$n)
      id <- showNotification(
        "Rendering report...",
        duration = NULL, closeButton = FALSE)
      on.exit(removeNotification(id), add = TRUE)
      rmarkdown::render("report.Rmd",
        output_file = file,
        params = params,
        envir = new.env(parent = globalenv()))
    })
}
```

다른 출력 형식을 생성하려면 .Rmd의 출력 형식을 변경하고 확장자를 업데이트하면 됩니다(예: .pdf). 라이브 앱에서 실제로 어떻게 작동하는지 확인하세요.

일반적으로 .Rmd를 렌더링하는 데 최소 몇 초가 걸리므로, "알림"에서 알림을 사용하기 좋은 곳입니다.

알 가치가 있는 몇 가지 다른 트릭이 있습니다:

1. RMarkdown는 현재 작업 디렉토리에서 작동하며, 많은 배포 시나리오(예: shinyapps.io)에서 실패합니다. 앱이 시작될 때(즉, 서버 함수 외부에서) 보고서를 임시 디렉토리로 복사하여 이를 해결할 수 있습니다:

```r
report_path <- tempfile(fileext = ".Rmd")
file.copy("report.Rmd", report_path, overwrite = TRUE)
```

그런 다음 rmarkdown::render() 호출에서 "report.Rmd"를 report_path로 교체합니다:

```r
rmarkdown::render(report_path,
  output_file = file,
  params = params,
  envir = new.env(parent = globalenv()))
```

2. 기본적으로 RMarkdown는 현재 프로세스에서 보고서를 렌더링합니다. 이는 Shiny 앱의许多 설정(로드된 패키지, 옵션 등)을 상속한다는 뜻입니다. 더 큰 견고성을 위해 callr 패키지를 사용하여 별도의 R 세션에서 render()를 실행하는 것을 추천합니다:

```r
render_report <- function(input, output, params) {
  rmarkdown::render(input,
    output_file = output,
    params = params,
    envir = new.env(parent = globalenv()))
}

server <- function(input, output) {
  output$report <- downloadHandler(
    filename = "report.html",
    content = function(file) {
      params <- list(n = input$slider)
      callr::r(
        render_report,
        list(input = report_path,
             output = file,
             params = params))
    })
}
```

Mastering Shiny GitHub 저장소 내부의 rmarkdown-report/에서 이러한 조각들이 모두 결합된 것을 볼 수 있습니다.

shinymeta 패키지는 관련된 문제를 해결합니다: 때로는 Shiny 앱의 현재 상태를 향후 다시 실행할 수 있는 재현 가능한 보고서로 전환할 수 있어야 합니다. Joe Cheng의 useR! 2019 기조연설 "Shiny's Holy Grail: Interactivity with Reproducibility"에서 더 많은 정보를 얻을 수 있습니다.

### 사례 연구

마무리로, 파일을 업로드하고(사용자가 제공한 구분자 사용) 미리 보고, Sam Firke의 janitor 패키지를 사용하여 선택적 변환을 수행한 다음, .tsv로 다운로드할 수 있는 작은 사례 연구를 작업해 보겠습니다. 앱 사용법을 이해하기更容易하게 하기 위해 sidebarLayout()을 사용하여 앱을 세 가지 주요 단계로 나눴습니다:

1. 파일 업로드 및 파싱:

```r
ui_upload <- sidebarLayout(
  sidebarPanel(
    fileInput("file", "Data",
              buttonLabel = "Upload..."),
    textInput("delim",
              "Delimiter (leave blank to guess)",
              ""),
    numericInput("skip", "Rows to skip", 0,
                 min = 0),
    numericInput("rows", "Rows to preview", 10,
                 min = 1)
  ),
  mainPanel(
    h3("Raw data"),
    tableOutput("preview1")
  )
)
```

2. 파일 정리:

```r
ui_clean <- sidebarLayout(
  sidebarPanel(
    checkboxInput("snake",
      "Rename columns to snake case?"),
    checkboxInput("constant",
      "Remove constant columns?"),
    checkboxInput("empty",
      "Remove empty cols?")
  ),
  mainPanel(
    h3("Cleaner data"),
    tableOutput("preview2")
  )
)
```

3. 파일 다운로드:

```r
ui_download <- fluidRow(
  column(width = 12,
    downloadButton("download", class = "btn-block"))
)
```

이것들은 단일 fluidPage()로 조립됩니다:

```r
ui <- fluidPage(
  ui_upload,
  ui_clean,
  ui_download
)
```

동일한 조직은 앱을 이해하기 쉽게 만듭니다:

```r
server <- function(input, output, session) {
  # 업로드 -------------------------------------------------------
  raw <- reactive({
    req(input$file)
    delim <- if (input$delim == "") NULL
             else input$delim
    vroom::vroom(input$file$datapath,
                 delim = delim,
                 skip = input$skip)
  })
  output$preview1 <- renderTable(
    head(raw(), input$rows))

  # 정리 ---------------------------------------------------------
  tidied <- reactive({
    out <- raw()
    if (input$snake) {
      names(out) <- janitor::make_clean_names(
        names(out))
    }
    if (input$empty) {
      out <- janitor::remove_empty(out, "cols")
    }
    if (input$constant) {
      out <- janitor::remove_constant(out)
    }
    out
  })
  output$preview2 <- renderTable(
    head(tidied(), input$rows))

  # 다운로드 -----------------------------------------------------
  output$download <- downloadHandler(
    filename = function() {
      paste0("data_", Sys.Date(), ".tsv")
    },
    content = function(file) {
      vroom::vroom_write(tidied(), file)
    })
}
```

이 앱의 스크린샷은 그림 9-4에 나와 있습니다.

그림 9-4. 파일 다운로드 앱. https://hadley.shinyapps.io/ms-upload-download 에서 실시간으로 확인하세요.

## 요약

이 장은 파일을 주고받는 UI와 서버 컴포넌트를 다루었습니다. 파일 업로드는 fileInput()과 reactive()를 사용하여 처리하고, 파일 다운로드는 downloadButton()과 downloadHandler()를 사용하여 처리합니다. 다음 장에서는 앱이 실행되는 동안 사용자 인터페이스를 동적으로 수정하는 방법을 살펴볼 것입니다.


```r
ui <- fluidPage(
  sidebarLayout(
    sidebarPanel(
      selectInput("dist", "Distribution",
        choices = c("normal", "uniform", "exponential")),
      numericInput("n", "Number of samples",
                   value = 100),
      parameter_tabs,
    ),
    mainPanel(
      plotOutput("plot")
    )
  )
)
```

서버 함수에서 샘플을 생성하고 히스토그램을 그리는 방법을 보여준 다음, 어떤 선택이 만들어졌는지에 따라 입력 제어를 전환합니다:

```r
server <- function(input, output, session) {
  observeEvent(input$dist, {
    updateTabsetPanel(inputId = "params",
                      selected = input$dist)
  })

  data <- reactive({
    switch(input$dist,
      normal = rnorm(input$n, input$mean, input$sd),
      uniform = runif(input$n, input$min, input$max),
      exponential = rexp(input$n, input$rate)
    )
  })

  output$plot <- renderPlot(
    hist(data(), breaks = 30, col = "grey50",
         border = "white"),
    res = 96
  )
}
```

라이브 앱에서 어떻게 작동하는지 확인하세요. 그림 10-6은 분포를 변경할 때마다 선택된 탭이 어떻게 바뀌는지 보여줍니다.

그림 10-6. 각 분포에 대한 입력 컨트롤을 표시합니다. https://hadley.shinyapps.io/ms-dynamic-conditional 에서 실시간으로 확인하세요.

### 동적 UI

update 함수와 조건부 탭 세트의 조합으로 꽤 많은 것을 할 수 있지만, UI의 한 부분을 동적으로 제어하는 데는 한계가 있습니다. 전체 레이아웃을 변경하거나 다른 데이터에 따라 완전히 새로운 UI 조각을 만들고 싶을 때도 있습니다. 그게 바로 uiOutput()과 renderUI()을 사용하는 이유입니다.

uiOutput()은 UI의 플레이스홀더를 만들고, renderUI()는 그 플레이스홀더의 내용을 동적으로 만듭니다. 이 조합을 사용하여 UI의 일부분을 완전히 동적으로 만들 수 있습니다. 예를 들어, 위에서 만든 히스토그램을 선택한 변수와 히스토그램의 bin 수를 사용자가 제어할 수 있는 앱으로 변환해 봅시다:

```r
ui <- fluidPage(
  selectInput("dataset", "Choose a dataset",
              ls("package:datasets")),
  selectInput("var", "Variable", character(0)),
  uiOutput("slider"),
  plotOutput("hist")
)
```

서버 함수에서 두 가지 새로운 것을 볼 수 있습니다:

1. updateSelectInput()을 사용하여 변수 선택기를 동적으로 업데이트합니다.
2. renderUI()를 사용하여 동적 sliderInput()을 만듭니다.

```r
server <- function(input, output, session) {
  data <- reactive({
    req(input$dataset)
    get(input$dataset, "package:datasets")
  })

  observeEvent(data(), {
    vars <- names(data())
    updateSelectInput(inputId = "var",
                      choices = vars,
                      selected = vars[1])
  })

  output$slider <- renderUI({
    req(input$var)
    x <- data()[[input$var]]
    if (is.numeric(x)) {
      rng <- range(x, na.rm = TRUE)
      sliderInput("breaks", "Breaks",
                  min = 1, max = 50, value = 30)
    }
  })

  output$hist <- renderPlot({
    req(input$var, input$breaks)
    x <- data()[[input$var]]
    if (is.numeric(x)) {
      hist(x, breaks = input$breaks,
           col = "grey50", border = "white",
           main = input$var)
    }
  })
}
```

결과는 그림 10-7에 나와 있습니다.

그림 10-7. 히스토그램의 bin 수를 동적으로 제어합니다. https://hadley.shinyapps.io/ms-dynamic-ui 에서 실시간으로 확인하세요.

동적 UI는 강력하지만, UI가 완전히 동적이 되면 UI를 파악하기 어려워질 수 있으므로, 문제가 있는 경우에만 동적 UI를 사용해야 합니다. 동적 UI가 없는 방법으로 문제를 해결할 수 있다면, 그 방법이 항상 더 좋습니다.

## 요약

이 장에서 동적 UI의 세 가지 주요 기법을 살펴보았습니다:

1. **update 함수**: 입력 컨트롤의 파라미터를 동적으로 변경합니다.
2. **조건부 탭 세트**: UI의 부분을 조건부로 표시하거나 숨깁니다.
3. **uiOutput()과 renderUI()**: 코드를 사용하여 UI의 일부를 동적으로 생성합니다.

이 세 가지 기법은 꽤 복잡한 상호작용 패턴을 구현할 수 있게 해주지만, 간단한 접근 방식으로 문제가 해결될 수 있다면 그 방법을 사용하는 것이 좋습니다.

다음 장에서는 Shiny 앱을 테스트하고 문제를 디버깅하는 방법에 대해 알아보겠습니다.


```r
mainPanel(
  plotOutput("hist")
)
)
)
```

input$dist의 선택지를 탭 패널의 이름과 정확히 일치시켰다는 점에 유의하세요. 이를 통해 분포가 변경될 때마다 자동으로 컨트롤을 전환하는 다음 observeEvent() 코드를 쉽게 작성할 수 있습니다. 앱의 나머지는 이미 익숙한 기법을 사용합니다:

```r
server <- function(input, output, session) {
  observeEvent(input$dist, {
    updateTabsetPanel(inputId = "params",
                      selected = input$dist)
  })

  sample <- reactive({
    switch(input$dist,
      normal = rnorm(input$n, input$mean, input$sd),
      uniform = runif(input$n, input$min, input$max),
      exponential = rexp(input$n, input$rate)
    )
  })

  output$hist <- renderPlot(hist(sample()), res = 96)
}
```

최종 결과는 그림 10-6에서 볼 수 있습니다. input$mean의 값은 사용자에게 보이는지 여부와 독립적이라는 점에 유의하세요. 기반 HTML 컨트롤은 여전히 존재합니다. 단지 보이지 않을 뿐입니다.

그림 10-6. 정규(왼쪽), 균등(가운데), 지수(오른쪽) 분포의 결과. https://hadley.shinyapps.io/ms-dynamic-conditional 에서 실시간으로 확인하세요.

### 마법사 인터페이스

이 아이디어를 사용하여 "마법사"를 만들 수도 있습니다. 여러 페이지에 걸쳐 정보를 수집하기 쉽게 만드는 인터페이스 유형입니다. 각 "페이지" 내부에 동작 버튼을 배치하여 앞뒤로 이동하기 쉽게 만들었습니다:

```r
ui <- fluidPage(
  tabsetPanel(
    id = "wizard",
    type = "hidden",
    tabPanel("page_1", "Welcome!",
             actionButton("page_12", "next")),
    tabPanel("page_2", "Only one page to go",
             actionButton("page_21", "prev"),
             actionButton("page_23", "next")),
    tabPanel("page_3", "You're done!",
             actionButton("page_32", "prev"))
  )
)

server <- function(input, output, session) {
  switch_page <- function(i) {
    updateTabsetPanel(inputId = "wizard",
                      selected = paste0("page_", i))
  }

  observeEvent(input$page_12, switch_page(2))
  observeEvent(input$page_21, switch_page(1))
  observeEvent(input$page_23, switch_page(3))
  observeEvent(input$page_32, switch_page(2))
}
```

결과는 그림 10-7에 나와 있습니다.

그림 10-7. 마법사 인터페이스는 복잡한 UI를 여러 페이지에 걸쳐 나눕니다. 매우 간단한 예시로 아이디어를 설명하며, "다음"을 클릭하여 다음 페이지로 이동합니다. https://hadley.shinyapps.io/ms-wizard 에서 실시간으로 확인하세요.

서버 코드의 중복을 줄이기 위해 switch_page() 함수를 사용한 점에 유의하세요. 이 아이디어는 18장에서 다시 살펴보고, "마법사"에서 마법사 인터페이스를 자동화하는 모듈을 만들겠습니다.

#### 연습 문제

1. 숨긴 탭 세트를 사용하여 사용자가 "고급" 체크박스를 선택한 경우에만 추가 컨트롤을 표시하세요.
2. ggplot(diamonds, aes(carat))을 플롯하지만 사용자가 어떤 geom을 사용할지 선택할 수 있는 앱을 만드세요: geom_histogram(), geom_freqpoly(), 또는 geom_density(). 숨긴 탭 세트를 사용하여 geom에 따라 사용자가 다른 인수를 선택할 수 있게 하세요: geom_histogram()과 geom_freqpoly()에는 binwidth 인수가 있고, geom_density()에는 bw 인수가 있습니다.
3. 이전 연습문제에서 만든 앱을 수정하여 사용자가 각 geom을 표시할지 여부를 선택할 수 있게 하세요(즉, 항상 하나의 geom을 사용하는 대신 0, 1, 2 또는 3개를 선택할 수 있음). 히스토그램과 빈도 다각형의 binwidth를 독립적으로 제어할 수 있는지 확인하세요.

### 코드로 UI 만들기

이전에 설명한 기법 중 어떤 것도 원하는 수준의 동적 기능을 제공하지 못하는 경우가 있습니다. update 함수는 기존 입력을 변경할 수 있게 해주고, 탭 세트는 고정된 알려진 가능한 조합 집합이 있는 경우에만 작동합니다. 때로는 다른 입력에 따라 다른 유형이나 수의 입력(또는 출력)을 만들어야 합니다. 이 마지막 기법을 사용하면 그렇게 할 수 있습니다.

이전까지 항상 코드로 사용자 인터페이스를 만들었지만, 앱이 시작되기 전에 그렇게 했다는 점에 유의하세요. 이 기법은 앱이 실행되는 동안 사용자 인터페이스를 생성하고 수정할 수 있게 해줍니다.

이 솔루션에는 두 부분이 있습니다:

1. uiOutput()은 ui에 플레이스홀더를 삽입합니다. 서버 코드가 나중에 채울 "구멍"을 남깁니다.
2. renderUI()는 server() 내부에서 동적으로 생성된 UI로 플레이스홀더를 채우기 위해 호출됩니다.

간단한 예시로 이것이 어떻게 작동하는지 살펴본 다음 실제 사용 사례를 살펴보겠습니다.

#### 시작하기

다음과 같이 동적으로 입력 컨트롤을 만드는 간단한 앱으로 시작해 봅시다. 유형과 레이블은 다른 두 입력에 의해 제어됩니다:

```r
ui <- fluidPage(
  textInput("label", "label"),
  selectInput("type", "type", c("slider", "numeric")),
  uiOutput("numeric")
)

server <- function(input, output, session) {
  output$numeric <- renderUI({
    if (input$type == "slider") {
      sliderInput("dynamic", input$label,
                  value = 0, min = 0, max = 10)
    } else {
      numericInput("dynamic", input$label,
                   value = 0, min = 0, max = 10)
    }
  })
}
```

결과는 그림 10-8에 나와 있습니다.

그림 10-8. 로드 시 앱(왼쪽), 유형을 numeric으로 변경(가운데), 레이블을 "my label"로 변경(오른쪽). https://hadley.shinyapps.io/ms-render-simple 에서 실시간으로 확인하세요.

이 코드를 직접 실행하면 앱 로드 후 약간의 지연 후에 나타나는 것을 알 수 있습니다. 이것은 반응적이기 때문입니다: 앱은 로드되고 반응적 이벤트를 트리거해야 하며, 이는 서버 함수를 호출하여 페이지에 삽입할 HTML를 생성합니다. 이것이 renderUI()의 단점 중 하나입니다. 이에 너무 의존하면 UI가 느려질 수 있습니다.

좋은 성능을 위해, 앞서 장에서 설명한 기법을 사용하여 가능한 한 사용자 인터페이스의 많은 부분을 고정된 상태로 유지하려고 노력하세요.

이 접근 방식의 또 다른 문제가 있습니다: 컨트롤을 변경하면 현재 선택된 값을 잃게 됩니다. 기존 상태를 유지하는 것은 코드로 UI를 만들 때의 큰 도전 중 하나입니다. 이것이 UI를 선택적으로 표시하고 숨기는 것이 더 나은 접근 방식인 이유之一입니다. 컨트롤을 파괴하고 다시 만들지 않으므로 값을 보존하기 위해 아무것도 할 필요가 없기 때문입니다.

그러나 많은 경우 다음 서버 함수에서 볼 수 있듯이, 새로운 입력의 값을 기존 컨트롤의 현재 값으로 설정하여 이 문제를 해결할 수 있습니다:

```r
server <- function(input, output, session) {
  output$numeric <- renderUI({
    value <- isolate(input$dynamic)
    if (input$type == "slider") {
      sliderInput("dynamic", input$label,
                  value = value, min = 0, max = 10)
    } else {
      numericInput("dynamic", input$label,
                   value = value, min = 0, max = 10)
    }
  })
}
```

isolate()의 사용이 중요합니다. 이것이 무엇을 하는지는 "isolate()"에서 다시 살펴보겠지만, 여기서는 사용자가 값을 수정할 때마다(그렇게 됩니다) 이 코드가 다시 실행되는 반응적 의존성을 만들지 않도록 합니다. input$type 또는 input$label이 변경될 때만 변경되도록 하고 싶습니다.

#### 여러 컨트롤

동적 UI는 임의의 수 또는 유형의 컨트롤을 생성할 때 가장 유용합니다. 즉, 코드로 UI를 생성하게 되며, 이 유형의 작업에는 함수형 프로그래밍을 사용하는 것을 추천합니다. 여기서는 purrr::map()과 purrr::reduce()를 사용하지만, 기본 lapply()와 Reduce() 함수로도 동일하게 할 수 있습니다:

```r
library(purrr)
```

함수형 프로그래밍의 map()과 reduce()에 익숙하지 않다면, 계속하기 전에 "함수형 프로그래밍"을 잠시 읽는 것이 좋습니다. 이 아이디어는 18장에서 다시 살펴보겠습니다. 이것들은 복잡한 개념이므로 첫 번째 읽기에서 이해되지 않으면 스트레스를 받지 마세요.

구체적으로 만들어 보겠습니다. 사용자가 자신의 색상 팔레트를 제공할 수 있기를 원한다고 상상해 보세요. 먼저 원하는 색상 수를 지정한 다음 각 색상에 대한 값을 제공합니다. UI는 꽤 단순합니다: 입력 수를 제어하는 numericInput(), 생성된 텍스트 상자가 들어갈 uiOutput(), 그리고 모든 것을 올바르게 연결했음을 보여주는 textOutput()이 있습니다:

```r
ui <- fluidPage(
  numericInput("n", "Number of colours",
               value = 5, min = 1),
  uiOutput("col"),
  textOutput("palette")
)
```

서버 함수는 짧지만 큰 아이디어를 담고 있습니다:

```r
server <- function(input, output, session) {
  col_names <- reactive(paste0("col", seq_len(input$n)))
  output$col <- renderUI({
    map(col_names(), ~ textInput(.x, NULL))
  })
  output$palette <- renderText({
    map_chr(col_names(), ~ input[[.x]] %||% "")
  })
}
```

반응적 col_names()를 사용하여 생성할 각 색상 입력의 이름을 저장합니다. 그런 다음 map()을 사용하여 각 이름에 대한 textInput() 리스트를 만듭니다. renderUI()는 이 HTML 컴포넌트 리스트를 가져와 UI에 추가합니다.

입력 값을 접근하는 새로운 트릭을 사용해야 합니다. 지금까지 항상 $를 사용하여 input의 컴포넌트에 접근했습니다(예: input$col1). 하지만 여기서는 `var <- "col1"`과 같은 문자열 벡터로 입력 이름을 가지고 있습니다. 이 시나리오에서는 $가 더 이상 작동하지 않으므로 [[를 사용해야 합니다(즉, input[[var]])).

map_chr()을 사용하여 모든 값을 문자열 벡터로 수집하고 output$palette에 표시합니다. 불행히도 브라우저에 의해 새로운 입력이 렌더링되기 직전에 값이 NULL인 짧은 기간이 있습니다. 이것이 map_chr()을 에러나게 하며, 편리한 %||% 함수를 사용하여 해결합니다. 왼쪽이 NULL이면 오른쪽을 반환합니다.

그림 10-9에서 결과를 볼 수 있습니다.

그림 10-9. 로드 시 앱(왼쪽), n을 3으로 설정 후(가운데), 색상을 입력 후(오른쪽). https://hadley.shinyapps.io/ms-render-palette 에서 실시간으로 확인하세요.

이 앱을 실행하면 정말 짜증나는 동작을 발견하게 될 것입니다: 색상 수를 변경할 때마다 입력한 모든 데이터가 사라집니다. 이전과 동일한 기법으로 이 문제를 해결할 수 있습니다: 값을 (격리된) 현재 값으로 설정합니다. 외형을 약간 더 좋게 조정하고 선택한 색상을 플롯에 표시하는 것도 추가합니다. 샘플 스크린샷은 그림 10-10에 나와 있습니다:

```r
ui <- fluidPage(
  sidebarLayout(
    sidebarPanel(
      numericInput("n", "Number of colours",
                   value = 5, min = 1),
      uiOutput("col"),
    ),
    mainPanel(
      plotOutput("plot")
    )
  )
)

server <- function(input, output, session) {
  col_names <- reactive(paste0("col", seq_len(input$n)))
  output$col <- renderUI({
    map(col_names(),
      ~ textInput(.x, NULL,
                  value = isolate(input[[.x]])))
  })
  output$plot <- renderPlot({
    cols <- map_chr(col_names(),
                    ~ input[[.x]] %||% "")
    # 빈 입력을 투명으로 변환
    cols[cols == ""] <- NA
    barplot(rep(1, length(cols)),
            col = cols,
            space = 0,
            axes = FALSE)
  }, res = 96)
}
```

그림 10-10. 무지개 색상을 채운 후(위), 색상 수를 3으로 줄임(아래). 기존 색상이 유지됨을 유의하세요. https://hadley.shinyapps.io/msrender-palette-full 에서 실시간으로 확인하세요.

### 동적 필터링

이 장을 마무리하기 위해, 어떤 데이터프레임이든 동적으로 필터링할 수 있는 앱을 만들겠습니다. 각 숫자 변수에는 범위 슬라이더가, 각 팩터 변수에는 다중 선택이 제공됩니다. 예를 들어, 데이터프레임에 숫자 변수 3개와 팩터 2개가 있으면 앱에 슬라이더 3개와 선택 상자 2개가 있습니다.

먼저 단일 변수의 UI를 만드는 함수로 시작하겠습니다. 숫자 입력의 경우 범위 슬라이더를, 팩터 입력의 경우 다중 선택을, 기타 모든 유형의 경우 NULL(아무것도 아닌)을 반환합니다:

```r
make_ui <- function(x, var) {
  if (is.numeric(x)) {
    rng <- range(x, na.rm = TRUE)
    sliderInput(var, var,
                min = rng[1], max = rng[2],
                value = rng)
  } else if (is.factor(x)) {
    levs <- levels(x)
    selectInput(var, var,
                choices = levs,
                selected = levs,
                multiple = TRUE)
  } else {
    # 지원하지 않음
    NULL
  }
}
```

그런 다음 이 함수의 서버 측 등가물을 작성합니다. 입력 컨트롤의 변수와 값을 가져와 각 관찰을 포함할지 여부를 나타내는 논리 벡터를 반환합니다. 논리 벡터를 사용하면 여러 열의 결과를 쉽게 결합할 수 있습니다:

```r
filter_var <- function(x, val) {
  if (is.numeric(x)) {
    !is.na(x) & x >= val[1] & x <= val[2]
  } else if (is.factor(x)) {
    x %in% val
  } else {
    TRUE
  }
}
```

데이터프레임을 변환하는 reactive()를 만든 다음, 모든 필터를 결합하기 위해 reduce()를 사용합니다. 처음에는 이 코드가 이해하기 어려울 수 있지만, 구성 요소로 나누어 보면 결과가 실제로 매우 단순하다는 것을 알게 될 것입니다:

```r
ui <- fluidPage(
  sidebarLayout(
    sidebarPanel(
      selectInput("dataset", "Choose", ls("package:datasets")),
      uiOutput("filters")
    ),
    mainPanel(
      tableOutput("data")
    )
  )
)

server <- function(input, output, session) {
  data <- reactive({
    req(input$dataset)
    get(input$dataset, "package:datasets")
  })

  output$filters <- renderUI({
    req(data())
    purrr::imap(names(data()), function(name, index) {
      make_ui(data()[[name]], paste0("var_", index))
    })
  })

  output$data <- renderTable({
    req(data())
    filters <- reactiveValuesToList(input)[
      grep("^var_", names(reactiveValuesToList(input)))
    ]
    req(filters)

    cols <- imap(data(), filter_var, filters)
    cols <- compact(cols)

    if (length(cols) == 0) {
      data()
    } else {
      filter(data(), !!!cols)
    }
  })
}
```

그림 10-11에서 결과를 볼 수 있습니다.

그림 10-11. 팩터 변수에는 범위 슬라이더가, 숫자 변수에는 다중 선택이 동적으로 생성됩니다. https://hadley.shinyapps.io/ms-render-filter 에서 실시간으로 확인하세요.

## 요약

이 장에서 동적 UI의 세 가지 주요 기법을 살펴보았습니다. 각 기법은 서로 다른 유형의 문제를 해결합니다:

1. update 함수는 기존 입력 컨트롤을 변경할 때 유용합니다.
2. 조건부 탭 세트는 UI의 부분을 표시하거나 숨길 때 유용합니다.
3. renderUI()는 완전히 새로운 UI 컴포넌트를 만들 때 유용합니다.

이 기법들은 강력하지만, 간단한 접근 방식으로 문제가 해결될 수 있다면 그 방법을 사용하는 것이 좋습니다. 복잡한 동적 UI는 유지보수하기 어렵고 성능 문제가 있을 수 있습니다.

다음 장에서는 Shiny 앱을 테스트하고 문제를 디버깅하는 방법에 대해 알아보겠습니다.


```r
# 제어 없으므로 필터링하지 않음
TRUE
}
}
```

그런 다음 이 함수들을 "수동으로" 사용하여 iris 데이터셋에 대한 단순한 필터링 UI를 생성할 수 있습니다:

```r
ui <- fluidPage(
  sidebarLayout(
    sidebarPanel(
      make_ui(iris$Sepal.Length, "Sepal.Length"),
      make_ui(iris$Sepal.Width, "Sepal.Width"),
      make_ui(iris$Species, "Species")
    ),
    mainPanel(
      tableOutput("data")
    )
  )
)

server <- function(input, output, session) {
  selected <- reactive({
    filter_var(iris$Sepal.Length, input$Sepal.Length) &
    filter_var(iris$Sepal.Width, input$Sepal.Width) &
    filter_var(iris$Species, input$Species)
  })
  output$data <- renderTable(head(iris[selected(), ], 12))
}
```

그림 10-11에서 필터 인터페이스를 볼 수 있습니다.

그림 10-11. iris 데이터셋에 대한 단순한 필터 인터페이스.

복붙에 지쳐서 앱이 세 개의 열에서만 작동한다는 것을 눈치챘을 수 있습니다. 함수형 프로그래밍을 약간 사용하여 모든 열에서 작동하도록 만들 수 있습니다:

- ui에서 map()을 사용하여 각 변수에 대한 컨트롤을 생성합니다.
- server()에서 map()을 사용하여 각 변수에 대한 선택 벡터를 생성합니다.
- 그런 다음 reduce()를 사용하여 각 변수의 논리 벡터를 가져와 & 연산으로 단일 논리 벡터로 결합합니다.

다시 한번, 여기서 정확히 무슨 일이 일어나고 있는지 이해하지 못해도 너무 걱정하지 마세요. 핵심은 함수형 프로그래밍을 마스터하면 복잡하고 동적인 앱을 생성하는 매우 간결한 코드를 작성할 수 있다는 것입니다:

```r
ui <- fluidPage(
  sidebarLayout(
    sidebarPanel(
      map(names(iris), ~ make_ui(iris[[.x]], .x))
    ),
    mainPanel(
      tableOutput("data")
    )
  )
)

server <- function(input, output, session) {
  selected <- reactive({
    each_var <- map(names(iris),
                    ~ filter_var(iris[[.x]], input[[.x]]))
    reduce(each_var, ~ .x & .y)
  })
  output$data <- renderTable(head(iris[selected(), ], 12))
}
```

이것은 그림 10-12에서 보이는 것을 만듭니다.

그림 10-12. 함수형 프로그래밍을 사용하여 iris 데이터셋에 대한 필터링 앱을 구축합니다.

거기서부터 어떤 데이터프레임이든 작동하도록 일반화하는 것은 간단합니다. 여기서는 datasets 패키지의 데이터프레임을 사용하여 설명하지만, 사용자가 업로드한 데이터로도 쉽게 확장할 수 있습니다:

```r
dfs <- keep(ls("package:datasets"),
            ~ is.data.frame(get(.x, "package:datasets")))

ui <- fluidPage(
  sidebarLayout(
    sidebarPanel(
      selectInput("dataset", label = "Dataset",
                  choices = dfs),
      uiOutput("filter")
    ),
    mainPanel(
      tableOutput("data")
    )
  )
)

server <- function(input, output, session) {
  data <- reactive({
    get(input$dataset, "package:datasets")
  })
  vars <- reactive(names(data()))
  output$filter <- renderUI(
    map(vars(), ~ make_ui(data()[[.x]], .x))
  )
  selected <- reactive({
    each_var <- map(vars(),
                    ~ filter_var(data()[[.x]], input[[.x]]))
    reduce(each_var, &)
  })
  output$data <- renderTable(head(data()[selected(), ], 12))
}
```

그림 10-13에서 결과를 볼 수 있습니다.

그림 10-13. 선택된 데이터셋의 필드에서 동적으로 생성된 사용자 인터페이스. https://hadley.shinyapps.io/ms-filtering-final 에서 실시간으로 확인하세요.

### 대화 상자

마무리하기 전에 관련 기법인 대화 상자에 대해 언급하겠습니다. "명시적 확인"에서 이미 보았으며, 대화 상자의 내용이 고정된 텍스트 문자열이었습니다. 하지만 modalDialog()가 서버 함수 내부에서 호출되므로, 실제로 renderUI()와 같은 방식으로 동적으로 내용을 생성할 수 있습니다. 일반적인 앱 흐름을 계속하기 전에 사용자가 어떤 결정을 내리도록 강제하고 싶을 때 백업으로 가지고 있기 좋은 기법입니다.

#### 연습 문제

1. 이 섹션의 첫 번째 예시를 기반으로 한 매우 단순한 앱을 가져가세요:

```r
ui <- fluidPage(
  selectInput("type", "type",
              c("slider", "numeric")),
  uiOutput("numeric")
)

server <- function(input, output, session) {
  output$numeric <- renderUI({
    if (input$type == "slider") {
      sliderInput("n", "n",
                  value = 0, min = 0, max = 100)
    } else {
      numericInput("n", "n",
                   value = 0, min = 0, max = 100)
    }
  })
}
```

동적 가시성을 사용하여 대신 구현할 수 있을까요? 동적 가시성을 구현하면 컨트롤을 변경할 때 값을 어떻게 동기화 상태로 유지할 수 있을까요?

2. 이 앱이 어떻게 작동하는지 설명하세요. "비밀번호 입력" 버튼을 두 번째로 클릭하면 비밀번호가 왜 사라질까요?

```r
ui <- fluidPage(
  actionButton("go", "Enter password"),
  textOutput("text")
)

server <- function(input, output, session) {
  observeEvent(input$go, {
    showModal(modalDialog(
      passwordInput("password", NULL),
      title = "Please enter your password"
    ))
  })

  output$text <- renderText({
    if (!isTruthy(input$password)) {
      "No password"
    } else {
      "Password entered"
    }
  })
}
```

3. "시작하기"의 앱에서 value <- isolate(input$dynamic)에서 isolate()를 떨어뜨리면 어떻게 될까요?

4. make_ui()와 filter_var()에 날짜 및 날짜시간 열 지원을 추가하세요.

5. (고급) S3 OOP 시스템을 알고 있다면, make_ui()와 filter_var()의 if 블록을 제네릭 함수로 대체하는 방법을 고려해 보세요.

## 요약

이 장을 읽기 전에는 서버 함수가 실행되기 전에 사용자 인터페이스를 정적으로 만드는 데 제한되어 있었습니다. 이제 사용자 동작에 응답하여 사용자 인터페이스를 변경하고 완전히 다시 만드는 방법을 배웠습니다. 동적 사용자 인터페이스는 앱의 복잡성을 크게 증가시킬 것이므로, 디버깅에 어려움을 겪더라도 놀라지 마세요. 항상 문제를 해결하는 가장 간단한 기법을 사용하고 "디버깅"의 디버깅 조언으로 돌아가세요.

다음 장은 북마킹에 대해 이야기하기 위해 방향을 전환합니다. 이를 통해 다른 사람과 앱의 현재 상태를 공유할 수 있습니다.

---

# Part 2: 기술

# 12장. 북마킹

## 소개

기본적으로 Shiny 앱은 대부분의 웹사이트에 비해 한 가지 큰 단점이 있습니다: 앱을 북마킹하여 나중에 같은 위치로 돌아가거나 이메일에 링크를 포함하여 다른 사람과 작업을 공유할 수 없습니다. 기본적으로 Shiny는 URL에 앱의 현재 상태를 노출하지 않기 때문입니다.

하지만 약간의 추가 작업으로 이 동작을 변경할 수 있으며, 이 장에서는 방법을 보여주겠습니다. 항상 그렇듯이 shiny를 로드하는 것으로 시작합니다:

```r
library(shiny)
```

### 기본 아이디어

북마킹 가능하게 만들고 싶은 단순한 앱으로 시작해 봅시다. 이 앱은 리사주 곡선을 그립니다. 리사주 곡선은 추의 움직임을 재현합니다. 이 앱은 공유하고 싶은 다양한 흥미로운 패턴을 생성할 수 있습니다:

```r
ui <- fluidPage(
  sidebarLayout(
    sidebarPanel(
      sliderInput("omega", "omega",
                  value = 1, min = -2, max = 2,
                  step = 0.01),
      sliderInput("delta", "delta",
                  value = 1, min = 0, max = 2,
                  step = 0.01),
      sliderInput("damping", "damping",
                  value = 1, min = 0.9, max = 1,
                  step = 0.001),
      numericInput("length", "length",
                   value = 100)
    ),
    mainPanel(
      plotOutput("fig")
    )
  )
)

server <- function(input, output, session) {
  t <- reactive(seq(0, input$length,
                    length.out = input$length * 100))
  x <- reactive(sin(input$omega * t() + input$delta) *
                input$damping ^ t())
  y <- reactive(sin(t()) * input$damping ^ t())
  output$fig <- renderPlot({
    plot(x(), y(), axes = FALSE,
         xlab = "", ylab = "",
         type = "l", lwd = 2)
  }, res = 96)
}
```

그림 11-1에서 결과를 볼 수 있습니다.

그림 11-1. 이 앱은 추의 모델을 사용하여 흥미로운 그림을 생성할 수 있게 해줍니다. 친구에게 링크를 공유하는 것은 멋지지 않을까요?

이 앱을 북마킹 가능하게 만들기 위해 세 가지를 해야 합니다:

1. UI에 bookmarkButton()을 추가합니다. 사용자가 북마킹 가능한 URL을 생성하기 위해 클릭하는 버튼을 만듭니다.

2. ui를 함수로 변환합니다. 북마킹된 앱은 북마킹된 값을 다시 재생해야 하기 때문에 이것이 필요합니다. 효과적으로 Shiny는 각 입력 컨트롤의 기본 값을 수정합니다. 즉, 더 이상 단일 정적 UI가 없고 URL의 매개변수에 의존하는 여러 가능한 UI가 있습니다(즉, 함수여야 합니다).

3. shinyApp() 호출에 enableBookmarking = "url"을 추가합니다.

이러한 변경을 하면 다음을 얻습니다:

```r
ui <- function(request) {
  fluidPage(
    sidebarLayout(
      sidebarPanel(
        sliderInput("omega", "omega",
                    value = 1, min = -2, max = 2,
                    step = 0.01),
        sliderInput("delta", "delta",
                    value = 1, min = 0, max = 2,
                    step = 0.01),
        sliderInput("damping", "damping",
                    value = 1, min = 0.9, max = 1,
                    step = 0.001),
        numericInput("length", "length",
                     value = 100),
        bookmarkButton()
      ),
      mainPanel(
        plotOutput("fig")
      )
    )
  )
}

shinyApp(ui, server, enableBookmarking = "url")
```

라이브 앱에서 몇 가지 흥미로운 상태를 북마킹하면 생성된 URL이 다음과 같이 보입니다:

```
https://hadley.shinyapps.io/ms-bookmark-url/?_inputs_&damping=1&delta=1&length=100&omega=1
https://hadley.shinyapps.io/ms-bookmark-url/?_inputs_&damping=0.966&delta=1.25&length=100&omega=-0.54
https://hadley.shinyapps.io/ms-bookmark-url/?_inputs_&damping=0.997&delta=1.37&length=500&omega=-0.9
```

무슨 일이 일어나고 있는지 이해하기 위해, 첫 번째 URL을 가져와 부분으로 나누어 보겠습니다:

- `http://`는 앱과 통신하는 데 사용되는 "프로토콜"입니다. 항상 http 또는 https입니다.
- `hadley.shinyapps.io/ms-bookmark-url`는 앱의 위치입니다.
- `?` 이후의 모든 것은 "매개변수"입니다. 각 매개변수는 &로 구분되며, 나누어 보면 앱의 각 입력 값을 볼 수 있습니다:
  - `damping=1`
  - `delta=1`
  - `length=100`
  - `omega=1`

따라서 "북마크 생성"은 URL의 매개변수에 현재 입력 값을 기록하는 것을 의미합니다.

로컬에서 앱을 사용하면 URL이 약간 다르게 보입니다:

```
http://127.0.0.1:4087/?_inputs_&damping=1&delta=1&length=100&omega=1
```

대부분의 부분은 동일하지만, hadley.shinyapps.io/ms-bookmark-url 대신 127.0.0.1:4087과 같은 것이 보입니다. 127.0.0.1은 항상 자신의 컴퓨터를 가리키는 특별한 주소이고, 4087은 무작위로 할당된 포트입니다. 일반적으로 다른 앱은 다른 경로 또는 IP 주소를 받지만, 자체 컴퓨터에서 여러 앱을 호스팅할 때는 이것이 불가능합니다.

### URL 업데이트

명시적 버튼을 제공하는 대신, 브라우저에서 URL을 자동으로 업데이트하는 옵션도 있습니다. 이를 통해 사용자는 브라우저의 북마크 명령을 사용하거나 주소 표시줄에서 URL을 복사하여 붙여넣을 수 있습니다.

URL 자동 업데이트에는 서버 함수에 약간의 보일러플레이트가 필요합니다:

```r
# 입력이 변경될 때마다 자동으로 북마킹
observe({
  reactiveValuesToList(input)
  session$doBookmark()
})
```

이 observe()는 모든 입력을 읽어서 앱의 현재 상태가 변경될 때마다 자동으로 트리거되도록 합니다. 반응적 표현식을 `reactiveValuesToList(input)`과 같이 "낭비"하는 것은 이상해 보이지만, 목표는 입력에 접근하여 Shiny에게 반응적 의존성을 설정하는 것입니다. Shiny가 앱의 현재 상태를 변경하는 것을 감지할 때마다 session$doBookmark()를 자동으로 실행합니다.

URL 자동 업데이트를 활성화하려면 shinyApp() 호출의 enableBookmarking = "url"을 유지해야 합니다. 사용자가 클릭할 수 있는 bookmarkButton()은 더 이상 필요하지 않지만, 코드가 확장되면 여전히 유용할 수 있으므로 계속 유지하는 것이 좋습니다.

### 상태 저장

URL에 상태를 저장하는 것은 간단하지만, 약간의 단점이 있습니다:

1. URL이 상당히 길어질 수 있습니다. 예를 들어, 많은 변수가 있는 데이터프레임에 대한 필터링 앱은 매우 긴 URL을 생성할 수 있습니다.
2. URL은 영구적이지 않습니다. 사용자가 앱의 새 버전을 배포하면 기존 북마크가 더 이상 작동하지 않을 수 있습니다.

이 문제를 해결하기 위해 Shiny는 상태를 파일로 저장하는 두 번째 북마킹 메커니즘을 제공합니다. 상태를 파일로 저장하면 URL이 짧아지고, 앱이 업데이트되어도 기존 북마크가 작동할 수 있습니다.

상태를 파일로 저장하려면 다음을 수행해야 합니다:

1. enableBookmarking = "dir"을 shinyApp()에 전달합니다. 이렇게 하면 Shiny가 상태를 파일로 저장합니다.
2. 저장소 디렉토리를 지정하는 storeDir 매개변수를 제공합니다.

다음은 이를 사용하는 예시입니다:

```r
ui <- function(request) {
  fluidPage(
    sidebarLayout(
      sidebarPanel(
        sliderInput("omega", "omega",
                    value = 1, min = -2, max = 2,
                    step = 0.01),
        sliderInput("delta", "delta",
                    value = 1, min = 0, max = 2,
                    step = 0.01),
        sliderInput("damping", "damping",
                    value = 1, min = 0.9, max = 1,
                    step = 0.001),
        numericInput("length", "length",
                     value = 100),
        bookmarkButton()
      ),
      mainPanel(
        plotOutput("fig")
      )
    )
  )
}

server <- function(input, output, session) {
  t <- reactive(seq(0, input$length,
                    length.out = input$length * 100))
  x <- reactive(sin(input$omega * t() + input$delta) *
                input$damping ^ t())
  y <- reactive(sin(t()) * input$damping ^ t())
  output$fig <- renderPlot({
    plot(x(), y(), axes = FALSE,
         xlab = "", ylab = "",
         type = "l", lwd = 2)
  }, res = 96)
}

shinyApp(ui, server,
         enableBookmarking = "dir",
         storeDir = "bookmarks")
```

이 앱을 실행하고 북마크를 만들면 bookmartk/ 디렉토리에 파일이 생성됩니다. 이 파일에는 앱의 현재 상태가 저장됩니다. 나중에 이 파일을 Shiny 서버에 복사하면 해당 상태를 복원할 수 있습니다.

이 방법의 장점은 URL이 짧다는 것입니다. 예를 들어, 이 앱의 북마크 URL은 다음과 같이 보입니다:

```
http://127.0.0.1:4087/?_state_id_=abc123
```

`abc123`은 북마크의 고유 ID일 뿐입니다. 실제 데이터는 서버에 저장됩니다.

하지만 이 방법에는 단점도 있습니다. 상태를 저장하고 복원하는 데 서버 측 코드가 필요하고, 사용자가 서버에 파일을 저장할 수 있어야 합니다.

## 요약

이 장에서 Shiny 앱을 북마킹하는 방법을 살펴보았습니다. 두 가지 주요 접근 방식이 있습니다:

1. **URL 기반 북마킹**: enableBookmarking = "url"을 사용하여 상태를 URL에 저장합니다. 간단하지만 URL이 길어질 수 있습니다.
2. **파일 기반 북마킹**: enableBookmarking = "dir"을 사용하여 상태를 파일로 저장합니다. URL이 짧지만 서버 측 코드가 필요합니다.

어떤 방법을 선택하든, 사용자가 앱의 특정 상태를 공유하고 나중에 복원할 수 있게 해줍니다.

다음 장에서는 Shiny 모듈에 대해 알아보겠습니다. 모듈은 복잡한 앱을 더 관리하기 쉽게 만드는 데 유용합니다.


# 쿼리 문자열 업데이트
onBookmarked(updateQueryString)
```

다음과 같은 업데이트된 서버 함수를 얻습니다:

```r
server <- function(input, output, session) {
  t <- reactive(seq(0, input$length,
                    length = input$length * 100))
  x <- reactive(sin(input$omega * t() + input$delta) *
                input$damping ^ t())
  y <- reactive(sin(t()) * input$damping ^ t())
  output$fig <- renderPlot({
    plot(x(), y(), axes = FALSE,
         xlab = "", ylab = "",
         type = "l", lwd = 2)
  }, res = 96)

  observe({
    reactiveValuesToList(input)
    session$doBookmark()
  })
  onBookmarked(updateQueryString)
}

shinyApp(ui, server, enableBookmarking = "url")
```

라이브 앱에서 결과를 볼 수 있습니다. URL이 이제 자동으로 업데이트되므로 UI에서 북마크 버튼을 제거할 수 있습니다.

### 더 풍부한 상태 저장

지금까지는 상태를 URL에 직접 저장하는 enableBookmarking = "url"을 사용했습니다. 시작하기에 좋은 곳입니다. 매우 간단하고 Shiny 앱을 배포할 수 있는 모든 곳에서 작동하기 때문입니다. 하지만 상상할 수 있듯이, 입력이 많으면 URL이 매우 길어지고, 업로드된 파일을 캡처할 수 없다는 것이 명백합니다.

이러한 경우 대신 서버의 .rds 파일에 상태를 저장하는 enableBookmarking = "server"를 사용하고 싶을 수 있습니다. 항상 짧고 불투명한 URL을 생성하지만, 서버에 추가 저장소가 필요합니다:

```r
shinyApp(ui, server, enableBookmarking = "server")
```

shinyapps.io는 현재 서버 측 북마킹을 지원하지 않으므로, 로컬에서 테스트해 보아야 합니다.这样做하면 북마크 버튼이 다음과 같은 URL을 생성하는 것을 볼 수 있습니다:

```
http://127.0.0.1:4087/?_state_id_=0d645f1b28f05c97
http://127.0.0.1:4087/?_state_id_=87b56383d8a1062c
http://127.0.0.1:4087/?_state_id_=c8b0291ba622b69c
```

이것들은 작업 디렉토리의 일치하는 디렉토리와 쌍을 이룹니다:

```
shiny_bookmarks/0d645f1b28f05c97
shiny_bookmarks/87b56383d8a1062c
shiny_bookmarks/c8b0291ba622b69c
```

서버 북마킹의 주요 단점은 서버에 파일을 저장해야 하고, 이 파일들이 얼마나 오래 유지되어야 하는지 명확하지 않다는 것입니다. 복잡한 상태를 북마킹하고 이러한 파일을 절대 삭제하지 않으면 앱이 시간이 지남에 따라 점점 더 많은 디스크 공간을 차지합니다. 파일을 삭제하면 일부 오래된 북마크가 작동하지 않게 됩니다.

### 북마킹 과제

자동 북마킹은 반응형 그래프에 의존합니다. 저장된 값으로 입력을 채운 다음 모든 반응형 표현식과 출력을 다시 재생하며, 앱의 반응형 그래프가 간단한 한과 같은 앱을 생성합니다.

이 섹션은 약간의 추가 주의가 필요한 몇 가지 경우를 간략히 다룹니다:

1. 앱이 무작위 숫자를 사용하면 모든 입력이 같아도 결과가 다를 수 있습니다. 항상 같은 숫자를 생성하는 것이 정말 중요하면 무작위 프로세스를 재현 가능하게 만드는 방법을 생각해야 합니다. 가장 쉬운 방법은 repeatable()을 사용하는 것입니다. 자세한 내용은 문서를 참조하세요.

2. 탭이 있고 활성 탭을 북마킹하고 복원하려면 tabsetPanel() 호출에 id를 제공해야 합니다.

3. 북마킹되면 안 되는 입력이 있다면(예: 공유하면 안 되는 개인 정보를 포함하는 경우), 서버 함수 어딘가에 setBookmarkExclude() 호출을 포함하세요. 예를 들어, setBookmarkExclude(c("secret1", "secret2"))은 secret1과 secret2 입력이 북마킹되지 않도록 합니다.

4. reactiveValues() 객체에서 반응 상태를 수동으로 관리하는 경우(16장에서 설명하겠지만), onBookmark()과 onRestore() 콜백을 사용하여 추가 상태를 수동으로 저장하고 로드해야 합니다. 자세한 내용은 "고급 북마킹"을 참조하세요.

#### 연습 문제

1. ambient::noise_simplex()의 결과를 시각화하는 앱을 생성하세요. 이 앱은 사용자가 frequency, fractal, lacunarity, gain을 제어할 수 있게 하고 북마킹 가능해야 합니다. 북마크에서 다시 로드할 때 이미지가 정확히 같도록 어떻게 할 수 있을까요? (seed 인수가 의미하는 바를 생각해 보세요.)

2. CSV 파일을 업로드하고 북마킹할 수 있는 단순한 앱을 만드세요. 몇 개의 파일을 업로드한 다음 shiny_bookmarks를 확인하세요. 파일은 북마크와 어떻게 대응됩니까? (힌트: readRDS()를 사용하여 Shiny가 생성하는 캐시 파일 내부를 볼 수 있습니다.)

## 요약

이 장은 앱에 북마킹을 활성화하는 방법을 보여주었습니다. 사용자가 다른 사람과 쉽게 작업을 공유할 수 있게 해주므로 사용자에게 제공하기에 좋은 기능입니다.

다음 장에서는 Shiny 앱에서 tidy evaluation을 사용하는 방법에 대해 이야기하겠습니다. tidy evaluation은 많은 tidyverse 함수의 기능이며, 사용자가(예를 들어) dplyr 파이프라인이나 ggplot2 그래픽에서 변수를 변경할 수 있게 하려면 이를 배워야 합니다.

---

# 13장. Tidy Evaluation

## 소개

Shiny를 tidyverse와 함께 사용하는 경우 거의 확실히 tidy evaluation으로 프로그래밍하는 도전에 직면하게 될 것입니다. tidy evaluation은 대화형 데이터 탐색을 더 원활하게 만들기 위해 tidyverse 전체에서 사용되지만, 비용이 따릅니다: 변수를 간접적으로 참조하기 어렵고, 따라서 프로그래밍하기 어렵습니다. 이 장에서는 Shiny 앱에서 ggplot2와 dplyr 함수를 래핑하는 방법을 배우게 됩니다. (tidyverse를 사용하지 않는 경우 이 장을 건너뛸 수 있습니다.)

ggplot2와 dplyr 함수를 다른 함수나 패키지에 래핑하는 기법은 약간 다르며, "패키지에서 ggplot2 사용하기"나 "dplyr로 프로그래밍하기"와 같은 다른 자료에서 다룹니다.

시작해 봅시다:

```r
library(shiny)
library(dplyr, warn.conflicts = FALSE)
library(ggplot2)
```

### 동기

숫자 변수를 필터링하여 임곗값보다 큰 행을 선택할 수 있는 앱을 만들고 싶다고 상상해 보세요. 다음과 같이 작성할 수 있습니다:

```r
num_vars <- c("carat", "depth", "table",
              "price", "x", "y", "z")

ui <- fluidPage(
  selectInput("var", "Variable",
              choices = num_vars),
  numericInput("min", "Minimum", value = 1),
  tableOutput("output")
)

server <- function(input, output, session) {
  data <- reactive(
    diamonds %>% filter(input$var > input$min)
  )
  output$output <- renderTable(head(data()))
}
```

그림 12-1에서 보듯이, 앱은 오류 없이 실행되지만 올바른 결과를 반환하지 않습니다. 모든 행의 carat 값이 1보다 작습니다.

그림 12-1. 사용자가 선택한 변수에서 임곗값보다 큰 행을 선택하려고 시도하는 앱. https://hadley.shinyapps.io/ms-tidied-broken 에서 실시간으로 확인하세요.

이 장의 목표는 이것이 왜 작동하지 않는지, 그리고 dplyr가 filter(diamonds, "carat" > 1)을 요청한 것으로 생각하는 이유를 이해하는 데 도움을 주는 것입니다.

이것은 간접 참조의 문제입니다. 일반적으로 tidyverse 함수를 사용할 때 함수 호출에 변수 이름을 직접 입력합니다. 하지만 이제는 간접적으로 참조하고 싶습니다: 변수(carat)가 다른 변수(input$var) 안에 저장되어 있습니다.

이 문장은 직관적으로 이해되었을 수 있지만, "변수"를 약간 다른 두 가지 의미로 사용하고 있기 때문에 약간 혼란스러울 수 있습니다. 두 가지 새로운 용어를 도입하여 두 가지 사용을 명확히 하면 무슨 일이 일어나고 있는지 이해하기 쉬워집니다:

- **env-variable(환경 변수)**: <-로 만든 "프로그래밍" 변수입니다. input$var은 env-variable입니다.
- **data-variable(데이터 변수)**: 데이터프레임 안에 있는 "통계" 변수입니다. carat은 data-variable입니다.

이 새로운 용어를 사용하면 간접 참조의 문제를 더 명확하게 만들 수 있습니다: data-variable(carat)이 env-variable(input$var) 안에 저장되어 있으며, 이를 dplyr에 알려주는 방법이 필요합니다.

작업하는 함수가 "데이터 마스킹" 함수인지 "tidy-selection" 함수인지에 따라 약간 다른 두 가지 방법이 있습니다.

### 데이터 마스킹

데이터 마스킹 함수는 추가 구문 없이 "현재" 데이터프레임에서 변수를 사용할 수 있게 해줍니다. 데이터 마스킹은 arrange(), filter(), group_by(), mutate(), summarise()와 같은 많은 dplyr 함수와 ggplot2의 aes()에서 사용됩니다.

데이터 마스킹이 유용한 이유는 data-variable을 추가 구문 없이 사용할 수 있게 해주기 때문입니다.

#### 시작하기

다음 filter() 호출로 시작해 봅시다. data-variable(carat)과 env-variable(min)을 사용합니다:

```r
min <- 1
diamonds %>% filter(carat > min)
#> # A tibble: 17,502 × 10
#>    carat cut       color clarity depth table price     x     y     z
#>    <dbl> <ord>     <ord> <ord>   <dbl> <dbl> <int> <dbl> <dbl> <dbl>
#>  1  1.17 Very Good J     I1       60.2    61  2774  6.83  6.9   4.13
#>  2  1.01 Premium   F     I1       61.8    60  2781  6.39  6.36  3.94
#>  3  1.01 Fair      E     I1       64.5    58  2788  6.29  6.21  4.03
#>  4  1.01 Premium   H     SI2      62.7    59  2788  6.31  6.22  3.93
#>  5  1.05 Very Good J     SI2      63.2    56  2789  6.49  6.45  4.09
#>  6  1.05 Fair      J     SI2      65.8    59  2789  6.41  6.27  4.18
#> # … with 17,496 more rows
```

기본 R 등가물과 비교해 보세요: `diamonds[diamonds$carat > min, ]`

대부분의(하지만 모든 것은 아닌)1 기본 R 함수에서는 data-variable을 $로 참조해야 합니다.这意味着 데이터프레임 이름을 여러 번 반복해야 하는 경우가 많지만, data-variable과 env-variable이 정확히 무엇인지 명확하게 만듭니다. 간접 참조를2 사용하기도 straightforward합니다. data-variable의 이름을 env-variable에 저장한 다음 $에서 [[로 전환할 수 있기 때문입니다:

```r
var <- "carat"
diamonds[diamonds[[var]] > min, ]
```

tidy evaluation으로 동일한 결과를 어떻게 얻을 수 있을까요? $를 다시 장면에 추가하는 방법이 필요합니다. 다행히 데이터 마스킹 함수 내부에서는 data-variable인지 env-variable인지 명확히 하려면 .data 또는 .env를 사용할 수 있습니다:

```r
diamonds %>% filter(.data$carat > .env$min)
```

이제 $에서 [[로 전환할 수 있습니다:

```r
diamonds %>% filter(.data[[var]] > .env$min)
```

이 장을 시작할 때 만든 서버 함수를 업데이트하여 작동하는지 확인해 봅시다:

```r
num_vars <- c("carat", "depth", "table",
              "price", "x", "y", "z")
ui <- fluidPage(
  selectInput("var", "Variable",
              choices = num_vars),
  numericInput("min", "Minimum", value = 1),
  tableOutput("output")
)
server <- function(input, output, session) {
  data <- reactive(
    diamonds %>%
      filter(.data[[input$var]] > .env$input$min)
  )
  output$output <- renderTable(head(data()))
}
```

그림 12-2에서 carat 값이 1보다 큰 다이아몬드만 보이는 성공적인 결과를 확인할 수 있습니다.

그림 12-2. .data와 .env, 그리고 [[와 $를 명확히 한 후 앱이 작동합니다. https://hadley.shinyapps.io/ms-tidied-up 에서 실시간으로 확인하세요.

기본을 읽었으므로, 몇 가지 더 현실적이지만 여전히 단순한 Shiny 앱을 개발해 보겠습니다.

#### 예시: ggplot2

x축과 y축에 표시될 변수를 선택하여 산점도를 만들 수 있게 하는 동적 플롯에 이 아이디어를 적용해 봅시다:

```r
ui <- fluidPage(
  selectInput("x", "X variable",
              choices = names(iris)),
  selectInput("y", "Y variable",
              choices = names(iris)),
  plotOutput("plot")
)

server <- function(input, output, session) {
  output$plot <- renderPlot({
    ggplot(iris, aes(.data[[input$x]], .data[[input$y]])) +
      geom_point(position = ggforce::position_auto())
  }, res = 96)
}
```

결과는 그림 12-3에 나와 있습니다.

그림 12-3. x축과 y축에 어떤 변수가 플롯될지 선택할 수 있는 단순한 앱. https://hadley.shinyapps.io/ms-ggplot2 에서 실시간으로 확인하세요.

여기서는 ggforce::position_auto()를 사용하여 x와 y 변수가 연속적이든 이산적이든 geom_point()가 잘 작동하도록 했습니다.

대안으로, 사용자가 geom을 선택할 수 있게 할 수 있습니다. 다음 앱은 switch() 문을 사용하여 나중에 플롯에 추가되는 반응적 geom을 생성합니다:

```r
ui <- fluidPage(
  selectInput("x", "X variable",
              choices = names(iris)),
  selectInput("y", "Y variable",
              choices = names(iris)),
  selectInput("geom", "geom",
              c("point", "smooth", "jitter")),
  plotOutput("plot")
)

server <- function(input, output, session) {
  plot_geom <- reactive({
    switch(input$geom,
      point = geom_point(),
      smooth = geom_smooth(se = FALSE),
      jitter = geom_jitter()
    )
  })
  output$plot <- renderPlot({
    ggplot(iris, aes(.data[[input$x]], .data[[input$y]])) +
      plot_geom()
  }, res = 96)
}
```

이것은 사용자가 선택한 변수로 프로그래밍하는 도전 중 하나입니다: 코드가 사용자가 생성할 수 있는 모든 경우를 처리하기 위해 더 복잡해져야 합니다.

#### 예시: dplyr

동일한 기법은 dplyr에도 작동합니다. 다음 앱은 이전 단순한 예시를 확장하여 필터링할 변수, 선택할 최소값, 정렬할 변수를 선택할 수 있게 합니다:

```r
ui <- fluidPage(
  selectInput("var", "Select variable",
              choices = names(mtcars)),
  sliderInput("min", "Minimum value", 0,
              min = 0, max = 100),
  selectInput("sort", "Sort by",
              choices = names(mtcars)),
  tableOutput("data")
)

server <- function(input, output, session) {
  observeEvent(input$var, {
    rng <- range(mtcars[[input$var]])
    updateSliderInput(session, "min",
      value = rng[[1]],
      min = rng[[1]],
      max = rng[[2]])
  })
  output$data <- renderTable({
    mtcars %>%
      filter(.data[[input$var]] > input$min) %>%
      arrange(.data[[input$sort]])
  })
}
```

그림 12-4에서 업데이트된 결과를 볼 수 있습니다.

그림 12-4. 임곗값을 적용할 변수를 선택하고 결과 정렬 방법을 선택할 수 있는 단순한 앱. https://hadley.shinyapps.io/ms-dplyr 에서 실시간으로 확인하세요.

대부분의 다른 문제는 .data와 기존 프로그래밍 기술을 결합하여 해결할 수 있습니다. 예를 들어, 오름차순 또는 내림차순으로 조건부 정렬을 하고 싶다면 어떻게 할까요?

```r
ui <- fluidPage(
  selectInput("var", "Sort by",
              choices = names(mtcars)),
  checkboxInput("desc", "Descending order?"),
  tableOutput("data")
)

server <- function(input, output, session) {
  sorted <- reactive({
    if (input$desc) {
      arrange(mtcars, desc(.data[[input$var]]))
    } else {
      arrange(mtcars, .data[[input$var]])
    }
  })
  output$data <- renderTable(sorted())
}
```

더 많은 제어를 제공할수록 코드가 점점 더 복잡해지고, 포괄적이면서도 사용자 친화적인 사용자 인터페이스를 만드는 것이 점점 더 어려워집니다. 이것이 바로 데이터 분석을 위한 코드 도구에 항상 집중해 온 이유입니다. 좋은 UI를 만드는 것은 정말로 정말로 어렵습니다!

### 사용자가 제공한 데이터

tidy-selection에 대해 이야기하기 전에 마지막으로 논의해야 할 주제가 있습니다: 사용자가 제공한 데이터입니다. 그림 12-5에 나와 있는 이 앱을 가져가 보세요. 사용자가 TSV 파일을 업로드한 다음 변수를 선택하고 필터링할 수 있게 합니다. 시도할 수 있는 대부분의 입력에서 작동합니다:

```r
ui <- fluidPage(
  fileInput("data", "dataset", accept = ".tsv"),
  selectInput("var", "var", character()),
  numericInput("min", "min", 1, min = 0, step = 1),
  tableOutput("output")
)

server <- function(input, output, session) {
  data <- reactive({
    req(input$data)
    vroom::vroom(input$data$datapath)
  })
  observeEvent(data(), {
    updateSelectInput(session, "var",
                      choices = names(data()))
  })
  observeEvent(input$var, {
    val <- data()[[input$var]]
    updateNumericInput(session, "min",
                       value = min(val))
  })
  output$output <- renderTable({
    req(input$var)
    data() %>%
      filter(.data[[input$var]] > input$min) %>%
      arrange(.data[[input$var]]) %>%
      head(10)
  })
}
```

그림 12-5. 사용자가 제공한 데이터를 필터링하는 앱. 놀라운 실패 모드가 있습니다. https://hadley.shinyapps.io/ms-user-supplied 에서 실시간으로 확인하세요.

여기서 filter()를 사용하는 것에 미묘한 문제가 있습니다. filter() 호출을 빼서 앱 외부에서 직접 사용해 보겠습니다:

```r
df <- data.frame(x = 1, y = 2)
input <- list(var = "x", min = 0)
df %>% filter(.data[[input$var]] > input$min)
#>   x y
#> 1 1 2
```

이 코드로 실험하면 대부분의 데이터프레임에서 잘 작동하는 것처럼 보입니다. 하지만 미묘한 문제가 있습니다: 데이터프레임에 input이라는 변수가 있으면 어떻게 될까요?

```r
df <- data.frame(x = 1, y = 2, input = 3)
df %>% filter(.data[[input$var]] > input$min)
#> Error: Problem with `filter()` input `..1`.
#> x `$` operator is invalid for atomic vectors
#> ℹ Input `..1` is `.data[["x"]] > input$min`.
```

filter()가 df$input$min을 평가하려고 시도하기 때문에 오류 메시지를 받습니다:

```r
df$input$min
#> Error in df$input$min: $ operator is invalid for atomic vectors
```

이 문제는 data-variable과 env-variable의 모호성과 데이터 마스킹이 둘 다 사용 가능한 경우 data-variable을 선호하기 때문에 발생합니다. .env를3 사용하여 filter()에게 env-variable에서만 min을 찾도록 하여 이 문제를 해결할 수 있습니다:

```r
df %>% filter(.data[[input$var]] > .env$input$min)
#>   x y input
#> 1 1 2     3
```

사용자가 제공한 데이터로 작업할 때만 이 문제에 대해 걱정하면 됩니다. 자체 데이터로 작업할 때는 data-variable의 이름이 env-variable의 이름과 충돌하지 않도록 할 수 있으며(실수로 충돌하더라도 즉시 알게 됩니다).

### 기본 R을 사용하지 않는 이유

이 시점에서 filter() 없이 더 나은지, 대신 등가 기본 R 코드를 사용해야 하는지 궁금할 수 있습니다:

```r
df[df[[input$var]] > input$min, ]
#>   x y input
#> 1 1 2     3
```

filter()가 대신 수행하는 작업을 인식하는 한, 완전히 합법적인 입장입니다. 이 경우:

1. df에 단일 열만 포함된 경우 drop = FALSE가 필요합니다(그렇지 않으면 데이터프레임 대신 벡터를 얻게 됩니다).
2. 결측치를 제거하려면 which() 또는 유사한 것을 사용해야 합니다.
3. 그룹별 필터링(예: `df %>% group_by(g) %>% filter(n() == 1)`)은 할 수 없습니다.

일반적으로 dplyr를 매우 단순한 경우에 사용하는 경우 데이터 마스킹을 사용하지 않는 기본 R 함수를 사용하는 것이 더 쉬울 수 있습니다. 하지만 제 생각에 tidyverse의 장점 중 하나는 가장자리 경우에 신중하게 적용된 생각으로 함수가 더 일관되게 작동한다는 것입니다. 이를 과장하고 싶지 않지만, 동시에 특정 기본 R 함수의 특이점을 잊고 95%는 작동하지만 나머지 5%는 이상한 방식으로 실패하는 코드를 작성하기 쉽습니다.

### Tidy-Selection

데이터 마스킹 외에 tidy evaluation의 또 다른 중요한 부분이 있습니다: tidy-selection입니다. tidy-selection은 위치, 이름 또는 유형별로 열을 선택하는 간결한 방법을 제공합니다. dplyr::select()와 dplyr::across(), 그리고 pivot_longer(), pivot_wider(), separate(), extract(), unite()와 같은 tidyr의 많은 함수에서 사용됩니다.

#### 간접 참조

변수를 간접적으로 참조하려면 any_of() 또는 all_of()를4 사용합니다. 둘 다 data-variable의 이름을 포함하는 문자열 env-variable을 기대합니다. 유일한 차이점은 입력에 존재하지 않는 변수 이름을 제공하면 어떻게 되는지입니다: all_of()는 오류를 발생시키고, any_of()는 조용히 무시합니다.

다음 앱은 multiselect 입력과 all_of()를 사용하여 사용자가 아무 변수나 선택할 수 있게 합니다:

```r
ui <- fluidPage(
  selectInput("vars", "Variables",
              names(mtcars), multiple = TRUE),
  tableOutput("data")
)

server <- function(input, output, session) {
  output$data <- renderTable({
    req(input$vars)
    mtcars %>% select(all_of(input$vars))
  })
}
```

그림 12-6에서 결과를 볼 수 있습니다.

그림 12-6. tidy-selection을 사용하여 선택된 변수를 표시합니다. https://hadley.shinyapps.io/ms-tidyselect-all_of 에서 실시간으로 확인하세요.

var<- "var1"과 같은 환경 변수에 저장된 변수 이름을 사용하는 경우도 동일하게 작동합니다. 예를 들어 다음 앱은 변수를 선택하고 해당 변수의 고유 값 수를 표시합니다:

```r
ui <- fluidPage(
  selectInput("var", "Variable",
              choices = names(mtcars)),
  textOutput("n")
)

server <- function(input, output, session) {
  output$n <- renderText({
    paste0("Number of unique values: ",
           n_distinct(mtcars[[input$var]]))
  })
}
```

그림 12-7에서 결과를 볼 수 있습니다.

그림 12-7. 단일 변수의 고유 값 수를 표시합니다. https://hadley.shinyapps.io/ms-tidied-select-one 에서 실시간으로 확인하세요.


```r
mtcars %>% select(all_of(input$vars))
})
}
```

### Tidy-Selection과 데이터 마스킹

tidy-selection을 사용하는 함수로 작업할 때 여러 변수를 다루는 것은 간단합니다. 문자열 벡터 변수 이름을 any_of() 또는 all_of()에 전달하기만 하면 됩니다. 데이터 마스킹 함수에서도 그렇게 할 수 있다면 좋지 않을까요? 이것이 dplyr 1.0.0에 추가된 across() 함수의 아이디어입니다. 데이터 마스킹 함수 내부에서 tidy-selection을 사용할 수 있게 해줍니다.

across()는 일반적으로 인수 한 개 또는 두 개와 함께 사용됩니다. 첫 번째 인수는 변수를 선택하며, group_by()나 distinct()와 같은 함수에서 유용합니다. 예를 들어, 다음 앱은 아무 변수나 선택하고 고유 조합을 세는 것을 가능하게 합니다:

```r
ui <- fluidPage(
  selectInput("vars", "Variables",
              names(mtcars), multiple = TRUE),
  tableOutput("count")
)

server <- function(input, output, session) {
  output$count <- renderTable({
    req(input$vars)
    mtcars %>%
      group_by(across(all_of(input$vars))) %>%
      summarise(n = n(), .groups = "drop")
  })
}
```

두 번째 인수는 각 선택된 열에 적용되는 함수(또는 함수 목록)입니다. 이를 통해 mutate()와 summarise()에 적합하게 하며, 일반적으로 각 변수를 어떤 방식으로든 변환하고자 합니다. 예를 들어 다음 코드는 사용자가 아무 그룹핑 변수나 선택하고 평균으로 요약할 변수를 선택할 수 있게 합니다:

```r
ui <- fluidPage(
  selectInput("vars_g", "Group by",
              names(mtcars), multiple = TRUE),
  selectInput("vars_s", "Summarise",
              names(mtcars), multiple = TRUE),
  tableOutput("data")
)

server <- function(input, output, session) {
  output$data <- renderTable({
    mtcars %>%
      group_by(across(all_of(input$vars_g))) %>%
      summarise(across(all_of(input$vars_s), mean),
                n = n())
  })
}
```

### parse()와 eval()

마무리하기 전에 paste() + parse() + eval()에 대한 간략한 언급이 가치가 있습니다. 이 조합이 무엇인지 모르면 이 섹션을 건너뛸 수 있지만, 사용한 적이 있다면 작은 주의 사항을 전하고 싶습니다. 새로운 아이디어를 거의 배울 필요가 없기 때문에 유혹적인 접근 방식입니다. 하지만 몇 가지 큰 단점이 있습니다: 문자열을 연결하고 있기 때문에 실수로 잘못된 코드를 만들거나, 원하지 않는 일을 할 수 있게 악용될 수 있는 코드를 만들기 쉽습니다. 이것이 자신만 사용하는 Shiny 앱이라면 중요하지 않지만, 좋은 습관은 아닙니다. 그렇지 않으면 더 널리 공유하는 앱에 실수로 보안 구멍을 만들기 쉽습니다. 이 아이디어는 22장에서 다시 다루겠습니다.

## 요약

이 장에서는 사용자가 dplyr::filter() 및 ggplot2::aes()와 같은 tidyverse 함수에 어떤 변수를 전달할지 선택할 수 있는 Shiny 앱을 만드는 방법을 배웠습니다. 이전까지 생각할 필요가 없었던 핵심 구분, data-variable과 env-variable의 차이를 이해해야 합니다. 이것이 본능이 되기까지 약간의 연습이 필요하지만, 이 개념을 마스터하면 non-R 사용자에게 tidyverse의 데이터 분석 기능을 노출하는 힘을 얻게 됩니다.

이것이 "동작하는 Shiny" 부분의 마지막 장입니다. 이제 다양한 유용한 앱을 만들기 위한 도구를 갖추었으므로, Shiny의 기반이 되는 이론에 대한 이해를 높이는 데 집중하겠습니다.

---

# Part 3: 반응형 마스터하기

이제 많은 유용한 기술을 가지고 있어 다양한 유용한 앱을 만들 수 있습니다. 다음은 Shiny의 마법 뒤에 있는 반응형 이론에 초점을 맞추겠습니다:

- **14장**: 왜 반응형 프로그래밍 모델이 필요한지, 그리고 R 외부의 반응형 프로그래밍에 대한 약간의 역사를 배웁니다.
- **15장**: 반응형 컴포넌트가 정확히 언제 업데이트되는지 결정하는 반응형 그래프의 전체 세부 사항을 배웁니다.
- **16장**: 특히 옵저버와 시간 기반 무효화를 포함하는 기반 빌딩 블록에 대해 배웁니다.
- **17장**: reactiveVal()과 observe()를 사용하여 반응형 그래프의 제약에서 벗어나는 방법을 배웁니다.

일상적인 Shiny 앱 개발을 위해 이 모든 세부 사항을 이해할 필요는 없습니다. 하지만 이해를 높이면 처음부터 올바른 앱을 작성할 수 있고, 무언가 예상과 다르게 작동할 때 기반 문제를 더 빠르게 좁힐 수 있습니다.

---

# 14장. 왜 반응형인가?

## 소개

Shiny의 첫인상은 종종 "마법"입니다. 마법은 시작할 때 좋습니다. 매우 매우 빠르게 단순한 앱을 만들 수 있기 때문입니다. 하지만 소프트웨어의 마법은 종종 환멸로 이어집니다. 견고한 정신적 모델 없이는 데모와 예시의 경계를 넘어설 때 소프트웨어가 어떻게 작동할지 예측하기 극히 어렵습니다. 그리고 일이 예상대로 되지 않으면 디버깅은 거의 불가능합니다.

다행히 Shiny는 "좋은" 마법입니다. Tom Dale이 그의 Ember.js JavaScript 프레임워크에 대해 말한 것처럼: "우리는 많은 마법을 하지만, 이것은 좋은 마법입니다. 이는 건전한 프리미티브로 분해될 수 있다는 뜻입니다." Shiny 팀이 Shiny에 대해 열망하는 품질이며, 특히 반응형 프로그래밍에 있어서 그렇습니다.

반응형 프로그래밍의 층을 벗기면 휴리스틱, 특수한 경우, 해킹의 더미를 찾지 못할 것입니다. 대신 영리하지만 궁극적으로 꽤 단순한 메커니즘을 찾게 될 것입니다. 반응형에 대한 정확한 정신적 모델을 형성하면 Shiny의 소매 안에 아무것도 없다는 것을 볼 것입니다. 마법은 단순한 개념이 일관된 방식으로 결합된 것에서 나옵니다.

이 장에서는 반응형 프로그래밍 없이 해보려고 시도함으로써 반응형 프로그래밍의 동기를 부여하고, Shiny와 관련된 반응형의 간략한 역사를 제공하겠습니다.

### 왜 반응형 프로그래밍이 필요한가?

반응형 프로그래밍은 값이 시간이 지남에 따라 변하는 것과 그 값에 의존하는 계산 및 동작에 초점을 맞추는 프로그래밍 스타일입니다. 반응형은 Shiny 앱에 중요합니다. 앱이 대화형이기 때문입니다: 사용자가 입력 컨트롤을 변경하면(슬라이드를 드래그하고, 텍스트 상자에 입력하고, 체크박스를 선택하는 등) 서버에서 로직이 실행됩니다(CSV 읽기, 데이터 부분집합, 모델 피팅 등), 궁극적으로 출력이 업데이트됩니다(플롯 다시 그리기, 테이블 업데이트 등).

이것은 꽤 정적인 데이터를 다루는 대부분의 R 코드와 상당히 다릅니다. Shiny 앱이 최대한 유용하려면, 반응형 표현식과 출력이 입력이 변경될 때만, 그리고 오직 그럴 때만 업데이트되어야 합니다.

### 왜 변수를 사용할 수 없는가?

어느 정도来说, "시간이 지남에 따라 변하는 값"을 처리하는 방법을 이미 알고 있습니다. 그것들은 변수라고 부릅니다. R의 변수는 값을 나타내고 시간이 지남에 따라 변할 수 있지만, 그것이 변할 때 도움이 되도록 설계되지 않았습니다.

섭씨를 화씨로 변환하는 다음 단순한 예시를 가져가 보세요:

```r
temp_c <- 10
temp_f <- (temp_c * 9 / 5) + 32
temp_f
#> [1] 50
```

여기까지는 괜찮습니다: temp_c 변수는 값 10을 가지고 있고, temp_f 변수는 값 50을 가지고 있으며, temp_c를 변경할 수 있습니다:

```r
temp_c <- 30
```

하지만 temp_c를 변경해도 temp_f에 영향을 주지 않습니다:

```r
temp_f
#> [1] 50
```

변수는 시간이 지남에 따라 변할 수 있지만, 자동으로 변하지는 않습니다.

### 함수는 어떨까요?

함수로 이 문제를 공격할 수도 있습니다:

```r
temp_c <- 10
temp_f <- function() {
  message("Converting")
  (temp_c * 9 / 5) + 32
}
temp_f()
#> Converting
#> [1] 50
```

이것은 반응형이 해결하려는 첫 번째 문제를 해결합니다: temp_f()에 접근할 때마다 최신 계산을 얻습니다:

```r
temp_c <- -3
temp_f()
#> Converting
#> [1] 26.6
```

하지만 계산을 최소화하지는 않습니다. temp_c가 변경되지 않아도 temp_f()를 호출할 때마다 다시 계산합니다:

```r
temp_f()
#> Converting
#> [1] 26.6
```

### 이벤트 기반 프로그래밍

변수도 함수도 작동하지 않으므로 새로운 것을 만들어야 합니다. 지난 수십 년 동안 우리는 직접 이벤트 기반 프로그래밍으로 뛰어들었을 것입니다. 이벤트 기반 프로그래밍은 매력적으로 단순한 패러다임입니다: 이벤트에 응답하여 실행될 콜백 함수를 등록합니다.

다음 예시와 같이 R6를 사용하여 매우 단순한 이벤트 기반 도구 모음을 구현할 수 있습니다. 여기서는 세 가지 중요한 메서드를 가진 DynamicValue를 정의합니다: get()과 set()은 기반 값에 접근하고 변경하고, onUpdate()는 값이 수정될 때마다 실행할 코드를 등록합니다:

```r
DynamicValue <- R6::R6Class("DynamicValue",
  list(
    value = NULL,
    on_update = NULL,
    get = function() self$value,
    set = function(value) {
      self$value <- value
      if (!is.null(self$on_update))
        self$on_update(value)
      invisible(self)
    },
    onUpdate = function(on_update) {
      self$on_update <- on_update
      invisible(self)
    }
  ))
```

이벤트 기반 프로그래밍은 불필요한 계산 문제를 해결하지만, 새로운 문제를 만듭니다: 어떤 입력이 어떤 계산에 영향을 미치는지 신중하게 추적해야 합니다.

### 반응형 프로그래밍

반응형 프로그래밍은 이전 솔루션의 기능을 결합하여 두 문제를 우아하게 해결합니다. 이제 실제 Shiny 코드를 보여드릴 수 있습니다. 콘솔에서 직접 반응형으로 실험할 수 있게 하는 특수 Shiny 모드인 reactiveConsole(TRUE)를 사용합니다:

```r
library(shiny)
reactiveConsole(TRUE)
```

Shiny에서는 reactiveVal()로 반응적 값을 만듭니다. 반응적 값은 값을 가져오기 위한 특수 구문(인수 없는 함수처럼 호출)과 값을 설정하기 위한 특수 구문(인수 하나짜리 함수처럼 호출하여 값을 설정)을 가지고 있습니다:

```r
temp_c <- reactiveVal(10) # 생성
temp_c() # 가져오기
#> [1] 10
temp_c(20) # 설정
temp_c() # 가져오기
#> [1] 20
```

이제 이 값에 의존하는 반응적 표현식을 만들 수 있습니다:

```r
temp_f <- reactive({
  message("Converting")
  (temp_c() * 9 / 5) + 32
})
temp_f()
#> Converting
#> [1] 68
```

앱을 만들 때 배웠듯이, 반응적 표현식은 모든 의존성을 자동으로 추적합니다. 따라서 나중에 temp_c가 변경되면 temp_f가 자동으로 업데이트됩니다:

```r
temp_c(-3)
temp_c(-10)
temp_f()
#> Converting
#> [1] 14
```

하지만 temp_c()가 변경되지 않았다면 temp_f()는 다시 계산할 필요가 없고 캐시에서 가져올 수 있습니다:

```r
temp_f()
#> [1] 14
```

반응적 표현식에는 두 가지 중요한 속성이 있습니다:

1. **게으릅니다**: 호출될 때까지 아무 작업도 하지 않습니다.
2. **캐시됩니다**: 두 번째 이후 호출에서는 이전 결과를 캐시하기 때문에 아무 작업도 하지 않습니다.

### 반응형 프로그래밍의 간략한 역사

반응형 프로그래밍의 기원은 40년 이상 전의 최초 스프레드시트인 VisiCalc에서 볼 수 있습니다:

> 마법의 칠판을 상상했습니다. 하나의 숫자를 지우고 새로운 것을 쓰면, 숫자로 된 워드 프로세싱처럼 다른 모든 숫자가 자동으로 변할 것입니다.
> —Dan Bricklin

스프레드시트는 반응형 프로그래밍과 밀접하게 관련되어 있습니다: 수식을 사용하여 셀 간의 관계를 선언하고, 하나의 셀이 변경되면 모든 의존성이 자동으로 업데이트됩니다.

반응형 프로그래밍 연구는 FRAN, 함수형 반응형 애니메이션, 시간 경과에 따른 변화와 사용자 입력을 함수형 프로그래밍 언어에 통합하는 새로운 시스템으로 시작되었습니다. 이것은 풍부한 문헌을 낳았지만 프로그래밍 관행에 거의 영향을 미치지 않았습니다. 2010년대에야 반응형 프로그래밍이 JavaScript UI 프레임워크의 빠르게 움직이는 세계를 통해 프로그래밍 주류에 진입했습니다. Knockout, Ember, Meteor와 같은 선구적인 프레임워크는 반응형 프로그래밍이 UI 프로그래밍을 극적으로 쉽게 만들 수 있음을 입증했습니다.

## 요약

이제 왜 반응형 프로그래밍이 필요한지 이해하고 약간의 역사를 배웠으므로, 다음 장에서는 기반 이론의 세부 사항을 논의할 것입니다. 가장 중요하게는, 반응적 값, 반응적 표현식, 옵저버를 연결하고 정확히 무엇이 언제 실행되는지 제어하는 반응형 그래프에 대한 이해를 공고히 할 것입니다.


요소들 사이에 연결이 없습니다. Shiny가 반응형 간의 관계에 대한 사전 지식이 없기 때문입니다. 모든 반응형 표현식과 출력은 시작 상태, 즉 무효화된 상태(회색)에 있습니다. 이는 아직 실행되지 않았다는 뜻입니다. 반응형 입력은 준비된 상태(초록색)이며, 값이 계산에 사용 가능함을 나타냅니다.

### 실행 시작

이제 그림 14-3에 보이는 것처럼 실행 단계를 시작합니다.

그림 14-3. Shiny는 임의의 옵저버/출력을 실행하기 시작합니다(노란색).

이 단계에서 Shiny는 무효화된 출력을 선택하고 실행을 시작합니다(노란색). Shiny가 어떤 무효화된 출력을 선택하는지 궁금할 수 있습니다. 짧게 말하면, 무작위인 것처럼 행동해야 합니다. 옵저버와 출력은 어떤 순서로 실행되든 상관하지 않아야 합니다. 독립적으로 기능하도록 설계되었기 때문입니다.

### 반응형 표현식 읽기

출력을 실행하려면 반응형의 값이 필요할 수 있습니다. 그림 14-4와 같습니다.

그림 14-4. 출력은 반응형 표현식의 값이 필요하므로, 표현식 실행을 시작합니다.

반응형을 읽는 것은 그래프를 두 가지 방식으로 변경합니다:

1. 반응형 표현식도 값을 계산하기 시작해야 합니다(노란색으로 변경). 출력은 여전히 계산 중이라는 점에 유의하세요. 반응형 표현식이 값을 반환할 때까지 기다리고 있습니다. 마치 R의 일반 함수 호출과 같습니다.
2. Shiny는 출력과 반응형 표현식 사이의 관계를 기록합니다(즉, 화살표를 그립니다). 화살표의 방향이 중요합니다: 표현식은 출력에 의해 사용됨을 기록합니다. 출력은 표현식을 사용한다는 것을 기록하지 않습니다. 이것은 미묘한 구분이지만, 무효화에 대해 배울 때 중요성이 더 명확해질 것입니다.

### 입력 읽기

이 특정 반응형 표현식은 반응형 입력을 읽습니다. 다시 한번 의존/의존 관계가 설정되므로 그림 14-5에서 또 다른 화살표를 추가합니다.

그림 14-5. 반응형 표현식은 반응형 값에서도 읽으므로, 또 다른 화살표를 추가합니다.

반응형 표현식과 출력과 달리, 반응형 입력은 실행할 것이 없으므로 즉시 반환할 수 있습니다.

### 반응형 표현식 완료

우리의 예에서 반응형 표현식은 또 다른 반응형 표현식을 읽고, 이것은 다시 또 다른 입력을 읽습니다. 이 단계들의 상세한 설명은 우리가 이미 설명한 것의 반복이므로 건너뛰고 그림 14-6으로 직접 이동합니다.

그림 14-6. 반응형 표현식이 계산을 완료했으므로 초록색으로 변경됩니다.

반응형 표현식이 실행을 완료했으므로, 준비되었음을 나타내기 위해 초록색으로 변경됩니다. 결과를 캐시하여 입력이 변경되지 않는 한 다시 계산할 필요가 없습니다.

### 출력 완료

이제 반응형 표현식이 값을 반환했으므로, 출력은 실행을 완료하고 초록색으로 변경될 수 있습니다. 그림 14-7과 같습니다.

그림 14-7. 출력이 계산을 완료하고 초록색으로 변경됩니다.

### 다음 출력 실행

첫 번째 출력이 완료되었으므로, Shiny는 실행할 다른 출력을 선택합니다. 이 출력은 그림 14-8에서 보듯이 노란색으로 변경되고 반응형 프로듀서에서 값을 읽기 시작합니다.

그림 14-8. 다음 출력이 계산을 시작하고 노란색으로 변경됩니다.

완료된 반응형은 값을 즉시 반환할 수 있지만, 무효화된 반응형은 자체 실행 그래프를 시작할 것입니다. 이 사이클은 모든 무효화된 출력이 완료(초록색) 상태에 들어갈 때까지 반복됩니다.

### 실행 완료, 출력 플러시

이제 모든 출력이 실행을 완료하고 유휴 상태입니다. 그림 14-9와 같습니다.

그림 14-9. 모든 출력과 반응형 표현식이 완료되고 초록색으로 변경됩니다.

이제 멈추고 우리가 한 일을 생각해 봅시다. 일부 입력을 읽고, 일부 값을 계산하고, 일부 출력을 생성했습니다. 하지만 더 중요하게도, 반응형 객체들 사이의 관계를 발견했습니다. 반응형 입력이 변경되면, 어떤 반응형을 업데이트해야 하는지 정확히 알고 있습니다.

### 입력 변경

이전 단계는 Shiny 세션이 완전한 유휴 상태인 것으로 끝났습니다. 이제 앱 사용자가 슬라이더 값을 변경한다고 상상해 보세요. 이렇게 하면 브라우저가 서버 함수에 메시지를 보내 Shiny에게 해당 반응형 입력을 업데이트하도록 지시합니다. 이것은 세 부분으로 구성된 무효화 단계를 시작합니다: 입력 무효화, 의존성 알림, 기존 연결 제거.

#### 입력 무효화

무효화 단계는 변경된 입력/값에서 시작됩니다. 그림 14-10과 같이 무효화의 일반적인 색상인 회색으로 채웁니다.

그림 14-10. 사용자가 앱과 상호작용하여 입력을 무효화합니다.

#### 의존성 알림

이제 앞에서 그린 화살표를 따라가며 각 노드를 회색으로, 따라간 화살표를 밝은 회색으로 칠합니다. 이것이 그림 14-11을 생성합니다.

그림 14-11. 무효화는 입력에서 흘러나와 왼쪽에서 오른쪽으로 모든 화살표를 따릅니다. Shiny가 무효화 중에 따른 화살표는 밝은 회색으로 칠해집니다.

#### 관계 제거

다음으로, 각 무효화된 반응형 표현식과 출력은 자신에게 들어오고 나가는 모든 화살표를 "지웁니다". 이것이 그림 14-12를 생성하고 무효화 단계를 완료합니다.

그림 14-12. 무효화된 노드는 이전 관계를 모두 잊어 새로운 관계를 발견할 수 있습니다.

나가는 화살표는 값이 변경될 때 발사될 일회성 알림입니다. 이제 발사되었으므로 목적을 달성했고 지울 수 있습니다. 들어오는 화살표를 왜 지우는지 덜 명확합니다. 비록 그들이 나오는 노드가 무효화되지 않더라도, 무효화된 노드는 더 이상 그것에 관심이 없습니다: 반응형 소비자는 자신을 무효화하기 위해 알림에만 관심이 있으며, 그것은 이미 일어났습니다.

우리가 이 관계에 큰 가치를 두었는데 이제 버렸다는 것은 별로인 것처럼 보일 수 있습니다! 이것이 Shiny의 반응형 프로그래밍 모델의 핵심 부분입니다: 이 특정 화살표는 중요했지만, 이제는 오래되었습니다. 그래프가 정확한 상태를 유지하는 유일한 방법은 화살표가 오래되면 지우고, 이 노드들이 다시 실행될 때 Shiny가 이 노드들 주변의 관계를 새로 발견하도록 하는 것입니다. 이 중요한 주제는 "동적성"에서 다시 다루겠습니다.

### 다시 실행

이제 유효하고 무효화된 반응형이 혼합된 상황에서 두 번째 출력을 실행한 것과 매우 유사한 상황에 있습니다. 그때 했던 일을 정확히 할 시간입니다: 무효화된 출력을 하나씩 실행합니다. 그림 14-13부터 시작합니다.

그림 14-13. 이제 다시 실행이 동일한 방식으로 진행되지만, 처음부터 시작하지 않으므로 수행할 작업이 적습니다.

이 프로세스의 깔끔한 점은 Shiny가 최소한의 작업을 수행했다는 것입니다. 변경된 입력에 의해 실제로 영향을 받는 출력을 업데이트하는 데 필요한 작업만 수행했습니다.

#### 연습 문제

1. 다음 서버 함수에 대한 반응형 그래프를 그리고, 왜 반응형이 실행되지 않는지 설명하세요:

```r
server <- function(input, output, session) {
  sum <- reactive(input$x + input$y + input$z)
  prod <- reactive(input$x * input$y * input$z)
  division <- reactive(prod() / sum())
}
```

2. 다음 반응형 그래프는 Sys.sleep()을 사용하여 장시간 실행되는 계산을 시뮬레이션합니다:

```r
x1 <- reactiveVal(1)
x2 <- reactiveVal(2)
x3 <- reactiveVal(3)

y1 <- reactive({ Sys.sleep(1); x1() })
y2 <- reactive({ Sys.sleep(1); x2() })
y3 <- reactive({ Sys.sleep(1); x2() + x3() + y2() + y2() })

observe({
  print(y1())
  print(y2())
  print(y3())
})
```

x1이 변경되면 그래프를 다시 계산하는 데 얼마나 걸릴까요? x2 또는 x3는 어떨까요?

3. 순환이 있는 반응형 그래프를 만들려고 하면 어떻게 될까요?

```r
x <- reactiveVal(1)
y <- reactive(x + y())
y()
```

### 동적성

"관계 제거"에서 Shiny가 기록하는 데 많은 노력을 기울인 반응형 컴포넌트 사이의 연결을 "잊는다는" 것을 배웠습니다. 이를 통해 Shiny의 반응형은 앱이 실행되는 동안 변경될 수 있습니다. 이 동적성을 단순한 예시로 강조하고 싶습니다:

```r
ui <- fluidPage(
  selectInput("choice", "A or B?", c("a", "b")),
  numericInput("a", "a", 0),
  numericInput("b", "b", 10),
  textOutput("out")
)

server <- function(input, output, session) {
  output$out <- renderText({
    if (input$choice == "a") {
      input$a
    } else {
      input$b
    }
  })
}
```

반응형 그래프가 그림 14-14처럼 생길 것으로 예상할 수 있습니다.

그림 14-14. Shiny가 반응형을 정적으로 분석했다면, 반응형 그래프는 항상 choice, a, b를 out에 연결할 것입니다.

하지만 Shiny는 출력이 무효화된 후 그래프를 동적으로 재구성하기 때문에, input$choice의 값에 따라 그림 14-15의 그래프 중 하나처럼 보입니다. 이를 통해 Shiny는 입력이 무효화될 때 최소한의 작업을 수행합니다. 이 경우 input$choice가 "b"로 설정되면 input$a의 값은 output$out에 영향을 미치지 않으므로 다시 계산할 필요가 없습니다.

그림 14-15. 하지만 Shiny의 반응형 그래프는 동적이므로, 그래프는 out을 choice와 a(왼쪽) 또는 choice와 b(오른쪽)에 연결합니다.

Yindeng Jiang이 블로그에서 언급했듯이, minor한 변경은 출력이 항상 a와 b에 의존하도록 만들 수 있습니다:

```r
output$out <- renderText({
  a <- input$a
  b <- input$b
  if (input$choice == "a") {
    a
  } else {
    b
  }
})
```

이것은 일반 R 코드의 출력에는 영향을 미치지 않지만, 여기서는 값을 읽을 때 반응형 의존성이 설정되고 값을 사용할 때 설정되지 않기 때문에 차이가 있습니다.

### reactlog 패키지

반응형 그래프를 수동으로 그리는 것은 단순한 앱을 이해하고 반응형 프로그래밍에 대한 정확한 정신적 모델을 구축하는 데 강력한 기법입니다. 하지만有许多 이동 부품이 있는 실제 앱에서는 수행하기 고통스럽습니다. Shiny가 알고 있는 것을 사용하여 그래프를 자동으로 그릴 수 있다면 좋지 않을까요? 이것이 reactlog 패키지의 역할이며, 소위 reactlog를 생성합니다. 반응형 그래프가 시간이 지남에 따라 어떻게 진화하는지 보여줍니다.

reactlog를 보려면 먼저 reactlog 패키지를 설치하고 reactlog::reactlog_enable()로 활성화한 다음 앱을 시작해야 합니다. 그런 다음 두 가지 옵션이 있습니다:

1. 앱이 실행 중일 때 Cmd+F3(Windows에서는 Ctrl+F3)을 눌러 그때까지 생성된 reactlog를 표시합니다.
2. 앱이 닫힌 후 shiny::reactlogShow()를 실행하여 전체 세션의 로그를 봅니다.

reactlog는 이 장과 동일한 그래픽 규칙을 사용하므로 즉시 익숙할 것입니다. 가장 큰 차이점은 reactlog가 자동 레이아웃을 안정적으로 유지하기 위해 현재 사용되지 않더라도 모든 의존성을 그린다는 것입니다. 현재 활성화되지 않았지만(과거에는 있었거나 미래에 있을) 연결은 얇은 점선으로 그려집니다.

그림 14-16은 우리가 앞서 사용한 앱에 대해 reactlog가 그린 반응형 그래프를 보여줍니다. 이 스크린샷에는 놀라운 점이 있습니다: 소스 코드에 나타나지 않는 세 개의 추가 반응형 입력(clientData$output_x_height, clientData$output_x_width, clientData$pixelratio)이 있습니다.这些东西는 플롯이 출력 크기에 암묵적으로 의존하기 때문에 존재합니다. 출력 크기가 변경될 때마다 플롯을 다시 그려야 합니다.

그림 14-16. reactlog가 그린 우리 가상 앱의 반응형 그래프.

반응형 입력과 출력에는 이름이 있지만, 반응형 표현식과 옵저버에는 이름이 없으므로 내용으로 레이블이 지정됩니다. 이해하기 쉽게 하려면 reactive()와 observe()의 label 인수를 사용할 수 있으며, 이것이 reactlog에 나타납니다. 이모지를 사용하여 특히 중요한 반응형을 시각적으로 돋보이게 할 수 있습니다.

## 요약

이 장에서 반응형 그래프가 정확히 어떻게 작동하는지 배웠습니다. 특히 무효화 단계에 대해 처음 배웠으며, 이는 즉시 재계산을 유발하지 대신 반응형 소비자를 무효화로 표시하여 필요할 때 재계산되도록 합니다. 무효화 사이클은 이전에 발견된 의존성을 지워 자동으로 재발견될 수 있게 하여 반응형 그래프를 동적으로 만들기 때문에 중요합니다.

이제 전체 그림을 이해했으므로, 다음 장에서는 반응형 값, 표현식, 출력을 구동하는 기반 데이터 구조에 대한 추가 세부 사항을 제공하고, 관련 개념인 시간 기반 무효화를 논의할 것입니다.

---

# 16장. 반응형 빌딩 블록

반응형 그래프를 뒷받침하는 이론을 가지고 있고 실무 경험도 있으므로, 반응형이 R 프로그래밍 언어에 어떻게 맞는지에 대해 더 자세히 논의할 좋은 시기입니다. 반응형 프로그래밍의 세 가지 기본 빌딩 블록이 있습니다:

1. reactiveVal()로 만든 단일 반응형 값
2. reactiveValues()로 만든 반응형 값 리스트

반응형 값과 표현식의 중요한 부분의 대부분을 이미 보았으므로, 이 장은 옵저버와 출력(그것들이 특수한 유형의 옵저버임을 배우게 됩니다)에 더 많은 시간을 할애할 것입니다. 반응형 그래프를 제어하는 두 가지 다른 도구, 격리와 시간 기반 무효화도 배우게 됩니다.

이 장은 다시 반응형 콘솔을 사용하여 매번 Shiny 앱을 시작하지 않고 콘솔에서 직접 반응형으로 실험할 수 있게 합니다. 시작하기 위해 shiny를 로드하고 대화형 실험을 위해 반응형을 켭니다:

```r
library(shiny)
reactiveConsole(TRUE)
```

### 반응형 값

반응형 값에는 두 가지 유형이 있습니다:

1. reactiveVal()로 만든 단일 반응형 값
2. reactiveValues()로 만든 반응형 값 리스트

값을 가져오고 설정하기 위한 약간 다른 인터페이스를 가지고 있습니다:

```r
x <- reactiveVal(10)
x() # 가져오기
#> [1] 10
x(20) # 설정
x() # 가져오기
#> [1] 20

r <- reactiveValues(x = 10)
r$x # 가져오기
#> [1] 10
r$x <- 20 # 설정
r$x # 가져오기
#> [1] 20
```

이 두 유사한 객체가 상당히 다른 인터페이스를 가지고 있는 것은 불행하지만, 이를 표준화할 방법은 없습니다. 하지만 다르게 보이지만 동일하게 동작하므로, 선호하는 구문에 따라 선택할 수 있습니다.

이 책에서는 reactiveValues()를 사용합니다. 구문이 한눈에 이해하기 쉬우므로, 하지만 제 자신의 코드에서는 reactiveVal()을 사용하는 경향이 있습니다. 구문이 무언가 이상한 일이 일어나고 있음을 명확하게 만들기 때문입니다.

두 유형의 반응형 값 모두 소위 참조 시맨틱을 가지고 있다는 점에 유의하는 것이 중요합니다. 대부분의 R 객체는 복사-수정 시맨틱을 가지고 있습니다. 이는 동일한 값을 두 이름에 할당하면 하나를 수정하는 즉시 연결이 끊어진다는 뜻입니다:

```r
a1 <- a2 <- 10
a2 <- 20
a1 # 변경되지 않음
#> [1] 10
```

반응형 값은 그렇지 않습니다. 항상 동일한 값에 대한 참조를 유지하므로 어떤 사본을 수정해도 모든 값이 수정됩니다:

```r
b1 <- b2 <- reactiveValues(x = 10)
b1$x <- 20
b2$x
#> [1] 20
```

왜 자체 반응형 값을 만들 수 있는지는 16장에서 다시 살펴보겠습니다. 그렇지 않으면, encounter할 대부분의 반응형 값은 서버 함수의 input 인수에서 나옵니다. 이것들은 당신이 만든 reactiveValues()와 약간 다릅니다. 읽기 전용이기 때문입니다: 값을 수정할 수 없습니다. Shiny가 브라우저의 사용자 동작에 따라 자동으로 업데이트하기 때문입니다.

#### 연습 문제

1. 이 두 반응형 값 리스트의 차이점은 무엇인가요? 개별 반응형 값을 가져오고 설정하는 구문을 비교하세요:

```r
l1 <- reactiveValues(a = 1, b = 2)
l2 <- list(a = reactiveVal(1), b = reactiveVal(2))
```

2. reactiveVal()도 참조 시맨틱을 가지고 있음을 확인하는 작은 실험을 설계하고 수행하세요.

### 반응형 표현식

반응형은 두 가지 중요한 속성을 가지고 있다는 것을 기억하세요: 게으르고 캐시됩니다. 이는 실제로 필요할 때만 작업을 수행하고, 연속으로 두 번 호출되면 이전 값을 반환한다는 뜻입니다. 아직 다루지 않은 두 가지 중요한 세부 사항이 있습니다: 반응형 표현식이 오류를 어떻게 처리하는지, 그리고 on.exit()가 왜 그 안에서 작동하는지입니다.

#### 오류

반응형 표현식은 값을 캐시하는 것과 정확히 같은 방식으로 오류를 캐시합니다. 예를 들어, 다음 반응형을 가져가 보세요:

```r
r <- reactive(stop("Error occurred at ",
                    Sys.time(), call. = FALSE))
r()
#> Error: Error occurred at 2021-03-05 16:38:19
```

1~2초 기다리면 이전과 같은 오류를 얻는 것을 볼 수 있습니다:

```r
Sys.sleep(2)
r()
#> Error: Error occurred at 2021-03-05 16:38:19
```

오류는 반응형 그래프에서 값과 동일한 방식으로 처리됩니다: 오류는 일반 값과 정확히 같은 방식으로 반응형 그래프를 통해 전파됩니다. 유일한 차이점은 오류가 출력이나 옵저버에 닿을 때 일어나는 일입니다:

1. 출력의 오류는 앱에 표시됩니다.
2. 옵저버의 오류는 현재 세션을 종료시킵니다. 이것이 일어나지 않도록 하려면 코드를 try() 또는 tryCatch()로 감싸야 합니다.

이 동일한 시스템은 req()를 구동합니다. req()는 특수한 유형의 오류를 방출합니다. 이 특수 오류는 옵저버와 출력이 작업을 중지하도록 하지만 그 외에는 실패하지 않습니다. 기본적으로 출력을 초기 빈 상태로 리셋하지만, req(..., cancelOutput = TRUE)를 사용하면 현재 표시를 유지합니다.

#### on.exit()

reactive(x())를 function() x()의 단축키로 생각할 수 있습니다. 자동으로 게으름과 캐싱을 추가합니다. 이것은 Shiny가 구현되는 방식을 이해하고 싶을 때만 중요하지만, 함수 안에서만 작동하는 함수를 사용할 수 있다는 뜻입니다. 그중 가장 유용한 것은 on.exit()입니다. 반응형 표현식이 성공적으로 반환하든 오류로 실패하든, 반응형 표현식이 완료될 때 코드를 실행할 수 있게 해줍니다. 이것이 "완료 시 제거"에서 on.exit()가 작동하는 이유입니다.

#### 연습 문제

1. 다음 코드가 어떻게 작동하는지 설명하세요:

```r
ui <- fluidPage(
  actionButton("go", "Go"),
  textOutput("result")
)

server <- function(input, output, session) {
  data <- eventReactive(input$go, {
    Sys.sleep(2)
    "done"
  })
  output$result <- renderText(data())
}
```

2. 다음 코드의 오류를 수정하세요:

```r
ui <- fluidPage(
  selectInput("dataset", "Choose", ls("package:datasets")),
  tableOutput("data")
)

server <- function(input, output, session) {
  data <- reactive({
    get(input$dataset, "package:datasets")
  })
  output$data <- renderTable(head(data()))
}
```

dataset이 데이터프레임이 아닌 경우 오류가 발생합니다. 이 문제를 해결하는 방법을 찾아보세요.


1. reactlog 패키지를 사용하여 다음 앱에서 반응형을 통한 오류 전파를 관찰하세요. 값 전파와 동일한 규칙을 따르는지 확인하세요:

```r
ui <- fluidPage(
  checkboxInput("error", "error?"),
  textOutput("result")
)

server <- function(input, output, session) {
  a <- reactive({
    if (input$error) {
      stop("Error!")
    } else {
      1
    }
  })
  b <- reactive(a() + 1)
  c <- reactive(b() + 1)
  output$result <- renderText(c())
}
```

2. 이전 앱을 수정하여 stop() 대신 req()를 사용하세요. 여전히 동일한 방식으로 이벤트가 전파되는지 확인하세요. cancelOutput 인수를 사용하면 어떻게 되나요?

### 옵저버와 출력

옵저버와 출력은 반응형 그래프의 터미널 노드입니다. 반응형 표현식과 두 가지 중요한 면에서 다릅니다:

1. **간절하고 잊습니다**: 가능한 한 빨리 실행되고 이전 작업을 기억하지 않습니다. 이 간절함은 "감염적"입니다. 반응형 표현식을 사용하면 그 반응형 표현식도 평가되기 때문입니다.

2. 옵저버가 반환하는 값은 무시됩니다. cat()이나 write.csv()와 같은 부작용을 위해 호출되는 함수와 함께 작동하도록 설계되었기 때문입니다.

옵저버와 출력은 동일한 기반 도구인 observe()에 의해 구동됩니다. 이것은 사용하는 반응형 값 또는 표현식이 업데이트될 때마다 실행되는 코드 블록을 설정합니다. 옵저버는 생성될 때 즉시 실행된다는 점에 유의하세요. 반응형 의존성을 결정하기 위해这样做해야 합니다:

```r
y <- reactiveVal(10)
observe({ message("y is ", y()) })
#> Warning: Error in y: could not find function "y"
y(5)
y(4)
```

이 책에서는 거의 observe()를 사용하지 않습니다. 사용자 친화적인 observeEvent()를 구동하는 저수준 도구이기 때문입니다. 일반적으로 원하는 작업을 수행할 수 없는 경우가 아니면 observeEvent()를 고수해야 합니다. 이 책에서는 observe()가 필요한 경우 한 가지 경우만 보여줄 것입니다: "일시정지 애니메이션".

observe()는 또한 반응형 출력을 구동합니다. 반응형 출력은 두 가지 중요한 속성을 가진 특수한 유형의 옵저버입니다:

1. output에 할당될 때 정의됩니다(즉, output$text <-...는 옵저버를 만듭니다).
2. 보이지 않을 때(즉, 비활성 탭에 있을 때)를 감지하는 일부 제한된 능력이 있어 다시 계산할 필요가 없습니다.

observe()와 반응형 출력이 무언가를 "수행"하는 것이 아니라 무언가를 "만든다"(그런 다음 필요에 따라 조치를 취함)는 점에 유의하는 것이 중요합니다. 이 마인드셋은 이 예시에서 무슨 일이 일어나고 있는지 이해하는 데 도움이 됩니다:

```r
x <- reactiveVal(1)
y <- observe({
  x()
  observe(print(x()))
})
#> Warning: Error in x: could not find function "x"
x(2)
x(3)
```

x의 각 변경은 옵저버를 트리거합니다. 옵저버 자체는 observe()를 호출하여 다른 옵저버를 설정합니다. 따라서 x가 변경될 때마다 다른 옵저버를 얻으므로 값이 한 번 더 출력됩니다.

일반적인 규칙으로, 옵저버나 출력은 서버 함수의 최상위 수준에서만 만들어야 합니다. 중첩하거나 출력 안에 옵저버를 만들려고 시도하면, 만들려는 반응형 그래프를 스케치하세요. 더 나은 접근 방식이 거의 확실히 있습니다.

더 복잡한 앱에서 이 실수를 직접 발견하기 어려울 수 있지만, 항상 reactlog를 사용할 수 있습니다. 옵저버(또는 출력)에서 예상치 못한 변화를 찾은 다음, 그것을 만드는 것으로 추적하세요.

### 코드 격리

이 장을 마무리하기 위해, 반응형 그래프가 정확히 어떻게 그리고 언제 무효화되는지를 제어하는 두 가지 중요한 도구를 논의할 것입니다. 이 섹션에서는 observeEvent()와 eventReactive()를 구동하고 필요하지 않을 때 반응형 의존성을 만들지 않게 해주는 isolate()를 논의합니다. 다음 섹션에서는 스케줄에 따라 반응형 무효화를 생성할 수 있게 해주는 invalidateLater()에 대해 배우게 됩니다.

#### isolate()

옵저버는 종종 시간이 지남에 따라 상태 변화를 추적하기 위해 반응형 값과 결합됩니다. 예를 들어, x가 변경되는 횟수를 추적하는 다음 코드를 가져가 보세요:

```r
r <- reactiveValues(count = 0, x = 1)
observe({
  r$x
  r$count <- r$count + 1
})
```

이것을 실행하면 즉시 무한 루프에 빠집니다. 옵저버가 x와 count에 대한 반응형 의존성을 가지기 때문입니다. 그리고 옵저버가 count를 수정하므로 즉시 다시 실행됩니다.

다행히 Shiny는 이 문제를 해결하기 위해 isolate()를 제공합니다. 이 함수는 의존성을 가지지 않고 반응형 값이나 표현식의 현재 값에 접근할 수 있게 해줍니다:

```r
r <- reactiveValues(count = 0, x = 1)
observe({
  r$x
  r$count <- isolate(r$count) + 1
})
#> Warning: Error in <observer>: object 'r' not found
r$x <- 1
r$x <- 2
r$count
#> [1] 0
r$x <- 3
r$count
#> [1] 0
```

observe()처럼, isolate()를 직접 사용할 필요가 없는 경우가 많습니다. 가장 일반적인 사용법을 감싸는 두 가지 유용한 함수가 있기 때문입니다: observeEvent()와 eventReactive().

#### observeEvent()와 eventReactive()

이전 코드를 보면 "옵저버"를 기억하고 왜 observeEvent()를 사용하지 않았는지 궁금할 수 있습니다:

```r
observeEvent(x(), {
  count(count() + 1)
})
```

사실 사용할 수 있었습니다. observeEvent(x, y)는 observe({x; isolate(y)})와 동일하기 때문입니다. 듣고 싶은 것과 수행할 조치를 우아하게 분리합니다. 그리고 eventReactive()는 반응형에 대한 유사한 작업을 수행합니다: eventReactive(x, y)는 reactive({x; isolate(y)})와 동일합니다.

observeEvent()와 eventReactive()는 작동 세부 사항을 제어할 수 있는 추가 인수를 가지고 있습니다:

1. **기본적으로** 두 함수는 NULL을 생성하는 모든 이벤트를 무시합니다(또는 동작 버튼의 특수한 경우 0). ignoreNULL = FALSE를 사용하여 NULL 값도 처리하세요.
2. **기본적으로** 두 함수는 생성될 때 한 번 실행됩니다. ignoreInit = TRUE를 사용하여 이 실행을 건너뛰세요.
3. **observeEvent()만** once = TRUE를 사용하여 핸들러를 한 번만 실행할 수 있습니다.

이것들은 거의 필요하지 않지만 필요할 때 문서에서 세부 사항을 찾을 수 있도록 알아두는 것이 좋습니다.

#### 연습 문제

1. 다음 앱을 서버 함수로 완성하세요. 버튼을 누를 때만 x의 값으로 out을 업데이트합니다:

```r
ui <- fluidPage(
  numericInput("x", "x", value = 50,
               min = 0, max = 100),
  actionButton("capture", "capture"),
  textOutput("out")
)
```

### 시간 기반 무효화

isolate()는 반응형 그래프가 무효화되는 횟수를 줄입니다. 이 섹션의 주제인 invalidateLater()는 반대입니다: 데이터가 변경되지 않았을 때 반응형 그래프를 무효화할 수 있게 해줍니다. "시간 기반 무효화"에서 reactiveTimer()의 예시를 보았지만, 이제 그것을 구동하는 기반 도구를 논의할 때가 되었습니다: invalidateLater().

invalidateLater(ms)는 어떤 반응형 소비자든 미래에 ms(밀리세컨드) 후에 무효화되도록 합니다. 애니메이션을 만들고 Shiny의 반응형 프레임워크 외부에서 시간이 지남에 따라 변경될 수 있는 데이터 소스에 연결하는 데 유용합니다. 예를 들어, 이 반응형은 0.5초마다 10개의 새로운 무작위 정규 분포를 자동으로 생성합니다:

```r
x <- reactive({
  invalidateLater(500)
  rnorm(10)
})
```

그리고 이 옵저버는 무작위 숫자로 누적 합계를 증가시킵니다:

```r
sum <- reactiveVal(0)
observe({
  invalidateLater(300)
  sum(isolate(sum()) + runif(1))
})
```

다음 섹션에서는 invalidateLater()를 사용하여 디스크에서 변경되는 데이터를 읽는 방법, invalidateLater()가 무한 루프에 빠지지 않도록 하는 방법, 그리고 무효화가 정확히 언제 발생하는지에 대한 몇 가지 경우에 중요한 세부 사항을 배우게 됩니다.

#### 폴링

invalidateLater()의 유용한 응용은 Shiny를 R 외부에서 변경되는 데이터에 연결하는 것입니다. 예를 들어, 다음 반응형을 사용하여 매초마다 CSV 파일을 다시 읽을 수 있습니다:

```r
data <- reactive({
  on.exit(invalidateLater(1000))
  read.csv("data.csv")
})
```

이것은 변경되는 데이터를 Shiny의 반응형 그래프에 연결하지만, 심각한 단점이 있습니다: 반응형을 무효화하면 모든 하위 소비자도 무효화하므로, 데이터가 같더라도 모든 하위 작업을 다시 수행해야 합니다. 이 문제를 피하기 위해 Shiny는 reactivePoll()을 제공합니다. 두 함수를 취합니다: 하나는 데이터가 변경되었는지 비교적 저렴한 확인을 수행하고, 다른 하나는 실제로 계산을 수행하는 더 비싼 함수입니다.

reactivePoll()을 사용하여 이전 반응형을 다시 작성할 수 있습니다:

```r
server <- function(input, output, session) {
  data <- reactivePoll(
    1000, session,
    function() file.mtime("data.csv"),
    function() read.csv("data.csv")
  )
}
```

여기서 파일이 마지막으로 수정된 시간을 반환하는 file.mtime()을 파일을 다시 로드해야 하는지 확인하는 저렴한 확인으로 사용했습니다. 파일이 변경될 때 읽는 것은 일반적인 작업이므로, Shiny는 파일 이름과 리더 함수만 필요로 하는 더 구체적인 헬퍼를 제공합니다:

```r
server <- function(input, output, session) {
  data <- reactiveFileReader(
    1000, session,
    "data.csv", read.csv)
}
```

다른 소스(예: 데이터베이스)에서 변경되는 데이터를 읽어야 하는 경우 자체 reactivePoll() 코드를 만들어야 합니다.

#### 장시간 실행되는 반응형

장시간 실행되는 계산을 수행하는 경우 고려해야 할 중요한 질문이 있습니다: invalidateLater()를 언제 실행해야 할까요? 예를 들어, 다음 반응형을 가져가 보세요:

```r
x <- reactive({
  invalidateLater(500)
  Sys.sleep(1)
  10
})
```

Shiny가 0시에 반응형 실행을 시작하고 500시에 무효화를 요청한다고 가정합니다. 반응형을 실행하는 데 1000ms가 걸리므로 지금은 1000시이고, 즉시 무효화되어 다시 계산해야 하며, 이것은 또 다른 무효화를 설정합니다: 우리는 무한 루프에 갇힙니다.

반면에 invalidateLater()를 마지막에 실행하면, 완료 후 500ms 후에 무효화되므로 반응형은 1500ms마다 다시 실행됩니다:

```r
x <- reactive({
  on.exit(invalidateLater(500), add = TRUE)
  Sys.sleep(1)
  10
})
```

이것이 우리가 앞서 사용한 더 단순한 reactiveTimer()보다 invalidateLater()를 선호하는 주요 이유입니다: 무효화가 정확히 언제 발생하는지에 대한 더 큰 제어를 제공하기 때문입니다.

#### 타이머 정확도

invalidateLater()에 지정된 밀리세컨드 수는 정중한 요청이지 요구가 아닙니다. 무효화가 발생하기를 요청할 때 R은 다른 일을 하고 있을 수 있으므로 요청이 대기해야 합니다. 이것은 효과적으로 숫자가 최소값이고 무효화가 예상보다 오래 걸릴 수 있음을 의미합니다. 대부분의 경우 작은 차이는 앱에 대한 사용자의 인식에 영향을 미치지 않으므로 중요하지 않습니다. 하지만 많은 작은 오류가 축적되는 상황에서는 정확한 경과 시간을 계산하여 계산을 조정해야 합니다. 예를 들어, 다음 코드는 속도와 경과 시간을 기반으로 거리를 계산합니다. invalidateLater(100)이 정확히 100ms를 지연시킨다고 가정하는 대신, 경과 시간을 계산하여 위치 계산에 사용합니다:

```r
velocity <- 3
r <- reactiveValues(distance = 1)
last <- proc.time()[[3]]
observe({
  cur <- proc.time()[[3]]
  time <- last - cur
  last <<- cur
  r$distance <- isolate(r$distance) + velocity * time
  invalidateLater(100)
})
```

주의 깊은 애니메이션을 수행하지 않는다면, invalidateLater()의 고유한 변동을 무시해도 됩니다. 정중한 요청이지 요구라는 점만 기억하세요.

#### 연습 문제

1. 왜 이 반응형이 결코 실행되지 않을까요? 설명은 반응형 그래프와 무효화에 대해 이야기해야 합니다:

```r
server <- function(input, output, session) {
  x <- reactive({
    invalidateLater(500)
    rnorm(10)
  })
}
```

2. SQL에 익숙하다면, 새로운 행이 추가될 때마다 가상의 "Results" 테이블을 다시 읽도록 reactivePoll()을 사용하세요. Results 테이블에 레코드가 추가된 날짜시간을 포함하는 timestamp 필드가 있다고 가정할 수 있습니다.

## 요약

이 장에서는 Shiny를 작동시키는 빌딩 블록에 대해 더 배웠습니다: 반응형 값, 반응형 표현식, 옵저버, 시간 기반 평가. 이제 반응형 그래프의 제약(더 좋든 나쁘든)에서 벗어날 수 있게 해주는 반응형 값과 옵저버의 특정 조합에 초점을 맞추겠습니다.

---

# 17장. 그래프에서 벗어나기

## 소개

Shiny의 반응형 프로그래밍 프레임워크는 입력이 변경될 때 모든 출력을 업데이트하는 데 필요한 최소 계산 세트를 자동으로 결정하기 때문에 믿을 수 없을 정도로 유용합니다. 하지만 이 프레임워크는 의도적으로 제약을 두고 있으며, 때로는 위험하지만 필요한 일을 하기 위해 벗어나야 합니다.

이 장에서는 reactiveValues()와 observe()/observeEvent()를 결합하여 반응형 그래프의 오른쪽을 왼쪽과 다시 연결하는 방법을 배우게 됩니다. 이 기법들은 그래프의 일부에 대한 수동 제어를 제공하기 때문에 강력합니다. 하지만 앱이 불필요한 작업을 수행할 수 있게 하기 때문에 위험합니다. 가장 중요하게, 이제 앱이 결코 끝나지 않는 업데이트 사이클에 갇히는 무한 루프를 만들 수 있습니다.

이 장에서 탐구하는 개념이 흥미롭다면, shinySignals와 rxtools 패키지도 살펴볼 수 있습니다. 둘 다 다른 반응형에서 프로그래밍적으로 생성된 "고차원" 반응형을 탐구하도록 설계된 실험적인 패키지입니다. "실제" 앱에서 사용하는 것은 추천하지 않지만, 소스 코드를 읽으면 통찰력을 얻을 수 있을 것입니다.

시작하기 위해 shiny를 로드합니다:

```r
library(shiny)
```

### 반응형 그래프가 포착하지 않는 것은?

"입력 변경"에서 사용자가 입력을 무효화할 때 일어나는 일을 논의했습니다. 앱 작성자로서 입력을 무효화할 수 있는 두 가지 다른 중요한 경우가 있습니다:

1. value 인수를 설정하여 update 함수를 호출할 때. 이것은 브라우저에 입력 값을 변경하라는 메시지를 보내고, 이것은 R에 입력 값이 변경되었음을 알립니다.
2. reactiveVal() 또는 reactiveValues()로 만든 반응형 값의 값을 수정할 때.

이 두 가지 경우 모두 반응형 값과 옵저버 사이에 반응형 의존성이 생성되지 않는다는 점을 이해하는 것이 중요합니다. 이러한 동작은 그래프를 무효화하지만, 새로운 연결을 통해 기록되지 않습니다.

이 아이디어를 구체적으로 만들기 위해, 그림 16-1에 나와 있는 반응형 그래프와 함께 다음 단순한 앱을 가져가 보세요:

```r
ui <- fluidPage(
  textInput("nm", "name"),
  actionButton("clr", "Clear"),
  textOutput("hi")
)

server <- function(input, output, session) {
  hi <- reactive(paste0("Hi ", input$nm))
  output$hi <- renderText(hi())
  observeEvent(input$clr, {
    updateTextInput(session, "nm", value = "")
  })
}
```

그림 16-1. 반응형 그래프는 이름 없는 옵저버와 nm 입력 사이의 연결을 기록하지 않습니다. 이 의존성은 범위 밖에 있습니다.

지우기 버튼을 누르면 어떻게 될까요?

1. input$clr이 무효화되고, 이것은 옵저버를 무효화합니다.
2. 옵저버가 다시 계산되어 input$clr에 대한 의존성을 다시 만들고 브라우저에 입력 컨트롤의 값을 변경하라고 알립니다.
3. 브라우저가 nm의 값을 변경합니다.
4. input$nm이 무효화되고, hi()를 무효화한 다음 output$hi를 무효화합니다.
5. output$hi가 다시 계산되어 hi()를 다시 계산하도록 강제합니다.

이러한 동작 중 어떤 것도 반응형 그래프를 변경하지 않으므로 그림 16-1과 같이 유지되며, 그래프는 옵저버에서 input$nm까지의 연결을 포착하지 않습니다.

### 사례 연구

다음으로, reactiveValues()와 observeEvent() 또는 observe()를 결합하여 그렇지 않으면 매우 도전적인(아니면 불가능한) 문제를 해결할 수 있는 몇 가지 경우를 살펴보겠습니다. 이것들은 자체 앱에 유용한 템플릿입니다.

#### 여러 입력에 의해 수정되는 출력

시작하기 위해 매우 단순한 문제를 해결하겠습니다: 여러 이벤트에 의해 업데이트되는 공통 텍스트 상자가 필요합니다:

```r
ui <- fluidPage(
  actionButton("drink", "drink me"),
  actionButton("eat", "eat me"),
  textOutput("notice")
)

server <- function(input, output, session) {
  r <- reactiveValues(notice = "")
  observeEvent(input$drink, {
    r$notice <- "You are no longer thirsty"
  })
  observeEvent(input$eat, {
    r$notice <- "You are no longer hungry"
  })
  output$notice <- renderText(r$notice)
}
```

다음 예시에서는 값이 증가하고 감소할 수 있는 두 개의 버튼이 있는 앱이 있어 약간 더 복잡해집니다. reactiveValues()를 사용하여 현재 값을 저장한 다음 적절한 버튼이 눌리면 값을 증가시키고 감소시키기 위해 observeEvent()를 사용합니다. 여기서의 주요 추가 복잡성은 n()의 새 값이 이전 값에 의존한다는 것입니다:

```r
ui <- fluidPage(
  actionButton("up", "up"),
  actionButton("down", "down"),
  textOutput("n")
)

server <- function(input, output, session) {
  r <- reactiveValues(n = 0)
  observeEvent(input$up, {
    r$n <- r$n + 1
  })
  observeEvent(input$down, {
    r$n <- r$n - 1
  })
  output$n <- renderText(r$n)
}
```

그림 16-2에서 이 예시의 반응형 그래프를 볼 수 있습니다. 다시 한번, 반응형 그래프는 옵저버에서 반응형 값 n으로의 연결을 포함하지 않는다는 점에 유의하세요.

그림 16-2. 반응형 그래프는 옵저버에서 입력 값으로의 연결을 포착하지 않습니다.

#### 입력 축적

데이터를 축적하여 데이터 입력을 지원하려는 경우 유사한 패턴입니다. 여기서의 주요 차이점은 사용자가 추가 버튼을 클릭한 후 텍스트 상자를 재설정하기 위해 updateTextInput()을 사용한다는 것입니다:

```r
ui <- fluidPage(
  textInput("name", "name"),
  actionButton("add", "add"),
  textOutput("names")
)

server <- function(input, output, session) {
  r <- reactiveValues(names = character())
  observeEvent(input$add, {
    r$names <- c(input$name, r$names)
    updateTextInput(session, "name", value = "")
  })
  output$names <- renderText(r$names)
}
```

삭제 버튼을 제공하고 추가 버튼이 중복된 이름을 만들지 않도록 하여 약간 더 유용하게 만들 수 있습니다:

```r
ui <- fluidPage(
  textInput("name", "name"),
  actionButton("add", "add"),
  actionButton("del", "delete"),
  textOutput("names")
)

server <- function(input, output, session) {
  r <- reactiveValues(names = character())
  observeEvent(input$add, {
    r$names <- union(r$names, input$name)
    updateTextInput(session, "name", value = "")
  })
  observeEvent(input$del, {
    r$names <- setdiff(r$names, input$name)
    updateTextInput(session, "name", value = "")
  })
  output$names <- renderText(r$names)
}
```

#### 애니메이션 일시정지

일시정지와 중지 버튼을 제공하여 반복 이벤트를 제어할 수 있게 하는 또 다른 일반적인 사용 사례입니다. 이 예시는 실행 중인 반응형 값을 사용하여 숫자가 증가할지 여부를 제어하고, invalidateLater()를 사용하여 실행 중일 때 옵저버가 250ms마다 무효화되도록 합니다:

```r
ui <- fluidPage(
  actionButton("start", "start"),
  actionButton("stop", "stop"),
  textOutput("n")
)

server <- function(input, output, session) {
  r <- reactiveValues(running = FALSE, n = 0)
  observeEvent(input$start, {
    r$running <- TRUE
  })
  observeEvent(input$stop, {
    r$running <- FALSE
  })
  observe({
    if (r$running) {
      r$n <- isolate(r$n) + 1
      invalidateLater(250)
    }
  })
  output$n <- renderText(r$n)
}
```

## 요약

이 장에서는 reactiveValues()와 observe()/observeEvent()를 결합하여 반응형 그래프의 제약에서 벗어나는 방법을 배웠습니다. 이 기법들은 강력하지만 위험합니다. 무한 루프를 만들 수 있고, 앱이 불필요한 작업을 수행할 수 있기 때문입니다. 신중하게 사용하세요.

다음 장에서는 모듈에 대해 알아보겠습니다. 모듈은 복잡한 앱을 더 관리하기 쉽게 만드는 데 유용합니다.


이 경우 observeEvent()를 쉽게 사용할 수 없습니다. running()이 TRUE인지 FALSE인지에 따라 다른 동작을 수행하기 때문입니다. 대신 isolate()를 사용해야 합니다. 그렇지 않으면 이 옵저버가 n에 대한 반응형 의존성도 가지게 되어(그것을 업데이트하므로) 무한 루프에 빠지게 됩니다.

이 예시들이 이 함수들로 프로그래밍하는 것이 어떤 느낌인지에 대한 맛을 보여주었으면 합니다. 매우 명령적입니다: 이것이 발생하면 저것을 하세요; 저것이 발생하면 다른 것을 하세요. 이를 통해 소규모에서는 이해하기 쉽지만, 더 큰 조각이 상호작용하기 시작하면 이해하기 어려워집니다. 일반적으로 이를 최대한 적게 사용하고 격리된 상태로 유지하여 가장 적은 수의 옵저버만이 반응형 값을 수정하도록 해야 합니다.

#### 연습 문제

1. Normal을 클릭하면 정규 분포에서 100개의 무작위 숫자의 히스토그램을 그리고 Uniform을 클릭하면 균등 분포에서 100개의 무작위 숫자의 히스토그램을 그리는 서버 함수를 제공하세요:

```r
ui <- fluidPage(
  actionButton("rnorm", "Normal"),
  actionButton("runif", "Uniform"),
  plotOutput("plot")
)
```

2. 이전 코드를 수정하여 이 UI에서 작동하도록 하세요:

```r
ui <- fluidPage(
  selectInput("type", "type",
              c("Normal", "Uniform")),
  actionButton("go", "go"),
  plotOutput("plot")
)
```

3. 이전 답변의 코드를 다시 작성하여 observe()/observeEvent()의 사용을 제거하고 reactive()만 사용하세요. 왜 두 번째 UI에서는 할 수 있지만 첫 번째에서는 할 수 없는가요?

### 안티패턴

이 패턴을 익히면 나쁜 습관에 빠지기 쉽습니다:

```r
server <- function(input, output, session) {
  r <- reactiveValues(df = cars)
  observe({
    r$df <- head(cars, input$nrows)
  })
  output$plot <- renderPlot(plot(r$df))
  output$table <- renderTable(r$df)
}
```

이 단순한 경우, 이 코드는 reactive()를 사용하는 대안에 비해 많은 추가 작업을 하지 않습니다:

```r
server <- function(input, output, session) {
  df <- reactive(head(cars, input$nrows))
  output$plot <- renderPlot(plot(df()))
  output$table <- renderTable(df())
}
```

하지만 두 가지 단점이 있습니다:

1. 테이블이나 플롯이 현재 보이지 않는 탭에 있으면 옵저버는 여전히 그것들을 그리고/플롯할 것입니다.
2. head()가 오류를 발생시키면 observe()는 앱을 종료하지만, reactive()는 그것을 전파하므로 표시되는 반응형이 오류를 발생시키고 전파되지 않습니다.

그리고 앱이 더 복잡해질수록 상황은 점점 악화될 것입니다. "이벤트 기반 프로그래밍"에서 설명한 이벤트 기반 프로그래밍 상황으로 돌아가기 쉽습니다. 앱의 이벤트 흐름을 분석하는 많은 어려운 작업을 수행하게 되고, Shiny가 자동으로 처리하도록依赖하지 않습니다.

두 반응형 그래프를 비교하는 것은 유익합니다. 그림 16-3은 첫 번째 예시의 그래프를 보여줍니다.误导적입니다. nrows가 df()에 연결되어 있는 것처럼 보이지 않기 때문입니다. 그림 16-4와 같이 reactive를 사용하면 정확한 연결을 쉽게 볼 수 있습니다.

반응형 그래프가 가능한 한 단순한 것은 인간과 Shiny 모두에게 중요합니다. 단순한 그래프는 인간이 이해하기 쉽고, Shiny가 최적화하기 쉽습니다.

그림 16-3. 반응형 값과 옵저버를 사용하면 그래프의 일부가 연결되지 않은 상태로 남습니다.

그림 16-4. reactive를 사용하면 컴포넌트 간의 의존성이 매우 명확해집니다.

## 요약

지난 네 장에서 Shiny가 사용하는 반응형 프로그래밍 모델에 대해 많이 배웠습니다. 반응형 프로그래밍이 왜 중요한지(Shiny가 필요한 만큼만 작업을 수행하게 함), 반응형 그래프의 세부 사항, 기반 빌딩 블록이 내부적으로 어떻게 작동하는지, 그리고 필요할 때 반응형 그래프의 제약에서 벗어나는 방법을 배웠습니다.

이 책의 나머지는 소프트웨어 엔지니어링의 관점에서 Shiny를 다룹니다. 다음 7장에서는 앱이 규모와 영향력이 계속 커짐에 따라 유지 가능하고, 성능이 좋고, 안전하게 유지하는 방법을 배우게 됩니다.

---

# Part 4: 모범 사례

Shiny를 사용하기 시작하면, 기본을 배워야 하므로 작은 앱을 만드는 데 오랜 시간이 걸립니다. 하지만 시간이 지남에 따라 패키지의 기본 인터페이스와 반응형의 핵심 개념에 더 익숙해지고, 더 크고 복잡한 애플리케이션을 만들 수 있게 될 것입니다.

더 큰 앱을 작성하기 시작하면, 새로운 도전에 직면하게 될 것입니다: 복잡하고 성장하는 코드베이스를 구성하고, 안정적으로 유지하고, 유지보수 가능하게 만드는 것. 다음이 포함됩니다:

- "이 거대한 파일에서 찾고 있는 코드를 찾을 수 없습니다."
- "6개월 동안 이 코드를 작업하지 않았고, 변경하면 망칠까 봐 두렵습니다."
- "다른 사람이 나와 함께 앱을 작업하기 시작했는데, 서로 간섭하고 있습니다."
- "앱이 내 컴퓨터에서는 작동하지만 협력자 컴퓨터나 프로덕션에서는 작동하지 않습니다."

이 "모범 사례" 부분에서 소프트웨어 엔지니어링의 핵심 개념과 도구를 배우게 될 것입니다. 이것이 이러한 도전을 극복하는 데 도움이 될 것입니다:

- **18장**: 소프트웨어 엔지니어링의 큰 아이디어를 간략히 소개합니다.
- **19장**: Shiny 앱에서 코드를 독립된 앱으로 추출하는 방법을 보여주고, 그렇게 하고 싶은 이유를 논의합니다.
- **20장**: Shiny의 모듈 시스템에 대해 배웁니다. 이를 통해 결합된 UI와 서버 코드를 격리되고 재사용 가능한 컴포넌트로 추출할 수 있습니다.
- **21장**: 앱을 R 패키지로 전환하는 방법을 보여주고, 더 큰 앱에 대한 투자가 보상되는 이유를 보여줍니다.
- **22장**: 기존 비공식 테스트를 자동화된 테스트로 전환하는 방법을 배웁니다. 앱이 변경될 때마다 쉽게 다시 실행할 수 있습니다.
- **23장**: 앱을 안전하게 유지하기 위해 피해야 할 패턴을 배웁니다.
- **24장**: 앱의 병목 현상을 식별하고 해결하는 방법을 배웁니다. 수백 명의 사용자가 사용할 때도 빠르게 유지됩니다.

물론, 한 권의 책의 한 부분에서 소프트웨어 엔지니어링에 대해 모든 것을 배울 수는 없으므로, 더 배울 수 있는 좋은 곳도 안내할 것입니다.

---

# 18장. 일반 지침

## 소개

이 장은 Shiny 앱을 작성할 때 필요한 가장 중요한 소프트웨어 엔지니어링 기술을 소개합니다: 코드 구성, 테스트, 의존성 관리, 소스 코드 관리, 지속적 통합, 코드 리뷰.

이러한 기술은 Shiny 앱에 고유한 것이 아니지만, 시간이 지남에 따라 유지보수가 쉬워지는 복잡한 앱을 작성하고 싶다면 이 모든 것에 대해 약간 배워야 합니다.

소프트웨어 엔지니어링 기술을 향상시키는 것은 평생의 여정입니다. 배우기 시작할 때 좌절감을 예상하지만, 모든 사람이 동일한 문제를 경험하며, 끈기 있게 노력하면 극복할 수 있습니다.

대부분의 사람들은 새로운 기술을 배울 때 동일한 진화를 거칩니다: "이해하지 못하고 사용할 때마다 찾아봐야 한다"에서 "대략 이해하지만 여전히 문서를 많이 읽는다"에서 궁극적으로 "이해하고 유연하게 사용할 수 있다". 마지막 단계에 도달하는 데는 시간과 연습이 필요합니다.

매주 소프트웨어 개발 기술을 연습할 시간을 할당하는 것을 추천합니다. 이 시간 동안 앱의 동작이나 외형을 만지지 말고 대신 앱을 이해하고 개발하기 쉽게 만드는 데 노력을 집중하세요. 이를 통해 앱을 나중에 변경하기 쉬워지고, 소프트웨어 개발 기술이 향상됨에 따라 앱의 첫 번째 시도도 더 높은 품질이 될 것입니다. (이 장의 대부분을 기여해 준 동료 Jeff Allen에게 감사합니다.)

### 코드 구성

> 바보도 컴퓨터가 이해할 수 있는 코드를 쓸 수 있다. 좋은 프로그래머는 인간이 이해할 수 있는 코드를 쓴다.
> —Martin Fowler, "리팩터링: 기존 코드의 디자인 개선"

애플리케이션의 품질을 향상시키는 가장 분명한 방법 중 하나는 코드의 가독성과 이해 용이성을 향상시키는 것입니다. 세계 최고의 프로그래머도 이해할 수 없는 코드베이스를 유지보수할 수 없으므로, 이것은 좋은 시작점입니다.

좋은 프로그래머가 된다는 것은 이 코드베이스와 나중에 상호작용해야 할 다른 사람들(심지어 미래의 자신!)에 대한 공감을 개발하는 것을 의미합니다. 모든 형태의 공감과 마찬가지로, 이것은 연습이 필요하고 여러 번 수행한 후에야 쉬워집니다.

시간이 지남에 따라 특정 관행이 코드의 가독성을 향상시킨다는 것을 알기 시작할 것입니다. 보편적인 규칙은 없지만, 일반적인 지침은 다음과 같습니다:

1. 변수와 함수 이름이 명확하고 간결한가요? 그렇지 않다면, 코드의 의도를 더 잘 전달할 이름은 무엇인가요?
2. 복잡한 코드 부분을 설명하기 위해 주석이 필요한 곳에 있는가요?
3. 이 전체 함수가 내 화면에 맞는가요, 아니면 한 장의 종이에 인쇄될 수 있나요? 그렇지 않다면, 더 작은 부분으로 나눌 방법이 있나요?
4. 앱 전체에서 동일한 코드 블록을 여러 번 복사하여 붙여넣고 있나요? 그렇다면, 반복을 피하기 위해 함수나 변수를 사용할 방법이 있나요?
5. 앱의 모든 부분이 얽혀 있나요, 아니면 앱의 다양한 컴포넌트를 격리하여 관리할 수 있나요?

이 모든 점을 해결하는 만능_bullet은 없습니다. 많은 경우 주관적인 판단이 필요하지만, 두 가지 특히 중요한 도구가 있습니다:

1. **함수**: 19장의 주제. 함수를 사용하면 UI 코드의 중복을 줄이고, 서버 함수를 이해하고 테스트하기 쉽게 만들며, 앱 코드를 더 유연하게 구성할 수 있습니다.
2. **Shiny 모듈**: 20장의 주제. Shiny 모듈은 프론트엔드와 백엔드 동작을 조율하는 격리되고 재사용 가능한 코드를 작성할 수 있게 합니다. 모듈을 통해 관심사를 우아하게 분리할 수 있으므로, 예를 들어 앱의 개별 페이지가 독립적으로 작동하거나 반복되는 컴포넌트를 더 이상 복사하여 붙여넣을 필요가 없습니다.

### 테스트

애플리케이션에 대한 테스트 계획을 개발하는 것은 지속적인 안정성을 보장하는 데 중요합니다. 테스트 계획 없이는 모든 변경이 애플리케이션을 위태롭게 합니다. 애플리케이션이 작아서 머릿속에 모두 넣을 수 있을 때는 추가 테스트 계획이 필요하지 않다고 느낄 수 있습니다. 물론 매우 단순한 앱을 테스트하는 것은 그만한 가치보다 더 많은 문제처럼 보일 수 있습니다.

하지만 계획이 없으면 누군가가 앱에 기여하기 시작하거나, 앱과 충분히 멀어져서 모두가 어떻게 맞물리는지 잊어버렸을 때 고통이 발생할 가능성이 높습니다. 테스트 계획은 완전히 수동일 수도 있습니다. 시작하기에 좋은 곳은 모든 것이 정상인지 확인할 따라야 할 스크립트를 제공하는 단순한 텍스트 파일입니다.

하지만 그 스크립트는 애플리케이션이 더 복잡해짐에 따라 성장해야 하며, 점점 더 많은 시간을 수동 테스트에 할애하거나 스크립트의 일부를 건너뛰기 시작할 것입니다. 따라서 다음 단계는 테스트의 일부를 자동화하는 것입니다.

자동화는 설정하는 데 시간이 걸리지만, 테스트를 더 자주 실행할 수 있으므로 시간이 지남에 따라 보상이 따릅니다. 그런 이유로, 21장에서 설명하듯이 Shiny에 대한 다양한 형태의 자동화된 테스트가 개발되었습니다.

그 장에서 설명하듯이, 다음을 개발할 수 있습니다:

1. 개별 함수의 올바른 동작을 확인하는 **단위 테스트**
2. 반응형 간의 상호작용을 확인하는 **통합 테스트**
3. 브라우저에서의 엔드투엔드 경험을 검증하는 **기능 테스트**
4. 애플리케이션이 예상하는 트래픽량을 견딜 수 있는지 확인하는 **부하 테스트**

자동화된 테스트를 작성하는 아름다움은 작성하는 데 시간을 들인 후에는 해당 애플리케이션 부분을 다시 수동으로 테스트할 필요가 없다는 것입니다. 지속적 통합(곧 더 자세히 설명)을 활용하여 애플리케이션을 게시하기 전에 코드를 변경할 때마다 이 테스트를 실행할 수도 있습니다.

### 의존성 관리

다른 사람이 작성한 R 분석을 재현하려고 시도했거나, 심지어 스스로 작성한 분석이나 Shiny 애플리케이션을 다시 실행하려고 시도한 적이 있다면 의존성 문제에 부딪혔을 수 있습니다.

앱의 의존성은 소스 코드 외에 실행에 필요한 모든 것입니다. 하드 드라이브의 파일, 외부 데이터베이스 또는 API, 또는 앱이 사용하는 다른 R 패키지가 포함될 수 있습니다.

미래에 재현하고 싶은 분석의 경우 renv를 사용하는 것을 고려하세요. 이를 통해 재현 가능한 R 환경을 만들 수 있습니다. renv를 사용하면 앱이 사용하는 정확한 패키지 버전을 캡처할 수 있으므로, 다른 컴퓨터에서 이 앱을 사용할 때 정확히 같은 패키지 버전을 사용할 수 있습니다.

의존성을 관리하는 또 다른 도구는 config 패키지입니다. config 패키지는 실제로 의존성을 관리하지는 않지만, R 패키지 외의 의존성을 추적하고 관리하는 편리한 장소를 제공합니다. 예를 들어, 앱이 종속되는 CSV 파일의 경로나 필요한 API의 URL을 지정할 수 있습니다.这些东西을 config 파일에 열거하면 이러한 의존성을 추적하고 관리하는 단일 장소가 됩니다.

더 나은 것은, 서로 다른 환경에 대한 서로 다른 구성을 만들 수 있다는 것입니다. 예를 들어, 앱이 많은 데이터가 있는 데이터베이스를 분석하는 경우 여러 환경을 구성할 수 있습니다:

1. **프로덕션 환경**: 앱을 실제 "프로덕션" 데이터베이스에 연결합니다.
2. **테스트 환경**: 앱이 테스트 데이터베이스를 사용하도록 구성하여 테스트에서 데이터베이스 연결을 적절하게 작동시키지만, 데이터를 손상시키는 변경을 실수로 하더라도 프로덕션 데이터베이스를 손상시킬 위험이 없습니다.
3. **개발 환경**: 데이터의 부분 집합이 포함된 작은 CSV를 사용하도록 앱을 구성하여 더 빠른 반복을 가능하게 합니다.

마지막으로, 로컬 파일 시스템에 대한 가정을 주의하세요. 코드가 C:\data\cars.csv 또는 ~/my-projects/genes.rds와 같은 데이터를 참조하는 경우, 이러한 파일이 다른 컴퓨터에 존재할 가능성이 매우 낮다는 것을 인식해야 합니다. 대신 앱 디렉토리에 상대적인 경로(예: data/cars.csv 또는 genes.rds)를 사용하거나, config 패키지를 사용하여 외부 경로를 명시적이고 구성 가능하게 만드세요.

### 소스 코드 관리

오랫동안 프로그래밍한 사람이라면 누구든 실수로 앱을 망가뜨리고 이전 작동 상태로 되돌리고 싶은 상태에 도달한 적이 있을 것입니다. 수동으로这样做하는 것은 믿을 수 없을 정도로 고된 작업입니다. 다행히 "버전 관리 시스템"에 의존하여 원자적 변경을 쉽게 추적하고, 이전 작업으로 되돌리고, 여러 기여자의 작업을 통합할 수 있습니다.

R 커뮤니티에서 가장 인기 있는 버전 관리 시스템은 Git입니다. Git은 일반적으로 GitHub와 함께 사용되며, GitHub는 Git 저장소를 다른 사람과 쉽게 공유할 수 있는 웹사이트입니다. Git과 GitHub에 능숙해지는 데는 노력이 필요하지만, 경험 많은 개발자라면 그 노력이 충분히 가치가 있다고 확인할 것입니다. Git이 처음이라면, Jenny Bryan의 "Happy Git and GitHub for the useR"로 시작하는 것을 강력히 추천합니다.

### 지속적 통합/배포

버전 관리 시스템을 사용하고 자동화된 테스트의 견고한 세트를 갖추면 지속적 통합(CI)의 이점을 얻을 수 있습니다. CI는 애플리케이션에 대한 변경이 아무것도 망가뜨리지 않았음을 지속적으로 검증하는 방법입니다. 이를 과거적으로(방금 만든 변경이 앱을 망가뜨린 경우 알림) 또는 미래적으로(제안된 변경이 앱을 망가뜨릴 경우 알림) 사용할 수 있습니다.

새로운 커밋을 푸시하거나 변경을 제안할 때 자동으로 테스트를 실행할 수 있는 다양한 서비스가 있습니다. 코드가 호스팅되는 곳에 따라 GitHub Actions, Travis CI, Azure Pipelines, AppVeyor, Jenkins 또는 GitLab CI/CD를 고려할 수 있습니다.

그림 17-1은 CI 시스템이 GitHub에 연결되어 풀 리퀘스트를 테스트할 때 어떻게 보이는지 보여줍니다. 모든 CI 테스트가 녹색 체크를 표시하는 것을 볼 수 있습니다. 이는 각 자동화된 테스트 환경이 성공적이었음을 의미합니다. 테스트가 실패하면 변경을 앱에 병합하기 전에 실패에 대해 알림을 받게 됩니다.

CI 프로세스는 숙련된 개발자가 실수를 하는 것을 방지할 뿐만 아니라 새로운 기여자가 자신감을 가질 수 있게 도와줍니다.

### 코드 리뷰

많은 소프트웨어 회사들이 코드가 공식적으로 코드베이스에 통합되기 전에 다른 사람이 검토하는 것의 이점을 발견했습니다. 이 "코드 리뷰" 프로세스에는 여러 가지 이점이 있습니다:

1. **품질 향상**: 다른 사람이 코드를 검토하면 실수, 잠재적인 버그, 개선 기회를 발견할 수 있습니다.
2. **지식 공유**: 코드 리뷰는 팀 전체에 지식을 분산시키는 데 도움이 됩니다. 한 사람이 코드의 특정 부분을 이해하면 다른 사람도 이해할 수 있습니다.
3. **표준 유지**: 코드 리뷰는 팀 전체에서 일관된 코드 스타일과 관행을 유지하는 데 도움이 됩니다.
4. **교육 기회**: 새로운 팀원은 코드 리뷰를 통해 앱과 코드베이스에 대해 배울 수 있습니다.

## 요약

이 장에서는 소프트웨어 엔지니어링의 핵심 기술을 소개했습니다. 코드 구성, 테스트, 의존성 관리, 소스 코드 관리, 지속적 통합, 코드 리뷰. 이 기술들은 Shiny 앱에 고유한 것이 아니지만, 시간이 지남에 따라 유지보수가 쉬워지는 복잡한 앱을 작성하는 데 필요합니다.

다음 장에서는 Shiny 앱에서 코드를 추출하는 방법에 대해 알아보겠습니다.


버그를 앱에 통합되기 전에 잡아내므로 수정 비용이 훨씬 적게 듭니다. 교수 기회를 제공합니다—모든 수준의 프로그래머들이 다른 사람의 코드를 리뷰하거나 자신의 코드를 리뷰받을 때 새로운 것을 배우는 경우가 많습니다. 팀 내 지식 공유와 상호 학습을 촉진하여 앱을 이해하는 사람이 한 명뿐인 상황을 방지합니다. 이 과정에서 오가는 대화는 코드의 가독성을 높이는 경우가 많습니다. 일반적으로 코드 리뷰는 본인 외의 사람이 참여하지만, 혼자서 진행하더라도 이점을 얻을 수 있습니다. 대부분의 숙련된 개발자들은 코드를 작성한 후 최소한 몇 시간은 놔두고 다시 검토하면 작지만 중요한 결함을 발견할 수 있다고 동의할 것입니다. 코드를 리뷰할 때 염두에 두면 좋은 질문 몇 가지입니다: 새로운 함수의 이름이 간결하면서도 의미를 잘 전달하는가? 코드 중 이해하기 어려운 부분이 있는가? 향후 변경 가능성이 높고 자동화된 테스트의 혜택을 크게 받을 수 있는 부분은 어디인가? 코드 스타일이 앱의 나머지 부분(또는 더 나아가 팀이 문서화한 코드 스타일)과 일치하는가? 강한 엔지니어링 문화를 가진 조직에 소속되어 있다면, 데이터 사이언스 코드에 대한 코드 리뷰를 설정하는 것은 비교적 수월할 것이며, 기존 도구와 경험을 활용할 수 있습니다. 다른 소프트웨어 엔지니어가 거의 없는 조직에 있다면, 설득에 더 많은 노력이 필요할 수 있습니다. 추천하는 두 가지 자료입니다: thoughtbot 가이드

코드 리뷰 개발자 가이드 요약 소프트웨어 엔지니어의 사고방식에 대해 어느 정도 배웠으니, 다음 장에서는 Shiny 앱에 적용되는 함수 작성, 테스트, 보안, 성능에 대해 자세히 다룰 것입니다. 다른 장들 앞에 18장을 먼저 읽어야 하지만, 그 외에는 순서대로 읽지 않아도 됩니다.

18장. 함수 앱이 커질수록 모든 구성 요소를 머릿속에 담아두기가 어려워지고, 이해하기도 점점 더 힘들어집니다. 그로 인해 새로운 기능을 추가하기도 어려워지고, 문제가 발생했을 때 해결책을 찾기도 어려워집니다(즉, 디버깅이 어려워집니다). 의도적인 조치를 취하지 않으면 앱의 개발 속도는 느려지고, 점점 덜 즐거운 작업이 될 것입니다. 이 장에서는 함수를 작성하면 어떻게 도움이 되는지 배울 것입니다. UI와 서버 컴포넌트마다 약간 다른 양상을 보입니다: UI에서는 여러 곳에서 미세한 변형과 함께 반복되는 컴포넌트들이 있습니다. 반복되는 코드를 함수로 추출하면 중복이 줄어들고(여러 컨트롤을 한 곳에서 업데이트하기 쉬워지며), 함수형 프로그래밍 기술과 결합하여 여러 컨트롤을 한 번에 생성할 수 있습니다. 서버에서는 복잡한 리액티브를 디버깅하기 어렵습니다. 앱 한복olio에서 작업해야 하기 때문입니다. 리액티브를 별도의 함수로 추출하면, 설령 그 함수가 한 곳에서만 호출되더라도 디버깅이 훨씬 쉬워집니다. 리액티브와 독립적으로 계산을 시도해볼 수 있기 때문입니다. 함수는 Shiny 앱에서 또 다른 중요한 역할을 합니다: 앱 코드를 여러 파일에 분산시킬 수 있게 해줍니다. 하나의 거대한 app.R 파일로도 가능하겠지만, 여러 파일에 나누어 놓으면 관리가 훨씬 수월합니다. 함수의 기본은 이미 익숙할 것이라고 가정합니다.1 이 장의 목표는 기존 기능을 활성화하고, 함수 사용으로 앱의 명확성이 크게 향상될 수 있는 구체적인 사례를 보여주는 것입니다. 이 장의 내용을 익혔다면, 다음 단계는 UI와 서버에서 조율이 필요한 코드를 작성하는 방법을 배우는 것입니다. 그것은 19장에서 다룰 모듈(module)을 필요로 합니다. 항상那样, 먼저 shiny를 로드합니다: library(shiny) 파일 구성 앱에서 함수를 어떻게 사용할지 본격적으로 다루기 전에, 즉각적인 이점 하나를 먼저 짚고 넘어가겠습니다. 함수는 app.R 바깥에 위치할 수 있습니다. 함수의 크기에 따라 두 가지 위치를 고려할 수 있습니다: 큰 함수(및 필요한 작은 헬퍼 함수들)는 별도의 R/{function-name}.R 파일에 넣는 것을 추천합니다. 작고 간단한 함수들은 한 곳에 모으고 싶을 수 있습니다. 저는 주로 R/utils.R을 사용하지만, 주로 UI에서 사용된다면 R/ui.R을 사용할 수도 있습니다. 이전에 R 패키지를 만들어본 적이 있다면, Shiny가 함수를 저장하는 파일에 동일한 관례를 사용한다는 것을 눈치챘을 것입니다. 실제로 복잡한 앱을 만든다면, 특히 여러 저자가 있다면, 완전한 패키지로 만드는 것이 상당한 이점이 있습니다. 이렇게 하려면 Engineering Production-Grade Shiny Apps를 읽고 함께 제공되는 golem 패키지를 사용하는 것을 추천합니다. 테스트에 대해 더 논의할 때 패키지에 대해 다시 다루겠습니다. UI 함수 함수는 UI 코드의 중복을 줄이는 강력한 도구입니다. 구체적인 중복 코드 예시부터 시작하겠습니다. 각각 0에서 1까지의 범위를 가지며, 0.5에서 시작하고 0.1 단위로 움직이는 슬라이더를 여러 개 만든다고 가정해봅시다. 모든 슬라이더를 생성하기 위해 복사-붙여넣기를 여러 번 할 수 있습니다:

ui <- fluidRow( sliderInput("alpha", "alpha", min = 0, max = 1, value = 0.5, step = 0.1), sliderInput("beta", "beta", min = 0, max = 1, value = 0.5, step = 0.1), sliderInput("gamma", "gamma", min = 0, max = 1, value = 0.5, step = 0.1), sliderInput("delta", "delta", min = 0, max = 1, value = 0.5, step = 0.1) ) 하지만 반복되는 패턴을 인식하고 함수로 추출하는 것이 가치 있다고 생각합니다. 그렇게 하면 UI 코드가 크게 간결해집니다: sliderInput01 <- function(id) { sliderInput(id, label = id, min = 0, max = 1, value = 0.5, step = 0.1) } ui <- fluidRow( sliderInput01("alpha"), sliderInput01("beta"), sliderInput01("gamma"), sliderInput01("delta") ) 여기서 함수는 두 가지 방식으로 도움이 됩니다: 함수에 의미 있는 이름을 붙여 미래에 코드를 다시 읽을 때 무슨 일이 일어나고 있는지 이해하기 쉽게 합니다. 동작을 변경해야 한다면 한 곳에서만 수정하면 됩니다. 예를 들어, 단계를 더 세밀하게 조정해야 한다면 step = 0.01을 한 곳에서만 쓰면 되지, 네 곳에서 쓸 필요가 없습니다. 기타 응용 함수는 다른 많은 곳에서도 유용할 수 있습니다. 몇 가지 아이디어를 제안합니다:

특정 국가에 맞춘 커스텀 dateInput()을 사용하고 있다면, 일관된 인수를 사용할 수 있도록 한 곳에 추출하세요. 예를 들어, 미국인이 요일을 선택하는 데 사용할 날짜 컨트롤을 원한다고 가정해봅시다: usWeekDateInput <- function(inputId,...) { dateInput(inputId,..., format = "dd M, yy", daysofweekdisabled = c(0, 6)) } ...의 사용에 주목하세요. dateInput()에 다른 인수를 계속 전달할 수 있다는 뜻입니다. 또는 아이콘을 쉽게 제공하는 라디오 버튼을 원할 수도 있습니다: iconRadioButtons <- function(inputId, label, choices, selected = NULL) { names <- lapply(choices, icon) values <- if (is.null(names(choices))) names(choices) else choices radioButtons(inputId, label = label, choiceNames = names, choiceValues = values, selected = selected ) } 또는 여러 곳에서 반복 사용하는 여러 선택 항목이 있다면: stateSelectInput <- function(inputId,...) { selectInput(inputId,..., choices = state.name) }

조직 내에서 많은 Shiny 앱을 개발하고 있다면, 이런 함수들을 공유 패키지에 넣어 앱 간 일관성을 높이는 데 기여할 수 있습니다. 함수형 프로그래밍 동기 부여 예제로 돌아가서, 함수형 프로그래밍에 익숙하다면 코드를 더 줄일 수 있습니다: library(purrr) vars <- c("alpha", "beta", "gamma", "delta") sliders <- map(vars, sliderInput01) ui <- fluidRow(sliders) 여기서 핵심 아이디어 두 가지가 있습니다: map()은 vars에 저장된 각 문자열에 대해 sliderInput01()을 한 번씩 호출합니다. 슬라이더 목록을 반환합니다. 목록을 fluidRow()(또는 다른 HTML 컨테이너)에 전달하면, 목록을 자동으로 펼쳐서 요소들이 컨테이너의 자식이 되도록 합니다. map()에 대해 더 자세히 알고 싶다면(또는 기본 R의 동등 기능인 lapply()), Advanced R의 Functionals 장을 추천합니다. UI as 데이터 만약 컨트롤이 하나 이상의 가변 입력을 가진다면, 이 아이디어를 더 일반화할 수 있습니다. 먼저 tibble::tribble()을 사용하여 각 컨트롤의 매개변수를 정의하는 인라인 데이터 프레임을 만듭니다. UI 구조를 명시적인 데이터 구조로 변환하는 것입니다: vars <- tibble::tribble( ~ id, ~ min, ~ max, "alpha", 0, 1, "beta", 0, 10, "gamma", -1, 1,

"delta", 0, 1, ) 그런 다음 인수 이름이 열 이름과 일치하는 함수를 만듭니다: mySliderInput <- function(id, label = id, min = 0, max = 1) { sliderInput(id, label, min = min, max = max, value = 0.5, step = 0.1) } 마지막으로 purrr::pmap()을 사용하여 vars의 각 행에 대해 mySliderInput()을 한 번씩 호출합니다: sliders <- pmap(vars, mySliderInput) 이 코드가难以解的话 걱정하지 마세요. 복사-붙여넣기를 계속 사용하면 됩니다. 하지만 장기적으로는 함수형 프로그래밍을 더 배우는 것을 추천합니다. 그렇지 않으면 길어질 개념을 간결하게 표현할 수 있는 놀라운 능력을 제공하기 때문입니다. 사용자 동작에 반응하여 동적 UI를 생성하는 이러한 기술의 더 많은 예는 "코드로 UI 만들기"를 참조하세요. 서버 함수 긴 리액티브(예: 10줄 이상)가 있을 때마다, 리액티브를 사용하지 않는 별도의 함수로 추출하는 것을 고려해야 합니다. 두 가지 장점이 있습니다: 리액티브를 server() 안에, 복잡한 계산을 함수 안에 넣어 분리할 수 있다면 코드를 디버깅하고 테스트하기가 훨씬 쉬워집니다. 리액티브 표현식이나 출력을 볼 때, 코드 블록을 주의 깊게 읽는 것 외에는 어떤 값에 의존하는지 쉽게 알 수 있는 방법이 없습니다. 하지만 함수 정의는 정확히 어떤 입력을 가지는지 알려줍니다. UI에서 함수의 주요 이점은 주로 중복 줄이기에 있습니다. 서버에서 함수의 주요 이점은 주로 격리와 테스트에 있습니다. 업로드된 데이터 읽기 "데이터 업로드"의 이 서버를 살펴봅시다. 중간 정도의 복잡한 reactive()를 포함하고 있습니다: server <- function(input, output, session) { data <- reactive({ req(input$file) ext <- tools::file_ext(input$file$name) switch(ext, csv = vroom::vroom(input$file$datapath, delim = ","), tsv = vroom::vroom(input$file$datapath, delim = "\t"), validate("Invalid file; Please upload a.csv or.tsv file") ) }) output$head <- renderTable({ head(data(), input$n) }) } 이것이 실제 앱이라면, 업로드된 파일을 읽는 전용 함수를 추출하는 것을 진지하게 고려할 것입니다: load_file <- function(name, path) { ext <- tools::file_ext(name) switch(ext, csv = vroom::vroom(path, delim = ","), tsv = vroom::vroom(path, delim = "\t"), validate("Invalid file; Please upload a.csv or.tsv file") ) }

이런 헬퍼를 추출할 때는 리액티브를 입력으로 받거나 출력으로 반환하는 것을 피하세요. 대신 값을 인수로 전달하고, 호출자가 필요한 경우 결과를 리액티브로 변환할 것으로 가정하세요. 이것은 엄격한 규칙은 아닙니다. 때로는 함수가 리액티브를 입력이나 출력으로 하는 것이 합리적일 수 있습니다. 하지만 일반적으로 앱의 리액티브 부분과 비리액티브 부분을 가능한 한 분리하는 것이 좋습니다. 이 경우에도 여전히 validate()를 사용하고 있습니다. Shiny 바깥에서 validate()는 stop()과 유사하게 작동하기 때문입니다. 하지만 req()는 서버에 남겨둡니다. 파일 파싱 코드가 언제 실행되는지 알 책임이 없기 때문입니다. 이제 독립적인 함수가 되었으므로 별도의 파일(예: R/load_file.R)에 넣어 server()를 가볍게 유지할 수 있습니다. 이렇게 하면 서버 함수가 개별 컴포넌트의 세부 사항이 아닌 리액티브의 큰 그림에 집중할 수 있습니다: server <- function(input, output, session) { data <- reactive({ req(input$file) load_file(input$file$name, input$file$datapath) }) output$head <- renderTable({ head(data(), input$n) }) } 다른 큰 장점은 콘솔에서 Shiny 앱 밖에서 load_file()을 시험해볼 수 있다는 것입니다. 앱의 공식적인 테스트(21장 참조)로 나아간다면, 이것도 테스트를 쉽게 만들어줍니다. 내부 함수 대부분의 경우 함수를 서버 함수와 완전히 독립적으로 만들어 별도의 파일에 넣고 싶을 것입니다. 하지만 함수가 input, output, 또는 session을 사용해야 한다면, 함수가 서버 함수 안에 위치하는 것이 합리적일 수 있습니다:

server <- function(input, output, session) { switch_page <- function(i) { updateTabsetPanel(input = "wizard", selected = paste0("page_", i)) } observeEvent(input$page_12, switch_page(2)) observeEvent(input$page_21, switch_page(1)) observeEvent(input$page_23, switch_page(3)) observeEvent(input$page_32, switch_page(2)) } 이것은 테스트나 디버깅을 더 쉽게 만들지 않지만, 중복된 코드는 줄여줍니다. 물론 함수의 인수에 session을 추가할 수도 있습니다: switch_page <- function(i) { updateTabsetPanel(input = "wizard", selected = paste0("page_", i)) } server <- function(input, output, session) { observeEvent(input$page_12, switch_page(2)) observeEvent(input$page_21, switch_page(1)) observeEvent(input$page_23, switch_page(3)) observeEvent(input$page_32, switch_page(2)) } 하지만 이것은 좀 어색합니다. 함수가 여전히 이 앱에 본질적으로 결합되어 있기 때문입니다. "wizard"라는 특정 탭 세트를 가진 컨트롤에만 영향을 미치기 때문입니다. 요약 앱이 커질수록, 앱의 흐름에서 비리액티브 함수를 추출하는 것은 생활을 크게 쉽게 만들어줍니다. 함수를 사용하면 리액티브 코드와 비리액티브 코드를 분리하고 코드를 여러 파일에 분산시킬 수 있습니다. 이렇게 하면 앱의 전체적인 구조를 파악하기가 훨씬 쉬워지며, 복잡한 로직을 앱에서 일반 R

코드로 옮기면 실험, 반복, 테스트가 훨씬 쉬워집니다. 함수를 추출하기 시작하면 처음에는 좀 느리고 답답하게 느껴지겠지만, 시간이 지날수록 빨라질 것이며 곧 도구 상자에서 핵심 도구가 될 것입니다. 이 장의 함수들은 중요한 단점이 하나 있습니다. UI 또는 서버 컴포넌트 중 하나만 생성할 수 있지, 둘 다 생성할 수는 없습니다. 다음 장에서는 UI와 서버 코드를 단일 객체로 조율하는 Shiny 모듈을 만드는 방법을 배울 것입니다. 1 함수의 기본을 모르고 배우고 싶다면, R for Data Science의 Functions 장을 읽어보세요.

19장. Shiny 모듈 지난 장에서는 함수를 사용하여 Shiny 앱의 일부를 독립적인 조각으로 분해했습니다. 함수는 서버 측이든 클라이언트 측이든 완전히 한쪽에 있는 코드에 잘 작동합니다. 양쪽에 걸쳐 있는 코드(즉, 서버 코드가 UI의 특정 구조에 의존하는 경우)에는 새로운 기술이 필요합니다. 바로 모듈입니다. 가장 단순한 수준에서, 모듈은 UI와 서버 함수 한 쌍입니다. 모듈의 마법은 이 함수들이 특별한 방식으로 구성되어 "네임스페이스(namespace)"를 만들기 때문입니다. 지금까지 앱을 작성할 때, 컨트롤의 이름(id)은 전역(global)이었습니다. 서버 함수의 모든 부분이 UI의 모든 부분을 볼 수 있었습니다. 모듈은 모듈 내부에서만 볼 수 있는 컨트롤을 만들 수 있게 해줍니다. 이것이 네임스페이스라고 불리는 이유는 앱의 나머지 부분과 격리된 "이름"의 "공간"을 만들기 때문입니다. Shiny 모듈은 두 가지 큰 장점이 있습니다. 첫째, 네임스페이싱은 앱이 어떻게 작동하는지 이해하기 쉽게 만들어줍니다. 개별 컴포넌트를 격리된 상태에서 작성, 분석, 테스트할 수 있기 때문입니다. 둘째, 모듈은 함수이기 때문에 코드 재사용에 도움이 됩니다. 함수로 할 수 있는 것은 무엇이든 모듈로도 할 수 있습니다. shiny를 로드하면서 시작하겠습니다: library(shiny) 동기 부여 모듈을 만드는 세부 사항으로 들어가기 전에, 모듈이 앱의 "구조"를 어떻게 변경하는지 감을 잡는 것이 유용합니다. Eric Nantz의 예를 빌리겠습니다. 그는 rstudio::conf(2019)에서 모듈에 대해 발표했습니다. Eric은 그림 19-1과 같은 크고 복잡한 앱을 가지고 있었기 때문에 모듈을 사용하게 되었습니다. 이 앱의 구체적인 내용은 모르겠지만, 연결된 컴포넌트가 많다는 것에서 복잡성을 느낄 수 있습니다.

그림 19-1. 복잡한 앱의 개략적인 다이어그램. 가능한 한 단순하게 다이어그램으로 표시하려고 했지만, 모든 조각이 무엇인지 이해하기 어렵습니다. 그림 19-2는 모듈을 사용하여 리라이팅한 후 앱이 어떻게 보이는지 보여줍니다: 앱이 조각으로 나뉘어져 있고, 각 조각에 이름이 있습니다. 조각에 이름을 붙이면 컨트롤의 이름을 더 간단하게 할 수 있습니다. 예를 들어, 이전에는 앱에 "session manage"와 "session activate"가 있었지만, 이제는 "manage"와 "activate"만 필요합니다. 해당 컨트롤들이 session 모듈 안에 포함되어 있기 때문입니다. 이것이 네임스페이싱입니다! 모듈은 정의된 입력과 출력을 가진 블랙박스입니다. 다른 모듈은 모듈의 인터페이스(바깥)를 통해서만 통신할 수 있습니다. 내부의 컨트롤과 리액티브를 직접 검사하거나 수정할 수 없습니다. 이렇게 하면 앱 전체의 더 단순한 구조가 강제됩니다. 모듈은 재사용 가능하므로 함수를 작성하여 노란색과 파란색 컴포넌트를 모두 생성할 수 있습니다. 이렇게 하면 앱의 전체 코드량을 크게 줄일 수 있습니다.

그림 19-2. 모듈을 사용하여 앱을 리라이팅한 후, 앱의 전체적인 컴포넌트를 파악하기가 훨씬 쉬워졌으며, 여러 곳에서 재사용되는 부분(노란색과 파란색 컴포넌트)을 볼 수 있습니다. 모듈 기초 첫 번째 모듈을 만들려면, 히스토그램을 그리는 매우 간단한 앱에서 모듈을 추출하겠습니다: ui <- fluidPage( selectInput("var", "Variable", names(mtcars)), numericInput("bins", "bins", 10, min = 1), plotOutput("hist") ) server <- function(input, output, session) { data <- reactive(mtcars[[input$var]]) output$hist <- renderPlot({ hist(data(), breaks = input$bins, main = input$var) }, res = 96) } 이 앱은 너무 단순해서 모듈을 추출하는 것이 실제로 큰 이점이 없지만, 더 현실적이고 따라서 복잡한 사용 사례로 넘어가기 전에 기본 메커니즘을 설명하는 데 유용할 것입니다. 모듈은 앱과 매우 유사합니다. 앱처럼 두 가지

요소로 구성됩니다: UI 사양을 생성하는 모듈 UI 함수 서버 함수 안에서 코드를 실행하는 모듈 서버 함수 이 두 함수는 표준 형식을 가집니다. 둘 다 id 인수를 받아 모듈의 네임스페이스로 사용합니다. 모듈을 만들려면, 앱의 UI와 서버에서 코드를 추출하여 모듈 UI와 서버에 넣어야 합니다. 모듈 UI 모듈 UI부터 시작하겠습니다. 두 단계가 있습니다:

UI 코드를 id 인수를 가진 함수 안에 넣습니다. 기존 ID 각각을 NS() 호출로 래핑하여 (예를 들어) "var"가 NS(id, "var")가 되도록 합니다. 이렇게 하면 다음 함수가 생성됩니다: histogramUI <- function(id) { tagList( selectInput(NS(id, "var"), "Variable", choices = names(mtcars)), numericInput(NS(id, "bins"), "bins", value = 10, min = 1), plotOutput(NS(id, "hist")) ) } 여기서 UI 컴포넌트를 tagList()로 반환했습니다. tagList는 여러 컴포넌트를 실제로 레이아웃을 지정하지 않고 묶을 수 있게 해주는 특수한 레이아웃 함수 유형입니다. histogramUI()를 호출하는 사람이 column()이나 fluidRow() 같은 레이아웃 함수로 자신의 필요에 따라 결과를 감싸는 것은 호출자의 책임입니다. 모듈 서버 다음으로 서버 함수를 다루겠습니다. 이것은 또 다른 함수 안에 래핑되며, 반드시 id 인수를 가져야 합니다. 이 함수는 id와 일반 서버 함수와 같은 함수를 moduleServer()에 전달합니다: histogramServer <- function(id) { moduleServer(id, function(input, output, session) { data <- reactive(mtcars[[input$var]]) output$hist <- renderPlot({ hist(data(), breaks = input$bins, main = input$var) }, res = 96) }) }

여기서 두 계층의 함수가 중요합니다. 나중에 다시 다루겠지만, 요약하면 모듈의 인수와 서버 함수의 인수를 구별하는 데 도움이 됩니다. 이것이 매우 복잡해 보여도 걱정하지 마세요. 기본적으로 새로 만드는 각 모듈에 대해 복사-붙여넣기 할 수 있는 보일러플레이트입니다. moduleServer()가 네임스페이싱을 자동으로 처리한다는 점에 주목하세요: moduleServer(id) 안에서 input$var와 input$bins는 NS(id, "var")와 NS(id, "bins")라는 이름을 가진 입력을 참조합니다. 업데이트된 앱 이제 UI와 서버 함수를 만들었으니, 실험과 테스트에 사용할 앱을 생성하는 함수를 작성하는 것이 좋은 관례입니다: histogramApp <- function() { ui <- fluidPage( histogramUI("hist1") ) server <- function(input, output, session) { histogramServer("hist1") } shinyApp(ui, server) } 모든 Shiny 컨트롤과 마찬가지로, UI와 서버 모두에서 동일한 id를 사용해야 합니다. 그렇지 않으면 두 조각이 연결되지 않습니다.

모듈의 역사 모듈은 Shiny 0.13(2016년 1월)에서 callModule()과 함께 도입되었고, Shiny 1.5.0(2020년 6월)에서 moduleServer()의 도입과 함께 개편되었습니다. 모듈을 이전에 배웠다면, callModule()을 배웠을 수 있고 moduleServer()가 무슨 이유인지 궁금할 수 있습니다. 이 두 함수는 첫 번째 두 인수가 뒤바뀌었다는 것 외에는 동일합니다. 이 단순한 변경이 앱 전체의 상당히 다른 구조로 이어집니다: histogramServerOld <- function(input, output, session) { data <- reactive(mtcars[[input$var]]) output$hist <- renderPlot({ hist(data(), breaks = input$bins, main = input$var) }, res = 96) } server <- function(input, output, session) { callModule(histogramServerOld, "hist1") } 이 차이는 이 단순한 앱에서는 크게 표면적이지만, moduleServer()는 인수가 있는 더 복잡한 모듈을 이해하기 훨씬 쉽게 만듭니다. 네임스페이싱 이제 완전한 앱을 만들었으니, 네임스페이싱에 대해 좀 더 논의하겠습니다. 모듈을 작동하게 만드는 핵심 아이디어는 각 컨트롤의 이름(즉, id)이 이제 두 조각으로 결정된다는 것입니다: 첫 번째 조각은 histogramServer()를 호출하는 모듈 사용자, 즉 개발자에게서 옵니다. 두 번째 조각은 histogramServer()를 작성한 모듈 저자, 즉 개발자에게서 옵니다.

이 두 부분 사양은 모듈 저자인 여러분이 사용자가 만든 다른 UI 컴포넌트와 충돌하는 것에 대해 걱정할 필요가 없다는 뜻입니다. 여러분만의 고유한 "이름 공간"을 가지며 자신의 필요를 가장 잘 충족시키도록 배치할 수 있습니다. 네임스페이싱은 모듈을 블랙박스로 만듭니다. 모듈 바깥에서는 내부의 입력, 출력, 또는 리액티브를 볼 수 없습니다. 예를 들어, 다음 앱을 살펴봅시다. 텍스트 출력 output$out는 input$bins가 없기 때문에 업데이트되지 않습니다. bins 입력은 hist1 모듈 안에서만 볼 수 있습니다: ui <- fluidPage( histogramUI("hist1"), textOutput("out") ) server <- function(input, output, session) { histogramServer("hist1") output$out <- renderText(paste0("Bins: ", input$bins)) } 앱의 다른 곳에 있는 리액티브에서 입력을 가져오려면, 모듈 함수에 명시적으로 전달해야 합니다. 곧 다시 다루겠습니다. 모듈 UI와 서버가 네임스페이싱 표현 방식이 다르다는 점에 주목하세요: 모듈 UI에서는 네임스페이싱이 명시적입니다. 입력이나 출력을 만들 때마다 NS(id, "name")을 호출해야 합니다. 모듈 서버에서는 네임스페이싱이 암시적입니다. moduleServer() 호출에서 id만 사용하면 됩니다. Shiny가 input과 output의 네임스페이싱을 자동으로 처리하여, 모듈 코드에서 input$name은 NS(id, "name")이라는 이름의 입력을 의미합니다. 명명 규칙 이 예제에서는 모듈의 모든 컴포넌트에 특별한 명명 체계를 사용했으며, 여러분도自己的 모듈에 사용하는 것을 추천합니다.

여기서 모듈은 히스토그램을 그리므로 histogram 모듈이라고 불렀습니다. 이 기본 이름은 다양한 곳에서 사용됩니다: R/histogram.R에는 모듈의 모든 코드가 들어갑니다. histogramUI()는 모듈 UI입니다. 주로 입력이나 출력에 사용된다면 histogramInput()이나 histogramOutput()이라고 부를 수 있습니다. histogramServer()는 모듈 서버입니다. histogramApp()은 인터랙티브 실험과 더 공식적인 테스트를 위한 완전한 앱을 만듭니다. 연습 문제
1. 모듈을 R/의 별도 파일에 넣는 것이 왜 좋은 관례인가요? Shiny 앱에서 로드되도록 하려면 어떻게 해야 하나요?
2. 다음 모듈 UI에는 치명적인 실수가 있습니다. 그것이 무엇이며, 왜 문제를 일으키나요? histogramUI <- function(id) { tagList( selectInput("var", "Variable", choices = names(mtcars)), numericInput("bins", "bins", value = 10, min = 1), plotOutput("hist") ) }
3. 다음 모듈은 go를 클릭할 때마다 새로운 난수를 생성합니다:

randomUI <- function(id) { tagList( textOutput(NS(id, "val")), actionButton(NS(id, "go"), "Go!") ) } randomServer <- function(id) { moduleServer(id, function(input, output, session) { rand <- eventReactive(input$go, sample(100, 1)) output$val <- renderText(rand()) }) } 이 모듈의 사본 네 개를 단일 페이지에 표시하는 앱을 만드세요. 각 모듈이 독립적임을 확인하세요. randomUI()의 반환 값을 어떻게 변경하면 표시를 더 매력적으로 만들 수 있을까요?
4. 모듈 보일러플레이트를 타이핑하는 것에 이미 지쳤나요? RStudio 스니펫에 대해 읽고 다음 스니펫을 RStudio 설정에 추가하여 새로운 모듈을 더 쉽게 만드세요: ${1}UI <- function(id) { tagList( ${2} ) } ${1}Server <- function(id) { moduleServer(id, function(input, output, session) { ${3} } }

입력과 출력 때때로 모듈 UI와 서버에 id 인수만 있는 모듈이 유용할 수 있습니다. 복잡한 코드를 자체 파일에 격리할 수 있게 해주기 때문입니다. 이것은 각 탭이 각 사업 라인에 맞춤 보고서를 표시하는 기업 대시보드와 같은 독립적인 컴포넌트를 수집하는 앱에 특히 유용합니다. 여기서 모듈은 각 조각을 자체 파일에서 개발할 수 있게 해주며, 컴포넌트 간 ID 충돌에 대해 걱정할 필요가 없습니다. 하지만 대부분의 경우 모듈 UI와 서버에 추가 인수가 필요할 것입니다. 모듈 UI에 인수를 추가하면 모듈 모양을 더 세밀하게 제어할 수 있어, 앱의 더 많은 곳에서 동일한 모듈을 사용할 수 있습니다. 하지만 모듈 UI는 일반적인 R 함수이므로, Shiny에 특화된 것은 비교적 적고, 많은 것이 이미 18장에서 다루어졌습니다. 따라서 다음 섹션에서는 모듈 서버에 초점을 맞추고 모듈이 추가 리액티브 입력을 어떻게 받고 하나 이상의 리액티브 출력을 어떻게 반환할 수 있는지 논의하겠습니다. 일반적인 Shiny 코드와 달리, 모듈을 함께 연결하려면 입력과 출력에 대해 명시적이어야 합니다. 처음에는 지루하게 느껴질 것입니다. 그리고 확실히 Shiny의 일반적인 자유형식 연결보다 더 많은 작업입니다. 하지만 모듈은 특정 통신 라인을 강제합니다. 만들어지는 데 약간 더 많은 작업이 필요하지만, 이해하기 훨씬 쉬우며 훨씬 더 복잡한 앱을 구축할 수 있게 해줍니다. 세션$userData나 다른 기술을 사용하여 모듈의 구속에서 벗어나라는 조언을 볼 수 있습니다. 이런 조언에 주의하세요: 네임스페이싱이 부과하는 규칙을 우회하는 방법을 보여주어, 앱에 많은 복잡성을 다시 쉽게 도입하고 모듈 사용의 이점을 크게 떨어뜨립니다. 시작하기: UI 입력과 서버 출력 입력과 출력이 어떻게 작동하는지 보려면, datasets 패키지에서 제공하는 내장 데이터에서 사용자가 데이터 세트를 선택할 수 있는 모듈로 간단히 시작하겠습니다. 이 자체로는별로 유용하지 않지만, 몇 가지 기본 원칙을 설명하며, 이전에 "UI 컨트롤 추가"에서 보았듯이 더 복잡한 모듈에 유용한 빌딩 블록입니다. 모듈 UI부터 시작하겠습니다. 여기서 추가 인수를 하나 사용하여, 옵션을 데이터 프레임(is.data.frame)이거나 행렬(is.matrix)인 내장 데이터 세트로 제한할 수 있습니다. 이 인수를 사용하여 datasets 패키지에서 발견된 객체를 선택적으로 필터링한 다음 selectInput()을 만듭니다: datasetInput <- function(id, filter = NULL) { names <- ls("package:datasets") if (!is.null(filter)) { data <- lapply(names, get, "package:datasets") names <- names[vapply(data, filter, logical(1))] } selectInput(NS(id, "dataset"), "Pick a dataset", choices = names) } 모듈 서버도 간단합니다: 이름으로 데이터 세트를 가져오기 위해 get()을 사용합니다. 여기서 새로운 아이디어가 하나 있습니다: 함수와 마찬가지로, 일반 server()와는 달리, 이 모듈 서버는 값을 반환합니다. 여기서 함수에서 처리되는 마지막 표현식이 반환 값이 된다는 일반적인 규칙을 활용합니다.2 이 값은 항상 리액티브여야 합니다: datasetServer <- function(id) { moduleServer(id, function(input, output, session) { reactive(get(input$dataset, "package:datasets")) }) } 반환 값을 가지는 모듈 서버를 사용하려면, <-로 반환 값을 캡처하기만 하면 됩니다. 다음 모듈 앱에서 시연됩니다. 데이터 세트를 캡처한 다음 tableOutput()으로 표시합니다: datasetApp <- function(filter = NULL) { ui <- fluidPage(

datasetInput("dataset", filter = filter), tableOutput("data") ) server <- function(input, output, session) { data <- datasetServer("dataset") output$data <- renderTable(head(data())) } shinyApp(ui, server) } 이 함수를 설계할 때 몇 가지 결정을 내렸습니다: 모듈 UI에 전달되는 filter 인수를 받아 해당 입력 인수를 실험하기 쉽게 만듭니다. 모든 데이터를 표시하기 위해 테이블 출력을 사용합니다. 여기서 무엇을 사용하든 별로 중요하지 않지만, UI가 더 표현적일수록 모듈이 예상대로 작동하는지 확인하기 쉽습니다. 사례 연구: 수치형 변수 선택 다음으로, 지정된 유형의 변수를 리액티브 데이터 세트에서 선택할 수 있는 컨트롤을 만들겠습니다. 데이터 세트가 리액티브이기를 원하기 때문에, 앱을 시작할 때 선택 항목을 채울 수 없습니다. 이것이 모듈 UI를 매우 단순하게 만듭니다: selectVarInput <- function(id) { selectInput(NS(id, "var"), "Variable", choices = NULL) } 서버 함수는 두 개의 인수를 가집니다: 변수를 선택할 데이터. 이것이 리액티브이기를 원합니다. 이전에 만든 데이터 세트 모듈과 작동할 수 있도록 하기 위해서입니다. 어떤 변수를 나열할지 선택하는 데 사용되는 필터. 이것은 모듈의 호출자가 설정하므로 리액티브일 필요가 없습니다. 모듈 서버를 단순하게 유지하기 위해, 핵심 아이디어를 헬퍼 함수로 추출했습니다:

find_vars <- function(data, filter) { names(data)[vapply(data, filter, logical(1))] } 그런 다음 모듈 서버는 observeEvent()를 사용하여 데이터가 변경될 때 selectInput 선택 항목을 업데이트하고, 선택된 변수의 값을 제공하는 리액티브를 반환합니다: selectVarServer <- function(id, data, filter = is.numeric) { moduleServer(id, function(input, output, session) { observeEvent(data(), { updateSelectInput(session, "var", choices = find_vars(data(), filter)) }) reactive(data()[[input$var]]) }) } 앱을 만들기 위해, 다시 모듈 서버의 결과를 캡처하여 UI의 출력에 연결합니다. 모든 리액티브 배관이 올바른지 확인하고 싶으므로, 리액티브 데이터 프레임의 소스로 데이터 세트 모듈을 사용합니다: selectVarApp <- function(filter = is.numeric) { ui <- fluidPage( datasetInput("data", is.data.frame), selectVarInput("var"), verbatimTextOutput("out") ) server <- function(input, output, session) { data <- datasetServer("data") var <- selectVarServer("var", data, filter = filter) output$out <- renderPrint(var()) } shinyApp(ui, server) } 서버 입력

모듈 서버를 설계할 때, 각 인수에 값을 제공할 사람이 누구인지 생각해야 합니다: 모듈을 호출하는 R 프로그래머입니까, 아니면 앱을 사용하는 사람입니까? 이에 대해 생각하는 또 다른 방법은 값이 언제 변경될 수 있는지입니다: 앱의 수명 동안 고정되고 일정합니까, 아니면 리액티브로서 사용자가 앱과 상호작용하면서 변경됩니까? 이것은 인수가 리액티브인지 아닌지를 결정하는 중요한 설계 결정입니다. 이 결정을 내린 후에는 모듈의 각 입력이 리액티브인지 상수인지 확인하는 것이 좋은 관례라고 생각합니다. 그렇게 하지 않으면 사용자가 잘못된 유형을 제공했을 때 이해하기 어려운 오류 메시지를 받게 됩니다. 빠르고 더러운 stopifnot() 호출로 모듈 사용자의 삶을 훨씬 쉽게 만들 수 있습니다. 예를 들어, selectVarServer()는 다음 코드로 data가 리액티브이고 filter가 아닌지 확인할 수 있습니다: selectVarServer <- function(id, data, filter = is.numeric) { stopifnot(is.reactive(data)) stopifnot(!is.reactive(filter)) moduleServer(id, function(input, output, session) { observeEvent(data(), { updateSelectInput(session, "var", choices = find_vars(data(), filter)) }) reactive(data()[[input$var]]) }) } 모듈이 많은 사람들에 의해 여러 번 사용될 것으로 예상된다면, if 문과 stop() 호출을 사용하여 수제 오류 메시지를 만드는 것도 고려할 수 있습니다. 모듈 입력이 리액티브인지(아닌지) 확인하면 모듈을 다른 입력 컨트롤과 혼합할 때 흔히 발생하는 문제를 피할 수 있습니다. input$var는 리액티브가 아니므로, 입력 값을 모듈에 전달할 때마다 reactive()로 감싸야 합니다(예: selectVarServer("var", reactive(input$x))). 여기서 추천하는 것처럼 입력을 확인하면 명확한 오류를 받게 됩니다. 그렇지 않으면 could not find function "data"와 같은难以解한 오류가 발생합니다. 참고 이 전략을 find_vars()에도 적용할 수 있습니다. 여기서는 그다지 중요하지 않지만, Shiny 앱 디버깅이 일반 R 코드 디버깅보다 약간 더 어렵기 때문에, 입력 확인에 조금 더 시간을 투자하여 문제가 발생했을 때 더 명확한 오류 메시지를 얻는 것이 의미가 있다고 생각합니다: find_vars <- function(data, filter) { stopifnot(is.data.frame(data)) stopifnot(is.function(filter)) names(data)[vapply(data, filter, logical(1))] } 이것은 이 장에서 작업하는 동안 내가 범한 몇 가지 오류를 잡아냈습니다. 모듈 안의 모듈 서버 함수의 출력에 대해 더 논의하기 전에, 모듈이 합성 가능하다는 것을 강조하고 싶습니다. 자체적으로 모듈을 포함하는 모듈을 만드는 것이 합리적일 수 있습니다. 예를 들어, 데이터 세트와 selectVar 모듈을 결합하여 사용자가 내장 데이터 세트에서 변수를 선택할 수 있는 모듈을 만들 수 있습니다: selectDataVarUI <- function(id) { tagList( datasetInput(NS(id, "data"), filter = is.data.frame), selectVarInput(NS(id, "var")) ) } selectDataVarServer <- function(id, filter = is.numeric) { moduleServer(id, function(input, output, session) { data <- datasetServer("data") var <- selectVarServer("var", data, filter = filter) var }) } selectDataVarApp <- function(filter = is.numeric) {

ui <- fluidPage( sidebarLayout( sidebarPanel(selectDataVarUI("var")), mainPanel(verbatimTextOutput("out")) ) ) server <- function(input, output, session) { var <- selectDataVarServer("var", filter) output$out <- renderPrint(var(), width = 40) } shinyApp(ui, server) } 사례 연구: 히스토그램 이제 원래 히스토그램 모듈로 돌아가서 더 합성 가능한 것으로 리팩터링하겠습니다. 모듈을 만드는 핵심 과제는 여러 곳에서 사용할 수 있을 정도로 유연하면서도 쉽게 이해할 수 있을 정도로 단순한 함수를 만드는 것입니다. 좋은 빌딩 블록이 되는 함수를 작성하는 방법을 알아내는 것은 평생의 여정입니다. 올바르게 만들기 전에 몇 번은 잘못할 것을 예상하세요. (여기서 더 구체적인 조언을 제공하고 싶지만, 현재 이 기술은 연습과 의식적인 성찰을 통해 다듬어야 합니다.) 또한 이것은 출력 컨트롤로 간주하겠습니다. 입력(빈의 수)을 사용하지만, 표시를 조정하는 데만 사용되고 모듈에 의해 반환될 필요가 없기 때문입니다: histogramOutput <- function(id) { tagList( numericInput(NS(id, "bins"), "bins", 10, min = 1, step = 1), plotOutput(NS(id, "hist")) ) } 이 모듈에 두 개의 입력을 주기로 결정했습니다: 플롯할 변수 x와 히스토그램 제목. 둘 다 시간이 지나면서 변경될 수 있도록 리액티브입니다. (제목은 좀 경미하지만, 곧 중요한 기술을 동기 부여할 것입니다.) 제목의 기본값에 주목하세요. 리액티브여야 하므로, 상수 값을 reactive() 안에 감싸야 합니다: histogramServer <- function(id, x, title = reactive("Histogram")) { stopifnot(is.reactive(x)) stopifnot(is.reactive(title)) moduleServer(id, function(input, output, session) { output$hist <- renderPlot({ req(is.numeric(x())) main <- paste0(title(), " [", input$bins, "]") hist(x(), breaks = input$bins, main = main) }, res = 96) }) } histogramApp <- function() { ui <- fluidPage( sidebarLayout( sidebarPanel( datasetInput("data", is.data.frame), selectVarInput("var"), ), mainPanel( histogramOutput("hist") ) ) ) server <- function(input, output, session) { data <- datasetServer("data") x <- selectVarServer("var", data) histogramServer("hist", x) } shinyApp(ui, server) }

# histogramApp()

참고 모듈 사용자가 빈 컨트롤과 히스토그램을 앱의 서로 다른 곳에 배치할 수 있게 하려면 여러 UI 함수를 사용할 수 있습니다. 여기서는별로 유용하지 않지만, 기본 접근 방식을 이해하기에 유용합니다: histogramOutputBins <- function(id) { numericInput(NS(id, "bins"), "bins", 10, min = 1, step = 1) } histogramOutputPlot <- function(id) { plotOutput(NS(id, "hist")) } ui <- fluidPage( sidebarLayout( sidebarPanel( datasetInput("data", is.data.frame), selectVarInput("var"), histogramOutputBins("hist") ), mainPanel( histogramOutputPlot("hist") ) ) ) 여러 출력 선택된 변수의 이름을 히스토그램 제목에 포함할 수 있다면 좋을 것입니다. 현재 selectVarServer()는 변수의 값만 반환하고 이름은 반환하지 않으므로这样做할 방법이 없습니다. selectVarServer()를 이름을 반환하도록 다시 작성할 수는 있지만, 그러면 모듈 사용자가 서브세팅을해야 합니다. 더 좋은 접근 방식은 selectVarServer()가 이름과 값을 모두 반환하는 것입니다. 서버 함수는 어떤 R 함수든 여러 값을 반환할 수 있는 것과 정확히 같은 방식으로 여러 값을 반환할 수 있습니다: 목록을 반환하여. 여기서 selectVarServer()를 이름과 값을 모두 리액티브로 반환하도록 수정합니다: selectVarServer <- function(id, data, filter = is.numeric) { stopifnot(is.reactive(data))

stopifnot(!is.reactive(filter)) moduleServer(id, function(input, output, session) { observeEvent(data(), { updateSelectInput(session, "var", choices = find_vars(data(), filter)) }) list( name = reactive(input$var), value = reactive(data()[[input$var]]) ) }) } 이제 histogramApp()을 업데이트하여 이것을 활용할 수 있습니다. UI는 동일하게 유지되지만, 이제 선택된 변수의 값과 이름을 모두 histogramServer()에 전달합니다: histogramApp <- function() { ui <- fluidPage(...) server <- function(input, output, session) { data <- datasetServer("data") x <- selectVarServer("var", data) histogramServer("hist", x$value, x$name) } shinyApp(ui, server) } 이런 종류의 코드의 주요 과제는 리액티브를 사용할 때(예: x$value) 대신 값을 사용할 때(예: x$value())를 기억하는 것입니다. 모듈에 인수를 전달할 때는 값의 변경에 반응하기를 원하므로, 리액티브를 전달해야지 현재 값을 전달하면 안 된다는 점만 기억하세요. 리액티브에서 여러 값을 자주 반환하는 경우 zeallot 패키지를 사용하는 것도 고려할 수 있습니다. zeallot은 여러 변수에 대입할 수 있게 해주는 %<- % 연산자를 제공합니다(때로는 다중, 언패킹, 또는 디스트럭처링 대입이라고 함). 이것은 여러 값을 반환할 때 유용할 수 있습니다. 간접 계층을 피할 수 있기 때문입니다:

library(zeallot) histogramApp <- function() { ui <- fluidPage(...) server <- function(input, output, session) { data <- datasetServer("data") c(value, name) %<-% selectVarServer("var", data) histogramServer("hist", value, name) } shinyApp(ui, server) } 연습 문제
1. data와 filter가 모두 리액티브가 되도록 selectVarServer()를 다시 작성하세요. 그런 다음 데이터 세트 모듈로 데이터 세트를 선택하고 inputSelect()로 필터링 함수를 사용하는 앱 함수와 함께 사용하세요. 사용자가 수치형, 문자형, 또는 팩터 변수를 필터링할 수 있게 해주세요.
2. 다음 코드는 수치형 입력을 받아 세 가지 요약 통계의 글머리 목록을 생성하는 모듈의 출력과 서버 컴포넌트를 정의합니다. 이를 실험해볼 수 있는 앱 함수를 만드세요. 앱 함수는 데이터 프레임을 입력으로 받아 numericVarSelectInput()을 사용하여 요약할 변수를 선택해야 합니다: summaryOutput <- function(id) { tags$ul( tags$li("Min: ", textOutput(NS(id, "min"), inline = TRUE)), tags$li("Max: ", textOutput(NS(id, "max"), inline = TRUE)), tags$li("Missing: ", textOutput(NS(id, "n_na"), inline = TRUE)) ) }

summaryServer <- function(id, var) { moduleServer(id, function(input, output, session) { rng <- reactive({ req(var()) range(var(), na.rm = TRUE) }) output$min <- renderText(rng()[[1]]) output$max <- renderText(rng()[[2]]) output$n_na <- renderText(sum(is.na(var()))) }) }
3. 다음 모듈 입력은 ISO8601 형식(yyyy-mm-dd)으로 날짜를 타이핑할 수 있는 텍스트 컨트롤을 제공합니다. 입력된 값이 유효한 날짜가 아닌 경우 output$error를 사용하여 메시지를 표시하는 서버 함수를 제공하여 모듈을 완성하세요. 모듈은 유효한 날짜에 대해 Date 객체를 반환해야 합니다. (힌트: 문자열을 파싱하려면 strptime(x, "%Y-%m-%d")를 사용하세요. 값이 유효한 날짜가 아니면 NA를 반환합니다.) ymdDateUI <- function(id, label) { label <- paste0(label, " (yyyy-mm-dd)") fluidRow( textInput(NS(id, "date"), label), textOutput(NS(id, "error")) ) } 사례 연구 지금까지 배운 것을 요약하면:

모듈 입력(즉, 모듈 서버에 대한 추가 인수)은 리액티브이거나 상수일 수 있습니다. 선택은 누가 인수를 설정하고 언제 변경되는지에 기반한 설계 결정입니다. 항상 인수가 예상된 유형인지 확인하여 도움이 되지 않는 오류 메시지를 피해야 합니다. 앱 서버와는 달리, 일반 함수와 마찬가지로 모듈 서버는 값을 반환할 수 있습니다. 모듈의 반환 값은 항상 리액티브여야 하며, 여러 값을 반환하려면 리액티브의 목록이어야 합니다. 이 아이디어들이 익숙해지도록, 모듈 사용의 몇 가지 예를 보여주는 몇 가지 사례 연구를 제시하겠습니다. 안타깝게도 모듈을 사용하여 앱을 단순화할 수 있는 모든 가능한 방법을 보여드릴 공간은 없지만, these 예제들이 무엇을 할 수 있는지에 대한 약간의 감각을 주고 향후 고려할 방향을 제안하기를 바랍니다. 제한된 선택 및 기타 모듈의 또 다른 중요한 용도는 복잡한 UI 요소에 더 단순한 사용자 인터페이스를 제공하는 것입니다. 여기서는 Shiny가 기본으로 제공하지 않는 유용한 컨트롤을 만들겠습니다: 라디오 버튼으로 표시되는 작은 옵션 세트와 "기타" 필드를 결합한 것입니다. 이 모듈 내부는 여러 입력 요소를 사용하지만, 외부에서는 단일 결합된 객체로 작동합니다. UI 측을 label, choices, 그리고 selected로 매개변수화하여 radioButtons()에 직접 전달합니다.또한 기본값이 "Other"인 플레이스홀더를 포함하는 textInput()을 만듭니다. 텍스트 상자와 라디오 버튼을 결합하기 위해, choiceNames이 다른 입력 위젯을 포함하는 HTML 요소 목록이 될 수 있다는 사실을 활용합니다. 그림 19-3은 이것이 어떻게 보이는지 보여줍니다: radioExtraUI <- function(id, label, choices, selected = NULL, placeholder = "Other") { other <- textInput(NS(id, "other"), label = NULL, placeholder = placeholder) names <- if (is.null(names(choices))) choices else names(choices)

values <- unname(choices) radioButtons(NS(id, "primary"), label = label, choiceValues = c(names, "other"), choiceNames = c(as.list(values), list(other)), selected = selected ) } 그림 19-3. 보통 CSV 파일을 읽는 방법을 알아보기 위해 radioExtraUI()를 사용하는 예시. 서버에서는 플레이스홀더 값을 수정하면 자동으로 "기타" 라디오 버튼을 선택하도록 하려고 합니다. 기타가 선택된 경우 일부 텍스트가 존재하는지 확인하기 위해 유효성 검사를 사용하는 것도 상상할 수 있습니다: radioExtraServer <- function(id) { moduleServer(id, function(input, output, session) { observeEvent(input$other, ignoreInit = TRUE, { updateRadioButtons(session, "primary", selected = "other") }) reactive({ if (input$primary == "other") { input$other } else { input$primary } }) }) } 그런 다음 앱 함수로 두 조각을 모두 감싸서 테스트할 수 있습니다. 여기서는 radioExtraUI()에任意大数의 인수를 전달하기 위해 ...를 사용합니다:

radioExtraApp <- function(...) { ui <- fluidPage( radioExtraUI("extra",...), textOutput("value") ) server <- function(input, output, server) { extra <- radioExtraServer("extra") output$value <- renderText(paste0("Selected: ", extra())) } shinyApp(ui, server) } 그림 19-4는 이것이 어떻게 작동하는지 보여줍니다: 그림 19-4. CSV를 읽는 방법에 대한 동일한 질문으로 radioExtraApp()을 테스트합니다. 이제 other 필드에 무언가를 타이핑하면 해당 라디오 버튼이 자동으로 선택됩니다. 이 모듈을 더 구체적인 목적으로 계속 감쌀 수 있습니다. 예를 들어, 약간의 주의가 필요한 변수가 gender입니다. 사람들의 성별을 표현하는 방법이 많기 때문입니다: genderUI <- function(id, label = "Gender") { radioExtraUI(id, label = label, choices = c( male = "Male", female = "Female", na = "Prefer not to say" ), placeholder = "Self-described", selected = "na" ) }

여기서는 가장 일반적인 선택지인 male과 female을 제공하는 것이 중요합니다. 해당 데이터를 제공하지 않을 옵션, 그리고 사람들이 가장 편안하게 사용하는 모든 용어를 사용할 수 있는 직접 입력 옵션을 제공합니다. 여기서 "Other" 플레이스홀더를 사용하지 않는 것은 배려입니다. 이 문제에 대한 더 깊은 이해와 성별을 묻는 많은 일반적인 방법이 왜 어떤 사람들에게 해가 될 수 있는지에 대한 논의를 위해, Sabrina Fonseca의 "Designing Forms for Gender Diversity and Inclusion"이나 호주 통계국의 "Standard for Sex, Gender, Variations of Sex Characteristics and Sexual Orientation Variables" 읽기를 추천합니다. 위자드 다음으로 네임스페이싱의 미묘한 점을 파고드는 두 가지 사례 연구를 다루겠습니다. 여기서 UI는 서로 다른 사람이 서로 다른 시점에 생성합니다. 이러한 상황은 네임스페이싱이 어떻게 작동하는지 세부 사항을 기억해야 하기 때문에 복잡합니다. 위자드 인터페이스를 감싸는 모듈로 시작하겠습니다. 위자드 인터페이스는 복잡한 과정을 사용자가 하나씩 작업하는 일련의 간단한 페이지로 나누는 UI 스타일입니다. "위자드 인터페이스"에서 기본 위자드를 만드는 방법을 보여주었습니다. 이제 위자드를 만들 때 각 페이지의 내용에 집중할 수 있도록 프로세스를 자동화하여 전체를 형성하기 위해 어떻게 연결되는지에 신경 쓰지 않도록 하겠습니다. 이 모듈을 설명하기 위해, 아래에서부터 시작하여 위로 올라가겠습니다. 위자드 UI의 주요 부분은 버튼입니다. 각 페이지에는 두 개의 버튼이 있습니다: 다음 페이지로 이동하는 버튼과 이전 페이지로 돌아가는 버튼입니다. 먼저 이러한 버튼을 만드는 헬퍼를 만들겠습니다: nextPage <- function(id, i) { actionButton(NS(id, paste0("go_", i, "_", i + 1)), "next") } prevPage <- function(id, i) { actionButton(NS(id, paste0("go_", i, "_", i - 1)), "prev") }

여기서의 유일한 실제 복잡성은 id입니다: 각 입력 요소에 고유한 ID가 필요하므로, 각 버튼의 ID는 현재 페이지와 대상 페이지를 모두 포함해야 합니다. 다음으로 위자드의 페이지를 생성하는 함수를 작성합니다. 여기에는 "제목"(표시되지는 않지만 전환을 위해 페이지를 식별하는 데 사용됨), 페이지 내용(사용자가 제공), 그리고 두 버튼이 포함됩니다:3 wrapPage <- function(title, page, button_left = NULL, button_right = NULL) { tabPanel( title = title, fluidRow( column(12, page) ), fluidRow( column(6, button_left), column(6, button_right) ) ) } 그런 다음 이를 모두 결합하여 전체 위자드를 생성할 수 있습니다(그림 19-5). 사용자가 제공한 페이지 목록을 반복하고, 버튼을 만들고, 사용자가 제공한 페이지를 tabPanel로 감싼 다음, 모든 패널을 tabsetPanel로 결합합니다. 버튼에 두 가지 특수한 경우가 있다는 점에 주목하세요: 첫 번째 페이지에는 이전 버튼이 없습니다. 여기서는 조건이 FALSE이고 else 블록이 없으면 NULL을 반환하는 트릭을 사용합니다. 마지막 페이지는 사용자가 제공한 입력 컨트롤을 사용합니다. 위자드가 끝난 후 어떻게 할지 사용자가 제어할 수 있게 하는 가장 간단한 방법이라고 생각합니다. wizardUI <- function(id, pages, doneButton = NULL) { stopifnot(is.list(pages)) n <- length(pages) wrapped <- vector("list", n) for (i in seq_along(pages)) {

# 첫 번째 페이지는 다음만; 마지막 페이지는 이전 + 완료
lhs <- if (i > 1) prevPage(id, i) rhs <- if (i < n) nextPage(id, i) else doneButton wrapped[[i]] <- wrapPage(paste0("page_", i), pages[[i]], lhs, rhs) }
# tabsetPanel 생성
# https://github.com/rstudio/shiny/issues/2927
wrapped$id <- NS(id, "wizard") wrapped$type <- "hidden" do.call("tabsetPanel", wrapped) } 그림 19-5. 위자드 UI의 간단한 예시. tabsetPanel 코드를 만드는 데 약간의 설명이 필요합니다: 안타깝게도 tabsetPanel()은 탭 목록을 전달할 수 있게 해주지 않습니다. 따라서 작동시키려면 약간의 do.call() 마법을 사용해야 합니다. do.call(function_name, list(arg1, arg2, …)는 function_name(arg1, arg2, …)와 동일하므로, 여기서는 tabsetPanel(pages[[1]], pages[[2]], …,

id = NS(id, "wizard"), type = "hidden")와 같은 호출을 만드는 것입니다. Shiny의 향후 버전에서 이것이 단순화되기를 바랍니다. 모듈 UI를 완성했으니, 이제 모듈 서버에 관심을 돌려야 합니다. 서버의 본질은 간단합니다: 버튼이 작동하여 어느 방향으로든 페이지 간에 이동할 수 있도록 해야 합니다. 그렇게 하려면 updateTabsetPanel()을 호출하는 각 버튼에 대한 observeEvent()를 설정해야 합니다. 정확히 몇 페이지가 있는지 알면 비교적 단순할 것입니다. 하지만 모듈 사용자가 그것을 제어할 수 있으므로 알 수 없습니다. 따라서 대신 함수형 프로그래밍을 사용하여 (n - 1) * 2개의 옵저버를 설정해야 합니다(첫 번째와 마지막을 제외한 각 페이지에 두 개의 옵저버, 각각 하나만 필요). 다음 서버 함수는 먼저 changePage() 함수에서 하나의 버튼에 필요한 기본 코드를 추출합니다. "여러 컨트롤"에서처럼 input[[]]를 사용하여 동적으로 컨트롤을 참조할 수 있습니다. 그런 다음 lapply()를 사용하여 모든 이전 버튼(첫 번째를 제외한 모든 페이지에 필요)과 모든 다음 버튼(마지막을 제외한 모든 페이지에 필요)을 반복합니다: wizardServer <- function(id, n) { moduleServer(id, function(input, output, session) { changePage <- function(from, to) { observeEvent(input[[paste0("go_", from, "_", to)]], { updateTabsetPanel(session, "wizard", selected = paste0("page_", to)) }) } ids <- seq_len(n) lapply(ids[-1], function(i) changePage(i, i - 1)) lapply(ids[-n], function(i) changePage(i, i + 1)) }) } 여기서 map()/lapply() 대신 for 루프를 사용하는 것은 불가능하다는 점에 주목하세요. for 루프는 같은 i 변수의 값을 변경하여 작동하므로 루프가 완료될 때까지 모든 changePage()가 같은

값을 사용하게 됩니다. map()과 lapply는 각각 고유한 i 값을 가진 새로운 환경을 만들어 작동합니다. 이제 앱과 간단한 예시를 구성하여 모든 것이 올바르게 연결되었는지 확인할 수 있습니다: wizardApp <- function(...) { pages <- list(...) ui <- fluidPage( wizardUI("whiz", pages) ) server <- function(input, output, session) { wizardServer("whiz", length(pages)) } shinyApp(ui, server) } 안타깝게도 모듈을 사용할 때 약간 반복해야 하며, wizardServer()의 n 인수와 wizardUI()의 pages 인수가 일치하는지 확인해야 합니다. 이것은 모듈 시스템의 원칙적인 한계이며, "단일 객체 모듈"에서 더 자세히 논의할 것입니다. 이제 입력과 출력이 있고 그림 19-6을 생성하는 약간 더 현실적인 앱에서 위자드를 사용해봅시다. 주목해야 할 주요 포인트는 페이지가 모듈에 의해 표시되더라도 해당 ID는 모듈 사용자가 제어한다는 것입니다. 컴포넌트를 만드는 개발자가 이름을 제어합니다. 웹페이지에 최종적으로 표시하기 위해 컨트롤을 조립하는 누구인지와는 상관없습니다: page1 <- tagList( textInput("name", "What's your name?") ) page2 <- tagList( numericInput("age", "How old are you?", 20) ) page3 <- tagList( "Is this data correct?", verbatimTextOutput("info") )

ui <- fluidPage( wizardUI( id = "demographics", pages = list(page1, page2, page3), doneButton = actionButton("done", "Submit") ) ) server <- function(input, output, session) { wizardServer("demographics", 3) observeEvent(input$done, showModal( modalDialog("Thank you!", footer = NULL) )) output$info <- renderText(paste0( "Age: ", input$age, "\n", "Name: ", input$name, "\n" )) }

그림 19-6. 새로운 모듈로 만든 간단하지만 완전한 위자드. 동적 UI "동적 필터링"에서 찾은 동적 필터링 코드의 일부를 가져와 모듈로 전환하는 사례 연구로 마무리하겠습니다. 모듈 내 동적 UI의 주요 과제는 서버 함수 내에서 UI 코드를 생성할 것이므로, 명시적 네임스페이싱이 필요한 시점에 대한 더 정확한 정의가 필요하다는 것입니다. 항상那样, 먼저 모듈 UI로 시작하겠습니다. 여기서는 매우 단순합니다. 서버 함수가 동적으로 채울 "구멍"을 생성하고 있기 때문입니다: filterUI <- function(id) { uiOutput(NS(id, "controls")) } 모듈 서버를 만들려면 먼저 "동적 필터링"의 헬퍼 함수를 복사합니다: make_ui()는 각 열에 대한 컨트롤을 만들고, filter_var()는 최종 논리 벡터를 생성하는 데 도움을 줍니다. 여기서의 유일한 차이점은 make_ui()가 컨트롤을 모듈의 네임스페이스로 만들 수 있도록 추가 id 인수를 갖게 된다는 것입니다: library(purrr) make_ui <- function(x, id, var) { if (is.numeric(x)) { rng <- range(x, na.rm = TRUE) sliderInput(id, var, min = rng[1], max = rng[2], value = rng) } else if (is.factor(x)) { levs <- levels(x) selectInput(id, var, choices = levs, selected = levs, multiple = TRUE) } else {
# 지원하지 않음
NULL } } filter_var <- function(x, val) { if (is.numeric(x)) {!is.na(x) & x >= val[1] & x <= val[2] } else if (is.factor(x)) { x %in% val } else {

# 컨트롤이 없으므로 필터링하지 않음
TRUE

} } 이제 모듈 서버를 만들겠습니다. 두 가지 주요 부분이 있습니다: purrr::map()과 make_ui()를 사용하여 컨트롤을 생성합니다. 여기서 NS()의 명시적 사용에 주목하세요. 모듈 서버 안에 있더라도, 자동 네임스페이싱은 input, output, 그리고 session에만 적용되기 때문에 이것이 필요합니다. 논리적 필터링 벡터를 모듈 출력으로 반환합니다. filterServer <- function(id, df) { stopifnot(is.reactive(df)) moduleServer(id, function(input, output, session) { vars <- reactive(names(df())) output$controls <- renderUI({ map(vars(), function(var) make_ui(df()[[var]], NS(id, var), var)) }) reactive({ each_var <- map(vars(), function(var) filter_var(df() [[var]], input[[var]])) reduce(each_var, &) }) }) } 이제 내장 데이터 세트를 선택한 다음 임의의 수치형 또는 범주형 변수로 필터링할 수 있는 모듈 앱에 모두 결합할 수 있습니다: filterApp <- function() { ui <- fluidPage( sidebarLayout( sidebarPanel( datasetInput("data", is.data.frame), textOutput("n"), filterUI("filter"), ), mainPanel(

tableOutput("table") ) ) ) server <- function(input, output, session) { df <- datasetServer("data") filter <- filterServer("filter", df) output$table <- renderTable(df()[filter(),, drop = FALSE]) output$n <- renderText(paste0(sum(filter()), " rows")) } shinyApp(ui, server) 여기서 모듈을 사용하는 큰 장점은 여러 고급 Shiny 프로그래밍 기술을 감싸고 있다는 것입니다. 작동하게 만드는 동적 UI와 함수형 프로그래밍 기술을 이해하지 않고도 필터 모듈을 사용할 수 있습니다. 단일 객체 모듈 장을 마무리하면서, 모듈에 대한 일반적인 반응에 대해 간략히 논의하겠습니다. 그것이 당신의 반응이 아니었다면 이 섹션을 자유롭게 건너뛰세요. 어떤 사람들(나처럼!)이 모듈을 처음 접할 때, 즉시 모듈 서버와 모듈 UI를 단일 모듈 객체로 결합하려고 시도합니다. 문제를 설명하기 위해, 장의 첫 번째 부분에서 가져온 동기 부여 예제를 일반화하여 데이터 프레임이 이제 매개변수가 되도록 해봅시다: histogramUI <- function(id, df) { tagList( selectInput(NS(id, "var"), "Variable", names(df)), numericInput(NS(id, "bins"), "bins", 10, min = 1), plotOutput(NS(id, "hist")) ) } histogramServer <- function(id, df) { moduleServer(id, function(input, output, session) { data <- reactive(df[[input$var]]) output$hist <- renderPlot({

hist(data(), breaks = input$bins, main = input$var) }, res = 96) }) } 그리고 이것이 다음 앱으로 이어집니다: ui <- fluidPage( tabsetPanel( tabPanel("mtcars", histogramUI("mtcars", mtcars)), tabPanel("iris", histogramUI("iris", iris)) ) ) server <- function(input, output, session) { histogramServer("mtcars", mtcars) histogramServer("iris", iris) } UI와 서버 모두에서 ID와 데이터 세트 이름을 반복해야 하는 것은 바람직하지 않으므로, UI와 서버를 모두 반환하는 단일 함수로 감싸고 싶어지는 것은 자연스러운 일입니다: histogramApp <- function(id, df) { list( ui = histogramUI(id, df), server = histogramServer(id, df) ) } 그런 다음 UI와 서버 바깥에서 모듈을 정의하고, 필요에 따라 목록에서 요소를 추출합니다: hist1 <- histogramApp("mtcars", mtcars) hist2 <- histogramApp("iris", iris) ui <- fluidPage( tabsetPanel( tabPanel("mtcars", hist1$ui()), tabPanel("iris", hist2$ui()) ) ) server <- function(input, output, session) { hist1$server()

hist2$server() } 이 코드에는 두 가지 문제가 있습니다. 첫째, 작동하지 않습니다. moduleServer()는 서버 함수 안에서 호출되어야 하기 때문입니다. 하지만 그 문제가 존재하지 않거나 다른 방식으로 우회했다고 상상해봅시다. 여전히 큰 문제가 있습니다: 사용자가 데이터 세트를 선택할 수 있게 하려면(즉, df 인수를 리액티브로 만들려면) 어떻게 해야 할까요? 모듈이 서버 함수보다 먼저 인스턴스화되기 때문에(즉, 그 정보를 알기 전에) 작동할 수 없습니다. Shiny에서 UI와 서버는 본질적으로 분리되어 있습니다. Shiny는 어떤 UI 호출이 어떤 서버 세션에 속하는지 알지 못합니다. Shiny 전반에서 이 패턴을 볼 수 있습니다. 예를 들어, plotOutput()과 renderPlot()은 공유 ID에 의해서만 연결됩니다. 모듈을 별도의 함수로 작성하는 것은 그 현실을 반영합니다: 공유 ID를 통해서가 아니면 연결되지 않는 별개의 함수입니다. 요약 이 장에서는 Shiny 모듈을 사용하는 방법을 보여주었습니다. 모듈은 함수의 일반화로, 조율된 UI와 서버 코드를 재사용 가능한 컴포넌트로 추출할 수 있게 해줍니다. 모듈을 이해하는 데 시간이 걸리지만,一旦 이해하면 복잡한 앱을 단순화하기 위한 강력한 기술을 잠금 해제하게 됩니다. 다음 장에서는 R 패키지에서 사용할 수 있는 테스트 도구를 활용할 수 있도록 Shiny 앱을 패키지처럼 구조화하는 방법을 배울 것입니다. 1 앱과 달리, 모듈 UI와 서버 모두 함수입니다. 2 tidyverse 스타일 가이드는 return()을 일찍 반환하는 경우에만 예약하는 것을 권장합니다. 3 모든 페이지에 두 버튼이 있는 것은 아닙니다(곧 더 자세히 설명), 따라서 NULL 기본값을 제공하여 선택 사항으로 표시합니다.

20장. 패키지 크거나 장기적인 Shiny 앱을 만드는 경우, 앱을 R 패키지와 같은 방식으로 구성하는 것을 강력히 추천합니다. 이것은 다음을 의미합니다: 모든 R 코드를 R/ 디렉토리에 넣습니다. 앱을 시작하는 함수(즉, UI와 서버를 shinyApp()에 전달하는 함수)를 작성합니다. 앱의 루트 디렉토리에 DESCRIPTION 파일을 만듭니다. 이 구조는 패키지 개발의 물에 발을 담그게 합니다. 완전한 패키지와는 거리가 멀지만, 더 큰 앱을 작업하기 쉽게 만드는 새로운 도구를 활성화하기 때문에 여전히 유용합니다. 패키지 구조는 21장에서 테스트를 논의할 때 더 큰 보상을 줄 것입니다. 테스트를 실행하고 어떤 코드가 테스트되는지 보는 것을 쉽게 만드는 도구를 얻기 때문입니다. 장기적으로는 roxygen2를 사용하여 복잡한 앱을 문서화하는 데도 도움이 되지만, 이 책에서는 논의하지 않을 것입니다. Shiny, ggplot2, 또는 dplyr과 같은 거대하고 복잡한 것들로 패키지를 생각하기 쉽습니다. 하지만 패키지는 매우 단순할 수도 있습니다. 패키지의 핵심 아이디어는 코드와 관련 아티팩트를 구성하기 위한 관례의 집합입니다. 해당 관례를 따르면 무료로 많은 도구를 얻게 됩니다. 이 장에서는 가장 중요한 관례를 보여주고 다음 단계에 대한 몇 가지 힌트를 제공하겠습니다. 앱-패키지로 작업하기 시작하면 패키지 개발 과정을 즐기고 더 배우고 싶어질 수 있습니다. R Packages로 시작하여 패키지 개발 지형을 파악한 다음 Colin Fay, Sébastien Rochette, Vincent Guyader, Cervan Girard의 Engineering Production Grade Shiny Apps로 계속하여 R 패키지와 Shiny 앱의 교차점에 대해 더 자세히 배우는 것을 추천합니다.

항상那样, shiny를 로드하면서 시작합니다: library(shiny) 기존 앱 변환 앱을 패키지로 변환하려면 약간의 사전 작업이 필요합니다. myApp이라는 앱이 있고 이미 myApp/이라는 디렉토리에 있다고 가정하면, 다음 작업을 수행해야 합니다: R 디렉토리를 만들고 app.R을 옮깁니다. 래핑하여 앱을 독립적인 함수로 변환합니다: library(shiny) myApp <- function(...) { ui <- fluidPage(... ) server <- function(input, output, session) {... } shinyApp(ui, server,...) } usethis::use_description()를 호출하여 설명 파일을 만듭니다. 많은 경우 이 파일을 볼 필요가 없지만, 나중에 사용할 키보드 단축키를 제공하는 RStudio의 "패키지 개발 모드"를 활성화하려면 필요합니다. 아직 없다면 usethis::use_rstudio()를 호출하여 RStudio 프로젝트를 만드세요. RStudio를 재시작하고 프로젝트를 다시 엽니다.

이제 Cmd/Ctrl+Shift+L을 눌러 devtools::load_all()을 실행하고 모든 패키지 코드와 데이터를 로드할 수 있습니다. 이것은 이제 다음을 할 수 있다는 뜻입니다: load_all()이 자동으로 R/의 모든 .R 파일을 source()하기 때문에 source() 호출을 제거할 수 있습니다. read.csv() 또는 유사한 방법으로 데이터 세트를 로드하고 있다면, 대신 usethis::use_data(mydataset)을 사용하여 데이터를 data/ 디렉토리에 저장할 수 있습니다. load_all()이 자동으로 데이터를 로드해줍니다. 이 과정을 더 구체적으로 만들기 위해, "이점"에서 이 작업의 다른 이점으로 돌아가기 전에 간단한 사례 연구를 작업해보겠습니다. 단일 파일 지금까지 단일 app.R에 있는 비교적 복잡한 앱이 있다고 상상해봅시다: library(shiny) monthFeedbackUI <- function(id) { textOutput(NS(id, "feedback")) } monthFeedbackServer <- function(id, month) { stopifnot(is.reactive(month)) moduleServer(id, function(input, output, session) { output$feedback <- renderText({ if (month() == "October") { "You picked a great month!" } else { "Eh, you could do better." } }) }) } stones <- vroom::vroom("birthstones.csv") birthstoneUI <- function(id) { p( "The birthstone for ", textOutput(NS(id, "month"), inline =

TRUE), " is ", textOutput(NS(id, "stone"), inline = TRUE) ) } birthstoneServer <- function(id, month) { stopifnot(is.reactive(month)) moduleServer(id, function(input, output, session) { stone <- reactive(stones$stone[stones$month == month()]) output$month <- renderText(month()) output$stone <- renderText(stone()) }) } months <- c( "January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December" ) ui <- navbarPage( "Sample app", tabPanel("Pick a month", selectInput("month", "What's your favourite month?", choices = months) ), tabPanel("Feedback", monthFeedbackUI("tab1")), tabPanel("Birthstone", birthstoneUI("tab2")) ) server <- function(input, output, session) { monthFeedbackServer("tab1", reactive(input$month)) birthstoneServer("tab2", reactive(input$month)) } shinyApp(ui, server) 이 코드는 모듈을 사용하여 페이지를 격리하는 간단한 세 페이지 앱을 만듭니다. 장난감 앱이지만 여전히 현실적입니다. 실제 앱과의 주요 차이점은 여기서 개별 UI와 서버 컴포넌트가 훨씬 더 단순하다는 것입니다. 모듈 파일 패키지로 변환하기 전에, "명명 규칙"의 조언에 따라 두 모듈을 별도의 파일로 추출하는 것이 첫 단계입니다:

R/monthFeedback.R: monthFeedbackUI <- function(id) { textOutput(NS(id, "feedback")) } monthFeedbackServer <- function(id, month) { stopifnot(is.reactive(month)) moduleServer(id, function(input, output, session) { output$feedback <- renderText({ if (month() == "October") { "You picked a great month!" } else { "Eh, you could do better." } }) }) } R/birthstone.R: birthstoneUI <- function(id) { p( "The birthstone for ", textOutput(NS(id, "month"), inline = TRUE), " is ", textOutput(NS(id, "stone"), inline = TRUE) ) } birthstoneServer <- function(id, month) { stopifnot(is.reactive(month)) moduleServer(id, function(input, output, session) { stone <- reactive(stones$stone[stones$month == month()])

output$month <- renderText(month()) output$stone <- renderText(stone()) }) } 이것은 다음 app.R을 남겨줍니다: library(shiny) stones <- vroom::vroom("birthstones.csv") #> Rows: 12 #> Columns: 2 #> Delimiter: "," #> chr [2]: month, stone #> #> Use spec() to retrieve the guessed column specification #> Pass a specification to the col_types argument to quiet this message months <- c( "January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December" ) ui <- navbarPage( "Sample app", tabPanel("Pick a month", selectInput("month", "What's your favourite month?", choices = months) ), tabPanel("Feedback", monthFeedbackUI("tab1")), tabPanel("Birthstone", birthstoneUI("tab2")) ) server <- function(input, output, session) { monthFeedbackServer("tab1", reactive(input$month)) birthstoneServer("tab2", reactive(input$month)) } shinyApp(ui, server) 모듈을 별도의 파일로 추출하는 것만으로도 앱의 전체적인 그림을 이해하는 데 도움이 되기 때문에 유용합니다. 대신 세부 사항으로 들어가고 싶다면 모듈 파일을 볼 수 있습니다.

패키지 이제 이것을 패키지로 만들겠습니다. 먼저 usethis::use_description()을 실행하여 DESCRIPTION 파일을 만듭니다. 그런 다음 app.R을 R/app.R로 옮기고 shinyApp()을 함수로 래핑합니다: library(shiny) monthApp <- function(...) { stones <- vroom::vroom("birthstones.csv") months <- c( "January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December" ) ui <- navbarPage( "Sample app", tabPanel("Pick a month", selectInput("month", "What's your favourite month?", choices = months) ), tabPanel("Feedback", monthFeedbackUI("tab1")), tabPanel("Birthstone", birthstoneUI("tab2")) ) server <- function(input, output, session) { monthFeedbackServer("tab1", reactive(input$month)) birthstoneServer("tab2", reactive(input$month)) } shinyApp(ui, server,...) } 선택적 추가로, usethis::use_data("birthstones")를 실행하여 birthstones.csv를 패키지 데이터 세트로 변환했습니다. 이것은 data/birthstones.rda를 만들며, 패키지를 로드할 때 자동으로 로드됩니다. 이제 birthstones.csv를 삭제하고 그것을 읽는 줄을 제거할 수 있습니다: stones <- vroom::vroom("birthstones.csv"). 최종 결과물은 GitHub에서 볼 수 있습니다.

이점 이 모든 작업을 할 가치가 있나요? 가장 중요한 이점은 모든 앱 코드를 정확하게 다시 로드하고 앱을 다시 시작하는 것을更容易하게 만드는 새로운 워크플로입니다. 하지만 앱 간 코드를 공유하고 다른 사람들과 앱을 공유하는 것도 쉽게 만듭니다. 워크플로 앱 코드를 패키지 구조에 넣으면 새로운 워크플로가 잠금 해제됩니다: Cmd/Ctrl+Shift+L로 앱의 모든 코드를 다시 로드합니다. 이것은 devtools::load_all()을 호출하며, 자동으로 모든 열린 파일을 저장하고, R/의 모든 파일을 source()하고, data/의 모든 데이터 세트를 로드한 다음 커서를 콘솔에 둡니다. myApp()으로 앱을 다시 실행합니다. 앱이 커질수록, 두 가지 가장 중요한 코드 탐색 키보드 단축키를 아는 것도 가치가 있습니다: Ctrl/Ctrl+.(마침표)는 "퍼지 파일 및 함수 검색기"를 엽니다. 이동하려는 파일이나 함수의 시작 부분에 몇 글자를 타이핑하고 화살표 키로 선택한 다음 Enter를 누릅니다. 키보드에서 손을 떼지 않고 앱 안에서 빠르게 이동할 수 있습니다. 커서가 함수 이름 위에 있을 때 F2는 함수 정의로 점프합니다. 많은 패키지 개발을 한다면, usethis를 자동으로 로드하여 (예를 들어) usethis::use_description() 대신 use_description()을 타이핑하고 싶을 수 있습니다. .Rprofile에 다음 줄을 추가하여这样做할 수 있습니다. 이 파일은 R을 시작할 때마다 실행되는 R 코드를 포함하므로, 대화형 개발 환경을 사용자 정의하는 좋은 방법입니다:

if (interactive()) { require(usethis, quietly = TRUE) } 이 파일을 편집하는 가장 쉬운 방법은 usethis::edit_r_profile()을 호출하는 것입니다. 공유 앱이 이제 함수로 감싸여 있으므로, 동일한 패키지에 여러 앱을 포함하기 쉽습니다. 그리고 같은 곳에 여러 앱이 있으므로, 이제 앱 간 코드와 데이터를 공유하는 것이 훨씬 쉬워집니다. 관련 작업에 대한 여러 앱이 있다면 이것은 큰 이점입니다. 패키지는 앱을 공유하는 좋은 방법이기도 합니다. shinyapps.io와 RStudio Connect는 R에 익숙하지 않은 사람들과 앱을 공유하는 데 유용합니다. 하지만 때로는 R를 사용하는 동료들과 앱을 공유하고 싶을 수 있습니다. 사용자가 데이터 세트를 업로드하는 대신, 데이터 프레임을 사용하여 호출하는 함수를 제공하고 싶을 수 있습니다. 예를 들어, 다음 매우 단순한 앱은 R 사용자가 인터랙티브 요약을 위해 자체 데이터 프레임을 제공할 수 있게 합니다: dataSummaryApp <- function(df) { ui <- fluidPage( selectInput("var", "Variable", choices = names(df)), verbatimTextOutput("summary") ) server <- function(input, output, session) { output$summary <- renderPrint({ summary(df[[input$var]]) }) } shinyApp(ui, server) } RStudio Gadgets는 이 아이디어 위에 구축됩니다: RStudio IDE에 새로운 사용자 인터페이스를 추가할 수 있게 해주는 Shiny 앱입니다. 코드를 생성하는 가젯을 작성하는 것도 가능하여, 대화형으로 수행하기 쉬운 작업을 수행하고 가젯이 해당 코드를 생성하여 열린 파일에 다시 저장할 수 있습니다. 추가 단계 기본 사항 외에 수행할 수 있는 두 가지 일반적인 추가 단계가 있습니다: 앱-패키지를 쉽게 배포할 수 있게 만들고 "진짜" 패키지로 전환하는 것입니다. 앱-패키지 배포 앱을 RStudio Connect 또는 Shiny에1 배포하려면 두 가지 추가 단계가 필요합니다: 앱을 실행하는 방법을 배포 서버에 알려주는 app.R이 필요합니다. 가장 쉬운 방법은 pkgload로 코드를 로드하는 것입니다: pkgload::load_all(".") myApp() Engineering Shiny의 13장에서 다른 기술을 볼 수 있습니다. 일반적으로 앱을 배포할 때 rsconnect 패키지가 코드가 사용하는 모든 패키지를 자동으로 파악합니다. 하지만 이제 DESCRIPTION 파일이 있으므로, 명시적으로 지정해야 합니다. 가장 쉬운 방법은 usethis::use_package()를 호출하는 것입니다. shiny와 pkgload로 시작해야 합니다: usethis::use_package("shiny") usethis::use_package("pkgload") 이것은 약간 더 많은 작업이지만, 보상은 앱이 필요로 하는 모든 패키지의 명시적인 목록을 한 곳에 갖게 되는 것입니다.

이제 사용자와 앱의 업데이트된 버전을 공유할 준비가 될 때마다 rsconnect::deployApp()을 실행할 수 있습니다. R CMD check 최소한의 패키지는 R/ 디렉토리, DESCRIPTION 파일, 그리고 앱을 실행하는 함수를 포함합니다. 보셨듯이, 이미 유용합니다. 앱 개발을 가속화하는 몇 가지 워크플로를 잠금 해제하기 때문입니다. 하지만 "진짜" 앱은 무엇일까요? 제 생각에는 R CMD check를 통과시키려는 진지한 노력입니다. R CMD check는 패키지의 일반적인 문제를 확인하는 R의 자동화된 시스템입니다. RStudio에서 Cmd/Ctrl+Shift+E를 눌러 R CMD check를 실행할 수 있습니다. 패키지 구조를 시도하는 첫 번째, 두 번째, 심지어 세 번째 때에는这样做하는 것을 추천하지 않습니다. 대신 기본 구조와 워크플로에 익숙해진 다음 완전히 호환되는 패키지로의 다음 단계를 밟는 것을 추천합니다. 이것은 일반적으로 중요한 앱, 특히 다른 곳에 배포될 앱에 대해 예약합니다. R CMD check를 통과시키는 데 많은 작업이 필요할 수 있고, 단기적으로는 보상이 거의 없습니다. 하지만 장기적으로 이것은 많은 잠재적 문제로부터 보호해줄 것이며, R 개발자들이 익숙한 표준을 준수하도록 보장하기 때문에 다른 사람들이 앱에 기여하기 쉽게 해줍니다. 첫 번째 전체 앱-패키지를 만들기 전에, R Packages의 "The Whole Game" 장을 읽어야 합니다. 패키지 구조에 대한 더 완전한 감각을 제공하고 다른 유용한 워크플로를 소개해줄 것입니다. 그런 다음 다음 힌트를 사용하여 R CMD check를 깔끔하게 통과시키세요: library() 또는 require() 호출을 제거하고 대신 DESCRIPTION에 선언으로 대체합니다. usethis::use_package("name")을 사용하여 DESCRIPTION에 필요한 패키지를 추가합니다.2 그런 다음 각 함수를 ::로 명시적으로 참조할지 또는

@importFrom packageName functionName을 사용하여 한 곳에서 가져오기를 선언합니다. 최소한 usethis::use_package("shiny")가 필요하며, Shiny 앱의 경우 @import shiny를 사용하여 Shiny 패키지의 모든 함수를 쉽게 사용할 수 있도록 하는 것을 추천합니다. (@import를 사용하는 것은 일반적으로 모범 사례로 간주되지만, 여기서는 의미가 있습니다.) 라이선스를 선택한 다음 적절한 use_license_ 함수를 사용하여 올바른 위치에 넣습니다. 독점 코드의 경우 usethis::use_proprietary_license()를 사용할 수 있습니다. 자세한 내용은 R Packages의 9장을 참조하세요. usethis::use_build_ignore("app.R") 또는 유사한 것으로 .Rbuildignore에 app.R을 추가합니다. 앱에 작은 참조 데이터 세트가 포함되어 있다면, data 또는 inst/extdata에 넣으세요. usethis::use_data()에 대해 이전에 논의했습니다. 대신 inst/ext에 원시 데이터를 넣고 read.csv(system.file("exdata", "mydata.csv", package = "myApp")) 또는 유사한 것으로 로드할 수 있습니다. 앱을 패키지로 변경할 수도 있습니다. 이것은 배포 머신이 설치할 수 있는 곳에 패키지가 있어야 합니다. 공용 작업의 경우 이것은 CRAN 또는 GitHub 패키지를 의미합니다. 비공용 작업의 경우 RStudio Package Manager나 drat과 같은 도구를 사용해야 할 수 있습니다: myApp::myApp() 요약 이 장에서는 패키지 개발의 물에 발을 담갔습니다. ggplot2와 shiny와 같은 패키지를 생각하면 압도적으로 보일 수 있지만, 패키지는 매우, 매우 단순할 수 있습니다. 사실, 모든

프로젝트가 패키지가 되려면 R 파일 디렉토리와 DESCRIPTION 파일이 필요합니다. 패키지는 유용한 도구와 워크플로를 잠금 해제하는 가벼운 관례 세트일 뿐입니다. 이 장에서는 앱을 패키지로 전환하는 방법과 그렇게 하고 싶을 수 있는 몇 가지 이유를 배웠습니다. 다음 장에서는 앱을 패키지로 전환하는 가장 중요한 이유인 테스트를更容易하게 만드는 것에 대해 배울 것입니다. 1 app.R이 앱을 구조화하는 가장 일반적인 방법이므로 대부분의 다른 Shiny 앱 배포 방법도 작동할 것으로 예상합니다. 2 Imports와 Suggests의 차이는 일반적으로 앱 패키지에서 중요하지 않습니다. 차이를 두고 싶다면, 가장 유용한 방법은 앱이 작동하기 위해 배포 머신에 있어야 하는 패키지에는 Imports를 사용하고, 앱을 개발하기 위해 개발 머신에 있어야 하는 패키지에는 Suggests를 사용하는 것입니다.

21장. 테스트 단순한 앱의 경우, 앱이 어떻게 작동해야 하는지 기억하기가 충분히 쉬워서 새로운 기능을 추가할 때 기존 기능을 실수로 손상시키지 않습니다. 하지만 앱이 더 복잡해질수록 모든 것을 동시에 머릿속에 담아두는 것이 불가능해집니다. 테스트는 코드가 기대한 방식으로 계속 작동하는지 자동으로 확인할 수 있는 방식으로 코드의 원하는 동작을 캡처하는 방법입니다. 기존의 비공식 테스트를 코드로 전환하는 것은 처음에는 고통스럽습니다. 모든 키 누름과 마우스 클릭을 코드 한 줄로 조심스럽게 변환해야 하기 때문입니다. 하지만 한번 완료되면 테스트를 다시 실행하는 것이 엄청나게 빨라집니다. testthat 패키지로 자동화된 테스트를 수행할 것입니다. testthat은 앱을 패키지로 전환해야 하지만, 20장에서 논의했듯이 이것은 너무 많은 작업이 아니며, 다른 이유로도 보상이 있다고 생각합니다. testthat 테스트는 다음과 같습니다: test_that("as.vector() strips names", { x <- c(a = 1, b = 2) expect_equal(as.vector(x), c(1, 2)) }) 곧 세부 사항으로 돌아가겠지만, 테스트는 의도를 선언("as.vector() strips names")하는 것으로 시작한 다음 일반 R 코드를 사용하여 테스트 데이터를 생성한다는 점에 주목하세요. 그런 다음 테스트 데이터를 기대 결과와 비교합니다. 기대(expect_)로 시작하는 함수입니다. 첫 번째 인수는 실행할 코드이고, 두 번째 인수는 기대 결과를 설명합니다: 여기서는 as.vector(x)의 출력이 c(1, 2)와 같은지 확인합니다. 이 장에서 네 가지 수준의 테스트를 수행할 것입니다:

비리액티브 함수를 테스트하는 것으로 시작하겠습니다. 이것은 기본 테스트 워크플로를 배우고 서버 함수나 UI에서 추출한 코드의 동작을 확인하는 데 도움이 될 것입니다. 이것은 패키지를 작성할 때 수행하는 것과 정확히 같은 유형의 테스트이므로, R Packages의 테스트 장에서 더 많은 세부 사항을 찾을 수 있습니다. 그런 다음 서버 함수 내 리액티브의 흐름을 테스트하는 방법을 배울 것입니다. 입력 값을 설정한 다음 리액티브와 출력이 기대한 값을 가지는지 확인합니다. 그런 다음 백그라운드 웹 브라우저에서 앱을 실행하여 JavaScript를 사용하는 Shiny의 일부(예: update* 함수)를 테스트할 것입니다. 이것은 실제 브라우저를 실행하므로 높은 충실도의 시뮬레이션이지만, 단점으로는 테스트 실행이 더 느리고 앱 안을 쉽게 엿볼 수 없다는 것입니다. 마지막으로 선택된 요소의 스크린샷을 저장하여 앱 시각적 요소를 테스트할 것입니다. 앱 레이아웃, CSS, 플롯, HTML 위젯을 테스트하는 데 필요하지만, 스크린샷이 여러 이유로 쉽게 변경될 수 있어 깨지기 쉽습니다. 이것은 각 변경이 괜찮은지 확인하기 위해 인간의 개입이 필요하다는 뜻이며, 이것이 가장 노동 집약적인 테스트 형태입니다. 이 테스트 수준들은 자연스러운 계층을 형성합니다. 각 기술이 앱의 사용자 경험을 더 완전하게 시뮬레이션하기 때문입니다. 더 나은 시뮬레이션의 단점은 각 수준이 더 많은 작업을 해야 하므로 더 느리고, 더 많은 왜곡된 힘이 작용하므로 더 깨지기 쉽다는 것입니다. 항상 가능한 한 낮은 수준에서 작업하여 테스트가 가능한 한 빠르고 강건하도록 노력해야 합니다. 시간이 지나면서 이것이 코드를 작성하는 방식에도 영향을 미칠 것입니다: 어떤 유형의 코드가 테스트하기 쉬운지 아는 것이 자연스럽게 더 단순한 설계로 밀어붙일 것입니다. 다양한 테스트 수준 사이에, 테스트 워크플로와 더 일반적인 테스트 철학에 대한 조언도 제공할 것입니다. 시작하겠습니다:

library(shiny) library(testthat) # >= 3.0.0 library(shinytest) 함수 테스트 앱에서 테스트하기 가장 쉬운 부분은 Shiny와 가장 관련이 적은 부분입니다: 18장에서 설명한 대로 UI와 서버 코드에서 추출한 함수들. 비리액티브 함수를 테스트하는 방법을 논의하는 것으로 시작하겠습니다. testthat으로 단위 테스트의 기본 구조를 보여드리겠습니다. 기본 구조 테스트는 세 가지 수준으로 조직됩니다: 파일 모든 테스트 파일은 tests/testthat에 있으며, 각 테스트 파일은 R/의 코드 파일에 해당해야 합니다. 예를 들어, R/module.R의 코드는 tests/testthat/test-module.R의 코드로 테스트되어야 합니다. 안타깝게도 그 관례를 기억할 필요는 없습니다. usethis::use_test()를 사용하여 현재 열려 있는 R 파일에 해당하는 테스트 파일을 자동으로 만들거나 찾으면 됩니다. 테스트 각 파일은 테스트, 즉 test_that() 호출로 세분화됩니다. 테스트는 일반적으로 함수의 단일 속성을 확인해야 합니다. 이것이 정확히 무엇을 의미하는지 설명하기 어렵지만, 좋은 휴리스틱은 test_that()의 첫 번째 인수로 테스트를 쉽게 설명할 수 있다는 것입니다. 기대 각 테스트는 expect_로 시작하는 함수를 포함하는 하나 이상의 기대를 포함합니다. 이것들은 코드가 특정 값을 반환하거나, 오류를 던지거나, 기타 다른 것을 수행할지에 대해 정확히 정의합니다. 이 장에서는 Shiny 앱에 가장 중요한 기대를 논의할 것이지만, 전체 목록은 testthat 웹사이트에서 볼 수 있습니다.

테스트의 기술은 함수의 원하는 동작을 명확하게 정의하는 테스트를 작성하는 방법을 파악하는 것입니다. 향후 변경될 수 있는 부수적인 세부 사항에 의존하지 않으면서. 기본 워크플로 기본 구조를 이해했으니, 몇 가지 예시로 넘어가겠습니다. "업로드된 데이터 읽기"의 간단한 예시로 시작하겠습니다. 여기서는 서버 함수에서 코드를 추출하여 load_file()이라고 불렀습니다: load_file <- function(name, path) { ext <- tools::file_ext(name) switch(ext, csv = vroom::vroom(path, delim = ",", col_types = list()), tsv = vroom::vroom(path, delim = "\t", col_types = list()), validate("Invalid file; Please upload a.csv or.tsv file") ) } 이 예시를 위해, 이 코드가 R/load.R에 있다고 가정하겠습니다. 따라서 이것에 대한 테스트는 tests/testthat/test-load.R에 있어야 합니다. 그 파일을 만드는 가장 쉬운 방법은 load.R과 함께 usethis::use_test()를 실행하는 것입니다.1 이 함수에 대해 테스트하고 싶은 세 가지 주요 사항이 있습니다: CSV 파일을 로드할 수 있는지, TSV 파일을 로드할 수 있는지, 다른 유형에 대해 오류 메시지를 제공하는지. 이 세 가지를 테스트하려면 몇 가지 샘플 파일이 필요하며, 테스트 실행 후 자동으로 정리되도록 세션 임시 디렉토리에 저장합니다. 그런 다음 세 가지 기대를 작성합니다. 로드된 파일이 원래 데이터와 같은지 확인하는 두 가지, 그리고 오류가 발생하는지 확인하는 한 가지입니다: test_that("load_file() handles all input types", {
# 샘플 데이터 만들기
df <- tibble::tibble(x = 1, y = 2) path_csv <- tempfile() path_tsv <- tempfile() write.csv(df, path_csv, row.names = FALSE)

write.table(df, path_tsv, sep = "\t", row.names = FALSE) expect_equal(load_file("test.csv", path_csv), df) expect_equal(load_file("test.tsv", path_tsv), df) expect_error(load_file("blah", path_csv), "Invalid file") }) #> 테스트 통과 이 테스트를 실행하는 네 가지 방법이 있습니다: 개발하는 동안 콘솔에서 각 줄을 대화형으로 실행합니다. 기대가 실패하면 오류가 되며, 이를 수정합니다. 개발을 완료하면 전체 테스트 블록을 실행합니다. 테스트가 통과하면 Test passed와 같은 메시지를 받습니다. 실패하면 무슨 일이 일어났는지에 대한 세부 사항을 받습니다. 더 많은 테스트를 개발하면서 devtools::test_file()로 현재 파일2의 모든 테스트를 실행합니다. 이를 자주 수행하므로 특별한 키보드 단축키를 설정하여 가능한 한 쉽게 만들었습니다. 곧 어떻게 설정하는지 보여드리겠습니다. 가끔 devtools::test()로 전체 패키지의 모든 테스트를 실행합니다. 이렇게 하면 현재 파일 외부에서 실수로 무언가를 손상시키지 않았는지 확인할 수 있습니다. 핵심 기대 함수를 테스트할 때 자주 사용할 두 가지 기대가 있습니다: expect_equal()과 expect_error(). 모든 기대 함수와 마찬가지로, 첫 번째 인수는 확인할 코드이고, 두 번째 인수는 기대 결과입니다: expect_equal()의 경우 기대 값, expect_error()의 경우 기대 오류 텍스트입니다. 이 함수들이 어떻게 작동하는지 감을 잡으려면, 테스트 외부에서 직접 호출하는 것이 유용합니다.

expect_equal()을 사용할 때는 전체 객체를 테스트할 필요가 없다는 점을 기억하세요. 일반적으로 관심 있는 구성 요소만 테스트하는 것이 더 좋습니다: complicated_object <- list( x = list(mtcars, iris), y = 10 ) expect_equal(complicated_object$y, 10) expect_equal()의 특수한 경우에 대한 몇 가지 기대가 있어 약간의 타이핑을 절약할 수 있습니다: expect_true(x)와 expect_false(x)는 expect_equal(x, FALSE)와 expect_equal(x, TRUE)와 동일합니다. expect_null(x)는 expect_equal(x, NULL)과 동일합니다. expect_named(x, c("a", "b", "c"))는 expect_equal(names(x), c("a", "b", "c"))와 동일하지만 ignore.order와 ignore.case 옵션이 있습니다. expect_length(x, 10)는 expect_equal(length(x), 10)과 동일합니다. 벡터에 대한 expect_equal()의 이완된 버전을 구현하는 함수도 있습니다: expect_setequal(x, y)는 x의 모든 값이 y에 나타나고 y의 모든 값이 x에 나타남을 테스트합니다. expect_mapequal(x, y)는 x와 y가 같은 이름을 가지며 x[names(y)]가 y와 같음을 테스트합니다. 코드가 오류를 생성하는지 테스트하는 것이 종종 중요한데, expect_error()를 사용할 수 있습니다:

expect_error("Hi!") #> Error: "Hi!" did not throw the expected error. expect_error(stop("Bye")) expect_error()의 두 번째 인수는 정규식이라는 점에 주목하세요. 목표는 기대하는 오류와 일치하고 기대하지 않는 오류와는 일치하지 않을 가능성이 낮은 짧은 텍스트 조각을 찾는 것입니다: f <- function() { stop("Calculation failed [location 1]") } expect_error(f(), "Calculation failed [location 1]") #> Error in f(): Calculation failed [location 1] expect_error(f(), "Calculation failed \\[location 1\\]") 하지만 더 나은 것은 작은 조각을 선택하여 일치시키는 것입니다: expect_error(f(), "Calculation failed") 또는 곧 논의할 expect_snapshot()를 사용합니다. expect_error()에는 expect_warning()과 expect_message() 변형도 있어 오류와 같은 방식으로 경고와 메시지를 테스트할 수 있습니다. Shiny 앱을 테스트할 때는 거의 필요하지 않지만, 패키지를 테스트할 때는 매우 유용합니다. UI 함수 UI 코드에서 추출한 함수를 테스트할 때 동일한 기본 아이디어를 사용할 수 있습니다. 하지만 이것은 새로운 기대가 필요합니다. 수동으로 모든 HTML을 타이핑하는 것은 지루할 것이기 때문입니다. 따라서 대신 스냅샷 테스트를 사용합니다.3 스냅샷 기대는 다른 기대와 주요 차이점은 기대 결과가 코드 자체가 아닌 별도의 스냅샷 파일에 저장된다는 것입니다. 스냅샷 테스트는 복잡한 사용자 인터페이스 디자인 시스템을 설계할 때 가장 유용하지만, 대부분의 앱 범위를 벗어납니다. 따라서 여기서는 핵심 아이디어를 간략히 보여주고 더 배우기 위한 추가 리소스를 안내하겠습니다.

이전에 정의한 이 UI 함수를 살펴봅시다: sliderInput01 <- function(id) { sliderInput(id, label = id, min = 0, max = 1, value = 0.5, step = 0.1) } cat(as.character(sliderInput01("x"))) #> <div class="form-group shiny-input-container"> #> <label class="control-label" id="x-label" for="x">x</label> #> <input class="js-range-slider" id="x" data-skin="shiny" data-min="0" #> data-max="1" data-from="0.5" data-step="0.1" datagrid="true" #> data-grid-num="10" data-grid-snap="false" data-prettifyseparator="," #> data-prettify-enabled="true" data-keyboard="true" datadata-type="number"/> #> </div> 이 출력이 기대한 대로인지 어떻게 테스트할 수 있을까요? expect_equal()을 사용할 수 있습니다: test_that("shinyInput01() creates expected HTML", { expect_equal( as.character(sliderInput01("x")), "<div class=\"form-group shiny-input-container\">\n <label class=\"control-label\" id=\"x-label\" for=\"x\">x</label>\n <input class=\"js-range-slider\" id=\"x\" dataskin=\"shiny\" data-min=\"0\" data-max=\"1\" data-from=\"0.5\" data-step=\"0.1\" datagrid=\"true\" data-grid-num=\"10\" data-grid-snap=\"false\" dataprettify-separator=\",\" data-prettify-enabled=\"true\" data-keyboard=\"true\" data-data-type=\"number\"/>\n </div>" ) }) #> 테스트 통과

하지만 따옴표와 줄 바꿈의 존재는 문자열에서 많은 이스케이프를 필요로 합니다. 이것은 우리가 정확히 무엇을 기대하는지 보기를 어렵게 만들며, 출력이 변경되면 정확히 무슨 일이 일어났는지 보기를 어렵게 만듭니다. 스냅샷 테스트의 핵심 아이디어는 기대 결과를 별도의 파일에 저장하는 것입니다. 이것은 벌크 데이터를 테스트 코드에서 멀리 유지하고 문자열에서 특수 값을 이스케이프하는 것에 대해 걱정할 필요가 없게 만듭니다. 여기서는 콘솔에 표시되는 출력을 캡처하기 위해 expect_snapshot()를 사용합니다: test_that("shinyInput01() creates expected HTML", { expect_snapshot(sliderInput01("x")) }) 다른 기대와의 주요 차이점은 기대하는 내용을 설명하는 두 번째 인수가 없다는 것입니다. 대신 그 데이터가 별도의 .md 파일에 저장됩니다. 코드가 R/slider.R에 있고 테스트가 tests/testthat/test-slider.R에 있다면, 스냅샷은 tests/testthat/_snaps/slider.md에 저장됩니다. 테스트를 처음 실행하면 expect_snapshot()이 자동으로 참조 출력을 생성하며, 다음과 같이 보일 것입니다:

# shinyInput01()가 기대한 HTML을 생성함
코드 sliderInput01("x") 출력 <div class="form-group shiny-input-container"> <label class="control-label" id="x-label" for="x">x</label> <input class="js-range-slider" id="x" data-skin="shiny" datamin="0" data-max="1" data-from="0.5" data-step="0.1" datagrid="true" data-grid-num="10" data-grid-snap="false" data-prettifyseparator="," data-prettify-enabled="true" data-keyboard="true" datadata-type="number"/> </div> 출력이 나중에 변경되면 테스트가 실패합니다. 실패하게 만드는 버그를 수정하거나, 의도적인 변경이라면 testthat::snapshot_accept()를 실행하여 스냅샷을 업데이트해야 합니다. 이것을 테스트로 확정하기 전에 여기서 출력을 생각해볼 가치가 있습니다. 여기서 정말로 테스트하는 것이 무엇인가요? 입력이 출력이 되는 과정을 보면, 출력의 대부분은 Shiny에 의해 생성되고 극히 일부분만 여러분의 코드의 결과라는 것을 알게 될 것입니다. 이것은 이 테스트가 특별히 유용하지 않다는 것을 시사합니다: 이 출력이 변경된다면, 여러분의 코드 변경보다는 Shiny 변경의 결과일 가능성이 훨씬 높습니다. 이것은 테스트를 깨지기 쉽게 만듭니다; 실패하더라도 여러분의 잘못일 가능성은 낮으며, 실패를 수정하는 것이 여러분의 통제 하에 있을 가능성도 낮습니다. 스냅샷 테스트에 대해 "Snapshot tests" testthat 비네트에서 더 배울 수 있습니다. 워크플로 리액티브나 JavaScript를 사용하는 함수를 테스트하기 전에, 워크플로에 대해 잠시 이야기 나누겠습니다. 코드 커버리지 테스트가 여러분이 생각하는 것을 테스트하고 있는지 확인하는 것은 매우 유용합니다. 이를 수행하는 좋은 방법은 "코드 커버리지"입니다. 테스트를 실행하고 실행된 코드의 모든 줄을 추적합니다. 그런 다음 결과를 살펴보哪些 줄이 테스트에 의해 절대 건드려지지 않는지 볼 수 있으며, 이를 통해 가장 중요하거나, 가장 위험하거나, 가장 프로그래밍하기 어려운 부분을 테스트했는지 성찰할 수 있습니다. 이것은 코드에 대해 생각하는 것의 대체물이 아닙니다. 100% 테스트 커버리지를 가지더라도 버그가 있을 수 있습니다. 하지만 복잡한 중첩 코드가 있을 때 특히 중요한 것에 대해 생각하는 데 도움이 되는 재미있고 유용한 도구입니다. 여기서는 자세히 다루지 않겠지만, devtools::test_coverage() 또는 devtools::test_coverage_file()로 시도해보는 것을 강력히 추천합니다. 주목해야 할 주요 사항은 녹색 줄이 테스트되었고, 빨간색 줄은 테스트되지 않았다는 것입니다.

코드 커버리지는 약간 다른 워크플로를 지원합니다:
1. test_coverage() 또는 test_coverage_file()을 사용하여哪些 줄이 테스트되었는지 확인합니다.
2. 테스트되지 않은 줄을 살펴보고 특별히 테스트하기 위한 테스트를 설계합니다.
3. 모든 중요한 코드 줄이 테스트될 때까지 반복합니다. (100% 테스트 커버리지에 도달하는 것은 일반적으로 가치가 없지만, 앱의 가장 중요한 부분을 테스트하고 있는지 확인해야 합니다.) 코드 커버리지는 리액티브를 테스트하는 도구와 (어느 정도) JavaScript에서도 작동하므로 유용한 기초 기술입니다. 키보드 단축키 "이점"의 조언을 따랐다면, 콘솔에서 test() 또는 test_file()을 타이핑하여 이미 테스트를 실행할 수 있습니다. 하지만 테스트는 자주 수행하는 것이므로 키보드 단축키를 손끝에 두는 것이 가치가 있습니다. RStudio에는 하나의 유용한 단축키가 내장되어 있습니다: Cmd/Ctrl+Shift+T는 devtools::test()를 실행합니다. 세트를 완성하기 위해 세 가지를 추가하는 것을 추천합니다: Cmd/Ctrl+T를 devtools::test_file()에 바인딩 Cmd/Ctrl+Shift+R를 devtools::test_coverage()에 바인딩 Cmd/Ctrl+R를 devtools::test_coverage_file()에 바인딩 물론 의미 있는 어떤 단축키든 자유롭게 선택할 수 있지만, 이것들은 일부 기본 구조를 공유합니다. Shift를 사용하는 키보드 단축키는 전체 패키지에 적용되고, Shift 없는 단축키는 현재 파일에 적용됩니다. 그림 21-1은 Mac에서 나의 키보드 단축키가 어떻게 보이는지 보여줍니다.

그림 21-1. Mac용 나의 키보드 단축키. 워크플로 요약 지금까지 논의한 모든 기술의 요약입니다: R 파일에서 usethis::use_test()를 사용하여 테스트 파일을 만들거나(처음 실행될 때) 테스트 파일로 이동합니다(이미 존재하는 경우). 코드를 작성/테스트를 작성합니다. Cmd/Ctrl+T를 눌러 테스트를 실행하고 콘솔에서 결과를 검토합니다. 필요에 따라 반복합니다. 새로운 버그를 발견하면, 먼저 테스트에서 잘못된 동작을 캡처하는 것으로 시작하세요. 최소한의 코드를 만드는 과정에서 버그가 어디에 있는지 더 잘 이해하게 될 것이며, 테스트를 가지면 버그를 수정하지 않았는데 수정했다고 스스로를 속이지 않도록 보장해줄 것입니다. Cmd/Ctrl+R을 눌러 생각하는 것을 테스트하고 있는지 확인합니다. Cmd/Ctrl+Shift+T를 눌러 다른 것을 실수로 손상시키지 않았는지 확인합니다. 리액티브 테스트 일반 비리액티브 코드를 테스트하는 방법을 이해했으니, 이제 Shiny에 특화된 과제로 넘어갈 때입니다. 첫 번째 과제는 리액티브를 테스트하는 것입니다. 이미 보았듯이, 리액티브 코드를 대화형으로 실행할 수 없습니다: x <- reactive(input$y + input$z) x()

#> Error: Operation not allowed without an active reactive context. #> * You tried to do something that can only be done from inside a reactive #> consumer. 15장에서처럼 reactiveConsole()을 사용하는 것에 대해 궁금할 수 있습니다. 안타깝게도, 리액티브의 시뮬레이션은 근본적으로 대화형 콘솔에 의존하므로 테스트에서는 작동하지 않습니다. 리액티브를 평가하려고 시도할 때 오류가 발생할 뿐만 아니라, 작동하더라도 input$y와 input$z는 정의되지 않을 것입니다. 어떻게 작동하는지 보려면, 세 가지 입력, 하나의 출력, 그리고 세 가지 리액티브를 가진 간단한 앱으로 시작하겠습니다: ui <- fluidPage( numericInput("x", "x", 0), numericInput("y", "y", 1), numericInput("z", "z", 2), textOutput("out") ) server <- function(input, output, session) { xy <- reactive(input$x - input$y) yz <- reactive(input$z + input$y) xyz <- reactive(xy() * yz()) output$out <- renderText(paste0("Result: ", xyz())) } 이 코드를 테스트하기 위해 testServer()를 사용할 것입니다. 이 함수는 두 개의 인수를 받습니다: 서버 함수와 실행할 코드. 코드는 서버 함수 안의 특별한 환경에서 실행되므로, 출력, 리액티브, 그리고 사용자 상호작용을 시뮬레이션할 수 있는 특별한 세션 객체에 접근할 수 있습니다. 이 함수를 사용하는 주요 시간은 사용자가 브라우저에서 앱과 상호작용하는 것처럼 입력 컨트롤의 값을 설정할 수 있게 해주는 session$setInputs()입니다: testServer(server, { session$setInputs(x = 1, y = 1, z = 1) print(xy()) print(output$out) })

#> [1] 0 #> [1] "Result: 0" (testServer()를 남용하여 리액티브를 지원하는 대화형 환경에 들어갈 수도 있습니다: testServer(myApp(), browser()).) 서버 함수만 테스트하고 있다는 점에 주목하세요. 앱의 UI 컴포넌트는 완전히 무시됩니다. 이것은 입력을 검토하면 가장 분명하게 볼 수 있습니다. 실제 Shiny 앱과 달리 모든 입력은 NULL로 시작합니다. 초기 값이 UI에 기록되기 때문입니다. "JavaScript 테스트"에서 UI 테스트로 돌아가겠습니다. testServer(server, { print(input$x) }) #> NULL 이제 리액티브 환경에서 코드를 실행하는 방법을 알았으니, 기존에 알고 있는 코드 테스트와 결합하여 다음과 같은 것을 만들 수 있습니다: test_that("reactives and output updates", { testServer(server, { session$setInputs(x = 1, y = 1, z = 1) expect_equal(xy(), 0) expect_equal(yz(), 2) expect_equal(output$out, "Result: 0") }) }) #> 테스트 통과 testServer() 사용을 마스터하면, 리액티브 코드를 테스트하는 것이 비리액티브 코드를 테스트하는 것만큼 쉬워집니다. 주요 과제는 실패한 테스트를 디버깅하는 것입니다: 일반 테스트처럼 줄별로 단계별로 진행할 수 없으므로, 문제를 진단하기 위해 대화형으로 실험할 수 있도록 testServer() 안에 browser()를 추가해야 합니다.

모듈 앱 함수를 테스트하는 것과 유사한 방식으로 모듈을 테스트할 수 있지만, 여기서는 모듈의 서버 측만 테스트하고 있다는 것이 조금 더 명확합니다. 변수의 간단한 요약을 표시하기 위해 세 가지 출력을 사용하는 간단한 모듈로 시작하겠습니다: summaryUI <- function(id) { tagList( outputText(ns(id, "min")), outputText(ns(id, "mean")), outputText(ns(id, "max")), ) } summaryServer <- function(id, var) { stopifnot(is.reactive(var)) moduleServer(id, function(input, output, session) { range_val <- reactive(range(var(), na.rm = TRUE)) output$min <- renderText(range_val()[[1]]) output$max <- renderText(range_val()[[2]]) output$mean <- renderText(mean(var())) }) } 이전처럼 testServer()를 사용하겠지만, 호출은 약간 다릅니다. 이전과 마찬가지로 첫 번째 인수는 서버 함수(이제 모듈 서버)이지만, 이제는 args라는 이름의 목록에 추가 인수를 제공해야 합니다. 이것은 모듈 서버에 대한 인수 목록을 받습니다(id 인수는 선택 사항입니다. 생략하면 testServer()가 자동으로 채웁니다). 그런 다음 실행할 코드로 마무리합니다: x <- reactiveVal(1:10) testServer(summaryServer, args = list(var = x), { print(range_val()) print(output$min) }) #> [1] 1 10 #> [1] "1"

다시 말하지만, 이것을 test_that() 안에 넣고 expect_ 함수를 호출하여 자동화된 테스트로 전환할 수 있습니다. 여기서는 리액티브 입력이 변경될 때 모듈이 올바르게 반응하는지 확인하는 테스트로 모두 감싸습니다: test_that("output updates when reactive input changes", { x <- reactiveVal() testServer(summaryServer, args = list(var = x), { x(1:10) session$flushReact() expect_equal(range_val(), c(1, 10)) expect_equal(output$mean, "5.5") x(10:20) session$flushReact() expect_equal(range_val(), c(10, 20)) expect_equal(output$min, "10") }) }) #> 테스트 통과 여기서 중요한 트릭이 하나 있습니다: x는 testServer() 바깥에서 만들어지므로, x를 변경해도 리액티브 그래프가 자동으로 업데이트되지 않습니다. 따라서 session$flushReact()를 호출하여 수동으로这样做해야 합니다. 모듈이 반환 값(리액티브 또는 리액티브 목록)을 가지는 경우, session$getReturned()로 캡처할 수 있습니다. 그런 다음 다른 리액티브와 마찬가지로 해당 리액티브의 값을 확인할 수 있습니다: datasetServer <- function(id) { moduleServer(id, function(input, output, session) { reactive(get(input$dataset, "package:datasets")) }) } test_that("can find dataset", { testServer(datasetServer, { dataset <- session$getReturned()

session$setInputs(dataset = "mtcars") expect_equal(dataset(), mtcars) session$setInputs(dataset = "iris") expect_equal(dataset(), iris) }) }) #> 테스트 통과 input$dataset이 데이터 세트가 아닌 경우 어떻게 되는지 테스트해야 하나요? 이 경우에는 모듈 UI가 옵션을 유효한 선택지로 제한한다는 것을 알고 있으므로 테스트할 필요가 없습니다. 서버 함수만으로는 그것이 분명하지 않습니다. 한계 testServer()는 앱의 시뮬레이션입니다. 시뮬레이션은 리액티브 코드를 빠르게 테스트할 수 있게 해주므로 유용하지만, 완전하지는 않습니다. 실제 세계와 달리 시간은 자동으로 진행되지 않습니다. 따라서 reactiveTimer()나 invalidateLater()에 의존하는 코드를 테스트하려면 session$elapse(millis = 300)을 호출하여 시간을 수동으로 진행해야 합니다. testServer()는 UI를 무시합니다. 이것은 입력이 기본값을 받지 못하고 JavaScript가 작동하지 않는다는 뜻입니다. 가장 중요한 것은 update* 함수를 테스트할 수 없다는 것입니다. 사용자 상호작용을 시뮬레이션하기 위해 브라우저에 JavaScript를 보내기 때문입니다. 이런 코드를 테스트하려면 다음 기술이 필요합니다. JavaScript 테스트 testServer()는 전체 Shiny 앱의 제한된 시뮬레이션일 뿐이므로, "진짜" 브라우저 실행에 의존하는 코드는 작동하지 않습니다. 가장 중요한 것은, JavaScript가 실행되지 않는다는 것입니다. 이 책에서 JavaScript에 대해 논의하지 않았으므로 중요하지 않아 보일 수 있지만, 많은 중요한 Shiny 함수가 내부적으로 JavaScript를 사용합니다:

모든 update*() 함수: "입력 업데이트". showNotification()/removeNotification(): "알림". showModal()/hideModal(): "명시적 확인". insertUI()/removeUI()/appendTab()/insertTab()/r emoveTab(): 나중에 이 책에서 다루겠습니다. 이런 함수를 테스트하려면 실제 브라우저에서 Shiny 앱을 실행해야 합니다. 물론 runApp()을 사용하고 클릭하여 스스로这样做할 수 있지만, 테스트를 자주 실행하도록 이 프로세스를 자동화하고 싶습니다. shinytest 패키지의 오프라벨 사용으로这样做할 것입니다. shinytest는 웹사이트가 권장하는 대로 사용할 수 있습니다. 앱을 사용하여 테스트 코드를 자동으로 생성하지만, 이미 testthat에 익숙하므로 다른 접근 방식을 사용하여 수동으로 테스트를 구성할 것입니다. shinytest 패키지의 R6 객체인 ShinyDriver로 작업할 것입니다. 새로운 ShinyDriver 인스턴스를 만들면 Shiny 앱을 실행하는 새로운 R 프로세스와 헤드리스 브라우저가 시작됩니다. 헤드리스 브라우저는 일반 브라우저와 똑같이 작동하지만, 상호작용할 수 있는 창이 없습니다. 상호작용의 유일한 수단은 코드를 통해서입니다. 이 기술의 주요 단점은 다른 접근 방식보다 느리다는 것입니다(가장 단순한 앱에도 적어도 1초가 걸리며), 앱의 외부만 테스트할 수 있습니다(즉, 리액티브 변수의 값을 보기가 더 어렵습니다). 기본 작동 기본 작동을 시연하기 위해, 이름으로 인사하고 재설정 버튼을 제공하는 매우 단순한 앱을 만들겠습니다: ui <- fluidPage( textInput("name", "What's your name"), textOutput("greeting"), actionButton("reset", "Reset") ) server <- function(input, output, session) { output$greeting <- renderText({

req(input$name) paste0("Hi ", input$name) }) observeEvent(input$reset, updateTextInput(session, "name", value = "")) } shinytest를 사용하려면 app <- ShinyDriver$new()로 앱을 시작하고, app$setInputs() 및 관련 함수로 상호작용한 다음, app$getValue()로 반환된 값을 가져옵니다: app <- shinytest::ShinyDriver$new(shinyApp(ui, server)) app$setInputs(name = "Hadley") app$getValue("greeting") #> [1] "Hi Hadley" app$click("reset") app$getValue("greeting") #> [1] "" shinytest의 모든 사용은 ShinyDriver$new()로 ShinyDriver 객체를 만드는 것으로 시작하며, Shiny 앱 객체 또는 Shiny 앱에 대한 경로를 받습니다. 이전에 만난 세션 객체와 비슷하게 상호작용하는 R6 객체를 반환합니다. app$setInputs()를 사용하여 사용합니다: 이름-값 쌍 세트를 받아 브라우저의 컨트롤을 업데이트한 다음 모든 리액티브 업데이트가 완료될 때까지 기다립니다. 첫 번째 차이점은 app$getValue(name)을 사용하여 값을 명시적으로 가져와야 한다는 것입니다. testServer()와 달리, ShinyDriver를 사용하여 리액티브의 값에 접근할 수 없습니다. 앱의 사용자가 볼 수 있는 것만 볼 수 있기 때문입니다. 하지만 shinytest는 볼 수 있지만 사람은 볼 수 없는 특별한 출력을 만드는 exportTestValues()라는 특별한 Shiny 함수가 있습니다. 다른 작업을 시뮬레이션할 수 있는 두 가지 다른 방법이 있습니다: app$click(name)은 name이라는 이름의 버튼을 클릭합니다. app$sendKeys(name, keys)은 name이라는 이름의 입력 컨트롤에 키 누름을 보냅니다. keys는 일반적으로

app$sendKeys(id, "Hi!")와 같은 문자열이지만, webdriver::key를 사용하여 특수 키를 보낼 수도 있습니다. 예: app$keys(id, c(webdriver::key$control, "x")). 수정자 키는 모든 후속 키 누름에 적용되므로, 수정자가 있는 키 누름과 없는 키 누름을 모두 원하면 여러 번 호출해야 합니다. 자세한 내용과 더 까다로운 방법 목록은 ?ShinyDriver를 참조하세요. 이전처럼 적절한 작업 순서를 대화형으로 파악한 후, test_that()으로 감싸고 기대를 호출하여 테스트로 전환할 수 있습니다: test_that("can set and reset name", { app <- shinytest::ShinyDriver$new(shinyApp(ui, server)) app$setInputs(name = "Hadley") expect_equal(app$getValue("greeting"), "Hi Hadley") app$click("reset") expect_equal(app$getValue("greeting"), "") }) 백그라운드 Shiny 앱과 웹 브라우저는 앱 객체가 삭제되고 가비지 컬렉터에 의해 수집될 때 자동으로 종료됩니다. 그것이 무엇을 의미하는지 익숙하지 않다면, Advanced R의 "Unbinding and the Garbage Collector" 섹션이 도움이 될 수 있습니다. 사례 연구 testServer()와 shinytest를 모두 결합하여 더 현실적인 예시를 어떻게 테스트할 수 있는지 탐색하는 사례 연구로 마무리하겠습니다. 자유 텍스트 "기타" 옵션도 제공하는 라디오 버튼 컨트롤을 사용할 것입니다. 이것은 익숙해 보일 수 있습니다. "제한된 선택 및 기타"에서 모듈을 개발하기 위한 동기로 이전에 사용했기 때문입니다: ui <- fluidPage( radioButtons("fruit", "What's your favourite fruit?", choiceNames = list(

"apple", "pear", textInput("other", label = NULL, placeholder = "Other") ), choiceValues = c("apple", "pear", "other") ), textOutput("value") ) server <- function(input, output, session) { observeEvent(input$other, ignoreInit = TRUE, { updateRadioButtons(session, "fruit", selected = "other") }) output$value <- renderText({ if (input$fruit == "other") { req(input$other) input$other } else { input$fruit } }) } 실제 계산은 꽤 단순합니다. renderText() 표현식을 별도의 함수로 추출하는 것을 고려할 수 있습니다: other_value <- function(fruit, other) { if (fruit == "other") { other } else { fruit } } 하지만 여기서의 로직은 매우 단순하고 다른 상황에 일반화할 수 없으므로这样做할 가치가 없다고 생각합니다. 이 코드를 앱에서 별도의 파일로 추출하면 코드를 더 읽기 어렵게 만들 것이라고 생각합니다. 따라서 리액티브의 기본 흐름을 테스트하는 것으로 시작하겠습니다: 기존 옵션으로 fruit을 설정한 후 올바른 값을 얻나요? fruit을 other로 설정하고 일부 자유 텍스트를 추가한 후 올바른 값을 얻나요?

test_that("returns other value when primary is other", { testServer(server, { session$setInputs(fruit = "apple") expect_equal(output$value, "apple") session$setInputs(fruit = "other", other = "orange") expect_equal(output$value, "orange") }) }) #> 테스트 통과 이것은 other 상자에 입력을 시작할 때 other가 자동으로 선택되는지 확인하지 않습니다. 안타깝게도, updateRadioButtons()에 의존하기 때문에 testServer()를 사용하여这样做할 수 없습니다: test_that("returns other value when primary is other", { testServer(server, { session$setInputs(fruit = "apple", other = "orange") expect_equal(output$value, "orange") }) }) #> ── Failure (<text>:2:3): returns other value when primary is other ─────────── #> output$value (actual) not equal to "orange" (expected). #> #> actual: "apple" #> expected: "orange" #> Backtrace: #> 1. shiny::testServer(...) #> 22. testthat::expect_equal(output$value, "orange") 이제 ShinyDriver를 사용해야 합니다: test_that("automatically switches to other", { app <- ShinyDriver$new(shinyApp(ui, server)) app$setInputs(other = "orange") expect_equal(app$getValue("fruit"), "other") expect_equal(app$getValue("value"), "orange") }) 일반적으로, testServer()를 가능한 한 많이 사용하고 실제 브라우저가 필요한 부분에만 ShinyDriver를 사용하는 것이 가장 좋습니다.

시각적 요소 테스트 코드로 올바른 모양을 설명하기 어려운 플롯이나 HTML 위젯과 같은 컴포넌트는 어떻게 할까요? 마지막, 가장 풍부하고 가장 깨지기 쉬운 테스트 기술을 사용할 수 있습니다: 영향을 받는 컴포넌트의 스크린샷을 저장합니다. 이것은 shinytest의 스크린샷 기능과 testthat의 전체 파일 스냅샷 기능을 결합합니다. "UI 함수"에서 설명한 스냅샷과 유사하게 작동하지만, .md 파일에 텍스트를 저장하는 대신 .png 파일을 만듭니다. 이것은 또한 콘솔에서 차이점을 볼 방법이 없다는 뜻이며, 대신 testthat::snapshot_review()를 실행하라는 메시지가 표시됩니다. 이 함수는 Shiny 앱을 사용하여 차이점을 시각화합니다. 스크린샷을 사용한 테스트의 주요 단점은 가장 미세한 변경이라도 괜찮은지 확인하기 위해 인간이 필요하다는 것입니다. 이것이 문제인 이유는 다른 컴퓨터에서 동일한 픽셀의 스크린샷을 생성하기 어렵기 때문입니다. 운영 체제, 브라우저 버전, 심지어 글꼴 버전의 차이로 인간에게는 똑같아 보이지만 매우 미세하게 다른 스크린샷이 생성될 수 있습니다. 이것은 일반적으로 시각적 테스트를 한 사람이 자신의 로컬 컴퓨터에서 실행하는 것이 가장 좋으며, 지속적 통합 도구에서 실행하는 것은 일반적으로 가치가 없다는 뜻입니다. 이러한 문제를 우회하는 것이 가능하지만, 상당한 과제이며 이 책의 범위를 벗어납니다. shinytest의 개별 요소 스크린샷 기능과 testthat의 전체 파일 스냅샷 기능은 모두 매우 새로운 기능이며, 이상적인 인터페이스가 무엇인지 아직 명확하지 않습니다. 따라서 당분간은 다음과 같은 코드를 사용하여 조각들을 직접 연결해야 합니다: path <- tempfile() app <- ShinyDriver$new(shinyApp(ui, server))

# 스크린샷을 임시 파일에 저장
app$takeScreenshot(path, "plot") # expect_snapshot_file(path, "plot-init.png") app$setValue(x = 2) app$takeScreenshot(path, "plot") expect_snapshot_file(path, "plot-update.png")

expect_snapshot_file()의 두 번째 인수는 이미지가 파일 스냅샷 디렉토리에 저장될 파일명입니다. these 테스트가 test-app.R이라는 파일에 있다면, 이 두 파일 스냅샷은 tests/testthat/_snaps/app/plot-init.png와 tests/testthat/_snaps/app/plot-update.png에 저장됩니다. these 파일의 이름을 짧지만, 문제가 발생했을 때 무엇을 테스트하고 있는지 상기시킬 정도로 의미 있게 유지하고 싶을 것입니다. 철학 이 장은 주로 테스트의 메커니즘에 초점을 맞추었으며, 테스트를 시작할 때 가장 중요합니다. 하지만 곧 메커니즘을 익힐 것이며, 질문은 더 구조적이고 철학적이 될 것입니다. 오표양과 오탐에 대해 생각하는 것이 유용하다고 생각합니다: 실패해야 할 때 실패하지 않고, 실패하지 말아야 할 때 실패하는 테스트를 작성하는 것이 가능합니다. 테스트를 시작할 때 가장 큰 어려움은 오표양이라고 생각합니다: 테스트가 실제로 잘못된 동작을 포착하고 있는지 어떻게 확인할까요? 하지만 이 단계를 꽤 빨리 지나갈 것이라고 생각합니다. 언제 테스트를 작성해야 하나요? 언제 테스트를 작성해야 할까요? 세 가지 기본 옵션이 있습니다: 코드를 작성하기 전에 테스트 주도 개발이라는 코딩 스타일이며, 함수가 어떻게 작동해야 하는지 정확히 알면, 구현을 시작하기 전에 그 지식을 코드로 캡처하는 것이 의미가 있습니다. 코드를 작성한 후 코드를 작성하면서 기능에 대한 걱정 목록을 마음속으로 쌓아갈 것입니다. 함수를 작성한 후, these를 테스트로 전환하여 함수가 기대한 대로 작동한다는 확신을 가질 수 있습니다.

테스트를 작성하기 시작할 때 너무 빨리 작성하는 것을 조심하세요. 함수가 여전히 활발히 진화하고 있다면, 모든 변경 사항으로 테스트를 최신 상태로 유지하는 것은 답답하게 느껴질 것입니다. 이것은 조금 더 기다려야 한다는 것을 나타낼 수 있습니다. 버그를 발견할 때 버그를 발견할 때마다, 그것을 자동화된 테스트 케이스로 전환하는 것이 좋은 관례입니다. 두 가지 장점이 있습니다. 첫째, 좋은 테스트 케이스를 만들려면 매우 최소한의 reprex를 테스트에 포함할 수 있을 때까지 문제를 끊임없이 단순화해야 합니다. 둘째, 버그가 다시는 돌아오지 않도록 할 것입니다! 요약 이 장에서는 앱을 패키지로 구성하여 testthat 패키지에서 제공하는 강력한 도구를 활용하는 방법을 보여주었습니다. 이전에 패키지를 만들어본 적이 없다면 압도적으로 보일 수 있지만, 보셨듯이 패키지는 Shiny 앱에 쉽게 적용할 수 있는 단순한 관례 세트입니다. 이것은 약간의 사전 작업이 필요하지만 큰 보상을 잠금 해제합니다: 테스트를 자동화할 수 있는 능력은 복잡한 앱을 작성하는 능력을 극적으로 향상시킵니다. 다음 장에서는 앱을 느리게 만드는 것이 무엇인지 파악하고 더 빠르게 만드는 기술을 배울 것입니다. 1 RStudio를 사용하지 않는 경우 usethis::use_test("load")처럼 usethis에 파일 이름을 제공해야 합니다. 2 usethis::use_test()와 마찬가지로, RStudio를 사용할 때만 작동합니다. 3 스냅샷 테스트는 testthat의 세 번째 판이 필요합니다. 새로운 패키지는 자동으로 testthat 3e를 사용하지만, 이전 패키지를 수동으로 업데이트해야 합니다.

22장. 보안 대부분의 Shiny 앱은 회사 방화벽 내에서 배포되며, 일반적으로 동료들이 앱을 해킹하려 하지 않을 것이라고 가정할 수 있으므로1 보안에 대해 생각할 필요가 없습니다. 하지만 앱에 일부 동료만 접근할 수 있는 데이터가 포함되어 있거나, 앱을 공개에 노출하고 싶다면 보안에 시간을 투자해야 합니다. 앱을 보호할 때 보호해야 할 두 가지 주요 사항이 있습니다: 데이터: 공격자가 민감한 데이터에 접근할 수 없도록 해야 합니다. 컴퓨팅 자원: 공격자가 비트코인을 채굴하거나 서버를 스팸 농장의 일부로 사용할 수 없도록 해야 합니다. 다행히 보안은 팀 스포츠이므로 작업이 약간 더 쉬워집니다. 앱을 배포하는 사람은 앱 간 보안을 담당하여 앱 A가 앱 B의 코드나 데이터에 접근할 수 없고, 서버의 모든 메모리와 컴퓨팅 자원을 탈취할 수 없도록 합니다. 당신의 책임은 앱 내부의 보안이며, 공격자가 목표를 달성하기 위해 앱을 악용할 수 없도록 합니다. 이 장은 데이터 보호와 컴퓨팅 자원 보호로 나누어 Shiny 보호의 기본 사항을 제공합니다. 보안과 R에 대해 더 자세히 알고 싶다면, Colin Gillespie의 재미있고 교육적인 useR! 2029 발표 "R and Security"를 강력히 추천합니다. shiny를 로드하면서 시작하겠습니다: library(shiny) 데이터 가장 민감한 데이터는 개인 식별 정보(PII), 규제 데이터, 신용 카드 데이터, 건강 데이터, 또는 공개될 경우 회사에 법적 악몽이 될 수 있는 기타 모든 것입니다. 다행히 대부분의 Shiny 앱은 이러한 유형의 데이터를 다루지 않지만2, 실제로 걱정해야 하는 중요한 유형의 데이터가 있습니다: 비밀번호입니다. 앱의 소스 코드에 비밀번호를 포함해서는 안 됩니다. 대신 환경 변수에 넣거나, 많은 경우 config 패키지를 사용하세요. 어떤 경우든适当的 파일을 .gitignore에 추가하여 소스 코드 제어에 포함되지 않도록 합니다. 새로운 개발자가适当한 자격 증명을 얻는 방법을 문서화하는 것도 추천합니다. 대신 사용자별 데이터가 있을 수 있습니다. 사용자를 인증해야 하는 경우(즉, 사용자 이름과 비밀번호를 통해 식별), 직접 해결책을 만들려 하지 마세요. 잘못될 수 있는 것이 너무 많습니다. 대신 IT 팀과 협력하여 안전한 접근 메커니즘을 설계해야 합니다. "Kerberos with RStudio Pro Products"와 "Securing Deployed Content" RStudio 문서에서 일부 모범 사례를 볼 수 있습니다. server() 내부의 코드는 격리되어 있으므로 한 사용자 세션이 다른 세션의 데이터를 볼 방법이 없다는 점에 주목하세요. 유일한 예외는 캐싱을 사용하는 경우입니다. 자세한 내용은 "캐싱 범위"를 참조하세요. 마지막으로, Shiny 입력은 클라이언트 측 유효성 검사를 사용한다는 점에 주목하세요. 즉, 유효한 입력에 대한 확인은 R이 아닌 브라우저의 JavaScript에 의해 수행됩니다. 이것은 지식이 있는 공격자가 기대하지 않는 값을 보낼 수 있다는 뜻입니다. 예를 들어, 다음 단순한 앱을 살펴봅시다: secrets <- list( a = "my name", b = "my birthday", c = "my social security number", d = "my credit card" ) allowed <- c("a", "b") ui <- fluidPage( selectInput("x", "x", choices = allowed), textOutput("secret") ) server <- function(input, output, session) { output$secret <- renderText({

secrets[[input$x]] }) } 사용자가 내 이름과 생년월일에는 접근할 수 있지만 사회 보장 번호나 신용 카드 정보에는 접근할 수 없을 것이라고 기대할 수 있습니다. 하지만 지식이 있는 공격자는 브라우저에서 JavaScript 콘솔을 열고 Shiny.setInputValue("x", "c")를 실행하여 나의 SSN을 볼 수 있습니다. 안전하게 사용하려면 R 코드에서 모든 사용자 입력을 확인해야 합니다: server <- function(input, output, session) { output$y <- renderText({ req(secrets$x %in% allowed) secrets$y[[secrets$x == input$x]] }) } 의도적으로 사용자 친화적인 오류 메시지를 만들지 않았습니다. 앱을 깨뜨리려고 할 때만 보게 될 것이며, 공격자를 도울 필요가 없습니다. 컴퓨팅 자원 다음 앱이 매우 위험하다는 것은 아마 분명할 것입니다. 사용자가 원하는 모든 R 코드를 실행할 수 있게 하기 때문입니다. 중요한 파일을 삭제하거나, 데이터를 수정하거나, 앱 사용자에게 기밀 데이터를 보낼 수 있습니다: ui <- fluidPage( textInput("code", "Enter code here"), textOutput("results") ) server <- function(input, output, session) { output$results <- renderText({ eval(parse(text = input$code)) }) } 일반적으로, parse()와 eval()의 조합은 모든 Shiny 앱에 대한 큰 경고 신호입니다.3 이것들은 즉시 앱을 취약하게 만듭니다. 마찬가지로,

업로드된 .R 파일을 source()하거나 업로드된 .Rmd를 rmarkdown::render()해서는 안 됩니다. 하지만 these 경우는 꽤 분명하며 실제 문제의 원인이 될 가능성이 낮습니다. 더 큰 과제는 aware하지 못하는 방식으로 parse(), eval(), 또는 둘 다를 사용하는 여러 함수가 있기 때문에 발생합니다. 가장 일반적인 것들은 다음과 같습니다: 모델 수식 임의의 R 코드를 실행하는 모델을 구성하는 것이 가능합니다: df <- data.frame(x = 1:5, y = runif(5)) mod <- lm(y ~ {print("Hi!"); x}, data = df) #> [1] "Hi!" 이것은 사용자가 자체 모델을 안전하게 정의하기 어렵게 만듭니다. 글루 레이블 glue 패키지는 데이터로부터 문자열을 만드는 강력한 방법을 제공합니다: title <- "foo" number <- 1 glue::glue("{title}-{number}") #> foo-1 하지만 glue()는 {} 안의 모든 것을 평가합니다: glue::glue("{title}-{print('Hi'); number}") #> [1] "Hi" #> foo-1

사용자가 레이블을 생성하기 위해 글루 문자열을 제공할 수 있게 하려면, 대신 변수 이름만 찾고 코드를 평가하지 않는 glue::glue_safe()를 사용하세요: glue::glue_safe("{title}-{number}") #> foo-1 glue::glue_safe("{title}-{print('Hi'); number}") #> Error in.transformer(expr, env): object 'print('Hi'); number' not found 변수 변환 dplyr 또는 ggplot2의 변수를 변환하기 위해 사용자가 코드 조각을 제공할 수 있게 안전하게 하는 방법은 없습니다. log10(x)를 쓸 것이라고 기대할 수 있지만, {print("Hi"); log10(x)}를 쓸 수 있습니다. 이것은 또한 사용자가 제공하는 입력으로 이전 ggplot2::aes_string()을 사용해서는 안 된다는 뜻입니다. 대신 12장의 기술을 고수하세요. SQL에서도 같은 문제가 발생할 수 있습니다. 예를 들어, paste()로 SQL을 구성하면: find_student <- function(name) { paste0("SELECT * FROM Students WHERE name = ('", name, "');") } find_student("Hadley") #> [1] "SELECT * FROM Students WHERE name = ('Hadley');" 공격자가 악의적인 사용자 이름을 제공할 수 있습니다:4 find_student("Robert'); DROP TABLE Students; --") #> [1] "SELECT * FROM Students WHERE name = ('Robert'); DROP TABLE Students; --');"

이것은 좀 이상해 보이지만, 세 부분으로 구성된 유효한 SQL 쿼리입니다: SELECT * FROM Students WHERE name = ('Robert'); Robert라는 이름의 학생을 찾습니다. DROP TABLE Students; Students 테이블을 삭제합니다(!). --'는 추가 '가 구문 오류로 변하는 것을 방지하는 데 필요한 주석입니다. 이 문제를 피하려면 paste로 SQL 문자열을 생성하지 말고, 자동으로 사용자 입력을 이스케이프하는 시스템(예: dbplyr)을 사용하거나 glue::glue_sql()을 사용하세요: con <- DBI::dbConnect(RSQLite::SQLite(), ":memory:") find_student <- function(name) { glue::glue_sql("SELECT * FROM Students WHERE name = ({name});",.con = con) } find_student("Robert'); DROP TABLE Students; --") #> <SQL> SELECT * FROM Students WHERE name = ('Robert''); DROP TABLE Students; --'); 처음 보기에 안전해 보이기 어렵지만, 이것은 안전합니다. SQL의 \'에 해당하는 것이 ''이기 때문입니다. 따라서 쿼리는 이름이 문자 그대로 "Robert'); DROP TABLE Students; –"인 Students 테이블의 모든 행을 반환합니다. 1 그렇게 가정할 수 없다면 더 큰 문제가 있습니다! 하지만 일부 회사는 "제로 신뢰" 모델을 가지고 있으므로 IT 팀과 다시 확인해야 합니다. 2 앱이 these 유형의 데이터로 작동하는 경우, 보안 전문 지식을 가진 소프트웨어 엔지니어와 협력하는 것이 필수적입니다. 3 유일한 예외는 사용자 제공 데이터를 어떤 방식으로든 포함하지 않는 경우입니다. 4 이 예는 Little Bobby Tables에서 영감을 받았습니다.

23장. 성능 Shiny 앱은 올바르게 개발되면 수천 명에서 수만 명의 사용자를 지원할 수 있습니다. 하지만 대부분의 Shiny 앱은 시급한 분석 필요를 해결하기 위해 빠르게 만들어지며, 일반적으로 열악한 성능으로 시작합니다. 이것은 Shiny의 특징입니다: 많은 사람들이 동시에 사용할 수 있도록 빠르게 만들기 전에, 작동하는 개념 증명을 빠르게 프로토타이핑할 수 있게 해줍니다. 다행히 몇 가지 간단한 조정으로 일반적으로 10-100배 성능을 얻는 것이 비교적 쉽습니다. 이 장에서는 어떻게这样做하는지 보여줄 것입니다. 은유로 시작하겠습니다: Shiny 앱을 레스토랑처럼 생각하기. 그런 다음 shinyloadtest 패키지를 사용하여 많은 사람들이 동시에 앱을 사용하는 것을 시뮬레이션하여 앱을 벤치마킹하는 방법을 배울 것입니다. 이것이 시작점입니다. 문제가 있음을 파악하고 수행하는 모든 변경의 영향을 측정하는 데 도움이 되기 때문입니다. 그런 다음 profvis 패키지를 사용하여 R 코드의 느린 부분을 식별하기 위해 앱을 프로파일링하는 방법을 배울 것입니다. 프로파일링은 코드가 시간을 어디에 쓰고 있는지 정확히 볼 수 있게 해주어, 가장 효과적인 곳에 노력을 집중할 수 있습니다. 마지막으로, 코드를 최적화하고 필요한 곳에서 성능을 향상시키는 몇 가지 유용한 기술을 배울 것입니다. 리액티브를 캐싱하는 방법, 앱에서 데이터 준비 코드를 옮기는 방법, 그리고 앱이 가능한 한 빠르게 느껴지도록 적용 심리학을 약간 사용하는 방법을 배울 것입니다. 벤치마킹, 프로파일링, 최적화의 전체 과정 데모를 위해 Joe Cheng의 rstudio::conf(2019) 기조 발표 "Shiny in Production: Principles, Practices, and Tools"를 시청하는 것을 추천합니다. 그 발표(및 관련 사례 연구)에서 Joe는 현실적인 앱으로 전체 과정을 안내합니다. library(shiny)

이 장을 작성할 때 특히 도움이 되었던 RStudio 동료 Joe Cheng, Sean Lopp, Alan Dipert에게 특별히 감사드립니다. 레스토랑에서 식사하기 성능을 고려할 때, Shiny 앱을 레스토랑처럼 생각하는 것이 유용합니다.1 각 고객(사용자)이 레스토랑(서버)에 들어와 주문(요청)을 하면, 요리사(R 프로세스)가 준비합니다. 이 은유는 유용합니다. 레스토랑처럼 하나의 R 프로세스가 여러 사용자에게 동시에 서비스할 수 있으며, 증가하는 수요를 처리하는 유사한 방법이 있기 때문입니다. 시작으로 현재 요리사를 더 효율적으로 만드는 방법을 조사할 수 있습니다(R 코드를 최적화). 그렇게 하려면 먼저 요리사가 일하는 것을 관찰하여 방법의 병목 현상을 찾고(프로파일링), 더 빠르게 작업할 수 있는 방법에 대해 브레인스토밍합니다(최적화). 예를 들어, 첫 번째 고객이 오기 전에 들어와 채소를 썰 수 있는 준비 요리사를 고용하거나(데이터 준비), 시간 절약 가젯(더 빠른 R 패키지)에 투자할 수 있습니다. 또는 레스토랑(서버)에 더 많은 요리사(프로세스)를 추가하는 것을 고려할 수 있습니다. 다행히 훈련된 요리사를 고용하는 것보다 프로세스2를 추가하는 것이 훨씬 쉽습니다. 더 많은 요리사를 계속 고용하면 결국 주방(서버)이 너무 차서 더 많은 장비(메모리 또는 코어)를 추가해야 합니다. 서버가 더 많은 프로세스를 실행할 수 있도록 더 많은 자원을 추가하는 것을 스케일링 업이라고 합니다.3 어느 시점에서, 레스토랑에 더 이상 요리사를 넣을 수 없을 정도로 많은 요리사를 넣었지만, 수요를 충족하기에 충분하지 않을 것입니다. 그 시점에서 더 많은 레스토랑을 건축해야 합니다. 이것은 스케일링 아웃이라고 합니다.4 Shiny의 경우 여러 서버를 사용하는 것을 의미합니다. 스케일링 아웃을 사용하면 인프라 비용을 지불할 수 있는 한 모든 수의 고객을 처리할 수 있습니다. 이 장에서는 스케일링 아웃에 대해 더 논의하지 않겠습니다. 세부 사항은 간단하지만 배포 인프라에 완전히 의존하기 때문입니다.

은유가 무너지는 주요 장소가 하나 있습니다: 일반적인 요리사는 여러 요리를 동시에 만들 수 있으며, 한 레시피의 다운타임을 활용하여 다른 레시피에 작업할 수 있도록 단계를 신중하게 엮습니다. 하지만 R은 단일 스레드이므로 동시에 여러 일을 할 수 없습니다. 모든 요리가 빠르게 조리된다면 괜찮지만, 누군가 24시간 소비 Pork Belly를 요청하면, 이후의 모든 고객은 요리사가 그들의 요리를 시작하기 전에 24시간을 기다려야 합니다. 다행히 이 제한은本书의 범위를 벗어나는 복잡한 주제인 비동기 프로그래밍을 사용하여 우회할 수 있습니다. 벤치마킹 거의 항상 자신을 위해 앱을 개발하는 것으로 시작합니다: 앱은 한 번에 한 명의 고객(당신!)에게만 서비스하는 개인 요리사입니다. 현재 성능에 만족할 수 있지만, 동시에 앱을 사용해야 하는 10명을 처리할 수 없을까 걱정할 수도 있습니다. 벤치마킹을 사용하면 실제 사람들이 잠재적으로 느린 앱에 노출되지 않고 여러 사용자로 앱의 성능을 확인할 수 있습니다. 또는 수백 명이나 수천 명의 사용자에게 서비스하고 싶다면, 벤치마킹은 각 프로세스가 얼마나 많은 사용자를 처리할 수 있는지, 따라서 얼마나 많은 서버를 사용해야 하는지 파악하는 데 도움이 됩니다. 벤치마킹 프로세스는 shinyloadtest 패키지에 의해 지원되며 세 가지 기본 단계가 있습니다:
1. shinyloadtest::record_session()을 사용하여 일반적인 사용자를 시뮬레이션하는 스크립트를 녹화합니다.
2. shinycannon 명령줄 도구를 사용하여 여러 동시 사용자로 스크립트를 재생합니다.
3. shinyloadtest::report()를 호출하여 결과를 분석합니다.
여기서는 각 단계가 어떻게 작동하는지에 대한 개요를 제공할 것입니다. 더 자세한 내용이 필요하다면 shinyloadtest의 문서와 비네트를 확인하세요.

녹화 노트북에서 벤치마킹하는 경우, 두 개의 서로 다른 R 프로세스를 사용해야 합니다. 하나는 Shiny용, 다른 하나는 shinyloadtest용입니다.5 첫 번째 프로세스에서 앱을 시작하고 제공하는 URL을 복사합니다: runApp("myapp.R") #> Listening on http://127.0.0.1:7716 두 번째 프로세스에서 record_session() 호출에 URL을 붙여넣습니다: shinyloadtest::record_session("http://127.0.0.1:7716") record_session()은 앱의 버전을 포함하는 새 창을 열며, 당신이 하는 모든 것을 녹화합니다. 이제 "일반적인" 사용자를 시뮬레이션하기 위해 앱과 상호작용해야 합니다. 서면 스크립트로 시작하여 작업을 안내하는 것을 추천합니다. 나중에 중요한 부분이 누락된 것으로 발견되면 반복하기 더 쉬울 것이기 때문입니다. 벤치마킹은 시뮬레이션만큼만 좋을 것이므로, 앱과의 현실적인 상호작용을 시뮬레이션하는 방법에 대해 생각하는 시간을 투자해야 합니다. 예를 들어, 실제 사용자가 필요로 하는 사고 시간을 반영하기 위해 일시정지를 추가하는 것을 잊지 마세요. 완료하면 앱을 닫으십시오. shinyloadtest는 recording.log를 작업 디렉토리에 저장합니다. 이것은 쉽게 재생할 수 있는 방식으로 모든 작업을 기록합니다. 다음 단계에서 필요할 것이므로 보관해두세요. (벤치마킹은 노트북에서 잘 작동하지만, 가장 정확한 결과를 얻기 위해 최종 배포를 가능한 한 가깝게 시뮬레이션하고 싶을 것입니다. 따라서 회사가 Shiny 앱을 제공하는 특별한 방법이 있다면, 부하 테스트에 사용할 수 있는 환경을 설정하는 것에 대해 IT 팀에 문의하세요.)

재생 이제 단일 사용자의 작업을 나타내는 스크립트가 있으므로, shinycannon이라는 특별한 도구를 사용하여 많은 사람을 시뮬레이션하는 데 사용할 것입니다. shinycannon은 R 패키지가 아니므로 설치하는 데 약간의 추가 작업이 필요합니다. Java로 작성되었습니다. Java 언어는 가능한 한 적은 컴퓨팅 자원을 사용하여 수십 또는 수백 개의 웹 요청을 병렬로 수행하는 문제에 특히 적합하기 때문입니다. 이것은 노트북이 앱을 실행하면서도 많은 사용자를 시뮬레이션할 수 있게 해줍니다. 먼저 RStudio의 지침에 따라 shinycannon을 설치하세요. 그런 다음 다음과 같은 명령으로 터미널에서 shinycannon을 실행합니다: shinycannon recording.log http://127.0.0.1:7911 \ --workers 10 \ --loaded-duration-minutes 5 \ --output-dir run1 shinycannon에 대한 인수 여섯 가지가 있습니다: 첫 번째 인수는 이전 단계에서 만든 녹화에 대한 경로입니다. 두 번째 인수는 Shiny 앱의 URL입니다(이전 단계에서 복사하여 붙여넣었습니다). --workers는 시뮬레이션할 별렬 사용자 수를 설정합니다. 이전 명령은 10명이 동시에 사용하는 것처럼 앱의 성능을 시뮬레이션합니다. --loaded-duration-minutes는 테스트를 실행하는 시간을 결정합니다. 이것이 스크립트보다 길면 shinycannon은 스크립트를 처음부터 다시 시작합니다. --output-dir은 출력을 저장할 디렉토리 이름을 제공합니다. 성능 개선을 실험하면서 부하 테스트를 여러 번 실행할 것이므로, these 디렉토리에 유익한 이름을 부여하려고 노력하세요.

부하 테스트를 처음 실행할 때는 주요 문제를 빠르게 발견하기 위해 적은 수의 워커와 짧은 기간으로 시작하는 것이 좋습니다. 분석 이제 여러 사용자로 앱을 시뮬레이션했으니, 결과를 살펴볼 시간입니다. 먼저 load_runs()를 사용하여 데이터를 R로 로드합니다: library(shinyloadtest) df <- load_runs("scaling-testing/run1") 이것은 원하는 경우 수동으로 분석할 수 있는 깔끔한 tibble을 생성합니다. 하지만 일반적으로 표준 shinyloadtest 보고서를 생성할 것입니다. 이것은 Shiny 팀이 가장 유용하다고 발견한 그래픽 요약을 포함하는 HTML 보고서입니다: shinyloadtest_report(df, "report.html") 여기서 보고서의 모든 페이지를 논의하지는 않겠습니다. 대신 가장 중요한 플롯이라고 생각하는 세션 지속 시간에 초점을 맞추겠습니다. 다른 페이지에 대해 더 자세히 알고 싶다면 "분석 부하 테스트 로그" 기사를 읽는 것을 강력히 추천합니다.

세션 지속 시간 플롯은 각 시뮬레이션된 사용자 세션을 행으로 표시합니다. 각 이벤트는 시간에 비례하는 너비를 가진 직사각형으로, 이벤트 유형별로 색상이 지정됩니다. 빨간색 선은 원래 녹화에 걸린 시간을 나타냅니다. 이 플롯을 볼 때 다음 질문을 고려하세요: 앱이 단일 사용자와 동일하게 부하 하에서 작동합니까? 그렇다면 축하합니다! 앱이 이미 충분히 빠르며 이 장을 계속 읽을 필요가 없습니다. "홈페이지"에 느림이 있습니까? 그렇다면 UI 함수를 사용하고 있으며, 거기서 실수로 너무 많은 작업을 하고 있을 수 있습니다. "세션 시작"이 느립니까? 이것은 서버 함수의 실행이 느리다는 것을 시사합니다. 일반적으로 서버 함수를 실행하는 것은 빨라야 합니다. 수행하는 것이 리액티브 그래프를 정의하는 것(다음 단계에서 실행됨)뿐이기 때문입니다. 느리다면, 비용이 많이 드는 코드를 server() 바깥으로 옮기거나(앱 시작 시 한 번 실행) 리액티브 안으로 옮기세요(필요 시 실행). 그렇지 않으면, 그리고 가장 일반적으로, "계산"에 느림이 있을 것이며, 이것은 리액티브의 일부 계산이 느리다는 것을 나타내며, 나머지 장의 기술을 사용하여 병목 현상을 찾아 수정해야 합니다. 프로파일링 앱이 계산에 많은 시간을 소비하고 있다면, 다음으로 어떤 계산이 느린지 파악해야 합니다. 즉, 프로파일링하여 병목 현상을 찾아야 합니다. profvis 패키지를 사용하여 프로파일링할 것입니다. profvis는 utils::Rprof()가 수집한 프로파일링 데이터의 대화형 시각화를 제공합니다. 먼저 프로파일링에 사용되는 화염 그래프를 소개한 다음, profvis를 사용하여 R 코드와 Shiny 앱을 프로파일링하는 방법을 보여드리겠습니다. 화염 그래프

프로그래밍 언어 전반에서 프로파일링 데이터를 시각화하는 데 사용되는 가장 일반적인 도구는 화염 그래프입니다. 이해를 돕기 위해, 코드 실행의 기본 사항을 다시 검토한 다음, 최종 시각화까지 점진적으로 구축하겠습니다. 이 과정을 구체적으로 만들기 위해, 다음 코드로 작업할 것입니다. 여기서는 pause()(곧 더 자세히 설명)를 사용하여 수행 중인 작업을 나타냅니다: library(profvis) f <- function() { pause(0.2) g() h() 10 } g <- function() { pause(0.1) h() } h <- function() { pause(0.3) } f()를 정신적으로 실행한 다음 어떤 함수가 호출되었는지 설명하라고 한다면, 다음과 같은 말을 할 수 있을 것입니다: f()로 시작합니다. 그런 다음 f()는 g()를 호출합니다. 그런 다음 g()는 h()를 호출합니다. 그런 다음 f()는 h()를 호출합니다. 이것은 호출이 어떻게 중첩되는지 정확히 볼 수 없기 때문에 따라하기 어렵습니다. 대신 더 개념적인 설명을 채택할 수 있습니다: f f > g

f > g > h f > h 여기서는 "스택 트레이스 읽기"에서 디버깅에 대해 논의할 때 기억할 수 있는 호출 스택 목록을 기록했습니다. 호출 스택은 함수로 이어지는 호출의 완전한 시퀀스입니다. 각 함수 이름 주위에 직사각형을 그려 해당 목록을 다이어그램으로 변환할 수 있습니다: 시간이 위에서 아래로 흐르는 것이 가장 자연스럽다고 생각합니다. 코드 실행에 대해 일반적으로 생각하는 것과 같은 방식입니다. 하지만 관례에 따라 화염 그래프는 시간이 왼쪽에서 오른쪽으로 흐르도록 그려지므로, our 다이어그램을 90도 회전합니다:

이 다이어그램을 더 유익하게 만들려면 각 호출의 너비를 걸리는 시간에 비례하도록 할 수 있습니다. 작업을 확인하기 쉽게 하기 위해 배경에 격자선도 추가했습니다:

마지막으로, 인접한 동일 함수 호출을 결합하여 조금 정리할 수 있습니다:

이것이 화염 그래프입니다! f()가 실행되는 데 얼마나 걸리는지, 그리고 왜 그렇게 오래 걸리는지(즉, 시간이 어디에 쓰이는지)를 쉽게 볼 수 있습니다. 왜 화염 그래프라고 불리는지 궁금할 수 있습니다. 야생의 대부분의 화염 그래프는 컴퓨터가 "뜨겁게" 실행된다는 아이디어를 불러일으키는 "따뜻한" 색상으로 무작위로 채색됩니다. 하지만 those 색상은 추가 정보를 제공하지 않으므로, 우리는 일반적으로 그것들을 생략하고 흑백을 고수합니다. 이 색 구성표, 대안, 그리고 화염 그래프의 역사를 "The Flame Graph"에서 더 배울 수 있습니다. R 코드 프로파일링 화염 그래프를 이해했으니, profvis 패키지를 사용하여 실제 코드에 적용해봅시다. 사용하기 쉽습니다: 프로파일링할 코드를 profvis::profvis()로 감싸기만 하면 됩니다: profvis::profvis(f()) 코드가 완료되면 profvis가 그림 23-1에 표시된 대로 대화형 시각화를 표시합니다. 그림에 손으로 그린 것과 매우

비슷해 보이지만, 타이밍이 정확히 같지는 않을 것입니다. 이것은 R의 프로파일러가 10ms마다 실행을 중단하고 호출 스택을 기록하기 때문입니다. 안타깝게도, R이 중단할 수 없는 무언가를 수행 중일 때 원하는 정확한 시간에 항상 중단할 수는 없습니다. 이것은 결과에 약간의 무작위 변동이 있을 수 있다는 뜻입니다. 이 코드를 다시 프로파일링하면 약간 다른 결과가 나올 것입니다. 그림 23-1. profvis로 f()를 프로파일링한 결과. X축은 경과 시간(ms), Y축은 호출 스택의 깊이를 나타냅니다. 화염 그래프 외에도, profvis는 기반 코드를 찾아 표시하기 위해 최선을 다하므로 화염 그래프의 함수를 클릭하여 정확히 무엇이 실행되는지 볼 수 있습니다. Shiny 앱 프로파일링 Shiny 앱을 프로파일링할 때는 많이 변경되지 않습니다. 차이점을 보기 위해 f()를 감싸는 매우 단순한 앱을 만들겠습니다: ui <- fluidPage( actionButton("x", "Push me"), textOutput("y") ) server <- function(input, output, session) { output$y <- eventReactive(input$x, f()) }

# 여기서 runApp()의 명시적 호출에 주목하세요: 이것은 중요합니다

# 그렇지 않으면 앱이 실제로 실행되지 않습니다.

profvis::profvis(runApp(shinyApp(ui, server))) 결과는 그림 23-2에 표시됩니다.

그림 23-2. f()를 사용하는 Shiny 앱을 프로파일링한 결과. 호출 스택이 더 깊고 몇 개의 높은 탑이 있다는 점에 주목하세요. 출력은 마지막 실행과 매우 비슷해 보입니다. 몇 가지 차이점이 있습니다: f()는 더 이상 호출 스택의 바닥에 없습니다. 이제 eventReactiveHandler()(eventReactive()를 구동하는 내부 함수)에 의해 호출되므로 네 번째 단계에 있습니다. 이것은 output$y에 의해 트리거되며, runApp() 안에 래핑되어 있습니다. 두 개의 매우 높은 탑이 있습니다. 일반적으로 이것들은 무시할 수 있습니다. 많은 시간을 차지하지 않으며 샘플러의 확률적 특성으로 인해 실행마다 달라지기 때문입니다. 이것들에 대해 더 자세히 알고 싶다면, 호버하여 함수 호출을 알 수 있습니다. 이 경우 왼쪽의 짧은 탑은 eventReactive() 호출의 설정이고, 오른쪽의 높은 탑은 R의 바이트 코드 컴파일러가 트리거된 것입니다. 더 자세한 내용은 profvis 문서, 특히 FAQ를 추천합니다. 한계 프로파일링의 가장 중요한 한계는 작동 방식 때문입니다: R은 프로세스를 중단하고 현재 실행 중인 R 함수를 검사해야 합니다. 이것은 R이 제어권을 가져야 한다는 뜻니다. 이것이 일어나지 않는 몇 가지 장소가 있습니다: 특정 C 함수는 사용자 중단을 정기적으로 확인하지 않습니다. 이것은 Esc/Ctrl+C로 취소할 수 없는 것과 같은 C 함수입니다. 이것은 일반적으로 좋은 프로그래밍 관행이 아니지만, 실제로 존재합니다. Sys.sleep()은 운영 체제에 프로세스를 일정 시간 동안 "주차"하도록 요청하므로, R이 실제로 실행되지 않습니다. 이것이 이전에 profvis::pause()를 사용해야 했던 이유입니다.

인터넷에서 데이터를 다운로드하는 것은 일반적으로 다른 프로세스에서 수행되므로 R에 의해 추적되지 않습니다. 성능 개선 성능을 개선하는 가장 효율적인 방법은 프로파일에서 가장 느린 것을 찾아 가속화하는 것입니다. 느린 부분을 격리한 후 별도의 함수(18장)로 감싸져 있는지 확인하세요. 그런 다음 느림을 재현하는 최소한의 코드 조각을 만들고, 제대로 캡처했는지 확인하기 위해 다시 프로파일링하세요. 가능한 개선을 시도하면서 이 코드 조각을 여러 번 다시 실행할 것입니다. 테스트를 몇 개 작성하는 것도 추천합니다.2 제 경험상 코드를 더 빠르게 만드는 가장 쉬운 방법은 부정확하게 만드는 것이기 때문입니다. Shiny 코드는 그냥 R 코드이므로, 성능을 개선하는 대부분의 기술은 일반적입니다. 시작하기 좋은 두 곳은 Advanced R의 "Performance 개선" 섹션과 Colin Gillespie, Robin Lovelace의 Efficient R Programming입니다. 여기서 their 조언을 반복하지 않겠습니다. 대신 Shiny 앱에 가장 영향을 미칠 가능성이 높은 기술에 초점을 맞추겠습니다. Alan Dipert의 rstudio::conf(2018) 발표 "Make Shiny Fast by Doing as Little Work as Possible"도 강력히 추천합니다. 기존 코드가 기대보다 자주 실행되는 문제를 해결하는 것으로 시작하세요. 여러 리액티브에서 동일한 작업을 반복하고 있지 않은지, 그리고 리액티브 그래프가 기대보다 자주 업데이트되지 않는지 확인하세요("Reactlog 패키지"). 그런 다음 캐싱을 사용하여 느린 계산을 기억하고 재생하는 것으로 앱의 성능을 개선하는 가장 쉬운 방법을 논의할 것입니다. 비용이 많이 드는 전처리를 별도의 단계로 추출하고 사용자 기대를 신중하게 관리하는 두 가지 다른 기술로 마무리하겠습니다. 캐싱

캐싱은 코드 성능을 개선하는 매우 강력한 기술입니다. 기본 아이디어는 함수에 대한 모든 호출의 입력과 출력을 기록하는 것입니다. 캐시 함수가 이미 본 입력 세트로 호출되면, 다시 계산하지 않고 기록된 출력을 재생할 수 있습니다. memoise와 같은 패키지는 일반 R 함수를 캐싱하기 위한 도구를 제공합니다. 캐싱은 캐시를 사용자 간에 공유할 수 있으므로 Shiny 앱에 특히 효과적입니다. 이것은 많은 사람들이 동일한 앱을 사용할 때, 첫 번째 사용자만 결과가 계산될 때까지 기다려야 하며, 다른 모든 사용자는 캐시에서 빠른 결과를 얻는다는 뜻입니다. Shiny는 모든 리액티브 표현식이나 render 함수를 캐싱하기 위한 일반 도구인 bindCache()를 제공합니다.6 리액티브 표현식이 이미 가장 최근에 계산된 값을 캐싱한다는 것은 알고 있을 것입니다. bindCache()는 임의의 수의 값을 캐싱하고 해당 값을 사용자 간에 공유할 수 있게 해줍니다. bindCache()의 기본 사항을 소개하고 몇 가지 실용적인 예시를 보여준 다음, 캐시 "키"와 범위의 세부 사항을 논의할 것입니다. 더 자세히 알고 싶다면 "Shiny에서 캐싱을 사용하여 성능 극대화"와 "bindCache()를 사용하여 앱 가속화"로 시작하는 것을 추천합니다. 기본 사항 bindCache()는 사용하기 쉽습니다. 캐싱할 reactive() 또는 render* 함수를 bindCache()로 파이프하기만 하면 됩니다: r <- reactive(slow_function(input$x, input$y)) %>% bindCache(input$x, input$y) output$text <- renderText(slow_function2(input$z)) %>% bindCache(input$z) 추가 인수는 캐시 키입니다. 이것들은 계산이 이전에 수행되었는지 결정하는 데 사용되는 값입니다. 몇 가지 실용적인 용도를 보여준 후 캐시 키에 대해 더 자세히 논의할 것입니다. 리액티브 캐싱

캐싱을 사용하는 일반적인 방법은 웹 API와 함께 사용하는 것입니다. API가 매우 빠르더라도 요청을 보내고, 서버가 응답할 때까지 기다리고, 결과를 파싱해야 합니다. 따라서 API 결과를 캐싱하면 종종 큰 성능 개선을 얻을 수 있습니다. GitHub API와 통신하는 gh 패키지를 사용한 간단한 예시로 이를 설명하겠습니다. 사람들이 최근에 무엇을 하고 있는지 보여주는 앱을 설계하고 싶다고 상상해봅시다. 여기서는 GitHub 이벤트 API에서 데이터를 가져와 tibble로 변환하기 위한 몇 가지 단순한 rectangling을 수행하는 작은 함수를 작성했습니다: library(purrr) latest_events <- function(username) { json <- gh::gh("/users/{username}/events/public", username = username) tibble::tibble( repo = json %>% map_chr(c("repo", "name")), type = json %>% map_chr("type"), ) } system.time(hadley <- latest_events("hadley")) #> user system elapsed #> 0.138 0.033 0.743 head(hadley) #> # A tibble: 6 x 2 #> repo type #> <chr> <chr> #> 1 hadley/r4ds IssuesEvent #> 2 hadley/mastering-shiny IssuesEvent #> 3 hadley/mastering-shiny IssueCommentEvent #> 4 hadley/mastering-shiny IssuesEvent #> 5 hadley/mastering-shiny IssueCommentEvent #> 6 hadley/mastering-shiny IssuesEvent 그리고 그것을 매우 단순한 앱으로 전환할 수 있습니다: ui <- fluidPage( textInput("username", "GitHub user name"), tableOutput("events") ) server <- function(input, output, session) {

events <- reactive({ req(input$username) latest_events(input$username) }) output$events <- renderTable(events()) } 이 앱은 사용자 이름을 입력할 때마다 15초 전에 요청했더라도 데이터를 다시 요청해야 하므로 약간 느리게 느껴질 것입니다. bindCache()를 사용하여 성능을 극적으로 개선할 수 있습니다: server <- function(input, output, session) { events <- reactive({ req(input$username) latest_events(input$username) }) %>% bindCache(input$username) output$events <- renderTable(events()) } 이 접근 방식의 문제점을 눈치챘을 수 있습니다. 내일 돌아와서 동일한 사용자의 데이터를 요청하면 어떻게 될까요? 새로운 활동이 있었더라도 오늘의 데이터를 받게 될 것입니다. 시간에 대한 암묵적인 의존성이 있으므로 이를 명시해야 합니다. Sys.Date()를 캐시 키에 추가하여 캐시가 효과적으로 하루 동안만 지속되도록这样做할 수 있습니다: server <- function(input, output, session) { events <- reactive({ req(input$username) latest_events(input$username) }) %>% bindCache(input$username, Sys.Date()) output$events <- renderTable(events()) } 캐시가 다시는 보지 않을 과거의 데이터로 꾸준히 축적될까 걱정될 수 있지만, 다행히 캐시는 고정된 총 크기를 가지며 더 많은 공간이 필요할 때 가장 최근에 사용한 데이터를 자동으로 제거할 만큼 충분히 똑똑합니다.

플롯 캐싱 대부분의 경우 리액티브를 캐싱하지만, render 함수와 함께 bindCache()를 사용할 수도 있습니다. 대부분의 render 함수는 꽤 빠르지만, 복잡한 그래픽이 있는 경우 느릴 수 있는 것이 하나 있습니다: renderPlot()입니다. 예를 들어, 다음 앱을 살펴봅시다. 직접 실행하면 각 플롯을 처음 표시할 때 ~50,000개의 점을 그려야 하므로 눈에 띄는 수백 분의 1초가 걸린다는 것을 알게 될 것입니다. 하지만 다음에 각 플롯을 그릴 때 캐시에서 검색되므로 즉시 나타납니다: library(ggplot2) ui <- fluidPage( selectInput("x", "X", choices = names(diamonds), selected = "carat"), selectInput("y", "Y", choices = names(diamonds), selected = "price"), plotOutput("diamonds") ) server <- function(input, output, session) { output$diamonds <- renderPlot({ ggplot(diamonds, aes(.data[[input$x]],.data[[input$y]])) + geom_point() }) %>% bindCache(input$x, input$y) } (.data 구문이 익숙하지 않다면 세부 사항은 12장을 참조하세요.) 플롯을 캐싱할 때 특별한 고려 사항이 있습니다: 각 플롯은 다양한 크기로 그려집니다. 기본 플롯이 브라우저 크기를 조정할 때 변하는 사용 가능한 너비의 100%를 차지하기 때문입니다. 이 유연성은 캐싱에 잘 작동하지 않습니다. 크기에 1像素이라도 차이가 나면 캐시에서 플롯을 검색할 수 없기 때문입니다. 이 문제를 피하기 위해 bindCache()는 고정된 크기로 플롯을 캐싱합니다. 기본값은 대부분의 경우 "바로 작동하도록" 신중하게 선택되었지만, 필요하다면

sizePolicy 인수로 제어할 수 있으며 ? sizeGrowthRatio에서 더 배울 수 있습니다. 캐시 키 캐시 키에 대해 간략히 논의할 가치가 있습니다: 계산이 이전에 수행되었는지 파악하는 데 사용되는 값 세트입니다. these 값은 reactive의 첫 번째 인수나 observeEvent() 또는 eventReactive()와 유사하게 리액티브 의존성을 결정하는 데에도 사용됩니다. 이것은 잘못된 캐시 키를 사용하면 매우 혼란스러운 결과를 얻을 수 있다는 뜻입니다. 예를 들어, 이것이 캐시된 리액티브라고 상상해봅시다: r <- reactive(input$x + input$y) %>% bindCache(input$x) input$y가 변경되면 r()은 다시 계산되지 않습니다. 그리고 결과가 캐시에서 검색되면, 현재 x의 값과 값이 캐시될 때 y가 가졌던 값의 합이 될 것입니다. 따라서 캐시 키는 항상 표현식의 모든 리액티브 입력을 포함해야 합니다. 하지만 리액티브에서 사용되지 않는 추가 값을 포함하고 싶을 수도 있습니다. 가장 유용한 예는 현재 날짜 또는 반올림된 현재 시간을 추가하여 캐시된 값이 고정된 시간 동안만 사용되도록 하는 것입니다. 입력 외에도 다른 reactive()를 캐시 키로 사용할 수 있지만, 가능한 한 단순하게(즉, 원자 벡터 또는 원자 벡터의 단순한 목록) 유지해야 합니다. 큰 데이터 세트를 사용하지 마세요. 큰 데이터 프레임이 이미 존재하는지 파악하는 비용이 비싸고, 이것이 캐싱의 이점을 줄일 것이기 때문입니다. 캐시 범위 기본적으로 플롯 캐시는 메모리에 저장되며, 200MB보다 크지 않으며, 단일 프로세스의 모든 사용자 간에 공유되며, 앱이 재시작되면 사라집니다. 개별 리액티브 또는 전체 세션에 대해 이 기본값을 변경할 수 있습니다:

bindCache(…, cache = "session")는 각 사용자 세션이 별도의 캐시를 사용하도록 합니다. 이것은 개인 데이터가 사용자 간에 잠재적으로 공유되지 않도록 보장하지만, 캐시의 이점도 줄입니다. shinyOptions(cache = cachem::cache_mem()) 또는 shinyOptions(cache = cachem::cache_disk())를 사용하여 전체 앱에 대한 기본 캐시를 변경할 수 있습니다. 여러 프로세스에 걸쳐 캐시가 공유되고 앱 재시작 시 지속되도록 할 수 있습니다. 자세한 내용은 ?bindCache를 참조하세요. 여러 캐시를 함께 연결하거나 자체 사용자 정의 저장소 백엔드를 작성하는 것도 가능합니다. these 옵션에 대해 cachem, bindCache()를 구동하는 캐싱 패키지의 문서에서 더 배울 수 있습니다. 다른 최적화 앱에서 나타나는 두 가지 다른 최적화가 있습니다: 스케줄에 따른 데이터 가져오기 및 조작, 그리고 사용자 기대를 신중하게 관리하는 것입니다. 데이터 처리 스케줄링 Shiny 앱에 약간의 초기 데이터 정리가 필요한 데이터 세트를 사용한다고 상상해봅시다. 데이터 준비는 비교적 복잡하고 상당한 시간이 걸립니다. 이것이 앱의 병목 현상이며 더 잘하고 싶다고 발견했습니다. 코드를 이미 함수로 추출했다고 가정하면 다음과 같이 보일 것입니다: my_data_prep <- function() { df <- read.csv("path/to/file.csv") df %>% filter(!not_important) %>% group_by(my_variable) %>%

some_slow_function() } 그리고 현재 서버 함수에서それを 호출합니다: server <- function(input, output, session) { df <- my_data_prep()
# 많은 코드
} 서버 함수는 새 세션이 시작될 때마다 호출되지만, 데이터는 항상 동일하므로 데이터 처리를 server() 바깥으로 옮겨 앱을 더 빠르게(그리고 메모리를 적게 사용) 만들 수 있습니다: df <- my_data_prep() server <- function(input, output, session) {

# 많은 코드
} 이 코드에 주의를 기울이고 있으므로, 데이터를 로드하는 가장 효율적인 방법을 사용하고 있는지 확인하는 것도 가치가 있습니다: 평면 파일이 있는 경우 read.csv() 또는 read.table() 대신 data.table::fread() 또는 vroom::vroom()을 사용해보세요. 데이터 프레임이 있는 경우 arrow::write_feather()로 저장하고 arrow::read_feather()로 읽어보세요. Feather는 읽기와 쓰기가 상당히 빠를 수 있는 바이너리 파일 형식입니다.7 데이터 프레임이 아닌 객체가 있는 경우 readRDS()/saveRDS() 대신 qs::qread()/qs::qsave()를 사용해보세요. these 변경으로 병목 현상을 해결하기에 충분하지 않다면, 별도의 크론 작업이나 예약된 RMarkdown 보고서를 사용하여 my_data_prep()를 호출하고 결과를 저장하는 것을 고려할 수 있습니다. 그런 다음 앱이 미리 준비된

데이터를 로드하고 작업을 시작할 수 있습니다. 이것은 새벽 3시(고객이 없을 때)에 들어오는 준비 요리사를 고용하는 것과 같습니다. 점심 시간高峰 동안 요리사들이 최대한 효율적일 수 있도록 하기 위해서입니다. 사용자 기대 관리 마지막으로, 앱을 더 빠르게 느껴지게 하고 전반적인 사용자 경험을 개선하기 위해 앱 설계에 수행할 수 있는 몇 가지 조정이 있습니다. 많은 앱에서 사용할 수 있는 네 가지 팁입니다: tabsetPanel()을 사용하여 앱을 탭으로 나눕니다. 현재 탭의 출력만 다시 계산되므로, 사용자가 현재 보고 있는 것에 계산을 집중하는 데 사용할 수 있습니다. 긴 작업을 시작하기 위해 버튼 누름이 필요하도록 합니다. 작업이 시작되면 "알림" 기술을 사용하여 사용자에게 무슨 일이 일어나고 있는지 알려주세요. 가능한 경우 점진적 진행 표시줄("진행 표시줄")을 표시하세요. 진행 표시줄이 작업을 더 빠르게 느끼게 한다는 좋은 증거가 있기 때문입니다. 앱이 시작 시 상당한 작업을 수행해야 하고(전처리로 줄일 수 없는 경우), UI가 여전히 표시되고 사용자가 기다려야 한다는 것을 알 수 있도록 앱을 설계하세요. 마지막으로, 비용이 많이 드는 작업이 백그라운드에서 수행되는 동안 앱을 반응적으로 유지하고 싶다면, 비동기 프로그래밍에 대해 배울 시간입니다. 요약 이 장은 모든 Shiny 앱의 성능을 정확하게 측정하고 개선하기 위한 도구를 제공했습니다. shinyloadtest를 사용하여 성능을 측정하고 shinycannon을 사용하여 여러 사용자가 동시에 앱을 사용하는 것을 시뮬레이션하는 방법을 배웠습니다. 그런 다음 profvis를 사용하여 가장 비용이 많이 드는 작업을 찾고 이를 개선하기 위해 사용할 수 있는 기술 모음에 대해 배웠습니다. Mastering Shiny의 마지막 장입니다—끝까지 읽어주셔서 감사합니다! 이 책이 유용했고 제가 제공한 기술이 매력적인 Shiny 앱을 만드는 데 도움이 되었기를 바랍니다. 이 책이 유용했는지, 또는 향후 개선할 수 있는 것이 있는지 듣고 싶습니다. 연락하는 가장 좋은 방법은 Twitter(@hadleywickham) 또는 GitHub입니다. 다시 한번 읽어주셔서 감사하며,未来的 Shiny 앱에 행운을 빕니다! 1 rstudio::conf(2018) 발표 "Scaling Shiny: 10,000 User App"에서 Sean Lopp의 이 은유에 감사드립니다. Shiny 앱이 수천 명의 사용자를 처리할 수 있는지 의심이 있다면 시청하는 것을 강력히 추천합니다. 2 다시 말하지만, 이것은 앱이 정확히 어떻게 배포되는지에 따라 다르지만, 일반적으로 사용자 수에 따라 동적으로 프로세스 수를 제어할 수 있습니다. RStudio의 배포 제품에 대한 조언은 "RStudio Connect의 스케일링 및 성능 조정"을 참조하세요. 3 또는 수직 스케일링. 4 또는 수평 스케일링. 5 RStudio에서这样做하는 가장 쉬운 방법은 다른 RStudio 인스턴스를 여는 것입니다. 대신 터미널을 열고 R을 타이핑하세요. 6 이 함수는 Shiny 1.6.0에서 도입되었으며, 플롯에만 작동했던 이전 renderCachedPlot()을 일반화한 것입니다. 7 몇 가지 벤치마크는 이 Ursa Labs 블로그 게시물을 참조하세요.

색인 기호 %<-% 연산자, 여러 출력 <- (대입 연산자), 동작 추가, 시작하기: UI 입력과 서버 출력 A 액션 버튼 대화 상자 고려 사항, 명시적 확인 클릭 이벤트 시, 클릭 시 기타 버튼, 제한된 선택 및 기타 시작/중지 버튼 페어링, 애니메이션 일시정지 observeEvent()와 페어링, 액션 버튼 텍스트 조정, 간단한 사용 actionLink(), 액션 버튼 Angular, 리액티브 프로그래밍 간략한 역사 애니메이션, 일시정지, 애니메이션 일시정지 any_of(), 간접 참조 앱 생성 디렉토리 및 파일 만들기 대입 연산자 (<-), 동작 추가, 시작하기: UI 입력과 서버 출력

비동기 프로그래밍, 레스토랑에서 식사하기, 사용자 기대 관리 인증, 데이터 자동 북마크, 북마크 과제 B 배경색, Shiny 테마 벤치마킹, 벤치마킹-분석 모범 사례 (모듈 시스템; 성능 개선; 테스트도 참조) 코드 추출, 함수-내부 함수 패키지, 패키지-R CMD check 보안, 보안-컴퓨팅 자원 소프트웨어 엔지니어링, 소개-코드 리뷰 bindCache(), 캐싱 bookmarkButton(), 기본 아이디어 북마크, 북마크-요약 Bootstrap 프레임워크, Bootstrap browser(), 클릭 브러시 이벤트, 기본 사항, 브러싱 brushedPoints(), 브러싱, 플롯 수정 brushOpts(), 브러싱 bslib 패키지, 테마-연습 문제 C

캐시 키, 캐시 키 캐싱, 캐싱-캐시 범위 사례 연구 우발적 부상 조사 데이터 세트에서 내러티브 접근, 내러티브 데이터 탐색, 탐색 데이터 세트, 데이터 연습 문제, 연습 문제 프로토타입 앱, 프로토타입 비율 대 빈도, 비율 대 빈도 테스트 미학, 테스트 다듬기 디버깅, 사례 연구-사례 연구 파일 전송, 사례 연구 히스토그램, 사례 연구: 히스토그램 JavaScript 테스트, 사례 연구 모듈 시스템 동적 UI, 동적 UI-동적 UI 개요, 사례 연구 위자드 인터페이스, 위자드-위자드 리액티브 그래프, 사례 연구 재현 가능한 예시, 사례 연구 수치형 변수 선택, 사례 연구: 수치형 변수 선택

칼럼 시트, 연습 문제 checkboxGroupInput(), 제한된 선택 원형 참조, 원형 참조 클래스 인수, 액션 버튼 클릭 인수, 플롯, 기본 사항 클라이언트 측 유효성 검사, 데이터 코드 (모범 사례도 참조) 코드로 UI 만들기, 코드로 UI 만들기-연습 문제 명령형 추출, 동기 부여 독립적인 앱으로 추출, 함수-내부 함수 격리, 코드 격리 구성, 코드 구성 리액티브 표현식으로 중복 줄이기, 그래프 단순화 앱 간 공유, 공유 소스 코드 관리, 소스 코드 관리 코드 커버리지, 코드 커버리지 코드 예시, 가져오기 및 사용, 코드 예시 사용 코드 리뷰, 코드 리뷰 레이아웃 함수, 추가 UI 컨트롤 다중 페이지, 다중 페이지 레이아웃-네비게이션 목록 및 네비게이션 바 연습 문제, 연습 문제 단일 페이지, 단일 페이지 레이아웃-연습 문제 테마, 테마-연습 문제 load_all(), 기존 앱 변환 긴 실행 계산, 긴 실행 리액티브 M mainPanel(), 사이드바가 있는 페이지 make_ui(), 동적 UI memoise, 캐싱 메뉴, 드롭다운, 네비게이션 목록 및 네비게이션 바 메시지 인수, Shiny message(), 리액티브 디버깅 Meteor, 리액티브 프로그래밍 간략한 역사

MobX, 리액티브 프로그래밍 간략한 역사 modalDialog(), 명시적 확인, 대화 상자 modal_confirm, 명시적 확인 모델 수식, 컴퓨팅 자원 모듈 시스템 기본 사항, 모듈 기본 사항-연습 문제 이점, Shiny 모듈 입력과 출력, 입력과 출력-연습 문제 모듈 안의 모듈, 모듈 안의 모듈 리액티브 그래프와, 그래프 단순화 앱 구성에서의 역할, 동기 부여 단일 객체 모듈, 단일 객체 모듈 테스트, 모듈 moduleServer(), 단일 객체 모듈 마우스 이벤트, 기본 사항 다중 페이지 레이아웃, 다중 페이지 레이아웃-네비게이션 목록 및 네비게이션 바 다중 행 레이아웃, 다중 행 N 네임스페이싱, 네임스페이싱 명명 규칙, 명명 규칙 navbarMenu(), 네비게이션 목록 및 네비게이션 바 navbarPage(), 네비게이션 목록 및 네비게이션 바-네비게이션 목록 및 네비게이션 바

navlistPanel(), 네비게이션 목록 및 네비게이션 바 nearPoints(), 클릭 알림 점진적, 점진적 업데이트 완료 시 제거, 완료 시 제거 showNotification(), 알림 일시적, 일시적 알림 NS(), 동적 UI 수치형 변수, 사례 연구: 수치형 변수 선택 numericInput(), 입력, 수치형 입력, 여러 컨트롤 O observe(), 관찰자 및 출력, 사례 연구 observeEvent() 조건부 UI로 컨트롤 자동 전환, 조건부 UI 결과 캡처 및 저장, 조치 실행 취소 reactiveValues()와 결합, 사례 연구 관찰자로 디버깅, 관찰자 eventReactive() 대비, observeEvent() 및 eventReactive() observe() 대비, 관찰자 및 출력 액션 링크 및 버튼과 페어링, 액션 버튼 마우스 클릭 시 업데이트, 플롯 수정 입력 업데이트, 입력 업데이트-간단한 사용

관찰자, 관찰자 및 출력 on.exit(), 리액티브 표현식 onBookmark(), 북마크 과제 onRestore(), 북마크 과제 "기타" 버튼, 제한된 선택 및 기타 출력 인수, 출력 output$plot, 출력 출력 기본 UI 다운로드, 다운로드, 기본 사항 플레이스홀더 생성, 출력 플롯, 플롯 테이블, 테이블 텍스트, 텍스트 모듈 시스템, 입력과 출력-연습 문제 여러 출력, 여러 출력 관찰자 대비, 관찰자 및 출력 하나의 출력과 여러 입력, 여러 입력으로 수정되는 하나의 출력 출력 컨트롤, 추가 UI 컨트롤 페이지 레이아웃, 단일 페이지 레이아웃-네비게이션 목록 및 네비게이션 바 리액티브 그래프에서, 출력 완료-실행 완료, 출력 플러시

P 패키지 추가 리소스, 패키지 사용 이점, 이점 기존 앱 변환, 기존 앱 변환-패키지 핵심 아이디어, 패키지 앱-패키지 배포, 앱-패키지 배포 R CMD check, R CMD check 구조 개요, 패키지 페이지 함수, 페이지 함수 페이지 레이아웃 (레이아웃 참조) 매개변수화된 RMarkdown 문서, 보고서 다운로드 parse(), parse() 및 eval(), 컴퓨팅 자원 비밀번호, 무료 텍스트, 데이터 성능 개선 추가 리소스, 성능 개선 접근 방식, 성능 개선 벤치마킹, 벤치마킹-분석 캐싱, 캐싱-캐시 범위 데이터 처리, 데이터 처리 스케줄링 데모 비디오, 성능 사용자 기대 관리, 사용자 기대 관리

개요, 성능 프로파일링, 프로파일링-한계 레스토랑 은유, 레스토랑에서 식사하기 개인 식별 정보(PII), 데이터 사진, 이미지 플롯 테마, 플롯 테마 plotly 패키지, 상호작용 한계 plotOutput(), 플롯, 상호작용 폴링, 폴링 print 디버깅, 리액티브 디버깅 프로듀서, 리액티브 표현식 프로파일링, 프로파일링-한계 profvis 패키지, 프로파일링 진행 표시줄 과제, 진행 표시줄 Shiny, Shiny 스피너, 스피너 waiter 패키지, waiter 프로토타이핑, 프로토타이핑 purrr::map(), 동적 UI purrr::pmap(), 데이터로 UI Q

질문 및 의견, 문의 방법 R R CMD check, R CMD check R 패키지 추가 리소스, R CMD check 설치, 전제 조건 radioButtons(), 제한된 선택, 제한된 선택 및 기타 React, 리액티브 프로그래밍 간략한 역사 리액티브 컨텍스트, 입력 리액티브 의존성, 리액티브 그래프 리액티브 표현식 만들기, 리액티브 표현식으로 중복 줄이기, 그래프 단순화 오류 처리, 리액티브 표현식 필요성, 왜 리액티브 표현식이 필요한가? 관찰자 및 출력 대비, 관찰자 및 출력 개요, 리액티브 표현식 리액티브 그래프에서, 리액티브 표현식 읽기-리액티브 표현식 완료 리액티브 표현식으로 중복 줄이기, 리액티브 표현식으로 중복 줄이기 앱 구성에서의 역할 데이터 세트 비교, 동기 부여

이중 활동, 리액티브 표현식 여러 시뮬레이션 탐색, 앱 리액티브 그래프, 리액티브 그래프 리액티브 그래프 단순화, 그래프 단순화 리액티브 그래프 자동 북마크, 북마크 과제 제약에서 탈출 안티패턴, 안티패턴 장단점, 소개 사례 연구, 사례 연구-연습 문제 입력 무효화, 리액티브 그래프가 포착하지 못하는 것은? 개요, 리액티브 그래프 앱 구성에서의 역할 리액티브 그래프 그리기, 리액티브 그래프 리액티브 그래프 단순화, 그래프 단순화 단계별 안내 역동성, 역동성 입력 변경, 입력 변경-연습 문제 reactlog 패키지, reactlog 패키지 샘플 앱, 리액티브 실행 단계별 안내 세션 흐름, 세션 시작-실행 완료, 출력 플러시

리액티브 프로그래밍 장단점, 소개 간략한 역사, 리액티브 프로그래밍 간략한 역사 구성 요소 평가 타이밍 제어, 평가 타이밍 제어-클릭 시 코드 격리, 코드 격리 관찰자 및 출력, 관찰자, 관찰자 및 출력 리액티브 표현식, 리액티브 표현식 리액티브 값, 리액티브 값 디버깅, 리액티브 디버깅 용어 정의, 리액티브 프로그래밍 간략한 역사 리액티브의 본질, 동작 추가, 왜 리액티브 프로그래밍이 필요한가? 소개 실행 순서, 실행 순서 명령형 대 선언형 프로그래밍, 명령형 대 선언형 프로그래밍 리액티브 프로그래밍의 핵심 아이디어, 소개 지연 업데이트, 지연성 연습 문제, 연습 문제 리액티브 표현식, 리액티브 표현식, 리액티브 표현식-왜 리액티브 표현식이 필요한가? 리액티브 그래프, 리액티브 그래프

서버 함수, 서버 함수-출력 간단한 예시, 리액티브 프로그래밍 필요성, 왜 리액티브 프로그래밍이 필요한가?-리액티브 프로그래밍 reactive(), 그래프 단순화 reactiveConsole(), 리액티브 테스트 reactivePoll(), 폴링 reactiveTimer(), 시간 기반 무효화, 한계 reactiveVal(), 플롯 수정, 리액티브 값 reactiveValues(), 리액티브 값, 사례 연구 ReactiveX, 리액티브 프로그래밍 간략한 역사 reactlog 패키지, reactlog 패키지 직사각형화, 리액티브 캐싱 removeModal(), 명시적 확인 removeNotification(), 완료 시 제거 render 함수, 동작 추가, 출력, 출력 renderDataTable(), 테이블 renderImage(), 이미지 renderPlot(), 플롯, 동적 높이 및 너비, 플롯 캐싱 renderPrint(), 동작 추가, 텍스트 renderTable(), 동작 추가, 테이블 renderText(), 텍스트

renderUI(), 코드로 UI 만들기, 시작하기, 대화 상자 renv, 의존성 관리 보고서, 다운로드, 보고서 다운로드 재현 가능한 예시 이점, 도움 받기 사례 연구, 사례 연구 가장 작은 만들기, 최소한 재현 가능한 예시 만들기 만들기, 재현 가능한 예시 만들기 간단한 예시, 재현 가능한 예시 기본 사항 req(), 기본 사항, 유효성 검사-req() 및 유효성 검사, 데이터 업로드 RMarkdown, 보고서 다운로드 rsconnect 패키지, 앱-패키지 배포 RStudio 설치, 전제 조건 대화형 디버거, 대화형 디버거 키보드 단축키, 키보드 단축키 RStudio 커뮤니티 사이트, 도움 받기 RStudio Connect, 공유 RStudio Gadgets, 공유 세의 법칙, 그래프 단순화 앱 실행 버튼, 실행 및 중지 rxtools, 소개

S 스크린샷, 시각적 요소 테스트 보안 추가 리소스, 보안 인증, 데이터 컴퓨팅 자원, 컴퓨팅 자원 데이터, 데이터 보호할 주요 항목, 보안 비밀번호, 무료 텍스트, 데이터 책임, 보안 선택 상자, 계층적 선택 상자 selectInput(), 추가 UI 컨트롤, 입력, 제한된 선택, req()로 실행 취소 선택 도구, 기본 사항 separate(), Tidy-Selection 서버 함수 긴 리액티브 처리, 서버 함수 output$plot, 출력 개요, 동작 추가, 서버 함수-출력 서버 측 북마크, 더 풍부한 상태 저장 세션 지속 시간, 분석 session$elapse(), 한계

session$flushReact(), 모듈 session$getReturned(), 모듈 session$setInputs(), 리액티브 테스트 setBookmarkExclude(), 북마크 과제 앱 공유, 공유 앱 간 코드 공유, 공유 Shiny 학습 접근 방식, 무엇을 배울 것인가? 기본 리액티브 (리액티브 프로그래밍도 참조) 평가 타이밍 제어, 평가 타이밍 제어-클릭 시 리액티브 프로그래밍의 핵심 아이디어, 소개 관찰자, 관찰자 리액티브 표현식, 리액티브 표현식-왜 리액티브 표현식이 필요한가? 리액티브 프로그래밍, 리액티브 프로그래밍-연습 문제 서버 함수, 서버 함수-출력 기본 UI 확장 패키지, 소개 파일 전송, UI 입력, 입력-연습 문제 출력, 출력-다운로드 연습 문제, 연습 문제, 연습 문제

이점, Shiny란 무엇인가?, 소개, 소개 설치, 소개 소개 앱에 동작 추가, 동작 추가 UI 컨트롤 추가, UI 컨트롤 추가 앱 개발 프로세스, 실행 및 중지, 개발 워크플로-뷰 제어 (워크플로도 참조) 앱 디렉토리 및 파일 만들기, 앱 디렉토리 및 파일 만들기 작업 치트 시트, 연습 문제 연습 문제, 연습 문제 중복 코드 줄이기, 리액티브 표현식으로 중복 줄이기 앱 실행 및 중지, 실행 및 중지 학습 전제 조건, 전제 조건 대상 독자, 이 책을 읽어야 할 사람, Shiny란 무엇인가? (사례 연구도 참조) shinyapps.io, 공유 shinycannon, 재생 shinycssloaders 패키지, 스피너 shinyFeedback 패키지, 유효성 검사 shinyloadtest 패키지, 벤치마킹 shinySignals, 소개

shinytest 패키지, JavaScript 테스트 showModal(), 명시적 확인 showNotification(), 알림 sidebarLayout(), 사이드바가 있는 페이지, 다중 행 sidebarPanel(), 사이드바가 있는 페이지 단일 객체 모듈, 단일 객체 모듈 단일 페이지 레이아웃, 단일 페이지 레이아웃-연습 문제 sliderInput(), 입력, 수치형 입력 스냅샷 테스트, UI 함수, 시각적 요소 테스트 소프트웨어 엔지니어링 (모범 사례도 참조) 코드 구성, 코드 구성 코드 리뷰, 코드 리뷰 지속적 통합/배포, 지속적 통합/배포 의존성 관리, 의존성 관리 개선 권장 사항, 소개 소스 코드 관리, 소스 코드 관리 테스트, 테스트 source(), 기존 앱 변환, 컴퓨팅 자원 특수 조건, req()로 실행 취소 스피너, 스피너 스프레드시트, 리액티브 프로그래밍 간략한 역사

스트로크, 브러싱 T tableOutput(), UI 컨트롤 추가, 테이블 tabPanel(), 다중 페이지 레이아웃 tabsetPanel(), 탭 세트-네비게이션 목록 및 네비게이션 바, 북마크 과제 테스트 모범 사례, 테스트 오표양과 오탐, 철학 함수 테스트, 함수 테스트-UI 함수 계층 구조, 테스트 수준, 테스트 JavaScript 테스트, JavaScript 테스트-사례 연구 개요, 테스트 목적, 테스트 리액티브 테스트, 리액티브 테스트-한계 스냅샷 테스트, UI 함수 타이밍, 언제 테스트를 작성해야 하는가? 시각적 요소 테스트, 시각적 요소 테스트 워크플로, 워크플로 testServer(), 리액티브 테스트-모듈 testthat 패키지, 테스트 testthat::snapshot_accept(), UI 함수

testthat::snapshot_review(), 시각적 요소 테스트 textAreaInput(), 무료 텍스트 textInput(), 입력-무료 텍스트, req()로 실행 취소, req() 및 유효성 검사, 입력 업데이트 textOutput(), 텍스트, 여러 컨트롤 테마, 테마-연습 문제 Tidy 평가 장단점, Tidy 평가 데이터 마스킹, 데이터 마스킹-왜 기본 R을 사용하지 않는가? 간접 참조 문제, 동기 부여 parse() 및 eval(), parse() 및 eval() tidy-selection, tidy-selection-tidy-selection 및 데이터 마스킹 시간 기반 무효화, 시간 기반 무효화, 시간 기반 무효화-연습 문제 타이머 정확도, 타이머 정확도 타임스탬프, 연습 문제 titlePanel(), 사이드바가 있는 페이지 스택 트레이스, 스택 트레이스 읽기-Shiny에서 스택 트레이스 변환, 변수, 컴퓨팅 자원 휴지통, 휴지통 tReactive(), 액션 버튼 .tsv (탭으로 구분되는 값), 데이터 다운로드 이중 열 레이아웃, 사이드바가 있는 페이지

U UI 함수, UI 함수-데이터로 UI uiOutput(), 코드로 UI 만들기, 여러 컨트롤 조치 실행 취소, 조치 실행 취소 입력 업데이트 함수, 입력 업데이트 updateSelectInput(), 리액티브 값 동결 updateSliderInput(), 입력 업데이트, 간단한 사용 updateTextInput(), 입력 누적 파일 업로드, 파일 업로드, 업로드-데이터 업로드 사용자 북마크 명령, URL 업데이트 사용자 기대, 사용자 기대 관리 사용자 피드백 확인 및 실행 취소, 확인 및 실행 취소-휴지통 알림, 알림-진행 표시줄 진행 표시줄, 진행 표시줄-스피너 유효성 검사, 유효성 검사-알림 useShinyFeedback(), 유효성 검사 usethis, 워크플로 usethis::use_data(mydataset), 기존 앱 변환 usethis::use_description(), 기존 앱 변환, 패키지, 워크플로 usethis::use_package(), 앱-패키지 배포

usethis::use_rstudio(), 기존 앱 변환 use_waitress(), waiter V 유효성 검사 req()로 실행 취소, 무료 텍스트로 텍스트 속성 확인, 유효성 검사의 중요성, 유효성 검사 req()와, req() 및 유효성 검사 shinyFeedback 패키지, 유효성 검사 validate(), 출력 유효성 검사 변수 변환, 컴퓨팅 자원 변수, 왜 변수를 사용할 수 없는가?, 사례 연구: 수치형 변수 선택 verbatimTextOutput(), UI 컨트롤 추가, 텍스트 버전 관리 시스템, 소스 코드 관리 동적 가시성, 동적 가시성-연습 문제 VisiCalc, 리액티브 프로그래밍 간략한 역사 시각적 요소 테스트, 시각적 요소 테스트 Vue.js, 리액티브 프로그래밍 간략한 역사 W waiter 패키지, waiter, 스피너

너비, 동적, 동적 높이 및 너비 withProgress(), Shiny 위자드 인터페이스, 위자드 인터페이스, 위자드-위자드 워크플로 디버깅, 디버깅-리액티브 디버깅 개발, 개발 워크플로-뷰 제어 재현 가능한 예시, 도움 받기-사례 연구 테스트, 워크플로 패키지 사용 시, 워크플로 Z zeallot, 여러 출력

